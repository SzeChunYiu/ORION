# One-shot transport

This directory contains a SHA-bound base64 transport for the reviewed five-paper R2 hardening package. The pull-request workflow reconstructs the package, runs the theorem and publication gates, commits the materialized files, and removes this directory and itself. These transport files carry no scientific authority.
