# ORION-16 result and active-lane reconciliation

Two non-equivalent objects must remain separate:

- PR #1692 contains a completed three-system discriminator. It provides useful bounded
  evidence: Mathlib4 shows strong graph-quality sensitivity, nf-core supplies a shallow
  null, and Gene Ontology supplies a large non-code graph. Its own disposition is second
  tier and external independent validation remains unavailable.
- PR #1695 is the canonical design/acquisition lane for the stricter issue-#1649 target:
  authoritative dependency/change graphs and native-system consequences across at least
  two materially different systems.

The completed result cannot be silently relabelled as satisfying the stricter acquisition
contract, and the active acquisition must not be duplicated. The safe integration action
is to recover the unique PR-#1692 evidence path-by-path while leaving PR #1695 as owner of
any further top-tier empirical claim.
