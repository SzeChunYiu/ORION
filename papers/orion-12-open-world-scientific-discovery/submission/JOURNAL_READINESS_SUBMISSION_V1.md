# ORION-12 — submission readiness record

**Terminal: `READY_PENDING_TEMPLATE`** (Wave-1 filing bar, defined in
`WAVE1_UPGRADE_LANE_VENUE_DECISIONS_V1.md` — PR #1692, pending). A complete
standalone manuscript exists and compiles with zero undefined references; it is
not in the venue class and carries no author block. What remains is mechanical,
not authorial.

Supersession note: the earlier label on this record,
`READY_TO_SUBMIT_SECOND_TIER`, named the venue tier reached but hid the
venue-format conversion that this record itself lists below as a remaining
input. That label is superseded by this one.

**Venue tier:** second tier. Target venue Information Processing
& Management; fallback JASIST.

The scientific terminal is unchanged and remains narrowed: the framework and its
theorems stand, cross-arena superiority is untested, and the external matched
campaign remains undetermined rather than null. Nothing in this pass converted
an adverse or undetermined result into a pass.

## Gate table

| Gate | State | Evidence |
|---|---|---|
| Canonical manuscript designated | Closed | `CANONICAL_SOURCE_DECISION.md` |
| Duplicated section removed | Closed | One section file was typesetting the same five paragraphs twice; the older copy, which also carried sibling-paper cross-references, was removed |
| Compiles to submittable PDF | Closed | 47 pages, 0 undefined references, 0 overfull boxes |
| Page-level render and visual audit | Closed | All 47 pages rasterised and inspected; recorded in `journal_package/current_revision/RENDER_AND_VISUAL_AUDIT.md` |
| Claim ledger | Closed | `check_claim_ledger.py --check` clean, 0 violations |
| Assimilation and widening checks | Closed | `check_p2_assimilation.py`, `check_p2_v2.py` both pass |
| Manuscript integrity test | Closed | `tests/test_paper_manuscript_integrity.py` passes; three-valued boundary still stated |
| Render closure state | Closed | Regenerated by its own generator; correctly reports SUPERSEDED with 18 drifted inputs |
| Journal package inventory | Closed | `check_journal_package.py` reports P2 ok |
| Content binding | Restored to baseline | `BOUND_PARTIAL / PASS` after reconciling once, last, with `--write-hashes` |
| House style | Closed | Machine terminals and repository digests removed from narrative prose; availability section untouched |
| Venue, manifest, cover letter, availability | Closed | This directory |

## What changed, and what deliberately did not

**Changed.** Removed a duplicated section copy and its sibling-paper
cross-references. Replaced eight machine terminal tokens and nine repository
digests in narrative prose with plain English. Re-verified and updated four
claim-ledger locator sentences. Produced the current-revision PDF and the
page-level render and visual audit that the package had been missing, and closed
the claim that named them.

**Deliberately not changed.** `package_authority.current_submission_authorized`
remains `false` and the package remains `SUPERSEDED`. Producing the artifacts
retires an evidence gap; it does not grant authority. The manifest now carries an
accurately named open claim, `P2.PACKAGE_SUBMISSION_AUTHORITY`, which states that
what remains open is a human decision to supersede the retained historical
package rather than a missing file.

One exception to house style is recorded rather than hidden: the record-set
digest binding retains its two SHA-256 values inline in Results, because the
claim ledger's declared support artifacts require the bound digests to appear in
the sentence they bind. For that claim the digest is the claim.

## Venue-format conversion — named explicitly

**Bar note.** This paper has no section titled "Discussion". Its interpretive content sits in "Post-saturation unresolved-route authority", "Limitations and integrity", and a substantial Conclusion. Every other organ the bar names is present. Recorded here so the label rests on disclosed evidence rather than on a heading match.

The manuscript is `\documentclass[11pt]{article}` with `\author{Working framework
draft}`. It is a complete, sectioned, bibliography-resolved manuscript that
compiles cleanly, but it is **not** in Information Processing & Management's submission format: no venue
class or style file, no author block, and no anonymisation decision recorded.

Converting it requires the Elsevier elsarticle class, which is not obtainable from the toolchain used in
this pass, so this is left as an input rather than attempted and half-done.
Treat it as mechanical filing work, not evidence work: the science, the claim
ceiling, and the checks are complete and independent of the template.

## Remaining inputs — human only

1. **Author list** in final order; the title page reads "Working framework draft".
2. **Affiliations**, **ORCID** identifiers, **corresponding author** and email.
3. **Funding statement**, **competing-interests declaration**, and IP&M's
   CRediT author-contribution statement.
4. **Package submission authority.** A person must review the current-revision
   PDF and render audit and decide whether to set
   `current_submission_authorized` to true, superseding the retained historical
   package. This is the one open claim in the manifest.
5. **Concurrent-disclosure decision** if any sibling manuscript is filed at an
   overlapping venue.
6. **Public archival deposit** of the protocol, evidence, and results artifacts,
   with the DOI substituted into the availability statements.
7. **Licence election** for deposited artifacts, noting the MIT/CC0 distinction
   the manuscript records for one external snapshot.

Also required before filing: **convert to Information Processing & Management's submission template** (the Elsevier elsarticle class) and record the anonymisation decision.
