# Five-paper figure contracts R2

These are evidence contracts, not finished art. Every final display must be reproducible from declared source data and remain legible at target size.

## Paper A

### Figure A1 — from realized signatures to support

**Reader question:** Which object actually controls the normal form?
**Claim:** `support <= zsf(H;A) <= rank(H) <= s+1`.
**Panels:** (a) active coordinates mapped to signatures; (b) zero-sum subword deletion; (c) hierarchy of alphabet ceiling, rank ceiling and intrinsic lower witness.
**Boundary visible:** only R6M has intrinsic equality.
**Avoid:** decorative circuit art implying physical resource savings.

### Figure A2 — objective cone

Plot the line `mu=(b-1)t_R` for selected `b` as a schematic proof-validity map, not empirical phase data. Outside the cone label `certificate inapplicable`, never `higher support required`.

## Paper B

### Figure B1 — tight and loose controls

Show R6M `beta=kappa=2` beside R6I `beta=5, kappa=1`, with the transformation unavailable to the rank-only language named explicitly.

### Figure B2 — product search degree

For fixed `t`, display exponents `t` and `5t`, not wall-clock speed. The caption must say `direct support enumerator` and show ratio `Theta(n^(4t))`.

## Paper C

### Figure C1 — one information fiber, two values

Two instance families enter one pair-information node and branch to `12t-2` and `10t-1`. Overlay the midpoint additive radius and geometric-mean multiplicative interval. Show optimizer icons `triple+pair` versus `pairs+singletons`.

### Figure C2 — query hierarchy

Decision: exact from four-index clauses. Value: impossible from complete pairs. Witness: triple presence impossible from pairs. Exact value: impossible even from order `m-2`. The hierarchy should be a logical dependency diagram, not a performance bar chart.

### Figure C3 — Boolean-lattice parity kernel

For small `q`, show alternating signs over the Boolean cube and state that every proper upper marginal cancels. Include the exact signed mass `2^(q-1)` on each side.

## Paper D

### Figure D1 — typed fixed point

Claims are nodes; licenses are small labeled tokens. A rule edge is annotated by its cap. Show one license propagating, `PROSPECTIVE` blocked by a repair cap, and an unsupported cycle remaining empty.

### Figure D2 — three falsifier cases

Side-by-side claim/license retraction for TARE, Paper C, and non-quantum `D_4`. Preserve unaffected independent claims visibly.

## Non-quantum paper

### Figure N1 — width-one corridor

Plot lower line `5k+10` and upper line `5k+11`, with exact points at `k=2,3` and unresolved `k=4`. The conditional exact-tail arrow starts only from `D_4=30`.

### Figure N2 — rank-forcing phase

Discrete grid in `(s,c4)`. Mark admissible multiplicity patterns; highlight `s+c4<=24` as rank-three-forced and the residual boundary branches separately. This is a theorem diagram, not simulation.

### Figure N3 — atom budget

Decompose `31-s` into internal atom repetition and cross-atom overlap. Show conditional types `(6,6,6,13)` and `(6,6,7,12)` and the budget at most eight only under externally accepted support `s>=23`.

## Shared visual QA

- Use consistent mathematical notation across papers.
- Semantic colors, if used, must encode `proved`, `bounded`, `open`, and `refuted` consistently and accessibly.
- Do not hide open gates in grey footnotes.
- Supply source data for every plotted coordinate or count.
- Vector export is preferred for diagrams; raster previews require final-size inspection.
