# ORION-06 recorded-negative revival R1 — current-main successor closeout

Date: 2026-08-28  
Machine-readable authority: `ORION06_NEGATIVE_REVIVAL_R1_SUCCESSOR.json`  
Terminal: `ORION06_RECORDED_NEGATIVE_REVIVAL_COVERAGE_COMPLETE__PAPER_FREEZE_WITHHELD`

## What this closes

The immutable pre-execution coverage audit contains seven standalone recorded
negative rows.  Every row now has a mechanism-level adjudication against its
named strongest parent or donor.  The four rows that were `UNFINISHED` in that
historical audit received the separately frozen R1 execution; the audit itself
is not rewritten.

The final bounded partition is:

- two `IMPROVED` mechanism results;
- one `CORRECT_SUBTRACTION` credited to the stronger donor;
- four `RETAINED_NEGATIVE` results;
- one separately named cross-domain `CANNOT_CHECK` gate; and
- no `UNSOLVABLE` entry.

## Executed revival results

| Recorded negative | Mechanism-matched result | Preserved boundary |
|---|---|---|
| R4C H2 regime limit | all 135,135 perfect matchings; controlled Pauli support `45 -> 41` at unchanged normalization; `IMPROVED` | H2 was already open; this is not prospective confirmation or general superiority |
| R5B outer projection | all 15 H4 matchings and 341 candidate points; two strict parent-dominating points; `IMPROVED` | H4 was already open; the protected new-subject claim remains unearned |
| R6I exact rank-2 language | fresh R6K replay has no strict point on H4 or N2; `RETAINED_NEGATIVE` | the original R6I negative remains load-bearing |
| R6K exact Restore factor | fresh R6L donor replay has strict points on H4 and N2; `CORRECT_SUBTRACTION` | the gain is donor-owned and grants no candidate novelty or R6 authority |

The already exact R2, R3B and N1C parent/donor comparisons remain
`RETAINED_NEGATIVE` and were not relabelled.

## Custody

The R1 protocol was frozen at
`7483a04dae9d1bdc9a3e6faeb6383592dff91754`; execution source was
`e9d4ee1df73ff22fb5742ff2cd9c200b0a5f29f9`, with identical protocol bytes.
LUNARC jobs `3550252`–`3550255` completed `0:0` with empty stderr.  The source
archive SHA-256 is
`19dc73f450e71ce3b84f152d32fa535b2db05961316bfb9b2c391af39a9c85fd`;
the 12,836-file stage-manifest SHA-256 is
`2164cca8a3926a3af71554484b68cb85fbd832d2719bd127b48756b77be5a5fa`;
and the imported 35-file run-manifest SHA-256 is
`723be0b268c294a08aff2b31d6f89b507431b4b5cd6d77ea34dd122b8642b991`.

`verify_orion06_negative_revival_successor.py` rebuilds the historical
coverage denominator, invokes the execution-bundle verifier, checks every row
and outcome partition, and rejects authority promotion or disappearance of the
cross-domain gate.

## Remaining `CANNOT_CHECK`

The cross-domain general-method comparison still lacks prospectively admitted
matched non-quantum formal and computational or empirical Domain-B/C
programmes with independent scoring.  This is a named external evaluation
resource blocker, not a proof of impossibility.

## Freeze boundary

This closes **recorded-negative revival coverage only**.  It does not freeze
ORION-06, widen the manuscript, establish novelty or external independence, or
authorize submission.  A paper freeze still requires protected prospective
confirmation for the R4C/R5B mechanism claims, the named cross-domain study,
current independent novelty/external-authority review, and current
manuscript/PDF/package byte binding with visual QA.

