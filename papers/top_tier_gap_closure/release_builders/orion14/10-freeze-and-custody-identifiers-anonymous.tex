\section{Freeze and custody record}

The protected evaluation was launched only after the public preflight, harness state, and execution freeze were bound and checked. For double-blind review, author-linked run numbers, public commit locators, repository paths, and exact public transport identifiers are withheld from the manuscript because they can reveal authorship. The accompanying anonymous supplement retains the safe aggregate result objects and checksums needed to verify the bounded reported counts without those identity-bearing locators.

The named archival version restores the complete execution receipt, protected attack-set and candidate-manifest digests, signed freeze commit, and workflow-binding identifiers. This review-time redaction changes no scientific outcome, denominator, statistic, protocol rule, or claim boundary; it changes only the identity-bearing reproducibility locators exposed before double-blind review is complete.
