# Independent atomic-coverage pass — 2026-08-28

This pass was performed from the current TeX entry point, every included
section and the bibliography without inheriting the historical claim-ledger
adjudication. The resulting sentence-level ledger has 167 rows spanning title,
all eight current sections and all fourteen bibliography entries.

## Coverage result

| Status | Count |
|---|---:|
| `SUPPORTED_INTERNAL` | 131 |
| `VERIFIED` | 16 |
| `NOT_ASSESSABLE` | 14 |
| `UNRESOLVED` | 6 |

The pass found no basis for a verification-complete release state. In
particular:

- theorem, mechanization and quantitative sentences are supported only by
  same-programme implementations or proofs;
- the bibliography is present, but source entailment for the current
  load-bearing donors is not complete;
- the manuscript is an overlay that refers to historical versions and audit
  process rather than a standalone target article;
- the limits section exposes repository modules and machine-status vocabulary;
- the target manuscript is scientifically older than the designated current
  manuscript.

The ledger therefore fails closed. None of the internally supported assertions
is promoted to externally verified or submission-ready status.
