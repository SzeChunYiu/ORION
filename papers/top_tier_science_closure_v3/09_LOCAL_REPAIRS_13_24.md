## 13. ORION-24 — external-model is not external-authority

**Problem.** The latest branch salvage recovered Claude/Codex decision records. The models differ, but the packet selection, scientific framing and custody remain same-programme unless separately proven.

**Required evidence label:**

> `EXTERNAL_MODEL__SAME_PROGRAMME_CUSTODY__NO_INSTITUTIONAL_AUTHORITY`

**Required manuscript paragraph:**

> The current model-harvest records probe implementation/model sensitivity inside the ORION programme. They do not satisfy P14D's external-validity gate: no independently held packets, outside adjudication authority or external custody verifier is established. P14C therefore remains an internal specification-conformance result. A scientific-validity claim requires sealed inputs and adjudication held by parties outside the programme.

P14A's unattainable gate and P14B's circularity remain in the main chronology.

## 14. ORION-25 — keep signatures below scientific truth

**Problem.** Strong attestation results can be over-read as fact or science validation. The full-key-compromise arm establishes the opposite.

**Required abstract/threat-model sentence:**

> A valid composed signature is evidence that the registered bytes verify under a key set. It is not evidence of key custody, fact truth, protocol validity or scientific authority. Under full genuine-key compromise, six forged-clean fact sets are re-signed and the signature layer detects zero; this negative is part of the result.

**Production comparison guard.** Sigstore/in-toto/TUF-class comparisons must match threat model, services and information. Hardware-backed custody can add a premise; it cannot turn signatures into scientific truth.

## 15. ORION-02 — matched-control result and emission repair

**Problem.** R23's matched lexical-control separation resolves to p = 0.092. In R24 the lexical control is descriptively better, with 14 strict violations versus 20 for the geometric arm, but the emitter discarded per-dataset values after summing them.

**Required result paragraph:**

> The matched no-geometry control does not support the claimed treatment separation. In R23 the paired comparison is not significant (p = 0.092). In R24 the lexical control has fewer aggregate strict violations than the geometric arm (14 versus 20), but the result object retains only sums, so no paired uncertainty analysis is recoverable. R24 is therefore descriptively control-favoured and inferentially `CANNOT_CHECK`; it is not evidence for geometric refinement.

Before any successor, serialize all per-dataset values and require a positive-demonstration control on the same instrument.

## 16. ORION-08 — merged paired uncertainty and family-wise direction result

**Problem.** Current main now contains both the exact Holm-Bonferroni direction analysis and the merged PR #1738 manuscript/table/PDF correction.

**Required result paragraph:**

> Across the twelve registered paired comparisons, all nine rows whose mean-difference interval excludes zero also survive an exact two-sided sign test with Holm-Bonferroni adjustment. Two scoped-versus-never rows have mean intervals containing zero; their paired directions survive Holm, but this does not upgrade the registered mean-effect conclusion. The exact-tie negative control is the sole non-survivor and remains the prespecified correct null outcome.

The merged generator, source JSON, table and current-epoch PDF must remain content-bound; a future edit cannot drop the two undetermined mean contrasts.

## 17. ORION-11 — retire the historical 2,880-record campaign

**Problem.** The historical campaign's repeats are effectively vacuous, its lexical detector imposes a 1/48 ceiling, its sole success is responsibility-inconsistent, and the provider-backed arm discards 240/240 answers into empty traces.

**Required chronology paragraph:**

> The historical 2,880-record campaign is retired to instrument-validation and negative-control status. It cannot support H1 in either direction: the mechanical score was ceiling-limited by a lexical detector, the sole apparent success failed responsibility consistency, provider completions were not parsed or archived, and the full arm is record-identical to three ablations. This retirement does not alter the later bounded v2.2.4 result and does not weaken the 2026-08-28 comparative retraction.

A live successor must archive raw completions, parsed traces, parse failures, responsibility-consistent outcomes and pairing keys.

## 18. ORION-12 — asymmetric terminal-support correction

**Problem.** In the 390-task offline mechanism study, only ORION can remain open or return `CANNOT_CHECK`; the six named baselines are forced into terminal closure. A 1.00-versus-0.00 premature-closure gap is therefore partly definitional.

**Required result paragraph:**

> The offline premature-closure contrast demonstrates the consequence of the registered open-state mechanism under asymmetric action support. It is not a symmetric system-superiority comparison: the baseline interfaces cannot express the `CANNOT_CHECK` or continue decision available to ORION. External superiority requires a matched-action comparator in which every arm can continue, abstain and return `CANNOT_CHECK` under the same resource and stopping contract.

## 19. ORION-13 — pending baseline-degeneracy and coordinate-coverage correction

**Problem.** Open PR #1739 reports that both 32-case holdouts are predicate-equal, making the flat comparator an always-merge rule; six confirmatory and four initial discordances carry the result, and polarity is the only deciding coordinate. The PR is not current-main authority.

**Required adoption procedure and conditional text:**

> After independent reproduction from current-main code and gold, report the flat comparator as constant always-merge on these corpora, give the confirmatory (`b=6,c=0`, exact p = 0.031) and initial (`b=4,c=0`, exact p = 0.125) results separately, and state that only polarity decides any of the 64 cases. Record `pooled_significance_test = NOT_COMPUTED_BY_PROTOCOL`; the confirmatory protocol forbids pooling the holdouts to inflate n. Treat coordinate sparsity as a coverage limit, not evidence that other coordinates are dispensable. Until #1739 is reproduced, merged, rebound and rendered, this analysis is pending.

A decisive successor needs predicate-disagreeing pairs and cases turning on every claimed coordinate, against a baseline that attempts the same structured decision.

## 20. ORION-19 — exact small-n diagnostic interpretation

**Problem.** The five-case headline lacks an exact paired uncertainty statement and sometimes omits the correctly predicted `CANNOT_CHECK` row.

**Replacement abstract core:**

> Across five frozen diagnostic families, the selector matches all five registered categorical dispositions—including one `CANNOT_CHECK`—versus one of five for generic compute escalation. The paired table contains four ORION-only correct cases and zero generic-only correct cases, giving exact two-sided McNemar p = 0.125; false compute escalation is 0 versus 4. This is a complete bounded demonstration over five registered families, not population-level evidence of broad structured-learning superiority. The historical representation margin remains retired.

## 21. ORION-21 — state the gate's detectable capability

**Problem.** A bare miss of the >=8/10 gate does not tell readers whether the registered capability is excluded or merely hard to observe.

**Required result paragraph:**

> The >=8/10 family gate has 80% power only when true per-responsibility capability is at least 0.8424. Exact 95% intervals for LINEAR 3/10 and RBF/KNN 5/10 all end below 0.8424, so capability at the registered near-uniform bar is excluded rather than merely unobserved. The intervals do not exclude moderate capability in the 0.6-0.8 range; the result therefore licenses “below the registered bar,” not “family-scale capability absent.”

## 22. ORION-24 — report four discordances, not a broad margin

**Problem.** P14C's 0.142857 strongest-comparator margin is four cases among a self-authored 28-case register.

**Required result paragraph:**

> ORION-RSE conforms on 28/28 registered cases versus 24/28 for the strongest partial contract. The paired comparison is four discordant ORION wins and zero losses, exact two-sided McNemar p = 0.125. This is exact internal-register conformance, not population-level or externally adjudicated scientific superiority. The result remains bounded by same-programme case/specification authorship, and P14D external custody is the route to scientific-validity authority.

## 23. ORION-15 — preserve justified empty tables

**Problem.** Seven visually empty manuscript tables were plausibly but incorrectly flagged as missing instrumentation. Their headers explicitly state why the archive cannot support the rows and refuse to impute nearby diagnostic numbers.

**Required reading and artifact rule:**

> The empty tables are declared `CANNOT_CHECK` results, not missing values. Their reasons and no-imputation statements are part of the evidence. Before classifying any empty artifact as a gap, open it and distinguish a justified declination from an unexplained absence. Any future declination must be emitted as a named value with its protocol reason.

Do not populate the tables from the 21/24 diagnostic accuracy or prompt-version ablations; those are different estimands and arms.

## 24. Integration/rebinding checklist

For any repair applied to a hash-pinned paper:

1. identify every claim ledger, manifest, checksum file, render-closure state and PDF that binds the changed source;
2. make the scientific correction first;
3. regenerate derived tables/figures and PDF;
4. recompute manifests and checksums after source fixation;
5. run the independent/checker path and preserve its raw output;
6. compare full claim rows and adverse-language counts against the parent;
7. record why every changed digest moved;
8. keep the earlier artifact as historical if it carried a distinct terminal;
9. do not edit thresholds, outcomes or protocol identities while performing a prose correction;
10. merge only after current-head CI and paper-specific content binding are green.
