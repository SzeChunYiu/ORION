# ORION-P2 package license and restrictions

Repository-level license is `CANNOT_CHECK` (no root `LICENSE`).

Third-party constraints that this package must not violate:

- AutoResearchBench released data is a separately distributed artifact; this package does not bundle the decrypted benchmark file. Mirrored official evaluator outputs may contain task-level reference identifiers and are retained only for audit under the upstream benchmark terms.
- SAGE corpus/evaluator are unavailable and must not be substituted as “official”.
- MetaSyn probe archives evaluator-side metrics and FN ledgers, not upstream review full-texts.
- OpenAIRE/Crossref mirrors contain the public candidate capture, public provider responses, and evaluator artifacts; no evaluator gold entered candidate custody.
- Offline complete-gold under `evidence/offline_gold/` is the repository-owned synthetic denominator for the controlled companion.

Cover-letter draft: `notes/submission/COVER_LETTER_DRAFT.md`. It must not be sent until the manuscript matches the claim ledger.
