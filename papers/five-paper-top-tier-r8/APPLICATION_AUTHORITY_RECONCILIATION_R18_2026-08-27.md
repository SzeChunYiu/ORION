# R18 application authority reconciliation — 2026-08-27

This additive audit is cut directly from the live coordination anchor
`codex/five-paper-top-tier-r8-20260826@6bf15e3e950c645e7bc383203567149d53b2db29`.
It does not import protected Task-3 paths, rewrite historical receipts, or grant scientific authority to results merely because they exist on draft children.

The purpose is to make the current manuscript/application claim boundary explicit after the latest Codex/LUNARC/application receipts. Every statement below is bounded by the cited draft/issue evidence and should be read as a promotion ceiling, not a merge instruction.

## Executive disposition

The portfolio remains **OPEN**. There is meaningful new evidence, but it separates into three different classes that must not be conflated:

1. exact finite/theorem advances with strong hostile controls;
2. prospectively frozen application results, including adverse/null results; and
3. engineering/replay infrastructure that has not yet earned the intended numerical or external-domain authority.

The strongest new application-side positive evidence is FiberGuard R15's prospectively frozen two-of-three non-SAT timeout/tail transfer. The strongest new application-side refutations are FiberGuard R14's robust held-out failure and R16's failure of a marginal regret certificate to induce useful routing. AB's production-transfer attempt remains correctly rejected on move-registry completeness. D now has a sharper first-mixing theorem plus a real SAFE single-slot Agentgateway control, not a deployed unsafe result. NQ still lacks the proof-clean full independent D2/D3 census. Q1 still lacks a matched architecture/resource win against current compilers and still lacks a clean proof-independent Q1-B finite attack.

## Per-lane authority ledger

### NQ — independent replay remains the scientific gate

Accepted increments:

- #1425 materializes a structurally distinct Engine-B and passes the frozen 61-case rank-two discriminator with zero status/canonical mismatches.
- #1439 binds an external `drat-trim` checker and verifies a bounded UNSAT engineering control.
- #1453 provides an independent full-bundle verifier that recomputes normalization/canonical representatives/partition coverage and invokes the pinned external proof checker.
- #1463 proves a universal rooted completion/packing duality and Boolean-lattice certificate architecture. Its universal algebra is independent of any `D_k` value.

Authority ceiling:

- no complete 98,622/230,983 independently generated CR-B subject bundle has been consumed;
- no full LUNARC D2/D3 replay terminal exists;
- the `C_5^3` numerical specialization of the rooted/completion theory assumes `D_3(C_5^3)=25` and is therefore **post-replay consistency/search structure only**, never a pruning or acceptance condition for the computation intended to establish that value;
- `D_4(C_5^3)`, novelty, and journal authority remain open.

The next admissible NQ computation is exactly the proof/outcome-clean full CR-B census already frozen on #1383. Another pilot or a renamed replay of completed job `3542994` is nonresponsive.

### AB — theorem survives; production transfer remains rejected

Accepted increments:

- #1394 establishes the explicit XOR-grammar 5-to-1 realized certificate gap with a complete grammar-defined move registry and exhaustive critical-overlap controls.
- the all-finite-abelian-group extension and quantitative terminal-interaction layer strengthen the abstract theory, under their explicit completeness premises.
- LUNARC job `3543875` exhaustively replayed the frozen R6M n=2 production-transfer panel with 31,457,280 candidate evaluations and zero DP/brute-force disagreements.

Authority correction:

The authoritative production-transfer terminal remains
`FINITE_PRODUCTION_REALIZATION_CERTIFICATE_REJECTED`, with blocker
`PRODUCTION_REGISTRY_NOT_DECLARED_COMPLETE`.
An optimizer transition is not automatically a legal shortening rewrite. Empty or incomplete move lists cannot support a production certificate-waste statement.

The next AB application must prospectively bind one real executable parity/Boolean/equality-saturation registry, its extensional semantics and objective, omission-hostile completeness checks, independent semantic checking, null cases, and matched native/direct-enumeration baselines. A further abstract group enumeration cannot close this bridge.

### FiberGuard C — the application story is now explicitly non-monotone

The application evidence must remain separated by estimand and quantifier.

**R11 complete-corpus/transductive:** the exact same-corpus static `{Pre,lobjois}` result is a real finite decision-value result under one statewise-oracle accounting, but it is transductive.

**R14 prospective held-out:** exact equality does not transfer as an inductive selector. Exact-signature recurrence is only 3.22% under official CV and 5.08% under zero-family-overlap; the frozen 16-NN reference is materially better. A separate robust R14 gate also fails against no features even though mean excess improves. This is a quantifier/coverage refutation, not a refutation of feature usefulness.

**R15 untouched non-SAT transfer (#1462):** the prospectively frozen failure-aware selector passes both registered splits on ASP-POTASSCO and GRAPHS-2015, but fails on CSP-Minizinc-Time-2016, which remains in the denominator. Scientific terminal:
`C_MULTIDOMAIN_CATASTROPHE_TAIL_VALUE_TWO_OF_THREE`.
This establishes finite two-of-three timeout/tail value, not a universal worst-case guarantee or learned-selector superiority.

**R16 cross-scenario marginal certificate (#1461):** one prospectively frozen configuration is carried across SAT16-MAIN, SAT18-EXP and untouched SAT20-MAIN. The learned model itself has value and the registered marginal false-certificate rates stay below the frozen ceiling, yet abstention/routing harms SAT16 and SAT18 because the fallback is conditionally worse on the rejected subset. The terminal
`FIBERGUARD_R16_NO_PORTABLE_CERTIFICATE_VALUE`
is a substantive refutation: marginal action-regret calibration alone does not certify selective decision value.

**R17 fallback-alignment theory (#1465):** the exact identities
`E[S-L]=P(reject)E[F-L|reject]` and the analogous catastrophic/robust decompositions correctly isolate the missing obligation. Paired learned/fallback upper certificates can certify the selected route; a learned certificate alone cannot bound fallback harm. Feature acquisition cost is refunded only when routing occurs before the acquisition. Registered terminal:
`FIBERGUARD_FALLBACK_ALIGNMENT_R17_PASS`.

Current C promotion ceiling:

- no paired fallback certificate has yet been executed on ASlib;
- a no-feature fallback selected after feature acquisition does not erase sunk representation cost;
- strongest current learned algorithm-selection baselines, independent replay, domain-expert family splits, external production evidence, novelty and journal authority remain open;
- current reject-option/conformal/learning-to-defer literature owns generic abstention, risk-coverage and cost-aware routing ideas. The residual must stay scoped to the exact common-oracle/state-dependent-acquisition integration, prospectively preserved sign reversal, and auditable paired-route certificate architecture.

Next C experiment: freeze a **paired-route** protocol before outcomes. For each route action, fit/calibrate the corresponding loss certificate on proper-training/calibration data; compare pre-acquisition and post-acquisition routing separately; include the R16 frozen learned model, robust fallback, a standard learned selector, a frozen nearest-neighbour baseline, and at least one independently replayed non-SAT/production corpus. The primary result must report route-conditioned fallback-minus-learned loss, catastrophic error, mean/tail/robust loss, feature cost actually paid, coverage, and certificate failures. A positive marginal certificate with harmful fallback is a failure terminal, not a partial pass.

### Typed authority D — localize mixing, do not manufacture a vulnerability

Accepted increments:

- #1402 remains a standards-grounded synthetic hostile discriminator: typed coordinate preservation agrees with the frozen expected decisions; coordinate erasure yields 9/14 false authorizations on that synthetic corpus.
- #1413 supplies an exact typed-authority-to-SET-COVER reduction for Minimum Safe License Basis, while classical SET COVER complexity/approximation remains donor-owned.
- #1466 proves exact singleton-origin witness propagation for finite acyclic positive Horn systems and localizes every hybrid authorization to a first-mixing rule. Its 10,192-system audit contains 68 hybrid atoms and 68/68 local certificates; 2,000 unary origin-preserving controls produce zero hybrids.
- pinned `agentgateway/agentgateway@e136c7458b0fe0f51378dd31ffd60ab2b6939fc2` is a real **SAFE/null** control for the audited JWT-to-CEL bridge: one validated token is exposed as one typed JWT slot, not as a coordinatewise union of arbitrary token claim maps. Native rule-set merge safety remains unchanged.

Authority ceiling:

Generic Horn/Datalog provenance, why/where witnesses, provenance semirings, explanation graphs and bitset data-flow are donor-owned. Current Agentgateway documentation also presents authorization rules over a validated `jwt` context rather than an arbitrary cross-token claim union. The residual must therefore stay narrow: singleton-origin authorization semantics, the distinction between a mixed proof and absence of any complete one-origin proof, first-mixing localization, and typed bridge-safety discrimination.

The top-tier systems gate is still an independently maintained integration in which multiple real evidence records can reach one decision under both origin-preserving and coordinate-erased implementations, with cases and domain adjudication frozen before outcomes. Another same-owner synthetic corpus does not close this gate.

### Q1 — normal-form theorem is useful; resource value remains unearned

Accepted manuscript/theory increments include support-two sufficiency, the support-one sharpness control, the common auxiliary frame/Tag union `|U|<=9` with Restore support explicitly allowed outside the core, and the exact active-core counting/checker result.

Two authority gates remain decisive:

1. Q1-B/#1448 is **bounded corroboration only**, not a clean proof-independent finite attack, because its source manifest exposed the authoring lane to the registered proof. The proof-free Q1-B2 repair remains required.
2. #1449 is a `PARTIAL_RESOURCE_MAP`: it establishes conditional logical-count identities under a declared parity-ladder template but does not bind routing/cancellation, logical depth, precision, T synthesis, code distance, factories, physical qubits, spacetime or failure probability. Its primary QG-21 chemistry setting is adverse (90/90 donor ties; only the registered sensitivity slice improves 18/90 by two logical two-qubit Cliffords).

A current prior-art spot check reinforces the ceiling: Symphony, arXiv:2608.11579 (submitted 2026-08-12), performs global binary-symplectic controlled-Pauli Clifford simplification and reports large HamLib two-qubit-gate/depth reductions. The public PHOENIX repository exposes Symphony as its newer holistic compiler. Therefore bounded auxiliary support alone cannot support a broad compiler-resource headline.

The next Q1 application is the already-specified architecture-level matched benchmark: freeze Hamiltonians, connectivity/routing, precision/synthesis, ancillas/scheduling/cancellation and semantic comparability before outcomes; compare donor/original TARE when faithfully runnable, unrestricted R6M, support<=2, support<=1 RED control and a current Pauli compiler such as Symphony/PHOENIX. Null or baseline-better outcomes remain admissible terminals.

## Branch-custody reconciliation

The live coordination branch is still exactly `6bf15e3e950c645e7bc383203567149d53b2db29`.

Several historically important R9 drafts were cut from the older base `b598a7ba0806a06ec46581875f20b20106cbfe55`, including #1392, #1394, #1402 and #1413. Their scientific artifacts remain reviewable, but those branches are **not current canonical integration custody**. Any manuscript promotion must rematerialize only the owned paths/receipts on a clean child of the live anchor and verify immutable hashes rather than silently rebasing claims.

#1419 is already a clean application-programme child of `6bf15e3e...`, but it predates the authority-changing R14/R15/R16/R17 and D-R12 evidence. This R18 reconciliation is therefore additive and deliberately does not mutate #1419's historical registry.

## Current-primary-source boundary spot checks

These checks narrow, rather than certify, novelty:

- **Q1:** Symphony (arXiv:2608.11579, 2026-08-12) is a current holistic BSF/controlled-Pauli compiler with strong HamLib gate/depth results; any Q1 resource claim requires a matched semantic and architecture comparison.
- **C:** 2025–2026 reject-option/conformal and learning-to-defer work already studies calibrated rejection, cost-aware deferral and uncertainty in the rejector. Generic abstention or conformal routing is therefore not the contribution.
- **D:** modern Datalog provenance work continues to treat witness/proof provenance as established machinery; Agentgateway's own current documentation presents one validated JWT context to CEL authorization. Generic provenance propagation or "JWT authorization is unsafe" is not a defensible novelty statement.

A full specialist novelty adjudication remains separate for every paper.

## Cross-paper publication gate

No cross-paper strong application terminal is earned yet. Before a portfolio-level control-plane headline is admissible, at least two component lanes still need externally maintained, prospectively frozen task evidence under matched budgets **and** their own theorem/bridge authority gates must be independently satisfied.

Until then, preserve the exact current story rather than optimizing it away: NQ has stronger replay machinery without numerical replay; AB has a strong abstraction theorem plus a production-specification rejection; C has both real positive transfer and prospective refutations; D has a stronger localization theorem plus a real safe bridge control; Q1 has a useful normal form without production-resource victory.