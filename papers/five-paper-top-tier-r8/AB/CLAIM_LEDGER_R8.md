# Consolidated AB Claim Ledger R8

| ID | Claim | Evidence class | Boundary |
|---|---|---|---|
| AB-C1 | Persistent sound, non-increasing zero-signature deletion yields an optimum of support at most `zsf(H;A)`. | `VERIFIED` | Domain must prove deletion closure, semantic preservation, and dominance after every prior edit. |
| AB-C2 | Proper zero-sum deletion has exact maximum terminal length `zsf(H;A)`. | `VERIFIED` | Abstract nonzero-total word universe with `A` containing at least one nonzero element. |
| AB-C3 | A matching production certificate lower bound requires a realized maximum terminal word and irreducibility under the complete named move set. | `VERIFIED` | No production lower bound without the realization gate. |
| AB-C4 | Axis-separated alphabets have additive `zsf` budgets. | `VERIFIED` | Letters must lie on one declared axis. |
| AB-C5 | Terminal budgets add over connected components of the complete semantic interaction hypergraph. | `VERIFIED` | Read, test, guard, and write dependencies all count. |
| AB-C6 | Standard cyclic-axis alphabet has budget `sum_i(n_i-1)`. | `VERIFIED` | Standard independent generators only. |
| AB-C7 | Homomorphic images give lower obstructions; quotient/kernel and kernel-weighted formulas give upper caps. | `VERIFIED` | The upper bounds assume nonempty `A`; source-realizable block sums and exact alphabets are required, with `Z_omega=0` when no nonzero kernel sum is realized. |
| AB-C8 | Event- and coordinate-defect bounds give sparse feasible states with explicit additive objective allowance. | `VERIFIED` | The coordinate bound requires `delta>=0` and a deletion-only descent without coordinate reactivation; neither bound is a physical-resource claim. |
| AB-C9 | The dependent-triple Pauli grammar has intrinsic support one under whole-system Tag reconstruction. | `VERIFIED` in the separately declared grammar | Not asserted for TARE or another production compiler. |
| AB-C10 | The XOR-aggregation production grammar has weak terminal budget `d` and strong production budget `1`. | `VERIFIED`; bounded controls `FINITE_EXACT` | Complete grammar by definition; external compiler significance remains open. |
| AB-C11 | For a direct labeled-support enumerator, certificate waste changes the polynomial degree by `beta-kappa` and has the stated leading constant. | `VERIFIED` | Fixed local budgets are required; blockwise ratios multiply only for a Cartesian-product block enumerator. Not a complexity-class lower bound. |
| AB-C12 | The abstract five-bit Pauli terminal is a production certificate lower bound. | `NOT_CLAIMED` | Realization and full production irreducibility were not established. |
| AB-C13 | The broad idea that a stronger cancellation/XOR production language can beat a restricted XOR language is new. | `NOT_CLAIMED__DONOR_ABSORBED` | Boyar–Matthews–Peralta and Find–Boyar already establish such gaps. Novelty of the distinct destructive AB multiset contract remains `CANNOT_CHECK` because no external bidirectional representation/move/cost map has passed. |
| AB-C14 | Structural support savings imply T count, depth, runtime, or quantum advantage. | `FORBIDDEN` | No validated physical-resource map. |
| AB-C15 | Generic zero-sum, quotient, graph decomposition, rewrite-system, or weak-versus-strong XOR-separation ideas are new. | `DONOR_OWNED` | Any residual is limited to the calibrated certificate pipeline and exact grammar-specific statements; external significance and novelty remain unestablished. |
