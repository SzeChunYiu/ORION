# Internal Expert-Challenge Log R8

The entries below are adversarial perspectives, not claims of independent human review.

## Additive-combinatorics challenge

**Finding.** The NQ headline should move from the unresolved `D_4` bit to the exact early constants and their structural reduction. The exact `D_2` and `D_3` claims are scientifically meaningful only if the search space reduction and normalization are independently replayed. The 27-diagonal repeated-stratum census is not a full obstruction classification.

**Action.** Freeze clean-room engines and a lift-aware source-level solver. Keep every quotient and rank-two donor theorem explicitly attributed.

## Rewrite/algorithms challenge

**Finding.** Papers A and B were measuring two layers of one problem. Separating them inflated overlap and left each paper with an incomplete identity. The production-realization gate and normal-form theorem belong together.

**Action.** Consolidate A+B. Add a realized non-Q1 production separation and define interaction components using the complete semantic support of moves, not nominal module boundaries.

## Decision-theory/ML challenge

**Finding.** The original C manuscript had a strong exact construction but only one native domain. General minimax corollaries alone are classical.

**Action.** Build complete collision panels for graph colouring, set cover, and 2-CNF, with independent exact targets and a frozen feature-refinement comparison. Do not infer distributional prevalence.

## Datalog/formal-methods challenge

**Finding.** Generic minimal supports, blockers, robustness, and hardness overlap recent recursive-Datalog work. A typed system also needs an explicit account of unsafe coordinate merging; otherwise aliases can manufacture paths that neither coordinate supports separately.

**Action.** Withdraw generic novelty. State and test the exact safe-merge criterion. Require a real policy encoding before a systems-impact headline.

## Adversarial-verification challenge

**Finding.** Re-running the same code with another seed is not independent verification. A solver that only returns positives or only rejects is unsafe without negative controls. Completeness claims require range/orbit manifests.

**Action.** Require structurally different algorithms, primitive-semantics replay, controls of both polarities, immutable hashes, and resource-bounded terminals.

## Academic-editor challenge

**Finding.** “Top tier” cannot be created by adding applications to narrow model theorems. Each paper needs one coherent scientific identity, a contribution that survives nearest-work subtraction, and evidence matching its venue.

**Action.** Retain Q1, consolidate AB, elevate C through the exact multi-domain benchmark, narrow D to typed merge safety pending a real case, and reframe NQ around exact early constants rather than an unresolved promise.
