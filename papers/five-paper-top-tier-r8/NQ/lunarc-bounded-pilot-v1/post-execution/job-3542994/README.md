# NQ Engine-A bounded LUNARC pilot — job 3542994

This directory is the minimum repository subset for the terminal-completed engineering pilot:

- SLURM job `3542994`: `COMPLETED`, exit `0:0`, elapsed `00:00:15`;
- account `lu2026-2-51`, scheduler-selected partition `lu48`;
- frozen source `ce3ad440337c1bd413a8e5202c94a67374721403`;
- bound submit script `c11e46db77f48c8cb1674cea5a098bec8b2e8f32`;
- authorization packet SHA-256
  `9d3c06ef2ad4bb1ba4c0e0b1ed6a8c623431758cbd47914aeeb4f8a6d15252bf`;
- non-duplication key
  `5bbd43879aedf49bf9ac5e80ee1cc7b5b7f835675c5850e186b2de6b95f62307`.

The copied accounting, submission, run metadata, terminal, and pilot receipt files are byte-identical
subsets of the immutable collected bundle. `POST_EXECUTION_RECEIPT.json` binds those files to the
repository authorization packet and states the claim ceiling. `SHA256SUMS` is the subset manifest.

## Result boundary

The checkpoint processed 255 candidate edges in 37 resumable invocations, emitted 95 records, and
matched uninterrupted execution byte-for-byte. All 16 target-panel factorization calls returned
`NEGATIVE`. Those cases used an expected-outcome-exposed permitted lower witness, so they remain
engineering observations only with scientific and independence terminal `CANNOT_CHECK`.

This was not a full census and did not add a second engine:

- `full_census_executed=false`;
- `two_engine_pass_increment=0`.

It is not independent replay, a scientific negative, verification of `D_2` or `D_3`, a result on
`D_4`, novelty evidence, or paper/journal authority. The frozen prospective protocol and earlier
local outcomes are unchanged.
