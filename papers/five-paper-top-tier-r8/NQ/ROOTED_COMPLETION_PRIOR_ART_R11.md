# NQ R11 prior-art subtraction — rooted completion and factorization certificates

Date: 2026-08-26

Status: targeted primary-source positioning, not an external novelty opinion.

| Source | Donor contribution | R11 subtraction |
|---|---|---|
| Bashir, Geroldinger, Zhong, *On a zero-sum problem arising from factorization theory* (arXiv:2007.10094) | Standard sequence monoid, zero-sum atom, block-monoid, set-of-lengths, and transfer-Krull language; structural problems for maximal atoms. | No generic factorization or block-monoid language is claimed as new. |
| Zhong, *On the inverse problem of the k-th Davenport constants for groups of rank 2* (arXiv:2503.21231) | Definition and inverse structural study of `D_k`, including exact rank-two extremal sequences. | The inverse-`D_k` programme and rank-two classifications are donor-owned; R11 does not compete with those classifications. |
| Ordaz, Philipp, Santos, Schmid, *On the Olson and the Strong Davenport constants* (JTNB 23, 2011) | Zero-sumfree and minimal-zero-sum extremal invariants, including computer-aided finite examples. | Bounds through zero-sumfree residues and ordinary Davenport constants are donor theory. |
| Henning, Yeo, *Matching critical intersection hypergraphs* (Quaestiones Mathematicae 37, 2014) and related matching-criticality literature | Generic matching-number and criticality concepts in hypergraphs. | The phrase matching-critical and generic hypergraph theory are donor-owned. |

## Search result and conservative residual

Targeted searches for one-term completion by `-sigma(M)`, exact packing increment, and a rooted maximum-packing/factorization bijection did not return an exact match. This is insufficient to establish novelty because monographs, non-arXiv papers, and differently named factorization lemmas may subsume the observation.

The paper-specific residual under review is:

1. the universal exact identity `nu(M(-sigma(M)))=nu(M)+1` with a distinguished occurrence;
2. the bijection between maximum source packings and rooted maximum atom factorizations of the completion;
3. the induced Boolean lattice of exact packing subinstances; and
4. the rooted residue-length certificate used to partition a subsequent computer-assisted `C_5^3` D4 search.

The universal algebra is elementary. The likely publication value lies in the certificate architecture and its integration into an independently replayable proof, not in claiming a deep new zero-sum theorem.

## Required specialist search

Before novelty language, inspect:

- monographs on non-unique factorization and block monoids;
- inverse generalized-Davenport papers beyond rank two;
- literature on maximal zero-sum packings and residual sequences;
- matching-critical vertices in algebraically defined hypergraphs; and
- computer-assisted zero-sum proofs using rooted or unmatched-residue certificates.

The current terminal is `NOVELTY_NOT_ESTABLISHED`.
