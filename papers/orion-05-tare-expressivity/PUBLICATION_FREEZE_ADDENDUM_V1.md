# ORION-05 publication-freeze addendum V1

**Freeze date:** 2026-08-31  
**Status:** `CURRENT_EARNED_CEILING_FROZEN__PRODUCTION_VALUE_NULL__NEW_IDENTITY_SUCCESSOR_ONLY`

This addendum is part of the frozen ORION-05 paper-content packet. It records the
ceiling `CLAIM_LEDGER_V3.md` already establishes and the null the round accounting
already records, and it grants no authority beyond them.

## Earned scientific ceiling

The submission terminal is `READY_TO_SUBMIT_SECOND_TIER`, target venue *Quantum*
with npj Quantum Information as fallback. PRX Quantum was assessed and declined on
the significance gate. As the readiness record puts it, "second tier" names the
venue tier reached, not a defect in the work.

What is earned is a theorem, and it is grammar- and objective-specific. For the
frozen R6M three-block shared-one-bit-Tag TARE-M2 grammar under the frozen
support-count objective, every admitted instance at arbitrary `n` has an exact
optimum whose auxiliary frame Pauli support is at most two (`Q1V3-1`, carried by
the analytic proof in `HUMAN_PROOF_R6S_2026-08-22.md` with an R6S machine receipt).

The threshold is sharp rather than merely sufficient: on the frozen two-qubit
instance the complete support-one family costs 6 against an unrestricted optimum of
5, giving `kappa_R6M = 2` (`Q1V3-2`). The mechanism behind the threshold is a
weight-two parity obstruction exchanged for a frame-for-Tag optimum (`Q1V3-3`).

## Frozen boundary

**The production-value question was asked prospectively and answered null.** Round 2
compared the support-two solver against the unrestricted 512-state R6M referee on an
identical panel and returned
`ORION05_R12_EXACT_BUT_NO_PRODUCTION_VALUE`. `full_subject.support_two_complete` is
`false`. Splitting the 120 recorded attempts by arm is unambiguous: **all twelve
timeouts belong to the obstruction-guided arm, and the unrestricted referee
completed every attempt it was given.** Every `support_two` median is null. No
search reduction was demonstrated.

That null is frozen with the theorem, not filed away from it. The paper's own
`round_accounting` reads `consumed: 2, maximum: 3` with `science_status: OPEN`, and
the protocol permits the remaining round only for a distinct, predeclared
safe-ordering/parallelism mechanism — it explicitly does not permit parameter
retuning of this benchmark.

A re-measurement under a work metric rather than a wall-clock metric is **not
available as a repair**, and the reason is in the data rather than in policy: the
two arms record `planned_states` in incommensurable units — `frame_pair_triples`,
`ordered_anticommuting_pairs` on one side against `dp_transition_candidates_upper`,
`parity_states` on the other. There is no common work unit to compare, so collapsing
them to a single number after seeing outcomes would be inventing a metric, not
applying one. A states-primary comparison needs a new frozen identity outside the
R12 round budget.

Two further boundaries hold. The `O(n^12)` support-two frame candidate count is a
representation-count corollary and **not a DP runtime claim** (`Q1V3-4`). The
registered finite checks on the frozen benzene subject corroborate the structural
understanding but do not establish the all-`n` result (`Q1V3-5`).

Where the basis was tested it survived: the global obstruction basis census reached
full coverage at 33,755 of 33,755 instances, supporting `O05-C3` and falsifying
`O05-C2`. No new obstruction appeared, so the conditional that would have forced a
rescue basis never fired.

## Frozen content surface

The content packet consists of the canonical manuscript `MANUSCRIPT_V3_REFINED.md`,
`CLAIM_LEDGER_V3.md`, `FIGURE_CONTRACT_V3.md`, the R6S human proof and receipt, the
R12 protocol with its frozen decision rule and its result and raw attempts, the
global obstruction basis census, the submission directory, and this addendum.

Two custody facts belong with the freeze. This paper's committed PDF is a **curated,
de-identified artifact** whose title and author deliberately differ from its source;
it must never be rebuilt from that source, and it is excluded from the render path by
name. And the package-currency baseline records 40 entries for this paper as measured
debt arising from a path-convention difference, not as approval — reconciling it is
the package owner's call, not a re-pin.
