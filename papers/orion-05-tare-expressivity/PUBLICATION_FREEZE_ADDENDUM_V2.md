# ORION-05 publication-freeze addendum V2 (successor routing/status record)

**Date:** 2026-09-02
**Status:** `ROUTING_AND_STATUS_SUCCESSOR__SCIENCE_CEILING_UNCHANGED`
**Supersedes:** `PUBLICATION_FREEZE_ADDENDUM_V1.md` (2026-08-31) for publication routing and
bookkeeping only. The V1 earned ceiling (the grammar- and objective-specific support-two
theorem `Q1V3-1`, its sharpness `Q1V3-2`, the mechanism `Q1V3-3`), the frozen
production-value null (`ORION05_R12_EXACT_BUT_NO_PRODUCTION_VALUE`), the `O(n^12)`
representation-count boundary, and the frozen content surface all remain binding and
unchanged; V1 is not edited. This addendum grants no new claim authority
(`paper_authority_delta = NONE`).

## 2026-09-02 decisions recorded

1. **Companion filing on the QIC-class route (issue SzeChunYiu/ORION-paper#78, decision
   (b)).** ORION-05 files **separately** from ORION-01 on the same route: arXiv `quant-ph`
   preprint first, then a quantum-information-and-computation class journal submission. This
   supersedes for routing only (a) the 2026-09-01 absorption decision recorded in
   `papers/orion-01-certificate-realization/CLAIM_LEDGER_V4.md` ("ORION-05 is absorbed into
   this submission rather than filed separately") and
   `papers/orion-01-certificate-realization/submission/tier-b-closure-20260901/ORION05_MERGE_COVERAGE_AUDIT.md`
   ("standalone package is retired and must not be submitted"), and (b) the V1 addendum's
   *Quantum*-target readiness terminal for routing purposes. It does not reopen the
   production-value null or any frozen negative.
2. **Merge-coverage audit (2026-09-02).** Every V4-claim ledger entry (O5-P1..O5-P10) was
   audited against ORION-01's canonical V4 surfaces:
   `papers/orion-05-tare-expressivity/MERGE_COVERAGE_AUDIT_20260902.md`. O5-P1, O5-P2 and
   O5-P9 are covered in ORION-01 V4; O5-P3, O5-P8 and O5-P10 partially; O5-P4, O5-P5,
   O5-P6 and O5-P7 are not covered there (the latter two routed to the merged ORION-09/10
   object). Because ORION-05 files separately, these omissions are acceptable: the uncovered
   claims remain owned by this paper's own manuscript and ledger or by ORION-09/10.
3. **Single-author limitation sentence (cross-cutting requirement) — open item.** The
   canonical manuscript (`manuscript/sections/06-limitations.tex`) does not yet carry the
   same-programme single-author limitation sentence. The sentence to insert (owner: the lane
   that may edit `manuscript/`): "This work belongs to a single-author research programme
   using AI assistance, so the verification described here is author-side throughout and
   lacks the independent perspective of multi-author or externally replicated work."
   `MANUSCRIPT_V3_REFINED.md` is part of the V1 frozen content surface and was not edited.
4. **ORION-01 B-F4 narrowing (2026-09-02).** The companion paper's abstract was narrowed to
   the one-rule rank-only scope with the true-by-construction status explicit; no claim was
   widened there, and nothing in this paper's ceiling changes as a result. Record:
   `papers/orion-01-certificate-realization/PUBLICATION_FREEZE_ADDENDUM_V2.md`.

## Filing prerequisites before this paper is filed

- Insert the single-author limitation sentence (item 3).
- Re-verify the package against this paper's own ledger (`CLAIM_LEDGER_V4.md`) and the
  V1 frozen-content rules (the curated de-identified PDF must never be rebuilt from its
  source and stays excluded from the render path by name).
- Literature refresh on the submission day (the O5-P10 bounded-search statement must be
  current at filing).
