# ORION-10 publication-freeze addendum V1

**Freeze date:** 2026-08-31  
**Status:** `CURRENT_EARNED_CEILING_FROZEN__EXPLANATION_LAYER_OPEN__SUCCESSOR_ONLY`

This addendum is part of the frozen ORION-10 paper-content packet. It records the
ceiling already established by the paper's evaluator and hybrid-family artifacts,
and grants no authority they do not already carry.

## Earned scientific ceiling

The submission terminal is `READY_TO_SUBMIT_SECOND_TIER`, target venue *Quantum*.
Quantum Science and Technology was considered under the routing's impact condition
and is not warranted; the paper says so rather than routing upward on hope.

The claim hierarchy is explicit and is stated in this order: **theorem above finite
benchmark above unresolved explanation.** The headline numbers are re-derived from
the evaluator and hybrid-family artifacts — 9,547 compared instances at zero error,
64 witnesses, and 10,481 compared instances. The audited claim delta is
`scientific_authority_delta = NONE`.

## Frozen boundary

The explanation layer is **refuted, and the refutation is retained as a headline
result rather than buried**. The explanation itself stays open. That combination is
the honest state of this paper: the theorem and the finite benchmark stand, and the
account of *why* the forecast works does not.

The scope is bounded to the frozen grammar and objective. **No physical
quantum-advantage claim is made**, and none may be read out of the certificate
computation or the zero-error benchmark.

Content binding is recorded as `UNBOUND / CANNOT_CHECK`, unchanged from baseline and
unchanged by this freeze. That is a stated gap in coverage, not a pass: the bytes of
this paper are not currently watched by a binding, so a future change to them would
not be caught by that mechanism.

A new primitive would have to specify its mechanism first and freeze it before
testing, and any practical bridge to native compilers is secondary evidence.
Both are successor work and must not retroactively promote the present bounded
result.

## Frozen content surface

The content packet consists of the canonical manuscript designated in
`submission/CANONICAL_SOURCE_DECISION.md`, the evaluator and hybrid-family result
artifacts behind the three headline numbers, the submission directory (venue
decision, manifest, cover letter, availability statement), and this addendum. The
ORION-10 claim is about theorem-backed static cost forecasting with refutable
explanations; it does not own the compilation families it forecasts over.
