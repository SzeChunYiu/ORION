# Fallback venue record — ORION-09+10 (Quantum filing)

**Date:** 2026-09-02 (frozen before any decision letter, ORION-paper issue #78)
**Status:** frozen; no decision letter exists at freeze time
**Authority:** unbound routing record at the top level of `submission/` (same
pattern as `CANONICAL_SOURCE_DECISION.md` and orion-08's
`SUCCESSOR_ROUTING_20260902.md`), deliberately outside every package
`SHA256SUMS` manifest. It adds no claim and modifies no frozen surface.

## Primary route (unchanged)

arXiv `quant-ph` (optional `cs.DS` cross-list) first, then the Quantum journal,
per `submission/VENUE_DECISION.md` and the `tier-b-final-20260901/` package.

## Frozen fallback

**QIC-class venue** (Quantum Information & Computation and equivalent
specialist quantum-computation journals).

- A rejection at Quantum routes to the QIC-class fallback with the **same
  honest scope, byte-identical package surfaces, and unchanged claims** — the
  2026-09-02 lane work (full induction writeup for the envelope composition,
  exact 9,546/9,547 counts, S-M3 significance paragraph, nearest-work refresh)
  is venue-independent referee-risk reduction.
- **No post-rejection re-scoping.** Re-scoping requires re-running the
  paper-existence gate from `academic-paper-skills#32` (standing rule, issue
  #78).
- A revise-and-resubmit at Quantum is handled in place; this fallback governs
  outright rejection only.

## Why frozen now

Per issue #78: fallback venues are frozen **before** any decision letter so a
rejection cannot become an occasion for re-scoping. This file closes the one
Tier-B object whose tree previously recorded no fallback destination.
