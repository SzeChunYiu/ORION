\section{Conclusion}

A static compiler forecast can be exact without having a simple exact explanation, and a complete finite explanation can fail to transfer.  In the shared-tag model, a prospectively successful compact formula is refuted by one exact fresh instance.  A support-two theorem repairs the cost layer for every admitted size, while later hostile censuses refute the first repaired explanation.  A different per-block-permutation proof then closes the remaining commuting-support-two sector without laundering the failed alternative proof routes.

Weighted Clifford state preparation supplies the cross-model boundary within the formal programme: four features separate every complete $n\le3$ state, yet the frozen $n=4$ test remains adverse and the proposed sign mechanism is unsupported.  The strongest supported conclusion is therefore a certificate separation---exact cost, executable implementation, structural explanation, row-level verification and transfer are different scientific objects.  Keeping them separate makes negative results informative and prevents finite accuracy from being mistaken for proof or generality.
