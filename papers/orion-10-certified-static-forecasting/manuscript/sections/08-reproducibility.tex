\section{Reproducibility}

The primary chain is committed under \texttt{research/extensions/orion-qg/} with frozen protocols under \texttt{development/orion-qg-regime-geometry/}. QG-3 supplies prospective staging; QG-5 supplies the refuted $F_1$ benchmark; QG-5b supplies F2 and the enlarged-borrow repair; QG-7/QG-7b/QG-7c supply the hostile explanation stress tests. R6S under the parent ORION-Q tree is the all-$n$ cost theorem.

A reproduction must preserve the chronology. It should first re-establish the old forecast failure, then verify the theorem-backed F2 result, then independently reproduce the later $B'$ explanation refutation. Collapsing those stages into a final zero-error table would erase the scientific evidence that the certificate separation is meant to expose.

The root \texttt{REPRODUCE.md} lists the required checks and the distinction between direct referee rows, theorem-backed rows, and unverified prediction rows.

The complete fibre audit is committed under
\texttt{theory/\allowbreak vocabulary-\allowbreak minimality-\allowbreak
v2-\allowbreak full-\allowbreak census/}. Its runner serializes all
740 rows while reproducing the frozen 64-witness control exactly. The findings
and result JSON bind the six-of-seven mixed-fibre terminal and the selected-view
comparison.
