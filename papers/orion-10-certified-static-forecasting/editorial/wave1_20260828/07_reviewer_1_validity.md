# Reviewer 1 — validity, methods, inference

Frozen independently before editor synthesis.

Overall: the exactness claim is well scoped and the adverse objective is appropriately visible. The distinction between proof and benchmark agreement is unusually clear.

- **V-M1 (major):** The support theorem relies on a finite local inequality. Resolution test: state the local state space, exact case count, violation count, and how it composes into an all-size descent; provide a separate checker/evidence summary.
- **V-M2 (major):** The headline evaluator count could be mistaken for independent inputs. Resolution test: label 9,547 as comparison events, state 9,546 distinct inputs, and identify the repeated audit event.
- **V-M3 (major):** A changed-objective failure must be visible wherever generality is discussed. Resolution test: include 11 < 13 < 23 and 53 closure failures in Results and Limitations.
- **V-m4 (minor):** State why support two is a true proof boundary rather than an enumeration cap.

Recommendation after tests: technically sound if all four tests pass.
