# Paper Q3 claim ledger V2 — prospective dual-instrument case series

**Parent scientific base:** `main@c5ba39fef4f25c46de5fb69bf07f50530f4693ca`  
**Replacement scientific-result cut:** `codex/q-qg-nature-skills-publication-closure-20260821@ca561ada07301ee7e45fc10e195dce8f077ea50c`  
**Authority:** bounded benchmark/methodology evidence only. No Q/QG scientific result receives authority from Q3.

## Unit of analysis

The inferential unit is one **prospectively frozen frontier question**, not a receipt, matching, DP row, partition, host call, or internal controller state.

Valid scored units:

1. V0 — post-R6O diagnosis;
2. Q3-R1 / QG-19 — R6M outside-cone sharpness probe;
3. Q3-R2 / QG-20 — SixLCU objective-rescale boundary probe.

Retired contaminated candidate units, retained in the audit history and excluded from scored-valid-unit counts:

- Q3-V1 / QG-7d;
- Q3-V2 / QG-15c.

`n_valid = 3`. This sample size does not authorize a reliability estimate, kappa coefficient, calibration claim, or population generalization.

## Frozen claims

### Q3.C1 — heterogeneous instrument definition

Q3 compares two non-equivalent instruments over the same receipt-transcribed unresolved question state:

- Lane A: tool-capable LLM/host research diagnosis;
- Lane B: typed deterministic non-LLM campaign controller.

The instruments are not claimed independent in a statistical/causal sense: packet construction, ontologies and upstream research history are shared. `SUPPORTED_BY_DESIGN`.

### Q3.C2 — pre-outcome chronology

For Q3-R1 and Q3-R2, both Lane-A and Lane-B outputs and pre-outcome agreement receipts were committed before QG19/QG20 scientific analyzers/results existed. The workflow chronology gate passed before execution. V0 already had the corresponding pre-outcome freeze. `PROSPECTIVE_CUSTODY_VERIFIED`.

### Q3.C3 — inter-instrument agreement is an observation, not validation

The two instruments agreed on the primary diagnosed layer and primary next move in each of the three valid units. This is a three-case series fact, not a reliability statistic. `SUPPORTED_N3_CASE_SERIES`.

### Q3.C4 — V0 deferred alignment

V0 diagnosed representation/regime characterization and selected regime characterization as the primary move. Later R6Q found an exact finite-domain predicate; R6P closed the complementary support-two family on its registered domains. V0 is `AGREE + DEFERRED_ALIGNED` under its frozen scoring coordinates. `SUPPORTED_SINGLE_UNIT`.

### Q3.C5 — Q3-R1/QG19 outcome

At frozen objective `O19=(t_nc=4,t_c=3,t_tag=2,t_r=2,rho=0)`, just outside the central QG8 sufficient-cone face, QG19 compared unrestricted weighted R6M DP with exact support-<=2 D++ on 53 frozen rows:

- 3 hostile n=1;
- 2 hostile n=2;
- 24 deterministic random n=2;
- 24 deterministic random n=3.

Result: zero `C_DP < C_Dxx` gaps. Terminal:

`QG19_ZERO_GAP_ON_FROZEN_PANEL__SHARPNESS_REMAINS_OPEN`.

This is exact finite-panel evidence only; it does **not** enlarge QG8's all-n sufficient cone. `SUPPORTED_EXACT_FROZEN_PANEL`.

### Q3.C6 — Q3-R1 deferred score

Both instruments froze `R1_CERTIFICATE_SILENCE_SHARPNESS_OPEN` and `M1_TARGETED_EXACT_OUTSIDE_CONE_PANEL`. The predeclared zero-gap scoring branch assigns R1/M1. Therefore both responsibility and move align for R1. `SUPPORTED_SINGLE_UNIT`.

### Q3.C7 — Q3-R2/QG20 outcome

Under frozen `O20 = 2*SELECT + PREP + WIDTH`, QG20 recomputed the complete frozen SixLCU domains:

- n=1 ordered: 729 instances;
- n=2 reorder-quotiented multiset: 38,760 instances;
- total: 39,489 instances.

The original QG12 P0 predicate had zero mismatches with the reweighted incumbent-exact label on the complete frozen n=1/n=2 domain. There was one P0-positive/incumbent-exact n=2 instance and no n=1 positives under either label. Terminal:

`QG20_P0_ZERO_MISMATCH_ON_COMPLETE_N1_N2`.

This is exhaustive for the declared n=1/n=2 domain but **not** an all-n weighted theorem. `SUPPORTED_COMPLETE_FROZEN_DOMAIN`.

### Q3.C8 — agreement does not imply diagnostic correctness

Before QG20 existed, both instruments froze `S1_P0_BOUNDARY_OBJECTIVE_SCOPED` and selected `N1_COMPLETE_REWEIGHTED_CENSUS`. The predeclared zero-mismatch scoring branch assigns strongest finite-domain alignment to `S2_P0_STRUCTURALLY_INVARIANT_UNDER_SELECT_RESCALE` while still rewarding N1 as the discriminating move.

Therefore Q3-R2 is:

- inter-instrument responsibility relation: `AGREE`;
- Lane-A responsibility alignment: `false`;
- Lane-B responsibility alignment: `false`;
- Lane-A move alignment: `true`;
- Lane-B move alignment: `true`.

The result supports the methodological separation **agreement != correctness** on at least one prospectively scored unit. It does not estimate how often agreement is wrong. `SUPPORTED_EXPLICIT_COUNTEREXAMPLE_TO_AGREEMENT_AS_VALIDATION`.

### Q3.C9 — next-move performance is descriptive only

In all three valid units the frozen primary move was later aligned with the predeclared deferred scoring map. Because `n=3` and the questions are non-random/live-programme cases, this is reported as a three-row case-series fact only. Do not report `100% move accuracy`, a binomial interval, or a generalized success probability. `SUPPORTED_DESCRIPTIVELY_ONLY`.

### Q3.C10 — contamination is part of the measurement protocol

The originally proposed QG-7d and QG-15c slots became prospectively unsafe when result-oriented remote successor branches were visible before instrument freeze. No result file from those branches was opened for scoring; both slots were retired as `CONTAMINATED` and remain visible in the audit history. Replacement questions QG19/QG20 were confirmed branch/result-clean before their instrument freezes. `SUPPORTED_BY_CUSTODY_RECORD`.

### Q3.C11 — D2/D3 are accepted limitations, not repaired away

D2 (successful outer LLM envelope with semantically malformed content) and D3 (successful-malformed receipt pinning deterministic identity) remain known defects of the frozen harness. If triggered, an instance fails closed as instrument-invalid/CANNOT_CHECK. No in-place repair/retry is allowed after an instance becomes outcome-bearing. `LIMITATION_BOUND`.

### Q3.C12 — external novelty boundary

Current nearest work already owns broad delayed-ground-truth agreement gating (ValueBlindBench), LLM-judge meta-evaluation for research agents (REFLECT), agreement-metric methodology, and open-ended scientific-agent benchmarking. Q3's candidate residual is narrower: heterogeneous LLM-host versus typed non-LLM controller outputs frozen on live unresolved scientific frontier questions, with immutable chronology, contaminated-instance retention, and later independently generated scientific-result scoring. No `first` claim is authorized. `NOVELTY_CANDIDATE_BOUNDED`.

## Forbidden interpretations

Q3 does **not** establish:

- general reliability of either instrument;
- that agreement validates a diagnosis;
- statistical independence of Lane A and Lane B;
- human equivalence or superiority;
- scientific correctness of QG claims by vote;
- autonomous scientific governance;
- generic delayed-ground-truth evaluation novelty;
- R6/physical quantum advantage authority;
- defect-free harness operation.

## Final scientific terminal

Provided the completion/replay workflow passes on the committed artifacts:

`Q3_PROSPECTIVE_CASE_SERIES_COMPLETE__N3_VALID__AGREEMENT_NOT_VALIDATION_COUNTEREXAMPLE_OBSERVED__NO_RELIABILITY_GENERALIZATION`.
# Paper Q3 claim ledger V2

**Date:** 2026-08-22
**Status:** updates V1 for current-main instrument behavior and the top-tier prospective study. V1 remains historical.

## Preserved core claims

All V1 claims about deterministic request identity, request/result digest binding, create-only normal receipt persistence, project-root confinement, typed campaign control, protected-reference custody limits, authority non-escalation, the four-cycle R6 drive, and Benchmark V0 remain bounded exactly as in `CLAIM_LEDGER.md` unless superseded below.

## Updated claims

| ID | Maximum permitted claim | Authority | Status / forbidden upgrade |
|---|---|---|---|
| Q3V2-1 | Successful LLM/capability receipts whose content violates the reasoner's task schema no longer escape as an unstructured traceback in the recursive solve path: `ValueError`, `TypeError`, and `KeyError` from strict reasoner parsing are mapped to structured `HOST_CAPABILITY_FAILED` with the error labeled `reasoner content invalid`. | `packages/orion-research-harness/src/orion_research_harness/recursive_runner.py` current main; regression `tests/test_invalid_content_recovery.py` | REPAIRED CONTRACT. Do not generalize beyond the caught schema/content error classes. |
| Q3V2-2 | A successful receipt with invalid task content can be explicitly archived through `archive_invalid_result(request_id, reason=...)` / `retry-failed --invalid-content --reason ...`; the original bytes are preserved under `results/archived/*.invalid-*.json`, a reason sidecar is written, the deterministic identity becomes pending, and a corrected receipt can be ingested. | `workspace.py::archive_invalid_result`; `tests/test_invalid_content_recovery.py::test_invalid_content_archive_frees_identity_for_corrected_receipt` | REPAIRED CONTRACT. This is an explicit audited override, not ordinary successful-receipt mutability and not tamper-proof storage. |
| Q3V2-3 | V1 defects D2 and D3 are therefore historical defects discovered in live use and subsequently repaired/regression-tested; they must not be described as open defects of current main. | C1/C2 plus historical `MANUSCRIPT_V1.md`/`CLAIM_LEDGER.md` | HISTORICAL DEFECT + REPAIR. Do not erase them from the provenance narrative. |
| Q3V2-4 | Benchmark V0 remains exactly one prospectively frozen live frontier measurement: instruments agree on the registered diagnosis/move and the later R6P/R6Q coordinates are scored ALIGNED. | V1 benchmark protocol/results and receipts | ONE MEASUREMENT ONLY. No rate/calibration claim. |
| Q3V2-5 | Current literature establishes that model self-consistency/cross-model agreement can be a weak, regime-dependent signal and that consensus can preserve correlated errors. Q3 therefore treats AGREE/DISAGREE as variables to score later, never as scientific authority. | `NOVELTY_RESEARCH_2026-08-22.md` and cited external literature | POSITIONING. Not an empirical claim from ORION. |
| Q3V2-6 | A prospective multi-frontier protocol is frozen for a future >=20-item series across >=3 research programmes, with deferred ALIGNED/MISALIGNED/UNRESOLVED scoring and explicit CANNOT_CHECK outcomes. No outcome exists yet under this protocol. | `TOP_TIER_UPGRADE_PROTOCOL_2026-08-22.md` | REGISTERED RESEARCH ONLY. Do not describe the series as executed. |

## Updated allowed headline

> We present a receipt-replay research harness and a typed non-LLM campaign controller together with a prospective scientific-instrument agreement benchmark. In the first frozen live frontier instance, the instruments independently agreed on the responsible layer and next move and the later registered scientific outcomes aligned with that move. The live run also exposed malformed-receipt recovery defects that current main now repairs with structured failure and audited invalid-content archival. V0 is one measurement; a preregistered multi-frontier study is required before any calibration or predictive-validity claim.

## Still prohibited

- “Agreement proves correctness.”
- “Two instruments are independent in the causal/statistical sense.”
- Any agreement-rate or reliability claim from V0.
- “The harness is secure/tamper-proof/sandboxed.”
- “Successful receipts can be freely replaced.” Invalid-content archival is an explicit reason-bound recovery path only.
- “Q3 demonstrates autonomous scientific superiority.”
