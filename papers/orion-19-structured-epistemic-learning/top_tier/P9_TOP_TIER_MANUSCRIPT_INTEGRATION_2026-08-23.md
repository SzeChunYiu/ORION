# ORION-19 top-tier manuscript integration — 2026-08-23

This note bridges the historical bounded structural-learning TMLR package to the current causal information/accessibility/computation diagnosis. It changes no frozen scientific result.

## One-sentence paper identity

**ORION-19 is a causal failure-location paper.** Before spending more compute, determine whether the missing resource is semantic information, accessibility/representation, computation, or unavailable method/coverage.

## Current evidence hierarchy

### 1. Historical structural-learning package — mechanism history

Keep D0/M0/A*/D1 as the development path that established exact coordinate sensitivity and cross-domain representation effects. Do not delete the old evidence or rewrite its historical journal-readiness terminal.

However, it is no longer the strongest current paper-level authority.

### 2. Real same-information accessibility intervention

Use the real data study to show the access coordinate can matter while information is held fixed:

- breast-cancer: positive accessibility intervention;
- digits: positive registered accessibility result where the threshold is applicable;
- Wine: **null retained**.

The null must appear in the abstract/results narrative or a reviewer can reasonably read the paper as selective reporting.

### 3. Protected scaling negative

The Qwen2.5 0.5B/1.5B/3B monotone-scaling hypothesis is an authoritative negative. State it directly:

> More model scale, under the frozen registered condition, did not monotonically recover the targeted failure.

Do not turn this into "scaling does not work" or "larger language models are useless."

### 4. Main result — causal diagnostic

`P9_CAUSAL_DIAGNOSTIC_RESULT_RECEIPT_V1.md` should carry the revised paper.

Frozen outcome:

- diagnostic policy: **4/5** task families;
- generic compute escalation: **1/5**;
- exact executable diagnoses: **3/3**;
- false compute escalations: **0** vs **4**;
- registered-cost regret: **0**;
- held-out digits accessibility cell: **`CANNOT_CHECK`**, because the registered threshold does not transport;
- independent implementation: GREEN.

The scientific contribution is not merely higher accuracy. It is the non-compensatory decision to refuse a diagnosis when a registered discriminator cannot be transported.

### 5. Full resource accounting — required survival result

`P9_UNIFIED_RESOURCE_LEDGER_RESULT_RECEIPT_V2.md` repairs earlier resource under-counts and must be reported beside the causal diagnostic, not in an appendix.

Resource vector:

`R9 = (I_sem, A_dim, A_transform, M_state, C_fit, C_infer, C_explicit, R_registered)`.

Corrections:

- StandardScaler fitted state charged to `M_state`;
- base readout touches charged to `C_infer`;
- accessibility serialization charged to `A_transform`;
- decisions re-derived from the frozen causal executor.

Verdict: **`SURVIVES_FULL_ACCOUNTING`**.

The 4/5 vs 1/5 result, 0 vs 4 false compute escalations and the protected CANNOT_CHECK cell all survive. A second checker is GREEN; zero vector-dominance contradictions are observed. No scalar exchange rate across coordinates is introduced.

### 6. Hostile format-prior negative — retire the brittle D1 margin

`evidence/P9_U_T3_T4_HOSTILE_ATTACK_RECEIPT_2026-08-21.md` is load-bearing to the final manuscript because it prevents the paper from over-reading D1.

The frozen representation-length attack does **not** explain the typed-relational result.

But a semantics-preserving global reminting of the symbol alphabet changes the same-information typed-serialization comparator from `0.75` to `0.50`, with 32/128 answers changing. Therefore:

- the historical `typed relational - same-information serialized = +0.50` margin is format-prior sensitive;
- it must not be a headline, abstract number or primary novelty argument;
- the attack is a **negative result**, not a reason to tune the serializer;
- typed-relational vs untyped-pair remains invariant under the tested orbit and can remain a bounded structural observation;
- the order-reminting question has no denominator in the frozen corpus and remains `CANNOT_CHECK`.

This negative supports the higher ORION-19 object: diagnose the missing access/representation resource rather than fetishize a particular surface representation.

## Abstract replacement target

The revised abstract should say approximately:

> Failures in learned scientific systems are often treated as compute shortages, prompting larger models or longer inference. We study a different problem: identifying which resource is missing. We separate semantic information, accessibility/representation and computation, and freeze interventions that change one coordinate while holding the others fixed. On a five-family cross-domain diagnostic, the resulting policy identifies the registered failure class on 4/5 families versus 1/5 for generic compute escalation, with zero false compute escalations versus four; a held-out accessibility cell remains `CANNOT_CHECK` because its registered threshold does not transport. The conclusion survives a corrected eight-coordinate resource ledger that charges fitted preprocessing state, transformation work and inference touches explicitly. Real-data interventions include positive breast-cancer/digits cells and a retained Wine null, while a separately frozen Qwen scaling hypothesis is negative. A hostile semantics-preserving symbol-reminting attack also invalidates an earlier same-information serialization margin, which we retain as a negative rather than tune away. We claim bounded causal failure-location diagnosis under explicit resource accounting, not universal representation superiority or a general law against scaling.

## Introduction edits

1. Replace "which representation wins?" framing with "which resource is missing?"
2. Treat representation structure as one intervention class inside a larger I/A/C/M diagnosis.
3. Use generic compute escalation as the primary foil, not a weak serializer.
4. State the format-prior negative early as a reason the paper moved upward from representation-ranking to causal diagnosis.
5. State that `CANNOT_CHECK` is an outcome of the method, not a failed software run.

## Results section order

1. Exact coordinate/intervention definitions.
2. Real accessibility intervention: positives + Wine null.
3. Protected Qwen scaling negative.
4. Cross-domain causal diagnostic (main result).
5. Corrected full I/A/C/M/R accounting and survival.
6. Hostile representation audit and retirement of the brittle D1 margin.
7. Scope/limitations and optional external breadth.

## Sentences/tables that must be removed or revised

Remove or revise any language that:

- presents `+0.50` typed-vs-same-information-serialized as robust representation superiority;
- treats exact token/string length as the explanation for D1 after the length attack failed;
- hides the format-prior attack because it is adverse;
- reports only the positive real-data cells and omits Wine;
- interprets the Qwen negative as a universal anti-scaling law;
- converts the D-A `CANNOT_CHECK` cell into an error or negative diagnosis;
- scalarizes the eight resource coordinates without a preregistered exchange rate;
- claims natural-science, LLM-agent or formal-math generality not actually executed.

## Figure/table plan

The strongest final paper needs only a small number of figures:

1. **Causal resource diagram:** I / A / C / M intervention classes and observed diagnosis path.
2. **Five-family diagnostic table:** gold intervention, ORION-19 diagnosis, generic compute diagnosis, result disposition including CANNOT_CHECK.
3. **Resource-vector audit table:** pre-correction hidden work vs corrected V2 accounting, showing the conclusion survives.
4. **Outcome symmetry figure:** positive real cells + Wine null + Qwen negative + hostile format-prior negative.

Do not lead with a bar chart of historical serializer accuracies.

## Remaining top-tier work

For the bounded causal-diagnostic headline, the core scientific gaps are largely closed. Remaining work is mostly publication closure:

- final current nearest-work saturation around adaptive test-time compute, representation diagnosis and resource-rational inference;
- update `manuscript/main.tex` and generated tables/macros to the new hierarchy;
- clean-environment `reproduce_final.py` and TMLR build;
- anonymity, clipping and citation audit;
- exact final manuscript/evidence/environment/PDF binding.

Add a real procedural/open-weight agent workload only if the final headline explicitly says the law transfers to agents/LLMs. It is not necessary merely to increase dataset count.

## Submission authority

The current evidence can support a strong bounded top-tier submission candidate after the manuscript/package is regenerated, but repository prose must not self-emit `TOP_TIER_SUBMISSION_READY`. Final authority remains tied to the exact submission bytes and the final nearest-work pass.
