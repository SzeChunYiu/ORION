# P9 top-tier readiness addendum — 2026-08-23

This addendum supersedes `JOURNAL_READINESS.md` **only for current top-tier claim selection**. The historical readiness ledger remains an accurate record of the earlier bounded structural-learning package and its exact-head TMLR build.

## Current scientific gates

| Gate | State | Authority |
|---|---|---|
| real same-information accessibility intervention | **EARNED with mixed outcomes** | breast-cancer/digits positive; Wine null retained |
| protected Qwen monotone scaling hypothesis | **NEGATIVE / EARNED** | frozen result retained; no retuning |
| cross-domain causal I/A/C/M diagnosis | **EARNED** | 4/5 vs 1/5 generic compute; 0 vs 4 false compute escalations; independent checker |
| held-out threshold transport | **CANNOT_CHECK / EARNED BOUNDARY** | D-A threshold does not transport under frozen rule |
| full vector resource accounting | **EARNED** | V2 verdict `SURVIVES_FULL_ACCOUNTING`; causal rerun identical; second checker green |
| representation-length hostile attack | **DOES NOT EXPLAIN D1** | frozen hostile receipt |
| semantics-preserving format-prior attack | **ADVERSE / EARNED** | serialized comparator 0.75 -> 0.50; historical +0.50 margin retired as robust headline |
| order-reminting attack | **CANNOT_CHECK** | frozen corpus has no denominator |
| direct open-weight LLM T3/T4 outcome grid | **CANNOT_CHECK** | required outcomes were not acquired; do not infer from D1 |

## Top-tier paper disposition

The strongest current paper is **not** "typed relational serialization beats another serialization by +0.50." That sentence is now known to be prior-sensitive.

The strongest current paper is:

> A frozen causal diagnostic that distinguishes semantic information, accessibility/representation and computation identifies the registered failure class on 4/5 cross-domain families versus 1/5 for generic compute escalation, produces zero false compute escalations versus four, and retains a non-transportable cell as `CANNOT_CHECK`; this conclusion survives corrected full resource accounting, while an earlier representation margin is explicitly retired after a semantics-preserving format-prior attack.

## Remaining blockers before a top-tier submission package

1. Rewrite `manuscript/main.tex` around the current evidence hierarchy.
2. Regenerate evidence summary/macros/headline tables so no generated primary table promotes the retired serialized margin.
3. Update Related Work / nearest-work record to current adaptive test-time compute and representation-diagnosis literature.
4. Run `reproduce_final.py`, official-result verification and TMLR PDF build in a clean environment.
5. Run anonymity, clipping, content-binding and citation audits on the exact final PDF.
6. Bind exact final manuscript/evidence/environment/PDF bytes.

A new open-weight/procedural domain is **optional strengthening**, not a blocker, unless the abstract claims agent/LLM generality.

## Anti-overclaim gates

The final package must preserve all of these in the main results narrative:

- Wine null;
- Qwen negative;
- D-A `CANNOT_CHECK`;
- hostile format-prior negative;
- prohibition on scalar resource exchange rates;
- no universal anti-scaling claim;
- no universal representation-superiority claim.

No later packaging pass may alter the frozen scientific thresholds or reclassify these outcomes to improve the headline.
