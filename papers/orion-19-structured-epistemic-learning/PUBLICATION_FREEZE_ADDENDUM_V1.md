# ORION-19 publication-freeze addendum V1

**Freeze date:** 2026-08-31  
**Status:** `CURRENT_EARNED_CEILING_FROZEN__EXACT_TRACE_BOUNDARY__NULLS_RETAINED`

This addendum is part of the frozen ORION-19 paper-content packet. It records the
terminals `P9_SCIENTIFIC_CLOSURE_RECEIPT_V1.md` and `JOURNAL_READINESS.md` already
establish, and adds no authority to them.

## Earned scientific ceiling

Four terminals carry this paper, and they are deliberately separated rather than
merged into one verdict:

| terminal | value |
|---|---|
| scientific research | `P9_BOUNDED_STRUCTURAL_LEARNING_SUPPORTED` |
| publication novelty | `P9_BOUNDED_STANDALONE_RESIDUAL_SURVIVES` |
| model escalation | `P9_NEURAL_ESCALATION_NOT_JUSTIFIED` |
| protocol | `P9_FINAL_BOUNDED_PROTOCOL_FROZEN` |

The journal readiness terminal is
`P9_BOUNDED_STRUCTURAL_LEARNING_PEER_REVIEW_READY` targeting TMLR, and the document
labels it precisely: **a bounded peer-review terminal, not an acceptance claim.**

The evidence gates behind it are closed on integrated, replayable artifacts: the D0
exact hostile-world information lattice and its generated corpora; the M0
architecture-neutral task and evaluator with pre-outcome leakage corrections
preserved; A5 explicit local-transport inference at `BOUNDED_VERIFIED`; and the M1
and D1 official archives, protocols, code and tests content-address integrated and
independently verified with zero material discrepancies. The corrected A2/A4
executable hostile replay verified with zero discrepancies.

The ordering matters and is recorded: **independent expectations were frozen before
the official M1, D1, A5 and A2–A4 artifacts existed.** Verification that follows its
own predictions is worth more than verification that trails them.

## Frozen boundary

**The nulls and negatives are first-class evidence here, not footnotes.** The
closure explicitly retains each:

- the Wine result remains a **retained null**;
- the Qwen scaling result remains a **retained negative**;
- the D-A non-identifiable case remains `CANNOT_CHECK`;
- and the gate states the general rule: every null, sufficiency, negative and
  `CANNOT_CHECK` terminal remains first-class evidence.

`P9_NEURAL_ESCALATION_NOT_JUSTIFIED` is scoped with unusual care, and the scoping is
part of the claim: it says escalation was not justified **here**, and is explicitly
*not* a claim that neural models generally fail. A reader who converts it into the
general statement has exceeded the paper.

Two further boundaries hold. The exact-trace-only data boundary is retained, with no
natural-paper pseudo-gold promoted. And application implications remain **design
implications rather than claimed real-world performance** — the paper does not
convert a structural result into an operational one.

No promoted result exceeds its exact information ceiling, and the final synthesis
separates information, learning, computation and transfer rather than treating
generic "reasoning" as an unexplained residual.

The latent-reasoning, scaling, advanced-training-law, binding and causal-successor
lanes are recorded as **not load-bearing** for this bounded filing. Donor saturation
closed after two no-material-change rounds.

## Frozen content surface

The content packet consists of the canonical manuscript and its TMLR PDF,
`P9_SCIENTIFIC_CLOSURE_RECEIPT_V1.md`, `CLAIM_LEDGER_V1.md`,
`APPLICATIONS_CLAIM_LEDGER_V1.md`, `FINAL_CLAIM_CITATION_MAP_V1.md`,
`FINAL_RESULT_DISPOSITION_MAP_V1.md`, `P9_FINAL_NOVELTY_DISPOSITION_V1.md`,
`P10_P11_EXCLUSION_DISPOSITION_V1.md`, `PDF_VISUAL_AUDIT_2026-08-20.md`,
`JOURNAL_READINESS.md`, `CONTENT_MANIFEST_V1.json` with the paper's `SHA256SUMS`,
and this addendum. ORION-19's claim is about structured epistemic learning under an
exact information ceiling; it does not own the models it declines to escalate to.
