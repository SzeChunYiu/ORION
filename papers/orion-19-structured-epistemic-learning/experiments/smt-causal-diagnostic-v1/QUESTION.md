# ORION-19 exact-SMT causal diagnostic successor — frozen question

Identity: `ORION19.SMT_CAUSAL_DIAGNOSTIC.v1`.

Can the intervention-based diagnostic distinguish information failure, accessibility failure and computation failure on a fresh formal-reasoning domain where gold is supplied by exact construction plus a native SMT solver, while avoiding unnecessary compute escalation?

This is a coverage/transfer successor, not a representation-only repair and not a rerun of the Qwen scaling lane.
