# ORION-19 P9-U-T3 checkpoint-custody receipt

LUNARC jobs `3550340` and `3550343` attacked only the environment/custody
half of P9-U-T3. Job `3550343` placed four Qwen checkpoints in custody and
completed one GPU decode smoke probe. It executed **zero** cells from the
declared 1,344-cell scientific grid.

The receipt therefore grants no statement about an `S*`/`C*` crossing, model
quality, transfer, novelty, or submission readiness. The two gated Llama
points remain `CANNOT_CHECK`, and the cell executor named in
`AUTHORITY_DISPOSITION_V1.json` remains unbuilt.
