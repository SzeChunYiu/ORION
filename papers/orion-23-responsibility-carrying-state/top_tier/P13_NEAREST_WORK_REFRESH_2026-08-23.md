# P13 nearest-work refresh — 2026-08-23

**Programme:** #977
**Scope:** pre-submission donor saturation for the P13 maximum claim (responsibility-scoped sufficiency and certified reuse).
**Read state:** `TOP_TIER_PROMOTION_V1.md` + `MANUSCRIPT_RESULT_ADDENDUM_V1.md` at `research/top-tier-p6-p15-2026-08-23` (fdfcb53c), post-execution.
**Method:** changed-vocabulary saturation over two rounds: (R1) agent-memory/provenance vocabulary; (R2) accountability/attestation/governance vocabulary. No material ownership change between rounds.

## 1. Maximum claim vs strongest 2025-26 donors

> **P13 maximum claim:** state sufficiency is responsibility-relative — a state adequate for prediction/action may be unsafe for intervention, verification, diagnosis or repair; a responsibility-carrying state contract identifies supported responsibilities, omissions, reopen conditions and recoverability, reusing state more safely and cheaply than confidence-only, provenance-only, unqualified-compression or always-raw baselines.

The 2025-26 lane has converged on "memory reuse without provenance is unauditable/unsafe": systematic agent provenance reviews, provenance-tiered memory architectures, zero-trust memory governance, signed intent envelopes, memory expiry/attestation practices. **These donors grade state tiers by provenance/grounding; none grades them by the responsibility the state must support.** P13's two earned protected domains (digits responsibility upgrade; CNF epoch/semantic change) are exactly witnesses that provenance-complete, current, confident compact state can be structurally inadequate for a higher responsibility — the axis the donors do not carry.

## 2. Donor ownership matrix

| # | Donor | What it owns | Disposition | Rationale |
|---|-------|--------------|-------------|-----------|
| D1 | Evidence Tracing and Execution Provenance in LLM Agents (arXiv 2606.04990, 2026) | Systematic taxonomy: evidence attribution, tool-use provenance, runtime safety, memory provenance | ADAPT | Donor owns the attribution/tracing layer. P13 adapts it as the necessary-not-sufficient substrate; PROVENANCE_ONLY comparator outcomes are the witness. |
| D2 | A Provenance-Aware Tiered Memory for Agents (ICLR 2026) | Tiered agent memory graded by provenance, trading raw-log grounding cost vs reliability | COMPOSE | Strongest and nearest donor. Must be implemented as the donor-complete baseline arm in the next protected study; its tiering axis is provenance, P13's is responsibility — the composition is the two-dimensional support condition. |
| D3 | Zero-trust agentic-memory governance (Microsoft SFI, 2026) + Provenance Graphs with signed intent envelopes | Governance practice: retention/recall risk controls, signed intents, audit chains | ADOPT | Adopt attestation/signing mechanics as donor-owned components of the certificate transport layer (T13.2). |
| D4 | Agent memory expiry/attestation practice (2026 engineering line) | Expiry dates, scope, deletion, injection controls for memory | ADOPT | Adopt expiry/revocation mechanics; P13 owns WHEN revocation is required (responsibility/semantic-change conditions), the donors own HOW a record expires. |
| D5 | Cloudflare Agent Memory and production memory harnesses | Production memory/state management engineering | DEFER | Material, not load-bearing for the protected discriminator. |
| D6 | Awesome-Memory-for-Agents (Tsinghua C3I) survey index | Survey coverage of agent memory/RAG/skills | DEFER | Reference index for saturation checks. |

No `REJECT` entries: every donor is compatible and owns a strict sub-object (attribution, tiering-by-provenance, attestation mechanics, harness engineering).

## 3. Upward move per absorbed donor

1. **Two-dimensional support theorem (D1+D2):** formalize support as a function of provenance class x responsibility rung: `S supports r iff exists witness relation W(S, r)` with provenance completeness handling auditability and responsibility indexing handling adequacy. Derive: provenance-complete compact state can be inadequate for rung >= k (witnessed by the CNF and digits protected cells), so provenance tiering alone cannot decide reuse safety. This makes D2's architecture a projection of P13's support space onto the provenance axis — a stronger theorem relating donors, not a defensive delta.
2. **Donor-complete comparator (common gate B):** implement the D2-style provenance-tiered memory as the strongest baseline in the next protected responsibility-shift study (research/agent workflow scope), alongside confidence-only and always-raw (already earned). The discriminator: responsibilities where the provenance-tiered policy still performs unsupported reuse.
3. **Certificate transport (D3+D4):** adopt signed-envelope + expiry mechanics into T13.2's transport/revocation contract so the open "general transport under representation/evidence/evaluator change" gate is executed with donor-complete attestation machinery rather than bespoke formats.
4. **Composite maximum claim after absorption:** responsibility-relative sufficiency over a provenance-graded state space — reuse decisions require BOTH the donor-owned audit substrate AND the responsibility-indexed support contract. Strictly stronger than any donor claim; the upward experiment is the research-agent responsibility-shift study with the D2 baseline.

## 4. Gates this refresh feeds

- "current donor saturation and exact final package replay" (P13 gate, open item);
- donor-complete comparator requirement (common gate B);
- the conditional external-authority gate (research/agent scope) is fed by the upward experiment design above — it is widening only if the final headline retains that scope, per the promotion contract's own note.

## 5. Citation ledger

Verified by 2026-08-23 search unless marked: Evidence Tracing and Execution Provenance in LLM Agents arXiv 2606.04990 (verified); A Provenance-Aware Tiered Memory for Agents (ICLR 2026 virtual program page verified); Microsoft SFI agentic-memory zero-trust guidance (learn.microsoft.com, verified); Provenance-as-chain-of-accountability signed intent envelopes (Engineering Agents substack, verified); agent-memory provenance/expiry engineering line 2026 (verified); Cloudflare Agent Memory blog (verified); Tsinghua C3I Awesome-Memory-for-Agents (verified). Refresh must be repeated immediately before submission per common gate B.
