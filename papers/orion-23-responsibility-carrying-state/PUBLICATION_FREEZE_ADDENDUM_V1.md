# ORION-23 publication-freeze addendum V1

**Freeze date:** 2026-08-31  
**Status:** `CURRENT_EARNED_CEILING_FROZEN__CONTROLLED_FINITE_WORLD__EXTERNAL_VALIDATION_SUCCESSOR_ONLY`

This addendum is part of the frozen ORION-23 paper-content packet. It records the
ceiling `P13_ACTIVE_CLAIM_AUTHORITY_V3.json` already establishes, including one
authority it explicitly withholds, and grants nothing beyond it.

## Earned scientific ceiling

The active terminal is `P13_CONTROLLED_COMPOSED_SAFETY_EFFICACY_AUTHORITY_SUPPORTED`
and the paper-level outcome is
`SUPPORTED_CONTROLLED_COMPOSED_FINITE_WORLD_WITH_EXTERNAL_BOUNDARY`.

Three leaves are active. `P13.EXACT.RESPONSIBILITY_RELATIVE_SUPPORT` is
`SUPPORTED_EXACT` in the registered finite constructed world: sufficiency is
relative to a named downstream responsibility, established by the equivalence-class
support matrix.

`P13B.AUTHENTICATED_CERTIFICATE.CORRUPTION_SAFETY_COST` is supported in a controlled
finite world, at its authorized wording:

> In the registered 30-case finite state-task panel, every one of four
> certificate-corruption worlds had 30 live mutation opportunities; authenticated RCS
> rejected all mutated certificates, made zero gold-scored unsafe reuses in every
> world, and cost 0.6111 times always-raw on the valid-certificate panel.

`P13C.COMPOSED.SAFETY_EFFICACY` extends that to the composed world:

> In the registered composed finite world (12,288 episodes; 2,457 scheduled
> corruptions), authenticated RCS made zero unsafe reuses, rejected every scheduled
> corruption, was noninferior in verified correctness to both always-raw (0.95247)
> and unverified RCS (0.98063) at 0.97933, and cost 0.539 times always-raw overall
> and 0.498 on the parent-form subpanel, while unverified RCS committed 330 unsafe
> reuses (FORGED 66, OVERBROAD 87, STALE 177) and 123 adversary-induced unnecessary
> reopens under omitted support.

The contrast is what carries the paper: authentication is not decoration, because
the unverified arm fails in the same world by a wide and itemized margin.

## Frozen boundary

**One authority is withheld by the paper against itself.** The earlier claim that
RCS achieves zero unsafe reuse in P13A is recorded as `AUTHORITY WITHHELD /
CANNOT_CHECK`, terminal `P13A_EMPIRICAL_SAFETY_COST_AUTHORITY_WITHHELD`, reason
`SELF_SCORED_OUTCOME_NOT_CONTINGENT`. The ledger states the problem plainly: it was
a self-scored zero over zero reachable harm opportunities. A zero earned that way is
not evidence, and this freeze does not let the later composed result retroactively
rehabilitate it.

A second negative is retained and must never be relabelled: the historical P14A
combined gate did **not** pass — `0.0556640625 > 0.05`. The ledger's instruction is
literally "never relabel positive".

Two comparator arms are recorded false rather than quietly dropped: confidence-only
reuse has an unsafe rate of 0.215576 and provenance-only 0.396159, so neither may be
described as equivalent to responsibility-carrying state.

Seven promotions are forbidden and none is licensed here: external validation; real
agent safety; population generalization; certificate authority independent of the
authors; P13A's self-scored zero as empirical safety; and the composed P13C result
read as either external validation or population evidence.

The supporting checkers agree with that ceiling rather than exceed it.
`check_p13_p14_pinned_corpus_v1.py` reports the corpus freeze intact across 45
repositories and 22 organizations with no ORION subject among them, and
`check_lifecycle_consolidation_binding_v1.py` passes with
`population_inference: false` — an internal panel, not a population.

External validation by parties independent of the authors is successor work. It must
not retroactively promote the controlled finite-world result.

## Frozen content surface

The content packet consists of `MANUSCRIPT.md` and
`MANUSCRIPT_RESULT_ADDENDUM_V1.md`, `CLAIM_EVIDENCE_LEDGER.md`,
`P13_ACTIVE_CLAIM_AUTHORITY_V3.json` together with the V2 authority it retains
unchanged for the P13B leaf, the P13A protocol with its outcome-entailment
adjudication and safety-cost receipt, `HISTORICAL_P14A_NEGATIVE_ROOT_CAUSE.md`,
`JAAMAS_INFORMATION_SHEET_V1.md`, the two checkers named above, and this addendum.
The ORION-23 claim is about responsibility-relative reuse of learned and verified
state; it does not own the downstream tasks it is measured against.
