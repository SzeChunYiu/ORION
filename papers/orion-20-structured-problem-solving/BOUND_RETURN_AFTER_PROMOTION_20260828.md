# ORION-20 bounded return after #1649 promotion attempt

**Status:** `PROMOTION_STOPPED__BOUNDED_FORMAL_LANE`  
**scientific_authority_delta:** `NONE`

## Current scientific disposition

The final top-tier promotion attempt is complete and failed prospectively under its registered stop rule.

`ORION20.PRIMITIVE_SUFFICIENCY_MINIMALITY.v1` finds, under the frozen majority-3 composition template:

- admissible non-affine primitives: code `8` (AND) and code `14` (OR);
- minimal bases: `{8}` and `{14}`;
- indispensable primitives: **none**;
- the registered complexity order selects AND, but this is order-based selection, not structural indispensability.

The identity

`T_p(x,y,z) = p(x,y) xor p(x,z) xor p(y,z)`

is blind to the affine difference between AND and OR: the linear terms in OR cancel across the three pairs. The frozen expansion is therefore **a** minimal expansion, not **the** uniquely necessary primitive expansion.

The donor gate fails as registered. Classical clone/ANF theory owns the generic statement that a non-affine primitive is needed and that AND supplies one; the OR alternative removes the candidate indispensability delta.

## Consequence

Top-tier promotion is over. No vocabulary enlargement, new complexity order, or second rescue cycle is authorized under #1649.

The bounded manuscript may retain:

- the formal closure/unreachability object;
- the OCME measurement contract;
- exact finite non-vacuity studies;
- the distinction between an intended future empirical terminal and what has actually been executed.

It must not claim:

- verified-solving superiority;
- a uniquely necessary primitive;
- donor-subtracted primitive indispensability;
- executed H1--H6 evidence where the frozen donor-complete inputs were absent.

## Manuscript state

The current Wave-2 bounded-manuscript correction applies the audited narrowing from #1636 under the mandatory paper-writing protocol: superiority is removed from the title, a scope/status section states what is and is not established, and H1--H6 are explicitly `PROSPECTIVE_NOT_EXECUTED`.

## Binding state

The paper-level `SHA256SUMS` is **not** reconciled in this change. The older #1636 reconciliation predates the #1659 promotion packet and therefore does not describe the current paper tree. Reusing it would create a stale binding while claiming currency.

Submission authority remains denied until a fresh binding is built from the final bounded tree after all intended manuscript/source changes are complete.

## Stop rule

**STOP SCIENCE / FINISH BOUNDED PAPER.** Remaining work is current-tree binding, package generation/visual audit, venue choice, cover letter/licence/archive, and human filing inputs.

Refs #1659 #1649 #1636 #1609.
