# P10 reopen triggers

The merged technical-note terminal on `main` remains authoritative until a reopened experiment earns a stronger terminal. On `shadow/p10-positive-claim-expansion-20260820`, triggers 2 and 3 below are **ACTIVE** under the prospectively frozen `research/extensions/p10-structured-reasoning/P10_NATIVE_STATE_INCREMENTAL_VALUE_PROTOCOL_V1.md`.

1. **Phase 2B goal effect — DORMANT:** reopen only when a legally redistributable,
   revision-bound replacement for `HF_MATHLIB_TACTICS_SAMPLE.json` is available.
   Freeze its license, source revision, schema and digest before any outcome.
2. **Native trace representation — ACTIVE:** replace the conservative source projection
   with receipt-bound proof-state/tactic/dependency traces and rerun the exact blocked
   top-module split. Source-text surrogates do not discharge this trigger.
3. **Strong tactic baseline — ACTIVE:** implement a faithful TacMiner-class
   dependency-graph/state-aware comparator on the same corpus and split. If faithful
   reconstruction cannot be achieved, report `CANNOT_CHECK`; a weaker proxy cannot
   discharge the standalone novelty gate.
4. **Revision robustness — NEXT:** after the native-state primary endpoint is resolved,
   prospectively freeze at least two later Mathlib revisions and test whether the
   transfer conclusion changes. Later revisions are not selected by observed effect.
5. **Standalone residual — GATED:** route a surviving positive through P4 scientific
   verification and P8 authority review before standalone novelty language.

## Locked positive baseline

The reopen does not erase or retune V2.1. Its leave-top-module-out result remains the
locked comparator: Markov accuracy `0.3842`, unigram accuracy `0.2796`, difference
`0.1046`, module-bootstrap 95% interval approximately `[0.0863, 0.1223]`.

## Claim-expansion rule

The strongest current claim is coarse cross-module tactic-history transfer. Native-state,
structural-transfer, reusable-tactic, and standalone-novelty claims are permitted only by
the claim ladder frozen in `P10_NATIVE_STATE_INCREMENTAL_VALUE_PROTOCOL_V1.md`.
Null or negative reopened outcomes remain recorded and do not invalidate the existing
V2.1 source-transfer result.