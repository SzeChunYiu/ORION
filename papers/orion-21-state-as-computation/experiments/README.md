# Experimental evidence index

ORION-21's protocol, runner, receipt, negative-root-cause and replay artifacts remain at the package root so their historical paths and hashes do not move. The publication prose/typesetting lives under `paper/`. The root evidence sequence P11C–P11G remains authoritative for experiment history.