# ORION-08 publication-freeze addendum V2

**Freeze date:** 2026-09-02  
**Status:** `CURRENT_EARNED_CEILING_EXTENDED__REAL_DATA_EXECUTED__BOUNDED_TRANSFER_ONLY`  
**Supersedes:** `PUBLICATION_FREEZE_ADDENDUM_V1.md` (2026-08-31), by the additive
successor route. No V1 frozen state is regressed; every V1 boundary remains in force
where V2 does not explicitly extend it.

## Why a successor was required

V1 froze the packet when the paper's earned ceiling was exact-synthetic mechanism
isolation, and correctly routed any real-system study to successor work. Since then
the real-data successor studies executed and entered the manuscript: the corrected
finite criterion instantiated on five OpenML CC18 datasets, twelve Defects4J
projects (597 faults), and 1,533 WorkflowHub records, each interpreted through
held-out results rather than same-distribution identities. The claim authority for
that extension is `CLAIM_LEDGER_V4.md` (rows O8V4-9 and O8V4-10); this addendum
records the freeze surface, it grants no authority the ledger does not carry.

## Extended earned ceiling

The submission terminal remains `READY_TO_SUBMIT_SECOND_TIER`, target venue TMLR.
The evidence is the six matched-information mechanism families of V1 plus the
bounded real-data instantiations. Nothing in the real-data work upgrades a
synthetic claim: held-out transfer is mixed (adverse on three of five OpenML
datasets relative to the attainable gap; one genuine unexplained Defects4J failure
in the Cli project; WorkflowHub cannot test the no-value direction), and the
ledger's descriptors are `REAL DATA; SAME-DISTRIBUTION IDENTITY CHECK; NOT
EXTERNAL VALIDATION` and `DESCRIPTIVE BOUNDED TRANSFER; NO GENERALIZATION OR
SUPERIORITY CLAIM`.

## Multiplicity surface (executed, on the supported route)

The twelve synthetic mean comparisons carry per-comparison percentile-bootstrap
intervals. Family-wise-corrected bootstrap intervals on the mean remain
non-recomputable from the preserved publication analysis, which stores summary
statistics rather than paired difference vectors, and no such claim is made
(ledger forbidden-claim list, unchanged). The family-wise question is answered on
the route the preserved analysis does support: exact two-sided sign tests over the
stored win/loss counts with exact Holm--Bonferroni adjustment across the
twelve-comparison family (`analysis/familywise-multiplicity-v1/`). All nine
interval-positive comparisons survive; the only non-survivor is the prespecified
remint-unnecessary tie regime, where failing to survive is the registered outcome.
This route concerns direction reliability, not the mean, and upgrades no
undetermined row.

## Frozen boundary

Everything V1 froze stays frozen: exact-synthetic scope for the mechanism claims,
no deployment claim, no novelty claim, no retroactive promotion of synthetic
separations, and the rule that a tie observed under matched information is an
observability result, not a mechanism failure. V2 adds: the real-data studies are
descriptive and bounded; the fixed-heuristic proxy arms are fixed heuristics by
construction and are not measurements of any deployed model (including any
language model); all worlds, gold labels, audits and adjudications in this
programme are same-programme and single-author. A conditioned-transfer or
covariate-analysis revival is successor work and must not be back-fitted into
this packet.

## Frozen content surface

Unchanged from V1: the canonical manuscript designated in
`submission/CANONICAL_SOURCE_DECISION.md` (the LaTeX tree), the `N4_A`, `N1_C`
and `N2_F5B` result artifacts with the headline family report, the submission
directory, and the addendum chain (V1, now this file). The Tier-B package binds
these by exact hash through `submission/publication-ready-20260831` and
`submission/tier-b-final-20260901`, whose manifests are regenerated whenever a
bound file changes; content binding remains `BOUND_PARTIAL / PASS`.
