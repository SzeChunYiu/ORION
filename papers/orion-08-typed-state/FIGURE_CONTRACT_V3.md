# ORION-04 figure contract — V3

## Figure 1 — Epistemic-binding benchmark taxonomy

**Reader question:** What common experimental object connects six heterogeneous studies?

Center: `same visible factual world`.

Six outward binding axes:

- type prior;
- applicability scope;
- decision-relevant uncertainty;
- transport lineage;
- decision coupling;
- remint obligation.

Each axis should terminate in one downstream scientific decision and one hostile/no-value control. The visual must label the taxonomy `post-study synthesis`; do not imply one six-family preregistration.

## Figure 2 — Treatment/control effect matrix

Rows = N4-A/B/C/D/E/F3. Columns:

`typed relation | matched comparator | positive effect | hostile/no-value control | claim boundary`.

Use symbols for:

- separated paired effect;
- interval overlaps zero / no clear advantage;
- exact count/census;
- exact tie/no-value regime.

N4-B must visually distinguish the strong scoped-vs-unscoped effect from the inconclusive scoped-vs-never contrast.

## Figure 3 — Paired uncertainty for stochastic families

Plot paired effect estimates from `PUBLICATION_PAIRED_ANALYSIS_V1.json` with 95% bootstrap intervals. Do **not** pool different metrics on a single numerical axis. Use small multiples or normalized separate panels:

- A: utility differences;
- B: mean-round-utility differences by regime;
- C: regret reduction;
- E: utility differences;
- F3: utility differences by regime.

For N4-D use exact counts in Figure 2 or a separate panel; no invented confidence interval is required for the constructed census.

## Figure 4 — Why flattened state fails

Optional conceptual schematic:

`fact + scope/lineage/decision relation -> correct bounded use`

versus

`fact only -> stale reuse / over-reopening / irrelevant probing / invalid transport`.

This is a conceptual summary, not empirical evidence.

## Table 1 — Nearest-work and donor subtraction

Use `NEAREST_WORK_MATRIX_V3.md` plus N1-C/N2-F5B negative controls. The table should make clear that typed memory, provenance and VoI are donor-owned; ORION-04's contribution is the matched-information scientific-decision test suite.

## Visual reporting rules

- Do not encode all six effects as if they shared one scale.
- Always show the null/no-value control beside the positive regime.
- Keep exact-synthetic scope visible in legends/captions.
- Report `n` at the episode/regime level for stochastic panels.
- Use effect estimates and uncertainty rather than significance stars.
