# ORION-18 external-objective authorization/validity separation — frozen question

Identity: `ORION18.E15_AUTHORITY_SEPARATION.v1`.

Do real native trust/policy systems exhibit the formal separation between permission to act and technical evidence validity, including cases where an object is technically valid but insufficiently authorized and cases where authorization credentials are sufficient while the bound scientific/technical object is invalid?

To avoid duplicating external execution, this lane consumes the future frozen records from ORION-25 `ORION25.EXTERNAL_TRUST_DOMAIN_NATIVE_V1`. It defines a different two-axis analysis before those outcomes exist.

The result can earn external-objective conformance authority only. It cannot manufacture human scientific adjudication or external-investigator authority.
