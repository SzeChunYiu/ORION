# Governed-capability empirical companion

## Purpose

ORION-18 owns the formal separation between evidence-bearing capability and authority
to commit an effect. This companion records an executable and public-data
instance of the capability side without turning it into a separate novelty
claim.

## Absorbed mechanism

The learning machine retains provenance-bearing mechanics, competence evidence,
empirical state-effect contracts and an append-only failure ledger. Its
`CapabilityRoute` returns either a supported mechanic or abstention together
with every candidate competence estimate. The value is explicitly
`authorizes_execution=False`; even a successful route must pass through ORION-18's
external authorizer before execution.

This route/abstain structure assimilates the strongest relevant discipline from
algorithm selection, selective prediction, learned paradigm routing,
tool-necessity evaluation and capability self-assessment. The full extraction
and adoption receipts are in ORION-19's dated saturation ledger.

## Public discriminator

The protocol was frozen before outcome access and binds the public ASlib
`SAT11-HAND-ALGO` scenario at commit
`551b22beef8df17de59286b4822ef720e0aa4d6f`.

| System | Solve rate | Attempt coverage | Attempt rate on VBS-unsolved | Mean PAR10 |
|---|---:|---:|---:|---:|
| RF_ROUTER | 0.6655 | 1.0000 | 1.0000 | 17057.98 |
| FAILURE_AWARE_RF | 0.6419 | 0.7770 | 0.2857 | 18226.50 |
| FAILURE_MEMORY_KNN | 0.6554 | 0.9054 | 0.7013 | 17564.73 |

The pre-registered comparison passed: FAILURE_AWARE_RF reduced attempts on 77
VBS-unsolved instances by 0.7143 absolute (paired bootstrap 95% CI
0.6104–0.8182) while retaining 0.9645 of RF_ROUTER's solved instances. Two full
runs produced the same machine-readable SHA-256 digest, `8246d007…`.

## Interpretation

This is evidence that retained failure-aware routing can expose a useful
attempt/abstention trade-off in one pinned public scenario. It is not evidence
that the router is a new algorithm-selection method, is better than AutoFolio,
generalizes across domains, or has authority to act. Mean PAR10 and solve rate
worsen relative to the non-abstaining router; those costs remain visible.

The executable protocol, full-precision results, source digests, claim ledger
and reproduction commands remain under
`paper-09-executable-research-core/`. ORION-19 closes by merge because its empirical
object strengthens ORION-18 while no independent paper residual survives saturation.
