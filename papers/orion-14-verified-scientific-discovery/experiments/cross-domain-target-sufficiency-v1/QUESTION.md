# ORION-14 cross-domain target-sufficiency successor — frozen question

Identity: `ORION14.CROSS_DOMAIN_TARGET_SUFFICIENCY.v1`.

Across three objective-verifier domains, does a target-aware verification state achieve lower false scientific promotion at comparable clean-case coverage than a generic evidence product that receives the same factual payload but lacks the target-binding coordinates?

The three domains are fixed before outcomes: Defects4J bug/test verification, Lean/mathlib proof checking, and RO-Crate metadata/artifact conformance. Native verifiers supply objective gold only for what they actually establish; any semantic question outside native metadata is `CANNOT_CHECK`.
