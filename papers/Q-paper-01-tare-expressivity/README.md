# Q1 — Shared-Tag TARE expressivity

**Stable ID:** ORION-Q1  
**Canonical manuscript:** `manuscript/main.tex`  
**Status:** `INTERNAL_REVIEW_PASS__BOUNDED_CLAIM / SUBMISSION_GATES_OPEN`

## One job

This paper characterizes the exact optimization structure of the frozen six-term shared-Tag TARE grammar under the unit support-count objective. The durable result is the all-`n` support-two normal form: the unrestricted exact optimum is always attained inside the `D++` family of frame support at most two. The regime-classification story is deliberately weaker. Successive frozen closed forms were refuted by split, borrow, phantom-borrow, and split+borrow-hybrid configurations; the current all-`n` finite-basis classification still has one explicit lemma-open sector.

The rewrite replaces the stale V1 headline that implied a complete two-trade map. `MANUSCRIPT_V1.md` and the original `CLAIM_LEDGER.md` remain immutable historical inputs; they are not the current publication claim surface.

## Current evidence spine

1. local support-dominance audit: 688,041,472 complete local configurations, zero violations;
2. exact counterexamples that defeat progressively narrower closed forms;
3. `MAX_R6S_ALL_N_COMPOSITION_RESULTS.json`: support `>=3` never strictly pays for the frozen R6M grammar, so `C_DP = C_D++` for all `n`;
4. `QG5B_EXACT_FORECASTER_RESULTS.json`: theorem-backed `D++` forecaster, zero error on 9,547 compared instances;
5. `QG7_BPRIME_COMPLETENESS_RESULTS.json` and `QG7B_HYBRID_FAMILY_RESULTS.json`: fourth hybrid configuration discovered, then covered on 10,481 verified instances;
6. `QG7C_CLASSIFICATION_RESULTS.json`: all-n classification chain reduced to one pinned `comm-s2` lemma-open sector, with no fifth configuration found in its realized hostile panel;
7. chemistry grounding: H4, equilibrium N2, and Benzene matchings are donor-exact under the frozen objective, with the Benzene result prospectively staged before exact computation.

## Boundaries

No full-circuit or hardware optimality is claimed. No result transfers to QG-2's reweighted objectives without a fresh objective-indexed proof. TARE, Tag/Restore, anticommuting-unitary machinery, Pauli/symplectic compilation, and the donor family are donor-owned. The protected stretched-N2 subject remains outside this manuscript's evidence.

See `JOURNAL_READINESS.md`, `REPRODUCE.md`, and `CLAIM_LEDGER_V2.md`.