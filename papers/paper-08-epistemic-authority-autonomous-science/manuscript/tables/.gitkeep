This manuscript carries no table in its current version; the directory exists so the
tree matches papers 1-5 and so a future one lands where the build already looks
for it (\graphicspath / \input{tables/...}).
