\section{Exact bounded support}

Final X4 enumeration:

\begin{itemize}
  \item distinct exact authority states: \textbf{3,072}, each replayed across thirteen donor families for \textbf{39,936} evaluations; the donor axis changes no verdict in any of 239,616 sibling pairs, which is the conservativity result and not extra coverage;
  \item terminals: \textbf{19,968} \texttt{NO_DONOR_AUTHORITY}, \textbf{15,353} \texttt{BLOCK}, \textbf{3,328} \texttt{CANNOT_CHECK}, \textbf{1,287} \texttt{DISCHARGE};
  \item donor-conservativity violations: \textbf{0};
  \item minimal scientific-type separation witnesses: \textbf{65};
  \item protected-coercion successes: \textbf{65};
  \item matched unprotected-coercion countermodels: \textbf{65};
  \item blocker-law instances: \textbf{13} \texttt{REFUTED} successes, \textbf{13} \texttt{UNDETERMINED} \texttt{CANNOT_CHECK}, \textbf{13} \texttt{ESTABLISHED} blocks;
  \item one-support-family revocation survivals: \textbf{26};
  \item all-support-family revocation blocks: \textbf{13};
  \item heterogeneous ordered-chain composition successes: \textbf{169} --- one composition counted thirteen times thirteen, because the enumeration's chain loop ignores both of its donor variables and the thirteen families present a single profile to it; the mechanized derivation below states what that leaves;
  \item matched scientific-authority-widening chain countermodels: \textbf{169}, on the same replication;
  \item action/release-denied but independently supportable proposition examples: \textbf{13};
  \item ideal decentralized-product mismatches: \textbf{0};
  \item canonical row SHA-256:\\
    \idt{ed186b824692fd5b3ab31be718c75b84e2126b577ce921ca5cc01b2d08ae19e6}.
\end{itemize}

A separate checker independently reconstructs the final enumeration.
