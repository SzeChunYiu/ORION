\section{Data and code availability}
\label{sec:availability}

The implementation repository contains the controlled index, host-owned
evaluation harness, the frozen offline systems and ablations, the descriptive
analysis code, and the publication-layer regeneration commands.  Every artifact
named in the narrative is listed here with its path, so that no repository
location has to be carried in the prose.

\subsection*{Preregistration}

The study was preregistered and frozen before any outcome was accessed, under
the identifier \texttt{P2.open-world-discovery.v1}.  A reader checking whether a
reported analysis was planned or chosen after the fact should compare against
that freeze; it is the reason every number below can be traced to a decision
made in advance of seeing it.

\subsection*{Frozen design and measurement rules}

The design freeze is \idt{protocol/PROTOCOL_V1.json}; the measurement and
statistical rules are \idt{protocol/MEASUREMENT_PLAN_V1.md} and
\idt{protocol/STATISTICAL_PLAN_V1.json}; external-artifact access states are
\idt{protocol/EXTERNAL_ACCESS_AUDIT_V1.json}; and the prospective execution
binding for the result-bearing offline campaign, including the exact subject
revision and Git blob identities, is
\idt{protocol/OFFLINE_RUN_MANIFEST_V1.json}, which also binds the frozen
identifier of every system under test.  The identifiers are
\idt{orion_full} for the governed system; \idt{bm25_keyword},
\idt{dense_retrieval}, \idt{sparse_dense_hybrid}, \idt{one_pass_rag},
\idt{agentic_single_route} and \idt{protocol_driven_systematic_review} for the
six confirmatory comparators; and \idt{adaptive_multiroute_exploratory} for the
explicitly exploratory one.  Each is a key of the systems map in
\idt{evidence/offline_results/RESULTS_SUMMARY_V1.json}, alongside the six
ablation identifiers.  The mapping from every
result-bearing sentence in this manuscript to the archived artifact key that
supports it is \idt{protocol/CLAIM_LEDGER_V1.json}, with a human-readable view
at \idt{evidence/CLAIM_LEDGER_V1.md}.

The definitions in Section~\ref{sec:formalism} are implemented in
\idt{src/orion/knowledge/}: work identity and read state in \idt{identity.py}
and \idt{ledger.py}, route independence and coverage in \idt{routes.py}, and
route control in \idt{route_control.py}.

\subsection*{Offline complete-gold companion}

The frozen complete-gold world is distributed under
\idt{evidence/offline_gold/}; it is fully synthetic, regenerable from the
recorded seed, and verified on every clean CI run against its committed content
hashes and suite fingerprint.

The compact publication result is
\idt{evidence/offline_results/RESULTS_SUMMARY_V1.json}, and the mechanism
projection behind Figures~\ref{fig:p2-4}--\ref{fig:p2-5} is
\idt{evidence/offline_results/OFFLINE_MECHANISMS_V1.json}.  The full per-system
failure taxonomy is \idt{evidence/offline_results/TABLE_P2-3_failure_taxonomy.md}
and the gold-defined route-stop replay is
\idt{evidence/offline_results/TABLE_P2-S1_route_stop_oracle.md}.  The command
\texttt{python3 papers/orion-12-open-world-scientific-discovery/scripts/
run\_offline\_companion.py --check} reconstructs all \OfflineRunRecordCount{} runs and refuses drift.
With \texttt{--write-raw DIR} it also emits the complete normalized JSONL result
set and all rich run artifacts.  The committed summary records SHA-256 digests
of both complete sets, so a reproduction can be checked without requiring
hundreds of generated files in the repository.

\subsection*{External benchmark artifacts}

External benchmark artifacts are not redistributed.  Their licences, access
states and pinned revisions are recorded per artifact in
\idt{protocol/EXTERNAL_ACCESS_AUDIT_V1.json}, with source checks retained
under \texttt{evidence/access/} and summarized in
\idt{protocol/TABLE_P2-1_freeze_manifest.md}.  AutoResearchBench
is pinned to upstream commit
\texttt{a46c9bfb8968786f73f0a6a5b365b5384cd0f96d}; the released 1,000-record
bundle is split by its explicit task-type field into 600 Deep and 400 Wide
records before candidate custody.  The Wide scorer is deterministic and
credential-free.  Deep official scoring uses the released title-identity judge
and still requires an OpenAI-compatible endpoint; the repository workflow now
records this authority precondition without exposing credential values.  The
credential-free search-time self-exposure diagnostic over the frozen Wide
candidate run is
\idt{evidence/external_results/AUTORESEARCHBENCH_SEARCH_CONTAMINATION_V1.json}.

The completed MetaSyn probe is archived as
\idt{evidence/external_results/METASYN_ID_ONLY_PROBE_V1.json}.  It binds
all 86 released test reviews, the pinned upstream revision, dataset revision,
candidate-output tree and official ID-only evaluator output.  The evaluator-side
integer error ledger is separately archived as
\idt{METASYN_SCREENING_FN_LEDGER_V1.json}; its regeneration reloads the
pinned gold only after candidate output is frozen and verifies the official
per-review metrics before emitting exact retrieval and screening false-negative
counts.  These artifacts make the bounded MetaSyn retrieval/screening result
reproducible without reclassifying it as a result for the full system.

The public screening transport successor is archived under
\idt{development/p2-zenodo-transport-successor-2026-08-23/}.  The exact source
identity and licence are in \idt{SOURCE_FREEZE.json}; the accidental first-row
label exposure is preserved in \idt{SCHEMA_PREFLIGHT_INCIDENT.md}; and the
failed V1 binding result remains \idt{RESULT_V1_RETAINED.json}.  The post-schema
repair is frozen separately in \idt{PROTOCOL_FREEZE_V2.json} and
\idt{IMPLEMENTATION_FREEZE_V2.json}; the componentwise estimand and
non-transported coordinates are explicit in
\idt{P2_ONE_POOL_TRANSPORT_ESTIMAND_AMENDMENT_V1.json}. The executable ordering is
\idt{run_transport_successor_v2.py}, and the result and human-readable boundary
are \idt{RESULT_V2.json} and \idt{RESULT_REPORT_V2.md}.  The public source body
is not committed; it is retrievable as Zenodo record 10423427 under CC BY 4.0
and is accepted only at the frozen byte count and MD5.

The post-V2 active-comparator audit is archived separately under
\idt{development/p2-zenodo-active-comparator-audit-2026-08-23/}. Its protocol
and implementation were frozen before either comparator outcome; the pinned
ASReview archive and selected donor source files are hash-bound in
\idt{PROTOCOL_FREEZE_V1.json}. The cadence-matched runner and immutable
negative receipt are \idt{run_active_comparator_audit_v1.py} and
\idt{RESULT_V1.json}. The audit does not redistribute the Zenodo body or the
pinned donor archive and does not claim an exact ASReview application replay.

The cross-review SWIFT V1--V4 records are archived under
\idt{development/p2-swift-cross-review-controller-transport-2026-08-23/}.
The retained population-mechanics failures are \idt{RESULT_V1.json},
\idt{RESULT_V2.json}, \idt{POPULATION_BINDING_FAILURE_DIAGNOSIS_V1.json} and
\idt{POPULATION_BINDING_FAILURE_DIAGNOSIS_V2.json}.  V3 is bound by
\idt{PROTOCOL_FREEZE_V3.json}, \idt{IMPLEMENTATION_FREEZE_V3.json},
\idt{BINDING_RECEIPT_V3.json} and \idt{RESULT_V3.json}; its human report and
failure provenance are \idt{RESULT_REPORT_V3.md} and
\idt{FAILURE_ATLAS_V3.json}.  The post-outcome diagnostic-only freeze is
\idt{NEXT_CONTROLLER_SUCCESSOR_PROTOCOL_V4.json}.  The local PubMed
title/abstract snapshot is not redistributed: it is 90,520,080 bytes with
SHA-256
\idt{962aeb56416dc72fe54336eb4867d14e17505b9b28cb7baf2fd469e82439a568}.
\idt{PUBMED_SNAPSHOT_RECEIPT_V1.json} retains its acquisition receipt, while
\idt{SOURCE_PAYLOAD_MANIFEST.json} records the exact hashes and byte counts of
all omitted source payloads.  The V1--V3 frozen runners and original source
payloads remain hash-bound but are not redistributed with the manuscript
integration packet.

The content-disjoint SYNERGY V5 successor is archived under
\idt{development/p2-swift-content-disjoint-v5-2026-08-23/}.  Its before-outcome
ordering receipt, population/content freeze, protocol and implementation
freezes, result, failure atlas and independent arithmetic/hash audit are
\idt{PREREGISTRATION_BEFORE_OUTCOME_RECEIPT_V5.json},
\idt{POPULATION_AND_CONTENT_FREEZE_V5.json}, \idt{PROTOCOL_FREEZE_V5.json},
\idt{IMPLEMENTATION_FREEZE_V5.json}, \idt{RESULT_V5.json},
\idt{FAILURE_ATLAS_V5.json}, and \idt{SCIENTIFIC_VERIFICATION_V5.json}.  The 12
publisher files (287,754,769 bytes) matched their publisher SHA-1 and local
SHA-256 values but are not redistributed.  No title, abstract, label or active
ordering is stored in the integration packet.

The outcome-unopened KIFMS V6 source/protocol packet is archived under
\idt{development/p2-continuous-recall-effort-v6-2026-08-23/}.  The source
rights and exact 14-file revision-one identity are
\idt{SOURCE_RIGHTS_PROVENANCE_RECEIPT_V6.json}; the label-blind 5,074-to-4,934
population calculation and its one raw V5 match are
\idt{LABEL_BLIND_OVERLAP_RECEIPT_V6.json}; and the population and continuous
estimand freezes are \idt{SOURCE_FAMILY_AND_POPULATION_FREEZE_V6.json} and
\idt{PROTOCOL_FREEZE_V6.json}.  The feasibility result, negative ledger and
integrity receipt are \idt{SOURCE_FEASIBILITY_RESULT_V6.json},
\idt{NEGATIVE_RESULT_LEDGER_V6.json} and
\idt{SCIENTIFIC_VERIFICATION_V6.json}.  The final packet manifest has SHA-256
\idt{cb7aac3ea6cc4b1070515dff882c4e3cb0a85632e5829af379208dd70448f710};
the protocol has SHA-256
\idt{55a5363e6f9360c4722979d0cb11751575f050ce2eea2147a7883d679ea09633}.
The 14 CC-BY-4.0 source CSVs and comparison source bodies are retrievable at
their receipt-bound interfaces but are not redistributed in this packet.  All
KIFMS PubMed-identifier values are empty, so content hashes, not PMID matches,
establish the final disjoint population.  Historical public ASReview simulation
artifact paths exist for these datasets; their metric contents and all exact V6
labels, classes, seeds, rankings and comparative scores remained unopened.
Consequently the packet is not evidence of a globally unstudied family,
independent custody or comparative performance.

The subsequent transparent public-development execution is archived under
\idt{development/p2-kifms-transparent-execution-v7-2026-08-23/}. Its protocol
and implementation hashes were frozen before any KIFMS label, class count,
seed, order or V7 metric was opened. \idt{RESULT_V7.json} retains all four
complete rankings per review through hashes and metrics but no title, abstract
or row-level label; \idt{RESULT_REPORT_V7.md} and
\idt{NEGATIVE_RESULT_LEDGER_V7.json} give the scientific diagnosis, and the
packet-native validator checks the fixed hashes, 4,934-row population, both
classes in 14/14 reviews, gate arithmetic and 14-file manifest. Exact public
source CSVs remain outside the repository. V7 explicitly records
\texttt{independent\_custody=false}; it is not confirmation.

The nested donor-envelopment development successor is archived under
\idt{development/p2-donor-envelopment-v8-2026-08-23/}. The protocol,
implementation freeze, exact result, adverse scientific adjudication, recursive
ledger and next discriminator are retained separately because the raw execution
selector activated two residuals whereas the cross-fitted scientific result
admits none. \idt{SCIENTIFIC\_ADJUDICATION\_V8.json} is authoritative for the
adverse terminal; the packet validator checks 14 exact u4 reproductions and 84
residual executions. No public source text or row-level label is redistributed.

The source-disjoint title-emphasis stop and its provider-native identity
successor are archived under
\idt{development/p2-source-disjoint-title-emphasis-v9-2026-08-23/} and
\idt{development/p2-title-emphasis-conflict-resolved-v10-2026-08-23/}. V9
retains the single conflicting-label population stop and executes no model arm.
V10 retains both provider-native nonduplicate records, executes the frozen
three arms once on 21,897 rows, and records the failed coprimary magnitude and
sign gates. Its result SHA-256 is
\idt{c69e5634b8d0e82a1fa393dbae276d728e9a2f7ba4c9db81a27c0329e2e66742}.
No source text or row-level labels are redistributed, and neither packet is
independent confirmation.

The conditional safe-residual and donor-saturation theory is archived under
\idt{development/p2-safe-residual-envelope-theory-v11-2026-08-23/}. The theory,
finite witnesses, JSON claim ledger and receipt are bound by its
\idt{SHA256SUMS}; \idt{THEORY.md} SHA-256 is
\idt{a3c9107ad152766efc2a08d722657d550781f1f181f47f3ceeba041c0a710176}.
V11 performs no empirical or model run and does not establish that an unseen
positive family exists.

The source-state successors are archived under
\idt{development/p2-state-expanding-acquisition-v12-2026-08-24/} through
\idt{development/p2-state-expanding-acquisition-v15-2026-08-24/}.  Their
\idt{SHA256SUMS} files have SHA-256 values
\idt{f2a5cb0fb214d84807ee0396808edb36b619b59ca2066900a6026e163b98cef0},
\idt{e01b9e6bf2e2169d318455a95e1b642a403e228b159eadb7d70295cb73e2c86e},
\idt{8bb23e1c91d59328b64c70fc04af39840c78830f4d4cea1d588f2698495234e6},
and
\idt{c8bcfeafea18b06d58ab740da508a6d323de1a1b1ce946e43e0f79df1b114047},
respectively.  The corresponding result SHA-256 values are
\idt{99c054ac292a7ceb2602323ca011f651f8f3ca3f00fb1a0d25b2684979836ff4},
\idt{8f8681febfccc390f8e3392d321e01737c0ef4cfd2f4f3db45ab8410a15f2cbf},
\idt{95496f053d762173285b9fbb436ff19bad08f47a82e278bb44715b565a0cd333},
and
\idt{2c394297445ad68019e7f559697d4141f5930532aa6dfef2e1525687bfb07107}.

V12 retains the failed exact-V10-identity gate and its post-gate diagnosis;
V13 retains the positive two-record non-simulation witness and four-of-seven
support ceiling.  V14 records the frozen-commit index mismatch and the decision
not to substitute later commit
\idt{dc2dadfdbb98eb1b4259604789abd640aa3b693e}.  V15 binds the signed
commit/tree/tag, 61-blob dataset manifest and exact historical MIT licence while
keeping current CC0 metadata separate.  Its unexecuted template error and the
successor-only restored V14 route are recorded in
\idt{IMPLEMENTATION_CORRECTION_V15B.json}, SHA-256
\idt{55dda1c29a0e0a619f7234888ffdf0d20a2e35d4b8782972343254d694607980}.
The outstanding three-role custody handoff is
\idt{INDEPENDENT_SOURCE_CUSTODY_REQUEST_V15.json}, SHA-256
\idt{f07c8185c48d5e0a40d02e7bee4dcc0530922fe68e111c0a4cbd83c7c36e2a58}.
No packet contains an index parse, review census, model ranking or performance
metric that can promote the manuscript terminal.

The OpenAlex V1 development trace received a rights-preserving archival
redaction after an adversarial audit found decrypted \idt{Q0_RAW} strings in
two duplicate in-tree traces. Original bytes with SHA-256
\idt{10ffd34a8b3de5e3b9e45b0e309bf451ff201c2dd0c5d1837e70504959d684cd}
are quarantined outside the redistributed worktree at mode 0600. The in-tree
copies omit the plaintext and retain only per-task SHA-256 provenance; their
redacted SHA-256 is
\idt{3eaee226dfb323ad812b67b533be70041345b734e54a447fddedf9f1c2881797}.
The machine-readable mapping is
\idt{development/p2-openalex-public-successor-2026-08-23/Q0_RAW_QUARANTINE_RECEIPT_V1.json}.
Authoritative upstream permission for plaintext redistribution remains
\textsc{cannot-check}; no plaintext circulation is authorized and this
archival action is not a legal opinion.

All three OpenAIRE/Crossref Wide campaigns are preserved rather than left behind
an expiring Actions URL.  Their exact results and run receipts are stored under
\idt{evidence/external_results/}, one prospective freeze per campaign under
\idt{protocol/}; checksummed mirrors of the shared public candidate capture,
the failed first evaluator handoff, and the successful evaluator-only repair are
under \idt{evidence/ci_mirror/}.  The mirror manifest records both
archive-level and per-file SHA-256 digests.  Evaluator gold never entered any
candidate capture.  The first campaign is undetermined because 400 of
1,200 planned provider calls failed; the second because its transport probe
returned an empty result array below its frozen minimum, so acquisition never
began; the third because provider validity reached $0.716$ against a frozen
$0.90$.  None is undetermined because evidence was discarded.

The prospective freeze for the successor acquisition mechanic is
\idt{protocol/P2_LEXICAL_ECHO_SUCCESSOR_FREEZE_2026-08-21.json}, written before
any corpus was generated; the mechanic itself is registered there under the
identifier \idt{D4_DISCRIMINATIVE_TERM_GATING}.

The resolution audit that establishes the measurement-floor finding is under
\idt{evidence/audit/}.  It reads the published campaign results and reports
whether each could have produced a different verdict; it is reproducible from
the repository and carries, in the same artifact, a control campaign with
resolution so that a check firing on every input can be told from a finding.

The live-provider capture implementation and campaign manifest retain request
URL/parameters, query derivation, timestamp, raw body, hash and typed transport
status.  The frozen live protocol requires arXiv plus two OpenAlex-backed route
families.  A non-leaking Actions preflight is archived as
\idt{evidence/access/LIVE_PROVIDER_PREFLIGHT_2026-08-16.json}; it confirms
that arXiv is credential-free but that no funded OpenAlex API key is
available in the same-repository execution context.  Therefore the three-route
live campaign cannot be frozen for execution here, and no raw live
campaign or live monetary-cost ledger is fabricated.  Operator re-scope
2026-08-17 removed the empirical-cost requirement: the sanctioned cost axis
is the host-recorded search-query count
(Figure~\ref{fig:p2-2}, \idt{protocol/TABLE_P2-2_baseline_ablation_cost.md}),
not a live OpenAlex or arXiv API bill.

\subsection*{SIEVE external-arm preflight}

The exact candidate identity and terminal-preserving adapter requirements are
recorded in
\idt{protocol/P2_SIEVE_ADAPTER_PREFLIGHT_V1.json}. They bind
SIEVE/SkimSearchAgent to the paper-linked Apache-2.0 repository revision
\idt{4a640296770c39b8274e7678c6de844daea9541c} and prohibit its native
answer, empty-answer, error or timeout states from acquiring P2 closure
authority. The bounded one-instance stub fixture and its trace/result hashes are
archived under
\idt{development/comparator-binding-harness-2026-08-23/}; they establish only
that the pinned entrypoint can run. The current status is
\idt{P2_SIEVE_RUNNABLE_FIXTURE__NOT_A_FROZEN_ARM}: no archived P2 task-world
reproduction, matched provider/route/budget binding, or comparator performance
result is supplied by this preflight.

\section{Reproducibility}
\label{sec:reproducibility}

Four levels are distinguished, and we claim only what the current artifacts
support.

\textbf{Fully reproducible without network access.}  The \OfflineTaskCount{}-task offline
controlled-index companion regenerates from committed data and subject code.  A
clean GitHub Actions job independently rebuilds the \OfflineRunRecordCount{}-run publication
projection and compares it to the committed summary.  The frozen plan awards
the result its committed precision tier together with a
mandatory underpowered label on the primary offline comparison; reproduction
does not elevate that authority.

\textbf{Reproducible bounded external evidence.}  The pinned MetaSyn workflow
reconstructs the public/gold split, runs the credential-free BM25 plus
public-protocol screening candidate, executes the official ID-only evaluator,
and can regenerate the exact false-negative ledger.  This is external evidence
for that declared probe only.

\textbf{Runnable credential-free benchmark lanes.}  AutoResearchBench Wide
scoring is deterministic once the gold-blind candidate output is frozen.  The
same public-arXiv candidate machinery also supports the declared deterministic
Deep exact-ID deviation.  The official Deep title judge remains a distinct
authority and is not replaced by the ID deviation when no judge endpoint is
available.

\paragraph{Executed official Deep judge archive.}  The bounded keyless public-arXiv probe of the official Deep LLM title judge is archived as \idt{evidence/external_results/DEEP_OFFICIAL_ARCHIVE_V1.json} with its nine-case positive/negative judge control in \idt{DEEP_JUDGE_CONTROL_2026-08-17.json}.  The checkpointed judge runner and the archive finalizer are both reproducible from the repository as \idt{scripts/judge_checkpointed.py} followed by \idt{scripts/finalize_deep_scoring.py}; the archive records the judge model, the pinned prompt digest, the run manifest hashes, and the bounded probe scope label.

\textbf{Capture-capable but blocked as a final live campaign.}  The committed
live layer is designed to archive mutable provider evidence rather than
re-derive it, but the frozen route set requires funded OpenAlex access that is
absent in the verified execution context.  Until that authority is supplied and
the campaign is actually run, raw live-provider outcomes,
empirical live monetary cost and wall-clock, and the matched external discovery
claim all stay undetermined.  Query-count metrics from the offline companion
are the completed cost evidence in this revision.

\section{Ethics}
\label{sec:ethics}

This work does not collect new human-subject records or personally identifiable
information.  The frozen complete-gold companion is fully synthetic and
regenerable from a recorded seed.  External probes use publicly released
benchmark questions and evaluator interfaces; third-party corpora are not
redistributed where the access-audit table records a licence block or partial
redistribution.  Dual-use risk is that of any scientific-literature retrieval
system: the paper evaluates discovery governance and does not present a
deployment, surveillance, or human-facing study.
