# Manuscript Application Import Map R9

**Date:** 2026-08-26

This file supplies application language that may be inserted into the canonical manuscripts at the current evidence level. Stronger replacement language is listed separately and remains forbidden until the named terminal passes.

# 1. Q1 quantum-compilation paper

## Current admissible paragraph

> **Proof-carrying compiler use.** The sharp support theorem supplies a machine-checkable normal-form certificate for the frozen shared-Tag TARE grammar. A compiler can use the certificate to bound the exact state space it must inspect, provided that the production intermediate representation, semantic equivalence relation, and complete move set realize the theorem’s grammar. The current result establishes the formal support property, not a universal production resource reduction: a maintained-stack realization and complete-move audit remain separate application gates.

Fields:

- theorem/result: Q1 sharp support theorem;
- bridge: proof-carrying normal form for a declared compiler grammar;
- current evidence: theorem and internal checker;
- domain/query: frozen shared-Tag TARE support;
- quantitative result: only the theorem’s exact scoped support number;
- authority ceiling: formal grammar;
- prohibited inference: production or hardware benefit.

## Replacement after strong terminal

Permitted only after `APP_Q1_PRODUCTION_RESOURCE_MAPPING_MATERIAL`:

> On the frozen [stack/version] corpus, the Q1 normal form mapped exactly to [production resource], preserved [semantic equivalence test] on every registered instance, and changed [resource/search metric] from [baseline] to [result] under matched compiler passes. The content-bound move registry and certificate checker reproduced the result with [overhead]. This is a scoped production realization for [stack and workload], not a general hardware or compiler advantage.

# 2. Integrated A+B paper

## Current admissible paragraph

> **Certificate-aware exact search.** A finite-signature normal form can restrict an exact search to a bounded support universe, but the cap belongs to the declared transformation or proof language until a production state realizes it and every admitted cross move is audited. The interaction results make this distinction operational: an omitted global move can invalidate an apparently additive local budget. The accompanying XOR grammar and finite controls demonstrate the mechanism internally; transfer to a production exact optimizer requires a complete move registry, cross-move irreducibility, and a matched search experiment.

Fields:

- theorem/result: deletion normal form, proof-language exactness, production-realization gate, interaction-safe composition;
- bridge: cap-aware exact enumerator/branch-and-bound;
- evidence: theorem plus internal XOR/hostile controls;
- domain/query: exact search support;
- quantitative result: internal architecture-specific counts only;
- authority ceiling: internal executable application;
- prohibited inference: intrinsic production complexity or algorithm-independent exponent.

## Replacement after strong terminal

Permitted only after `APP_AB_CERTIFIED_CAP_MATERIAL_AND_TRANSFERRED`:

> Across [named domains], the content-bound move registry and realization checker certified an interaction-aware support cap before exact search. Relative to [native and abstract-cap baselines], the certified cap preserved every optimum and reduced [nodes/time/memory] by [registered effect], with certificate verification costing [overhead]. The result is owned by the declared solver architectures and move systems; it does not imply an algorithm-independent lower bound.

# 3. Paper C / FiberGuard

## Current admissible paragraph

> **Exact representation gate.** FiberGuard can be placed before a learned optimizer or algorithm selector to test whether the exact input representation determines the requested query on a frozen domain. A nonzero fibre diameter yields a content-bound lower bound on every representation-only estimate and supports an exact interval, action set, refinement request, abstention, or route-to-solver decision. On the registered finite graph-colouring, set-cover, and 2-CNF panels, the audit exposes exact target collisions and evaluates candidate repairs over the complete panel. These results are exact for the declared finite generators; they do not establish collision prevalence, model failure, or operational benefit on production data.

Fields:

- theorem/result: exact fibre radius and interval/action consequences;
- bridge: pre-deployment learned-optimization representation gate;
- evidence: finite exact multi-domain panels with dual target checks;
- domain/query: graph colouring, set cover, and 2-CNF model count on frozen small domains;
- quantitative result: registered finite counts and fibre diameters;
- authority ceiling: finite exact application;
- prohibited inference: production prevalence, all-size sufficiency, or end-to-end gain.

## Optional all-size graph paragraph

This paragraph is forbidden until the unbounded graph-collision theorem and finite controls from `codex/five-paper-top-tier-r8-c-cleanroom-20260826` are reviewed and integrated:

> For the representation consisting of sorted degree sequence and triangle count, chromatic-number fibre diameter is unbounded: for every target level, an explicit regular bipartite graph and a regular triangle-free graph of arbitrarily large chromatic number share the representation. Thus no method receiving only this feature map can have bounded worst-case chromatic-number error. The result concerns the exact declared feature map, not every graph neural network.

## Replacement after strong terminal

Permitted only after `APP_C_FIBERGUARD_REDUCES_DECISION_RISK_AT_MATCHED_COST`:

> On [frozen architecture/corpus], FiberGuard identified exact [value/action] collisions in the representation available to the learned optimizer. Under matched feature and solver cost, the enrich/abstain/route policy changed [selective risk, catastrophic error, exact-solver calls, runtime] from [baseline] to [result], including [high-confidence collision finding]. The evidence is specific to [architecture, corpus, query]; richer inputs fall outside the lower bound.

# 4. Paper D typed-authority paper

## Current admissible paragraph

> **Authority-safe evidence and agent workflows.** The typed least-fixed-point calculus can serve as an executable gate for workflows in which claims, memories, tool outputs, and actions carry declared evidence or permission coordinates. After refutation or record merge, an action is licensed only when the required coordinate has a finite seed-founded proof through every rule cap. The exact merge criterion detects hybrid proofs created only by combining records, and origin coordinates block incompatible evidence splicing absent an authorized bridge. The internal 16,777,216-case audit corroborates the implementation on its bounded universe; it does not establish the validity of a real policy or the truth of any claim.

Fields:

- theorem/result: typed nonpromotion, retraction, merge-safety, origin conservation;
- bridge: agent/tool/evidence authorization gate;
- evidence: analytic theorems plus bounded exhaustive internal audit;
- domain/query: finite positive-rule authority;
- quantitative result: bounded audit counts only;
- authority ceiling: internal executable application;
- prohibited inference: legal compliance, factual truth, or broad agent safety.

## Replacement after strong terminal

Permitted only after `APP_D_TYPED_AUTHORITY_PREVENTS_REVIEWED_OPERATIONAL_ERROR`:

> On the independently maintained [policy/domain] corpus, domain adjudicators identified [action/query] as forbidden because [typed license/origin reason]. Untyped reachability and [lineage baseline] authorized the action, whereas the typed merge/retraction gate blocked it and produced [proof/witness]. Clean-authorized acceptance was [result], false authorization was [result], and replay overhead was [result]. The conclusion is bounded to the reviewed positive-rule policy and is not a universal legal or governance claim.

# 5. Nonquantum exact-constants paper

## Current admissible paragraph

> **Proof-producing kernel-packing interpretation.** If the sequence elements are columns of a matrix over `F_5^3`, a zero-sum subsequence is exactly a nonzero binary selector in the mod-5 kernel, and pairwise-disjoint zero sums are disjoint-support binary kernel vectors. The exact early generalized Davenport constants therefore provide threshold oracles for proof-producing dynamic-programming, SAT/CP-SAT, and symmetry-reduced packing experiments. This is an exact algebraic interpretation and algorithm benchmark; it does not by itself establish a coding, scheduling, or production application.

Fields:

- theorem/result: exact early constants/spectrum under their current authority;
- bridge: disjoint binary-kernel-vector packing;
- evidence: direct algebraic translation plus registered computational theorem package;
- domain/query: `F_5^3` binary selector packing;
- quantitative result: exact thresholds only as currently proved/replayed;
- authority ceiling: mathematical/algorithmic interpretation;
- prohibited inference: generic coding or balanced-batching impact, or self-certification of the constants.

## Replacement after strong terminal

Permitted only after `APP_NQ_SEARCH_TO_PROOF_TRANSFER_REPRODUCED`:

> The proof-producing benchmark compared independent dynamic-programming, SAT/CP-SAT, and symmetry-aware engines on [instance classes]. The registered ORION invariants changed [solved strata/time/proof size] by [result], and a structurally independent implementation reproduced [terminal/digest]. AI-proposed cuts were accepted only after independent soundness checks. This demonstrates transfer of the search-to-proof method, not new authority for the mathematical constants or `D_4(C_5^3)`.

# 6. Cross-paper application manuscript

## Current admissible status

No standalone cross-paper application claim is currently admissible. The four-stage certificate control plane is an architecture and experimental hypothesis only.

## Replacement after strong terminal

Permitted only after `APP_PORTFOLIO_CERTIFICATE_CONTROL_PLANE_CROSS_DOMAIN_GAIN` and the A6 gate in `APPLICATION_PROMOTION_RULES_R9.md`:

> The integrated control plane combines a certified normal form, an exact representation sufficiency gate, typed authority propagation, and independent result verification. On [two or more external task families], the full system improved [primary endpoint] over native execution and every component ablation under matched budgets, while attributing interventions to the certificate layer that detected the failure. The result is a separate cross-domain systems contribution; component theorems retain their own ownership and evidence boundaries.

# 7. Audit rule

The manuscript integration workflow must search every canonical manuscript, abstract, conclusion, cover letter, graphical abstract, and submission metadata for stronger application language. Any statement exceeding this map or the claim ledger is a hard failure.

`MANUSCRIPT_IMPORT_MAP_R9_FROZEN__CURRENT_PARAGRAPHS_SCOPED__STRONG_REPLACEMENTS_GATED`
