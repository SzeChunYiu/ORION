# ORION-24 sealed longitudinal successor — frozen question

Protocol identity: `ORION24.SEALED_EXTERNAL_LONGITUDINAL.v1`.

## Question

Across a genuinely future sequence of independently published software releases, can the already-merged reusable sealed-promotion control plane preserve familywise false-promotion control while still accepting useful updates, compared with a naive per-event reset and with always-revalidate?

This is a temporal successor. Retrospective replay is derivation/control material only and cannot satisfy the primary endpoint.

The event stream is fixed to future non-prerelease releases of `sigstore/cosign`, `theupdateframework/python-tuf`, and `in-toto/in-toto` after the frozen cutoffs in `PROTOCOL.json`. Event identities do not exist yet and are therefore not outcomes that can be filled in now.
