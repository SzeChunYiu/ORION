# Reviewer Summary

Primary result: controlled theory/systems superiority for task-conditioned state construction under registered access mechanisms.

- exact fixed-access rank lower bound;
- dense universal/compiled representation ratios 91×–1820× and 4× to >32× sample-threshold gaps;
- P11D sparse-decoder ≥4×-both-cells gate permanently negative;
- P11E fresh deterministic sparse replication retains 2× and 4× gaps;
- P11F is diagnostic only after hostile review found a protocol mismatch (`n_jobs=-1` versus the frozen otherwise-default contract);
- P11G is the arm-scoped nonlinear attack: single-thread 96-tree ExtraTrees is `NOT_REACHED` at 0.95 through `n=1024` in both cells while compiled reaches `n=64`, with +0.4624/+0.3942 gaps at `n=64` and two fresh subprocess payloads byte-identical. `P11G_ARM_PLACEMENT_ADJUDICATION_V1.md` retains that terminal verbatim and states two things about it: on P11G's own data the sparse arm reaches the target at `n=128` in the first cell, so P11G's terminal is a function of which registered arm sits in its gate; and holding the decoder fixed puts 86.7% and 55.4% of the two gaps on the change of state rather than the change of decoder family;
- exact future-optionality laws quantify the cost of specialization.

The adverse sparse result is part of the mechanism claim: stronger downstream inductive bias buys back structural search that compilation would otherwise perform upstream. Protocol defects are also retained as evidence-history objects rather than being edited after outcomes.
