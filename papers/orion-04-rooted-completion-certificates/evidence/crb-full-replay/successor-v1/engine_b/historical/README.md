# Historical donor controls — do not execute

These byte-preserved objects came from the legacy NQ/five-paper path. The
authorization key is consumed by failed job 3544056, the review is stale, and
the former submit/job scripts are not live authority. They remain only for
provenance and adverse-result custody.

The only live successor entry points are the canonical ORION-04 scripts under
`../slurm/`, and they refuse execution without a new external one-shot
authorization.
