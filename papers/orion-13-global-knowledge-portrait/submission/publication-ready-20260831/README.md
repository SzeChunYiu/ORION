# ORION-13 dual submission package

This directory is the publication adapter for the current bounded manuscript.
`arxiv/` is attributed and source-complete. `journal/` follows the route-specific
identity mode `identified` for Semantic Web Journal. Scientific authority remains
with `SCOPED_PUBLICATION_TRACK_V1.md`; this package changes no result terminal.
