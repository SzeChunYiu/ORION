# ORION-09 — reproduction contract

Reproduce the **map components**, not merely the final prose synthesis.

## TARE / R6M

Verify the R6S all-`n` support-two theorem, QG-8's objective-indexed support cone, and the QG-5/QG-5b/QG-7/QG-7b/QG-7c refutation/repair ladder. The support theorem and closed-form classification have different statuses and must not be conflated.

## Rank-2 R6I

Replay the QG-9 ladder through V6. The current terminal is support `<=1` for all `n`, giving intrinsic support number `κ_R6I=1`. Earlier support-5/4/3/2 results are retained as proof-discovery history, not the final bound.

## SixLCU

Reproduce the second-family exact referee, the trade catalog/ladder, and QG-12's theorem that `C_F == C_U iff P0` for every admitted batch and every `n`. The pair-derived boundary is exact even though optimal witnesses may be globally larger.

## StabPrep

Rebuild the exact stabilizer-state graphs at `n=1..4`, the four trade classes, the no-strict-subextension result on the schedule axis, and QG-15's held-out prospective refutation. Then replay QG-15b: the same natural feature vector must contain mixed-label cells, establishing the irreducible 43/1146 error floor independent of predicate budget.

## Production-semantics rank

QG-6 should recover syndrome ranks 2 (R6M slot) and 5 (R6I block) from production transition semantics. Preserve the downstream observation that QG-9 tightens R6I to support 1: inferred rank is a sound route to a finite theorem candidate, not automatically the intrinsic support number.

## Integrity

Every lane's protocol hash/commit must predate outcome access. Refutations stay serialized after repairs. The protected stretched-N2 subject remains sealed. No finite-panel absence of a new trade is upgraded into an all-`n` theorem.