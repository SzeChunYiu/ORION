# ORION-09 main-figure contract V3

Scientific refresh cut: `main@c5ba39fef4f25c46de5fb69bf07f50530f4693ca`
Target routing: PRX Quantum stretch / Quantum fallback.

The five main displays must teach the **typed regime-geometry object** rather than act as a gallery of ORION-QG runs.

## Figure 1 — What a compilation regime geometry contains

Scientific job: distinguish six coordinates that are often collapsed in feature/performance maps.

Panels/objects:
1. donor/incumbent exactness region;
2. exact trade/counterexample witnesses;
3. intrinsic support number versus certified upper bound;
4. objective-indexed proof-validity region;
5. boundary representation/identifiability;
6. prospective confirmation/refutation.

Visual parent relation: put Rice/Instance Space Analysis on the `instance features -> performance footprint / algorithm selection` side. QG residual is the additional exact compiler authority objects, not the footprint idea itself.

Forbidden: implying every compiler family has all six components or a low-dimensional phase diagram.

## Figure 2 — R6M: support envelope closed, named taxonomy still refutable

Show nested families and exact witness ladder:
- common-anchor weight1 donor;
- split-anchor exact counterexample 8<9;
- frame-for-Tag borrow 5<6;
- all-n D++ support<=2 envelope;
- later phantom/hybrid support2 counterexamples inside that envelope.

The visual conclusion must be:
`support complexity closed != smallest named-family union closed`.

Do not plot 9,771 finite predicate hits as population accuracy.

## Figure 3 — R6I: proof-derived bound versus intrinsic support

Central new V3 display.

Left-to-right hierarchy:
- production conserved-syndrome quotient: rank 5 -> safe support<=5 certificate under QG6 conditions;
- intermediate fixed-Tag/local edit proof systems (shown as proof-language ladder, not headline numerical discoveries);
- whole-system Tag relocation: QG9 V6 -> support<=1 all n;
- support0 infeasible -> exact `kappa_R6I=1`.

Annotate the proof-language change: auxiliary Tag structure is rebuilt/relocated instead of preserved.

Visual conclusion:
`proof-derived ceiling can be sound and loose; intrinsic support belongs to the compiler+objective, not the first proof abstraction`.

Forbidden: calling QG6 rank5 wrong; it is a valid sufficient ceiling.

## Figure 4 — QG16 objective-indexed support-one certificate cone

Plot a mathematically faithful 2-D slice or schematic half-space intersection of the proved inequalities:
- `2*t_nc >= 5*t_r`;
- `t_c+t_nc >= 5*t_r`;
- `2*t_nc >= 2*t_r+2*t_tag`;
- `t_c+t_nc >= 2*t_r+2*t_tag`.

Under `t_c<=t_nc`, explain the two-halfspace reduced certificate.

The unit objective and registered interior/outside controls should be marked only if coordinates are extracted exactly from the receipt.

Outside region label: **certificate not applicable**.
Never label outside as `support2 required` or `failure region` because global sharpness is OPEN.

## Figure 5 — Boundary representation: SixLCU versus StabPrep

Left: SixLCU exact one-literal boundary `maxg2 == -2` on registered exhaustive n=2 domain.

Right: StabPrep frozen 13-feature cells; visually identify 12 mixed cells carrying both donor-exact and donor-inexact labels, with irreducible 43/1,146 floor.

Required caption wording: “donor exactness is not a function of the frozen feature vector on the registered domain.”

Forbidden: “information-theoretically impossible to predict StabPrep regimes” without the vocabulary/domain qualifier.

## Optional Extended Data / Supplement

- authority matrix across R6M/R6I/SixLCU/StabPrep;
- prospective TARE and StabPrep custody timelines;
- QG7c remaining pinned proof link;
- complete QG6 transition-table ranks/guards;
- complete QG9 V6 finite obligation table;
- QG16 full cone and exact control table.

## Source-data and regeneration gates

For each main figure:
- one machine-readable source table derived only from named committed receipts;
- deterministic generation script storing input blob/digest identities;
- final-size SVG/PDF plus target-required raster if any;
- no color-only semantics;
- theorem/exhaustive/counterexample/prospective/open states encoded redundantly by label/shape;
- caption states grammar/objective/domain;
- visual audit at PRX Quantum and Quantum fallback widths.

No new scientific claim may be added to make a figure visually complete.
