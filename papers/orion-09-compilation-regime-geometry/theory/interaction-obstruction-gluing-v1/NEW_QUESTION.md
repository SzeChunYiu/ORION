# ORION-09 non-rescue theorem successor — interaction obstructions and gluing

Identity: `ORION09.INTERACTION_OBSTRUCTION_GLUING.v1`.

The prior four-feature separator is exact on the frozen small domain and fails on unseen n=4; the subsequent capacity invariant was true but vacuous and its promotion stopped. This identity does not fit another feature family.

Using n<=4 only as already-accessed derivation data, define a canonical interaction hypergraph whose vertices are primitive support/interaction sites and whose labelled hyperedges encode the local structural relations that can change the exact compilation regime. Derive a finite symmetry-closed set of minimal mixed-regime obstruction signatures `O`.

The prospective question is whether local obstruction absence is stable under disjoint union and complete under gluing, and therefore predicts the regime on an unseen n=5 challenge fixed before any n=5 exact label is computed.
