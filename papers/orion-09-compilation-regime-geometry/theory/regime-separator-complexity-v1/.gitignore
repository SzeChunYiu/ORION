# Derived, not committed: regenerate with independent_checker/extract_frozen_matrix.py
# (2.2s; rebuilt from committed modules whose SHA-256 is verified against the frozen receipt).
FROZEN_MATRIX.json
__pycache__/
