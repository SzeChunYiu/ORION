# ORION-02 journal package — V3

**Title:** When a Representation Can Certify: Sharp Fibre-Diameter Limits and Minimal Refinement

`SOURCE.md` is byte-identical to `../MANUSCRIPT_V3.md`; `CLAIM_LEDGER.md` is byte-identical to `../CLAIM_LEDGER_V3.md`. The claim ledger and the repository's V4 scientific-submission boundary remain the authority ceiling.

This closure package adds no new science. It preserves the exact representation-level hypotheses, the same-fibre indistinguishability obstruction, and the donor-owned status of generic metric/geometry constructions. Metadata is not promoted into a witness.

Run `python verify_package.py` before editing and `./build.sh` to generate and validate `main.pdf`. A filing build is closed only when `python verify_package.py --require-render` passes.

Human filing inputs remain deliberately unfilled: author/affiliation/ORCID metadata, target-journal style, conflict/funding declarations, publication licence choice, and immutable archive/DOI where required.
