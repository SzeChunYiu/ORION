# ORION-02 journal package status V1

**Date:** 2026-08-31

The current journal package is `journal_package/`, bound byte-for-byte to `MANUSCRIPT_V3.md` and `CLAIM_LEDGER_V3.md`. `SCIENTIFIC_SUBMISSION_BOUNDARY_V4.md` remains an additional authority boundary.

The package preserves the same-fibre negative result and the stated representation/reconstruction limits. It does not turn unavailable witness information into metadata-derived evidence and does not promote donor geometry as novelty.

Rendering is valid only with a fresh `main.pdf` and `BUILD_SHA256SUMS` produced by `build.sh` and accepted by `verify_package.py --require-render`. Human filing metadata and target-journal formatting remain external administrative inputs.
