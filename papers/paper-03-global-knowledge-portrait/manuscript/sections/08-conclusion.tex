\section{Conclusion}
\label{sec:conclusion}

Paper III establishes a positive systems result about scientific integration: \textbf{representational compatibility and scientific identity are different relations}. A source can be semantically alignable, schema-compatible or provenance-correct without thereby being authorized to denote the same scientific object for the target claim. ORION therefore makes identity itself explicit and claim-relative.

The architecture has three reusable parts. \emph{Projection before mapping} keeps source-local meaning and provenance intact before cross-source decisions. \emph{GLUE-or-obstruction} treats merge as a target-bound scientific transition rather than the default consequence of similarity: when the identity contract is not discharged, the system preserves a typed obstruction, contextual difference, plural portrait or unresolved state. \emph{Recoverability} records the source projections, mapping path, preservation conditions and non-preserved coordinates needed to audit any global statement back to its origins.

The empirical evidence supports both the safety and the stronger identity-authority interpretation. On the disjoint prospectively frozen 32-case public-reference confirmatory holdout, the typed mapping calculus produced zero false merges versus 0.1875 for flat predicate canonicalization, a paired difference of $-0.1875$ with 95\% bootstrap interval $[-0.34375,-0.0625]$, while matching the exact-coordinate conservative control on false splits ($0.000$ difference, interval $[0.000,0.000]$).

P3-X then raises the comparison standard by granting the strongest comparator structured construct, measurement, context, provenance and missingness information. Across 400 prospectively frozen heterogeneous exact contracts, P3-X reaches \textbf{400/400} exact scientific-integration decisions versus \textbf{250/400} for the strong semantic product and \textbf{50/400} for canonical matching. The P3-X-minus-strong-product effect is $+0.375$ with domain-stratified 95\% bootstrap interval $[0.3275,0.4225]$; P3-X makes zero false \glue{} decisions and preserves 1.0 clean-merge coverage.

The information-equivalent typed product also reaches 400/400. This is a positive portability theorem at the registered decision scope: the scientific-identity semantics are sufficient whether they are implemented as an ORION layer or in an equally informed integrated product. The contribution is therefore not centralized expressivity. It is the explicit target-bound identity relation that prevents a successful representation mapping from silently promoting itself into scientific sameness.

The strongest supported conclusion is consequently broader than a canonicalization benchmark result: \textbf{claim-relative scientific identity authority is a reusable interface between semantic interoperability and scientific integration}. In the registered families it is sufficient to retain clean integrations while eliminating the false integrations induced by weaker semantic products, and its decisions are representation-independent under an information-equivalent implementation.

Raw-text extraction, live ontology-engine transfer and downstream synthesis utility remain \conststatus{} as separate prospective questions. They do not weaken the proved structured-integration result; they test how far this identity-authority interface transfers when the upstream extraction and downstream task are no longer given.
This paper asked whether a global knowledge portrait --- an integration of
scientific claims across disciplines and measurement systems --- can be built
without treating surface or representational similarity as scientific
identity. Three structural commitments answer that.

First, \emph{projection before mapping}. Each source retains a local
projection whose scientific meaning is represented by typed coordinates and
bound to exact provenance before any cross-source decision is made. This keeps
the question of what a source means separate from the question of which
sources may be combined.

Second, \emph{glue or obstruction}. Integration is decided over typed mapping
hypotheses: projections are merged only when the relevant compatibility
conditions hold; otherwise the record carries a typed obstruction, a
contextual difference, a plural portrait, or an unresolved relation rather
than silently flattening the distinction.

Third, \emph{recoverability}. The intended global portrait exposes its
construction --- source projections, mapping path, preservation conditions,
and non-preserved coordinates --- so that a downstream consumer can audit a
global statement back to its origins.

The formal projection and mapping semantics are verified by local exact
worlds. The prospectively frozen public-reference route provides a bounded
external check of the mapping layer without requiring a newly commissioned
annotation team. On the disjoint 32-case confirmatory holdout assembled from
pinned public human and expert resources, the typed comparison produced no
false merges, where flat predicate canonicalization false-merged 0.1875 of
cases; the paired difference was $-0.1875$ with a 95\% bootstrap interval of
$[-0.34375,-0.0625]$. Against the exact-coordinate conservative control, the
false-split difference was $0.000$ with interval $[0.000,0.000]$, so the rule
declared in advance passed.

The separate post-saturation battery tests a wider systems interpretation
after granting the comparator strong semantic integration and current
interoperability and provenance mechanisms. On 400 prospectively frozen exact
already-structured contracts, the claim-relative compatibility layer made
400/400 exact scientific-integration decisions against 250/400 for a strong
semantic product and 50/400 for canonical matching; the paired effect against
the strong product was $+0.375$ with a domain-stratified 95\% bootstrap
interval of $[0.3275,0.4225]$. It made no false merges and preserved 1.0
clean-merge coverage. An ideal product receiving exactly the same scientific
coordinates and rules tied at 400/400, which is an explicit negative boundary
against inherent-expressivity and centralization readings.

Together the two studies support a principle stronger than the original
flat-canonicalization result: \emph{representational compatibility is not
itself scientific identity authority}. In the tested contracts, an explicit
claim-relative compatibility layer can preserve obstruction, plurality and
unresolved state after structured semantic alignment, rather than letting a
technically valid mapping self-authorize a scientific merge.

The later protected zero-error regime-transition study provides a complementary ownership boundary. ORION-JUMP and the strongest verified representation-regime revision parent tie exactly on both disjoint frozen splits, including correspondence/preservation and protected-consequence outcomes with zero false jumps. The result therefore does not establish a distinct ORION representation-invention mechanism. It instead reinforces the role Paper III can legitimately own: representation changes must carry explicit correspondence, preservation, obstruction and reopening semantics, while the transformation mechanism itself remains parent-owned when the strongest registered parent is extensionally sufficient.

That statement remains deliberately narrower than end-to-end scientific integration. I-ADOPT-style variable semantics, ontology/schema/entity alignment, provenance, provenance-aware pluralism and generic consistency checking remain donor territory. Raw-text extraction, live ontology-engine generality, expert agreement on new domain corpora, recoverability of generated global portraits and downstream scientific utility remain \conststatus{} until their own prospectively frozen evidence exists.
That statement is deliberately narrower than end-to-end scientific
integration. Variable semantics in the style of I-ADOPT, ontology, schema and
entity alignment, provenance, provenance-aware pluralism and generic
consistency checking all remain donor territory. Raw-text extraction, live
ontology-engine generality, expert agreement on new domain corpora,
recoverability of generated global portraits and downstream scientific utility
remain undetermined until their own prospectively frozen evidence exists.
Undetermined is the third outcome rather than a quiet negative: an outcome
that could not be determined is reported separately from an outcome determined
to be false, and no evidence gap in this paper is settled by reading one as the
other.

\endinput
