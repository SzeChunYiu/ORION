\documentclass[11pt]{article}
\usepackage{amsmath,amssymb}
\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{hyperref}
\usepackage{enumitem}
\setlength{\emergencystretch}{2em}

\title{When Should a Scientific Agent Change the Problem?\\
A Protected Test of Reformulation versus State, Interface, and Execution Repair}
\author{ORION Paper I Ultimate-Successor Working Manuscript}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
Scientific agents increasingly revise plans, hypotheses, objectives, representations, tools, and even the research problem itself. Existing ORION Paper I evidence supports a narrower conclusion: protected high-level escalation can outperform strong bounded parents on registered hidden-shift families, while an information-complete objective basis can make dynamic objective invention unnecessary. The next question is therefore not whether an agent can change its goal, but whether it can identify when the scientific problem is genuinely wrong rather than merely hard to access, poorly represented, or incorrectly executed. This successor prospectively defines a causal reformulation decision problem. Candidate systems must choose among keeping the problem and spending more search, compiling a better state or representation, repairing an implementation or harness, changing a measurement or objective, changing the problem boundary, or remaining unresolved. The benchmark contains paired cases that are deliberately confusable at the surface level, including correct objectives hidden behind poor representations and genuinely wrong objectives that no admissible state compiler can repair. A protected evaluator separately scores final scientific success, correct reformulation, false reformulation, harmful regression, calibration, and total resource cost. Strong comparators receive donor-complete reflection, goal-evolution, failure-localization, state-compilation, and objective-portfolio mechanisms under matched information and budget. Superiority is authorized only if a prospectively frozen primary success margin is met while false reformulation and harmful regression satisfy non-inferiority guards on a disjoint hidden replication and an independent implementation. Historical failed, tied, or saturated results remain immutable; a new negative outcome opens a responsibility-specific successor programme rather than being re-labelled through prose or post-hoc margin changes.
\end{abstract}

\section{Scientific question and residual}
The scientific question is:
\begin{quote}
When a research process is failing, can a governed agent distinguish a genuinely wrong scientific problem or objective from a correct problem that is merely inaccessible under the current state, representation, interface, implementation, or search budget?
\end{quote}

This question is narrower than generic reflection or goal evolution. Scientific Autonomous Goal-evolving Agent (SAGA) already demonstrates automated objective formulation in a bi-level scientific-design architecture \cite{saga2026}. Recent agent-failure work also shows that the same visible failure can originate in the model, harness, environment, tool interface, or benchmark, making failure-to-reformulation an invalid default rule \cite{modelorharness2026}. ORION therefore claims no novelty for objective evolution, reflection, failure taxonomy, or representation compilation by themselves. The candidate residual is a protected causal decision rule for \emph{when scientific reformulation is warranted after lower-level alternatives are explicitly tested}.

\section{Prior evidence that must remain immutable}
The predecessor paper contains two results that constrain this successor.
\begin{enumerate}[leftmargin=*]
    \item The historical broad formulation that effectively treated failure as evidence for high-level reformulation failed and remains a falsifier of indiscriminate escalation.
    \item A later bounded objective-basis sufficiency result ties ORION when the protected objective portfolio is already decision-complete. This is treated as a regime boundary: reformulation is unnecessary when the current basis is sufficient.
\end{enumerate}

The successor may not change those outcomes, their margins, their denominators, or their interpretation. New evidence can only add a new prospectively registered regime.

\section{Decision space}
For each episode, the candidate must return exactly one terminal decision from
\[
\mathcal{D}=\{\textsc{Keep+Search},\textsc{Keep+Compile},\textsc{Keep+Repair},\textsc{ReviseMeasurement},\textsc{ReformulateObjective},\textsc{ReformulateBoundary},\textsc{Unresolved}\}.
\]
The evaluator records a separate evidence-bound justification and does not infer correctness from the decision label alone.

\paragraph{Keep+Search.}
The scientific problem and representation are adequate, but additional evidence or reasoning is needed.

\paragraph{Keep+Compile.}
The scientific problem is adequate, but a better state, representation, coordinate system, or interface is required. This arm is the mandatory falsifier against false P1 escalation.

\paragraph{Keep+Repair.}
The visible failure is attributable to implementation, harness, tool, or environment rather than to the scientific problem.

\paragraph{ReviseMeasurement.}
The scientific target is meaningful, but its operational measurement is invalid or insufficient.

\paragraph{ReformulateObjective.}
The current objective portfolio is not decision-sufficient, contradictory, proxy-misaligned, or missing a load-bearing objective.

\paragraph{ReformulateBoundary.}
The scientific problem itself has the wrong system boundary, entities, variables, or admissible intervention space.

\paragraph{Unresolved.}
Available evidence cannot distinguish the competing responsibility hypotheses under the frozen budget.

\section{Causal responsibility ladder}
A high-level reformulation is admissible only after a lower-level causal ladder has been evaluated under the frozen resource policy:
\begin{enumerate}[leftmargin=*]
    \item \textbf{Execution test:} does a known-good implementation or environment remove the failure while the scientific formulation is unchanged?
    \item \textbf{Access test:} does a semantics-preserving state or representation compiler make the current objective solvable without changing the objective?
    \item \textbf{Evidence test:} does additional admissible search or experiment resolve the failure without changing the problem?
    \item \textbf{Measurement test:} does replacing an invalid measurement while preserving the scientific target restore valid decision-making?
    \item \textbf{Objective sufficiency test:} can any admissible combination of the registered objectives determine the protected decision?
    \item \textbf{Problem-boundary test:} if the registered objective space is inadequate, is the obstruction caused by the wrong scientific boundary rather than by missing information?
\end{enumerate}

A timeout, low score, or failed tool call is never sufficient evidence for method-basis or problem-basis inadequacy.

\section{Prospective benchmark}
The protected benchmark is generated before candidate outcomes and contains matched, surface-confusable pairs. At minimum it contains the following families.

\begin{table}[h]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lXX@{}}
\toprule
Family & Correct causal responsibility & Required hostile pair \\
\midrule
Latent objective & Reformulate objective & Same surface with sufficient objective hidden by representation \\
Contradictory objectives & Reformulate objective or unresolved & Apparent contradiction caused only by stale evidence \\
Wrong measurement & Revise measurement & Same target with merely noisy but still valid measurement \\
Wrong representation & Keep+Compile & Similar failure with a genuinely inadequate objective basis \\
Wrong problem boundary & Reformulate boundary & Same surface where extra evidence inside the old boundary is sufficient \\
Implementation/harness failure & Keep+Repair & Same symptom produced by a true scientific-model failure \\
Evidence scarcity & Keep+Search or unresolved & Same missing evidence where no allowed route can identify the answer \\
No-reformulation control & Keep current formulation & Attractive alternative objective that improves a proxy but harms protected utility \\
\bottomrule
\end{tabularx}
\caption{Mandatory causal families. Each family includes a paired case that defeats a shallow surface heuristic.}
\end{table}

The benchmark must include heterogeneous naturalistic tasks in addition to exact contract worlds. Naturalistic tasks are admitted only when the protected evaluator can independently establish the responsibility label or a valid \textsc{Unresolved} state.

\section{Systems and donor-complete baselines}
All systems receive the same model class, retrieval/tool access, candidate information, total token/time/tool budget, and evaluator-visible output contract. The comparator set must include:
\begin{enumerate}[leftmargin=*]
    \item fixed-goal agent with search and tool use;
    \item reflection/self-critique agent;
    \item meta-agent or evolutionary search over strategies;
    \item SAGA-style objective-evolution comparator \cite{saga2026};
    \item failure-localization comparator that can assign faults below the objective layer \cite{modelorharness2026};
    \item representation/state-compilation comparator that can repair accessibility without changing the problem;
    \item donor-complete product containing all preceding mechanics but lacking ORION's protected responsibility-to-authority rule;
    \item an ideal-product ceiling supplied with the correct responsibility coordinate, reported only as an analysis ceiling and never as a realistic baseline.
\end{enumerate}

A baseline is not called inferior because its public implementation is unavailable. Unavailable official implementations are labelled unavailable; any reimplementation is explicitly identified as a mechanism-matched proxy.

\section{Pre-registered hypotheses}
\paragraph{H1, scientific success.}
Under matched resources, ORION achieves higher protected final scientific success than the strongest runnable donor-complete comparator by a margin frozen before protected outcomes.

\paragraph{H2, reformulation precision.}
ORION reduces false high-level reformulation on cases where search, state compilation, representation change, or implementation repair is sufficient.

\paragraph{H3, necessary reformulation recall.}
ORION preserves high recall on cases where the existing objective/problem basis is genuinely inadequate.

\paragraph{H4, non-compensatory safety.}
Any gain in final utility is invalid for promotion if harmful regression, false reformulation, protected-sibling invalidation, or evaluator/authority compromise exceeds a frozen guard.

\paragraph{H5, transfer.}
The H1--H4 pattern replicates on a disjoint hidden split and an independent implementation, with at least one held-out domain or task family absent from development.

\section{Primary outcomes and statistics}
Primary outcomes are reported separately rather than collapsed into one score:
\begin{enumerate}[leftmargin=*]
    \item protected final scientific success;
    \item correct responsibility/reformulation decision;
    \item false reformulation rate;
    \item missed necessary reformulation rate;
    \item harmful regression rate and worst-family regression;
    \item correct \textsc{Unresolved} rate and false-resolution rate;
    \item total tokens, tool calls, wall-clock budget, and evaluator calls.
\end{enumerate}

Before protected outcome access, the protocol freezes the primary superiority margin, all non-inferiority margins, family weighting, multiplicity handling, confidence-interval method, missing-run handling, and catastrophic-harm rule. Repeats are not counted as independent tasks. A protected result with unavailable mandatory evidence is \textsc{CannotCheck}, not a win or a loss.

\section{Protected evaluation and authority}
Candidate generation and scientific authority remain separate. The candidate may propose a new objective, representation, or problem boundary, but cannot make that proposal valid by declaration. A host/protected evaluator must establish:
\begin{enumerate}[leftmargin=*]
    \item the old and new formulation identities;
    \item which lower-level alternatives were tested;
    \item the counterfactual consequence of the proposed reformulation;
    \item preservation or reopening of affected prior evidence and claims;
    \item evaluator and hidden-split custody;
    \item no post-outcome change to margins, labels, or benchmark construction.
\end{enumerate}

\section{Adversarial falsifiers}
The following attacks are mandatory.
\begin{enumerate}[leftmargin=*]
    \item \textbf{Representation masquerading as wrong problem:} the objective is correct but inaccessible in the supplied coordinates; a matched compiler solves it.
    \item \textbf{More-compute masquerading as reformulation:} additional reasoning/search under the same formulation closes the case.
    \item \textbf{Harness masquerading as science:} a tool/environment repair removes the failure.
    \item \textbf{Proxy improvement:} reformulation improves an exposed proxy while worsening protected scientific utility.
    \item \textbf{Attractive unnecessary reformulation:} a novel-sounding objective is available on a clean control but should not be adopted.
    \item \textbf{False impossibility:} candidate claims the method/objective basis is inadequate after timeout even though a registered basis solution exists under the bound.
    \item \textbf{Evaluator leakage:} hidden responsibility or target labels are indirectly encoded in templates, path names, source counts, or metadata.
    \item \textbf{Authority laundering:} a proposed reformulation is treated as adopted without the protected discharge chain.
\end{enumerate}

\section{Negative-result recursion}
Negative results are research inputs, not text-editing problems. Every material loss, tie, or harmful outcome is preserved and classified into one primary responsibility hypothesis:
\[
\{\text{evidence},\text{search},\text{representation},\text{measurement},\text{implementation},\text{objective},\text{problem boundary},\text{authority},\text{benchmark},\text{non-identifiable}\}.
\]
For each negative, the programme must freeze a discriminator that changes one suspected causal ingredient while holding the others fixed. A successor is promoted only if it improves the protected outcome on a fresh split without violating the non-compensatory safety guards. Margin relaxation, baseline weakening, outcome deletion, and post-hoc family exclusion are forbidden recovery mechanisms.

The existing P1-U programme in ORION issue \#649 is the authority-bearing research lane for these successors. A new child lane is opened only when a newly observed negative has a distinct causal responsibility and a discriminator not already represented in \#649.

\section{Claim ladder}
The manuscript uses the following claim states.
\begin{enumerate}[leftmargin=*]
    \item \textbf{Protocol frozen:} benchmark, margins, custody, and baselines are fixed; no superiority claim.
    \item \textbf{Mechanism supported:} causal responsibility decision improves on registered exact worlds; no broad scientific-agent claim.
    \item \textbf{Protected superiority supported:} H1 plus all non-compensatory guards pass on the protected split.
    \item \textbf{Replicated reformulation superiority:} protected superiority replicates on disjoint tasks and an independent implementation.
    \item \textbf{Bounded terminal:} the final title, abstract, and conclusion are rewritten to the strongest state actually earned. A negative, tie, or \textsc{CannotCheck} remains valid and visible.
\end{enumerate}

\section{Reviewer-facing decision test}
The paper is successful only if a hostile reviewer can distinguish it from generic reflection or goal evolution. The decisive demonstration must contain paired cases where:
\begin{enumerate}[leftmargin=*]
    \item changing the problem is necessary and lower-level repairs provably or empirically fail; and
    \item changing the problem is harmful or unnecessary because a cheaper state/interface/execution repair is sufficient.
\end{enumerate}
A positive mean score without this causal separation is insufficient for the headline claim.

\section{Current status}
This file is a prospective successor manuscript and protocol. It does not alter the predecessor Paper I result and does not assert that P1-U superiority has already been observed. The first scientifically valid next operation is to freeze the benchmark, baselines, margins, and evaluator identities, then run public preflight before protected outcomes.

\begin{thebibliography}{9}
\bibitem{saga2026}
Y. Du et al., ``Accelerating Scientific Discovery with Autonomous Goal-evolving Agents,'' arXiv:2512.21782, 2025; presented in 2026 workshop venues.

\bibitem{modelorharness2026}
H. Raj et al., ``Model or Harness? An Interaction-Centric Taxonomy for Localizing Agent Failures,'' arXiv:2607.28802, 2026.
\end{thebibliography}

\end{document}
