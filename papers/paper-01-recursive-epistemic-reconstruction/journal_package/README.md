# ORION-P1 journal package

**Scientific status:** `PEER_REVIEW_READY` for the bounded credential-free mechanical
mutation-necessity claim. **Rendered-package status:** `SCAFFOLDING` until the inspected
PDF binary is rebound and final package/repository CI passes.

The exact post-writeback render is 33 pages. Those bytes have completed independent
33/33-page inspection and are durably identified in `evidence/FINAL_SATURATION_AUDIT.md`, but
`journal_package/manuscript.pdf` is still intentionally absent. The package therefore
does not yet claim rendered-byte submission authority.

The historical 48-case H1 remains `NOT_SUPPORTED` / `UNDERPOWERED`; it is not rewritten
or pooled with the v2.2.4 successor. Model-general and open-ended scientific superiority
are not claimed.

Verify the currently bound package files from the paper directory:

```bash
sha256sum -c journal_package/SHA256SUMS
```

The final DOI and venue-specific cover letter are submission operations because no
archive or target venue has been selected. Repository redistribution terms also remain
unlicensed; see `LICENSE.md`.
