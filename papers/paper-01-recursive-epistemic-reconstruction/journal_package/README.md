# ORION-P1 journal package

**Scientific status:** `PEER_REVIEW_READY` for the bounded credential-free
mechanical mutation-necessity claim. **Static package inventory:** `SUBMISSION_READY`.
**Operational authority:** pending final repository/package CI and a successor
exact-main render confirmation.

`journal_package/manuscript.pdf` is the independently inspected 33-page PDF with
SHA-256 `06a60f0f6ec69bc142952b4fc9dc1030fcd0f80de41f941958debe375e6ea99e`.
The protected V3 renderer also binds the complete tracked TeX input closure in
`RENDER_INPUT_CLOSURE.json`; the package does not infer source authority from
`main.tex` alone.

The historical 48-case H1 remains `NOT_SUPPORTED` / `UNDERPOWERED`; it is not
rewritten or pooled with the v2.2.4 successor. Model-general and open-ended
scientific superiority are not claimed.

Verify all manifest-required package and evidence files from the paper directory:

```bash
sha256sum -c journal_package/SHA256SUMS
```

The final DOI and venue-specific cover letter are submission operations because
no archive or target venue has been selected. Repository redistribution terms
remain as stated in `LICENSE.md`.
