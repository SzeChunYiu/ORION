# Claim disposition — ORION01.OBSERVABLE_IDENTIFIABILITY.v1

**Terminal:** `OBSERVABLE_NONIDENTIFIABILITY_PROVED__PRODUCTION_HIDDEN_OP_STILL_CANNOT_CHECK`  
**Scientific authority delta:** `NONE`

## Earned

- Exact factorisation theorem: an observable identifies an extensional semantic object iff
  that object is constant on observable fibres.
- Exact decision corollary for any named semantic predicate.
- The frozen `MIMIC` witness is sufficient to refute identification of the normal-form map
  by `terminal_complexity` on the hostile extension class.
- The frozen `FALSE_IMPROVEMENT` witness refutes monotonic use of terminal complexity as a
  semantic progress score on that class.
- More enumeration using only the same scalar observable cannot repair a collision.

## Not earned

- A claim that the pinned PyZX build actually contains an undeclared mimicking operation.
- Production move completeness or production confluence.
- A statement that `terminal_complexity` is useless inside the original strictly-descending
  closed model; the parent theorem remains valid there.
- Novelty, external replication, submission, or venue authority.

## Consequence for the research programme

The productive successor is now sharply constrained: prove the production move registry is
closed under semantically effective operations, or bind a stronger observation that
separates the relevant semantic classes. A larger state cap under the same observation is
not a response to this impossibility result.
