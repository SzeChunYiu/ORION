# ORION-01B claim ledger V3 — certifiable support versus intrinsic support

**Canonical scientific successor:** `theory-B-MANUSCRIPT_V3.md`  
**Historical predecessor preserved:** `theory-B-MANUSCRIPT_V2.md`

| ID | Claim | Status | Boundary |
|---|---|---|---|
| B3-C1 | `kappa(F,C)` is the mathematical minimum uniform intrinsic bound for the support statistic explicitly assigned to compiler family `F` under objective `C`; lower witnesses establish exact values but are not part of the definition. | **DEFINITION FIXED** | Removes the epistemic clause from V2 and requires the statistic to remain fixed in every comparison. |
| B3-C2 | For every `A subseteq F_2^d` and `H=<A>`, the rank-only zero-sum deletion language has terminal length `zsf(H;A)=rank(H)`. | **PROVEN / DONOR-OWNED** | Exact only for the elementary-binary generated-span language; supporting scaffold, not standalone novelty. |
| B3-C3 | Under the frozen R6M unit objective, the rank-only frame-support budget and intrinsic maximum frame support satisfy `beta_rank(F_M;C_M)=kappa(F_M;C_M)=2`. | **PROVEN-EXACT** | Rank-only is the explicitly named proof system; the same frame-support statistic is used on both sides. |
| B3-C4 | Under the frozen R6I unit objective, the rank-only joint active-column budget is `beta_rank(F_I;C_I)=5` and intrinsic joint active-column support is `kappa(F_I;C_I)=1`. | **PROVEN-EXACT / DEFINITION CORRECTED** | A word position is one block column in `supp(R_j0) union supp(R_j1)`. The earlier maximum-individual-frame wording measured a different statistic and is superseded; the support-five lower bound remains only for the registered rank-only system. |
| B3-C5 | The R6I separation is explained by whole-system Tag relocation/reconstruction, an operation absent from the rank-only language. | **SUPPORTED BY PARENT THEOREM** | Mechanistic interpretation scoped to frozen R6I. |
| B3-C6 | Registered `t`-component products have rank-only and intrinsic summed joint-column budgets `5t` and `t`. | **PROVEN IN DECLARED PRODUCT MODEL** | Additive joint active-column support and no cross-component move are defining assumptions and are used explicitly in the proof. |
| B3-C7 | Direct block-column support enumeration has ratio `Theta(n^(4t))` for fixed `t` as `n->infinity`. | **PROVEN IN DECLARED ENUMERATION MODEL** | Not algorithm-independent; additive gap as `t->infinity` is a separate limit. |
| B3-C8 | Every local or unrestricted proof system needs support five on R6I. | **NOT CLAIMED** | No such lower bound. |
| B3-C9 | Generic zero-sum theory, proof-system-relative complexity or direct-product amplification is novel. | **FORBIDDEN / DONOR-OWNED OR DEFINITIONAL** | These support the bounded formal comparison only. |
| B3-C10 | Structural gap implies hardware speedup. | **FORBIDDEN** | No physical-resource model. |
| B3-C11 | The finite R6M production-realization gate was rejected and the source-bound follow-up retained production transfer as unestablished. | **ADVERSE / FINITE_PRODUCTION_REALIZATION_CERTIFICATE_REJECTED** | Optimizer corroboration does not establish a complete production registry or production terminal authority. |
| B3-C12 | The PyZX Round-1 transfer attempt ended after 74 of 4,681 ordered words at `H0,H0,H0`: callable guards admitted a second `pivot_boundary_simp` that changed dense semantics even up to nonzero scalar, while scheduled production `full_reduce` preserved semantics on the same word. | **ADVERSE / CANNOT_CHECK_MOVE_COMPLETENESS** | Refutes only free reordering of the twelve macros under callable guards. Production `full_reduce`, complete move coverage, a realized certificate gap, and a complete-domain null are not established; Round 1 is consumed and cannot be retuned or relabelled. |
| B3-C13 | The rank-five R6I block-deletion word counts joint active columns, not maximum individual-frame weight. | **SCIENTIFIC DEFINITION CORRECTION** | The word deletes both independent generators at one column; all manuscript, product, search-volume, and package surfaces must use `max_j |supp(R_j0) union supp(R_j1)|`. |

## Submission rule

The journal novelty claim is the matched formal tight/loose pair (R6M versus R6I) and the identified missing compiler operation. The abstract zero-sum lemma and product amplification must remain supporting scaffolds, and the adverse PyZX transfer boundary must remain visible.
