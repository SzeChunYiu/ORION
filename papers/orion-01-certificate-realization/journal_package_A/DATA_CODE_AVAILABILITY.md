# Data and code availability

## Code availability

Source code for the exact finite checks, theorem-corollary verifiers, manuscript claim audits, and focused regression tests is versioned in the ORION repository. The all-size theorems are proved analytically in the manuscript; finite programs corroborate stated local obligations and production bindings rather than replacing those proofs.

The final submitted version should identify the immutable submission commit or archive DOI. Until that immutable archive exists, this package does not claim permanent archival availability.

## Data availability

This work uses generated finite combinatorial instances and machine-readable result records rather than observational, personal, or access-restricted data. Exact instance generators, registered counterexamples, result JSON files, and supporting evidence records are versioned in ORION.

No confidential or personal data are used.

## Authority boundary

Internal independent implementations establish same-programme reproducibility; they are not described as external replication. Any result object whose metadata withholds theorem, novelty, or external-replay authority retains that ceiling in the submission.

## Filing-time update required

Before submission, replace the repository-only locator with the immutable release/DOI and confirm that the archived inventory contains the source, claim ledger, verifier commands, expected result hashes, generated instances or figure source data where applicable, licence statement, and external replay instructions.
