# ORION-01 bounded ancillary controls

This directory preserves the existing deterministic finite controls for both integrated formal families. Run `python3 proof_checker_v3.py`, `python3 verify_dependent_triple_lemmas.py`, and the accompanying tests from a clean copy. These checks corroborate transcription, enumerated cases, and exact witnesses; they do not replace the all-size proofs, establish external replication, or authorize production move completeness.

The `adverse-pyzx-round1/` directory records the consumed 74-of-4,681 transfer attempt. Its terminal remains `CANNOT_CHECK_MOVE_COMPLETENESS`.
