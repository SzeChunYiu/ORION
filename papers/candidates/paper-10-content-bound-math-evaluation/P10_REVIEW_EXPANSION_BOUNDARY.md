# ORION-20 review-expansion boundary — 2026-08-20

The ORION-20 review surface now has two strictly separated layers.

## Completed positive layer

The completed V2.1 Mathlib source-transfer experiment and existing native identity/acceptance receipts are admissible evidence. Derived module-wise robustness may be added because it reads only frozen V2.1 block receipts and does not alter the original endpoint.

## Prospective mechanism layer

Native proof-state incremental value, module-invariant proof coordinates, TacMiner-class comparison, and verifier-backed proof-search utility are prospective. Their protocols are frozen separately under `research/extensions/p10-structured-reasoning/` and `research/extensions/p9-p10-structural-scaling/`.

These experiments may raise the paper's claim ceiling only after exact execution. Until then they are future work, not supporting evidence.

## Maximum currently defensible headline

> Coarse tactic-family behavior exhibits broad, revision-bound cross-module predictive regularity on a prospectively frozen Mathlib source corpus, with exact native-audit and identity controls separating source recurrence from native acceptance and scientific authority.

The headline must remain explicitly source-projection bounded. It cannot say proof-state semantics, reusable tactics, prover utility, or general theorem-proving transfer until the corresponding frozen experiments pass.
