# ORION-P5 package license and restrictions

Repository-level license is `CANNOT_CHECK`.

The private hidden-cause suite must never be committed to a candidate-readable branch (`REPRODUCE.md`, `protocol/PROTECTED_SUITE_FREEZE_V1.md`). **That rule was broken, and this sentence used to assert it had been kept.** `evidence/hidden-cause-suite/PROTECTED_SUITE_V1.json` was described here as the candidate-visible freeze surface rather than the evaluator-held opening material; it in fact publishes every `protected_root_cause`, `root_cause_nonce`, `success_rubric` and `harm_rubric` in plaintext, so it *is* the opening material and always was. A commitment digest cannot withhold a value printed beside it, and the labels are recoverable from the case ordinal alone in any case, since families are emitted in eight consecutive blocks of three. The suite is not a protected artifact; `protocol/PROTECTED_SUITE_FREEZE_V1.md` records the full disposition. Nothing is re-sealed: a commitment issued by a party who already knows the answer is not a commitment, and the run has been scored. Re-establishing a prospective hidden-cause result needs new cases authored under custody, not a repair to these.

Attribution probe logs may contain model outputs; they are diagnostic, not a license to redistribute provider terms.
