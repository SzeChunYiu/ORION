\section{Falsifiers and empirical programme}

\subsection{Local hostile falsifier V1}

The current local suite exercises the governance semantics rather than claiming open-world improvement. Registered cases include recurrence without causality, competing-cause discrimination, negative intervention retention, replay/fresh-transfer separation, evidence-bound reopening, invention-readiness constraints and protected-path reward-hacking attacks. A candidate that modifies protected registry/governance state is rejected even when visible and fresh score deltas are constructed to look perfect.

These tests support the statement that the implementation can represent and refuse the registered failure modes. They do not show that Self-ORION discovers correct causes or produces beneficial repairs on real development problems.

\subsection{Prospective external comparison}

A publication-level evaluation requires matched fixed-agent, direct-self-edit and ADAS/DGM-style baselines; failure causes hidden prospectively from the candidate; fresh task/domain/model transfer; resource accounting; a protected evaluator outside challenger custody; frozen evaluator chronology/holdout telemetry; negative-history completeness; and explicit host-only promotion.

Primary outcome coordinates should include failure-localization accuracy, repair success, fresh-transfer improvement, regression/harm rate, authority violations, false promotion, correct abstention/\texttt{CANNOT\_CHECK}, cost to resolution and the fraction of candidate variants retained in negative history.

The external governed self-improvement state remains \texttt{CANNOT\_CHECK} until those prospective observations exist. Repository CI, structural specification closure and local hostile tests cannot promote that state.
