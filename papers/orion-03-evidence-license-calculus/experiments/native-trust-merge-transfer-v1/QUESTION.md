# ORION-03 native trust-system merge transfer — frozen question

Identity: `ORION03.NATIVE_TRUST_MERGE_TRANSFER.v1`.

Does the typed merge/nonpromotion principle survive operational transfer to genuinely different native trust semantics, without forcing Sigstore, TUF and in-toto into a false common algebra?

Each system keeps its own native semantics and verifier. The shared question is only whether a merge or reuse candidate remains licensed when evidence/authority objects from distinct roles, epochs or policy scopes are combined, compared with flat provenance/key-value merging and the system's native policy.
