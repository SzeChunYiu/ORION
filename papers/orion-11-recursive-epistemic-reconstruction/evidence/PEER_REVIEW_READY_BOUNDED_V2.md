# ORION-11 bounded peer-review-readiness attestation

**Terminal:** `ORION-11 = PEER_REVIEW_READY`

This attestation covers only the credential-free mechanical mutation-necessity
claim reported by the prospectively frozen
`ORION-11.epistemic-mutation-necessity.v2.2.4` successor. It does not promote or
rewrite the historical 48-case `ORION-11.H1` result, which remains
`NOT_SUPPORTED` / `UNDERPOWERED`.

The readiness terminal is authorized by all of the following:

- the 2,882-world primary and disjoint replication each report
  `P1_MUTATION_NECESSITY_SUPPORTED` under all frozen efficacy, safety,
  non-regression, and cost gates;
- both 40,348-row archives pass independent score and analysis reconstruction
  with zero mismatches and exact terminal agreement;
- both result directories pass their committed `SHA256SUMS` manifests;
- `PRIMARY_REPLICATION_CONCORDANCE.json` records every required concordance
  check as true and binds the exact result hashes;
- `EXECUTION_BINDING_AMENDMENT_V2.json` records the cost-statistic correction
  before any arm was executed or scientific terminal observed, without a
  protocol, margin, or confirmatory-world change; and
- the manuscript, claim ledger, result table, generated figure,
  reproducibility instructions, and limitation boundary all report the same
  scoped claim.

The authorized claim is the tested conjunction of lower-level exclusion,
same-task counterfactual effect, protected-invariant preservation, and
dependency-impact binding after absorbing the strongest runnable parent
structures. Attribution, replay, active diagnosis, minimal repair,
diagnosis-to-recovery admission, dependency rollback, causal-context slicing,
and certificate enforcement remain credited donor substrate.

Model-general, model-bearing, open-ended-science, and unrestricted external
superiority are outside this terminal and remain unclaimed.
