# ORION-17 publication-freeze addendum V1

**Freeze date:** 2026-08-31  
**Status:** `CURRENT_EARNED_CEILING_FROZEN__MECHANISM_PROVED__COVERAGE_UNTESTED`

This addendum is part of the frozen ORION-17 paper-content packet. It records the
terminal `theory/cross-bucket-exposure-identity-v1/CLAIM_DISPOSITION.md` already
establishes, including the adverse route it retains, and grants no authority beyond
it. It is bound additively through `CONTENT_MANIFEST_V2.json`, re-pinned to the
commit that introduces this file; the V1 manifest and its frozen subject are
untouched.

## Earned scientific ceiling

The terminal is
`CROSS_BUCKET_EXPOSURE_MECHANISM_PROVED__PROSPECTIVE_COVERAGE_UNTESTED`, with
`scientific_authority_delta: NONE`. The target venue is Artificial Intelligence.

The mechanism is closed exactly, not approximately:

- an exact **per-transition false-retention identity** for the frozen donor-coarse
  instrument;
- an exact **universal-soundness criterion** — false retention is impossible for
  every changed-module set if and only if there are no cross-bucket transitive
  dependency pairs;
- an exact **singleton-change exposure count**, with an explicit counterexample
  construction for when the universal certificate fails;
- exact **preserve** and **unnecessary-reopen** sets;
- an exact proof of the one-bucket and all-buckets-changed **always-reopen
  degeneracy**.

A new outcome-independent candidate family — `C0_SAFE`, `C1_MAX_EXPOSURE`,
`C2_TOTAL_EXPOSURE`, `C3_EXPECTED_EXPOSURE` — is registered under a **new scientific
identity**, so it inherits no authority from the retained history below.

## Frozen boundary

**The historical density route is adverse and stays that way.** Its terminal is
`NO_DISCRIMINATION`, and this freeze retains it as an adverse record rather than a
pending success. A route recorded as non-discriminating must not be revived by
relabelling, and the new candidate family exists precisely because the old route
cannot be rescued — that is why it carries a new identity rather than extending the
old one.

The terminal names its own gap: the mechanism is **proved**, and prospective
coverage is **untested**. Those are different words on purpose. Nothing here
licenses a claim that the exposure identity has been shown to hold prospectively
over real change sets; it licenses only that the identity, its soundness criterion,
its counterexamples and its degeneracies are exact.

## Frozen content surface

The content packet is the set bound by `CONTENT_MANIFEST_V1.json` and
`CONTENT_MANIFEST_V2.json`, including `submission/AIJ_MANUSCRIPT.tex` with its cover
letter, highlights and closure-carrying section,
`theory/cross-bucket-exposure-identity-v1/` with its claim disposition and
independent checker, the retained density-route record at `NO_DISCRIMINATION`, and
this addendum. The ORION-17 claim is about closure transport under representation
and objective change; it does not own the dependency graphs it is evaluated over.

## A note on this paper's committed PDF

`manuscript/main.pdf` is currently stale relative to a clean pinned rebuild.
Refreshing it flips a reproducibility target from `BOUND` to `PARTIAL`, which the
repository records as a decision rather than an implementer's call. This freeze
therefore leaves the PDF untouched and does not claim it is current. The scientific
content above is bound by digest and is unaffected by that render question.
