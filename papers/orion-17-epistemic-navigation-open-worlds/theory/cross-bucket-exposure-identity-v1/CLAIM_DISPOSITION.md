# Claim disposition — ORION17.CROSS_BUCKET_EXPOSURE_IDENTITY.v1

Terminal: **CROSS_BUCKET_EXPOSURE_MECHANISM_PROVED__PROSPECTIVE_COVERAGE_UNTESTED**  
`scientific_authority_delta: NONE`  
Historical density terminal: **NO_DISCRIMINATION**

## Closed

- Exact per-transition false-retention identity for the frozen donor-coarse instrument.
- Exact universal-soundness criterion: no cross-bucket transitive dependency pairs iff false retention is impossible for every changed-module set.
- Exact singleton-change exposure count and explicit counterexample construction when the universal certificate fails.
- Exact preserve and unnecessary-reopen sets.
- Exact proof of one-bucket/all-buckets-changed always-reopen degeneracy.
- A new, outcome-independent candidate family `C0_SAFE`, `C1_MAX_EXPOSURE`, `C2_TOTAL_EXPOSURE`, `C3_EXPECTED_EXPOSURE` under a new scientific identity.

## What is retired

The completed density-vs-size mechanism identity remains non-discriminating. Nothing here supports threshold `density >= 1.5`, module cutpoint `49`, edge cutpoint `216`, or a post-hoc replacement threshold.

## Remaining top-tier empirical gate

On correctly rooted untouched repositories, measure:

- prevalence and practical coverage of the exact `C0_SAFE` certificate;
- preservation/unnecessary-reopen efficiency among certified repositories;
- prospective risk ordering of C1/C2/C3 on protected future transitions;
- external validity of the instrument's dependency graph relative to real dependency semantics.

The theorem supplies mechanism clarity; those empirical quantities remain unmeasured.