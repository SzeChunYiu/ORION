# Failure case — parent-domain omission during RAKL saturation

Status: `OPEN_SELF_ORION_CASE`.

## Observation

During a knowledge-saturation request about the RAKL/ORION method, major directly relevant parent disciplines — especially computational linguistics/NLP and measurement science/psychometrics — were not surfaced until a human explicitly challenged the omission.

## Why this matters

The event is not yet classified as a method-basis defect. Competing causes include execution depth, routing, retrieval, fixation/representation, stopping, memory/context use and genuine method-basis insufficiency.

## Required replay

Reconstruct the original task with answers hidden. Execute the intended search/absorption/reframing machinery faithfully and localize the first stage that fails to make the missing parent discipline discoverable. Freeze the discriminator before evaluating candidate repairs.

## General learning target

Do not merely memorize `NLP` or `psychometrics`. Learn a reusable failure signature for situations where a mature parent discipline studies a canonical subproblem that the current ORION search universe fails to represent.

A successful repair must transfer to fresh hidden parent-domain omissions not used to design the repair.
