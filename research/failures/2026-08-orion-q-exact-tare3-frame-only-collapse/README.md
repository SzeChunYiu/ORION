# ORION-Q MAX-R6 exact TARE-3 frame-only collapse

Date: 2026-08-20
Programme: #679 / PR #689
Authority: preserved negative evidence only; not an R6 result.

## Frozen lane

The exact joint TARE-3 auxiliary-frame DP was evaluated under the frozen protocol and Errata 1--3 on the already-open H4 and equilibrium-N2 subjects. The protected stretched-N2 prospective discriminator remained unread.

GitHub Actions run `32412327149` replayed the exact lane before prospective access. The primary refused protected access because `open_pre_prospective_ready = false`.

## Negative result

All software/integrity gates passed:

- candidate-blind P10 generation: PASS;
- hostile exact DP vs structurally independent brute solver: PASS;
- exact four-row panel on H4 and equilibrium N2: PASS;
- proof-carrying symplectic witnesses: PASS;
- strict improvement over `B_CANONICAL_STRONG` on both open subjects: PASS;
- protected stretched-N2 unread: PASS.

The controlling scientific gate failed:

`joint_beats_frame_only_strong_on_at_least_one_subject = false`.

For every one of the four frozen H4 rows and four frozen equilibrium-N2 rows, the exact joint optimizer reached the same structural support cost as `B_FRAME_ONLY_STRONG`; representative rows had `C_joint = 8` for both. The candidate strongly improved the canonical TARE frame (typically by 6--7 support units) but added no incremental value once the frame-only donor was strengthened to its exact global Uanti-minimum set and exact downstream Tag/Restore optimization.

Therefore:

`MAX_R6_EXACT_TARE3_JOINT_DP_SUPPORTED = false`

and the stretched-N2 coefficient file was not opened.

## Responsibility diagnosis

This is not evidence that TARE frame optimization is useless. It is evidence that, for fixed independent three-term blocks under the frozen objective, **joint frame/label/Tag/Restore optimization collapses to the strongest frame-only donor on the tested open chemistry triples**.

The residual responsibility remains method-language inadequacy. Searching more fixed independent TARE-3 frames under the same objective is not justified as the primary next move.

## Successor residual

The next development lane may test a composition-level operation unavailable to the independent-block comparator: reuse one auxiliary frame and its Clifford `Tag/Tag†` conjugation across several outer-LCU-selected TARE blocks, allowing the common conjugation to be factored out of the selector while block-specific Uanti coefficients and Restore operations remain branch dependent.

Any successor must give donors the strongest independently optimized block implementation and must count common-subcircuit factorization as donor capability where already published. No threshold or negative in this file may be removed to obtain a positive result.
