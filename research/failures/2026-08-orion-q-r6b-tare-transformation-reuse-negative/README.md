# ORION-Q MAX-R6B TARE transformation-reuse donor negative

Date: 2026-08-21
Programme: #679 / PR #689
Authority: preserved negative donor-closure evidence only; not R6 and no novelty authority.

## Frozen lane

MAX-R6B gave donor-owned TARE transformation reuse first right of refusal after the exact single-block TARE-3 joint compiler collapsed to the strengthened frame-only incumbent.

The frozen batch used two deterministic disjoint P10 window champions per already-open subject. The protected stretched-N2 discriminator remained unread.

Verified GitHub Actions run: `32445611956` (`orion-q-max-r6b-tare-reuse-verified`).

## Integrity result

All hostile reuse checks passed:

- signed Hermitian-Pauli multiplication table: PASS;
- valid reusable signature accepted: PASS;
- phase mismatch rejected: PASS;
- impossible label signature rejected: PASS;
- non-anticommuting destination rejected: PASS.

The receipt preserved:

- `novelty_credit = false`;
- `reserved_stretched_n2_accessed = false`;
- `r6_authority = false`.

## Scientific result

`MAX_R6B_TARE_REUSE_DONOR_NEGATIVE__NO_NOVELTY`

### H4

- strongest independent frame-only implementation: `C_separate = 16`;
- best valid common signed-Tag/correction reuse found: `C_reuse = 34`;
- delta `C_separate - C_reuse = -18`.

The common signature exists and is proof-valid, but forcing the second block into the same transformation incurs `24` units of Uanti support, overwhelming the common Tag/correction saving.

### Equilibrium N2

- strongest independent frame-only implementation: `C_separate = 16`;
- best valid common signed-Tag/correction reuse found: `C_reuse = 31`;
- delta `C_separate - C_reuse = -15`.

Again, a valid common transformation exists, but the transferred frame costs `22` units of Uanti support on one block and loses badly to independent weight-one frames.

## Interpretation

This closes the straightforward donor-owned reuse route on the frozen chemistry batches. Merely finding a common `U_tag` / correction signature is not sufficient: the global cost is dominated by the price of forcing another block away from its independently optimal weight-one frame.

The result strengthens the method-language residual. A viable successor must optimize **global composition**, not simply:

- search more isolated TARE-3 frames;
- reuse a transformation chosen independently for one block;
- weaken the frame-only comparator;
- count TARE/common-Clifford reuse as novelty.

A plausible successor operation is to choose multiple block representations jointly, allowing each block to sacrifice some local frame cost only when the resulting shared transformation structure reduces the total batch cost against the strongest independently optimized and donor-reuse incumbents. That operation requires its own donor subtraction, frozen open-subject protocol, hostile exactness and proof-carrying implementation before any new prospective access.
