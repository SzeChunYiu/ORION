# X1-B k=4 — exact final min-rank result: both surviving quotient orbits admit rank-2 witnesses

Parent: #900.
Frozen protocol: `X1B_K4_GLOBAL_BILINEAR_MINRANK_PROTOCOL.md`.
Reduction used: `X1B_K4_DIAGONAL_COMPLETION_REDUCTION_2026-08-22.md`.
Committed before restoring any omitted condition.

## Exact result

The exhaustive principal-basis diagonal-completion search was run over every off-diagonal specialization allowed by the committed affine RREF spaces.

Both surviving quotient orbits admit rank-2 symmetric completions over `F_5` satisfying **every** global disjoint-zero-sum edge equation.

### Orbit `942777`

One off-diagonal parameter exists. At specialization `t=3`, choose principal basis `S=(0,1)` with diagonal entries `(2,4)`. The resulting rank-2 matrix is

```text
[2,0,0,0,0,4,4,4,4,1,1,1,1]
[0,4,4,4,4,4,4,4,4,0,0,0,0]
[0,4,4,4,4,4,4,4,4,0,0,0,0]
[0,4,4,4,4,4,4,4,4,0,0,0,0]
[0,4,4,4,4,4,4,4,4,0,0,0,0]
[4,4,4,4,4,2,2,2,2,2,2,2,2]
[4,4,4,4,4,2,2,2,2,2,2,2,2]
[4,4,4,4,4,2,2,2,2,2,2,2,2]
[4,4,4,4,4,2,2,2,2,2,2,2,2]
[1,0,0,0,0,2,2,2,2,3,3,3,3]
[1,0,0,0,0,2,2,2,2,3,3,3,3]
[1,0,0,0,0,2,2,2,2,3,3,3,3]
[1,0,0,0,0,2,2,2,2,3,3,3,3]
```

Primitive replay gives value 1 for all 860 disjoint zero-sum-pair edge equations.

### Orbit `1470123`

Two off-diagonal parameters exist. At specialization `(t1,t2)=(3,3)`, the **same matrix B above** satisfies all 800 edge equations and has rank 2.

## Explicit rank-2 factorization

Let `S=(0,1)`. Then

`C=B[S,S]=diag(2,4)`, so `C^{-1}=diag(3,4)` over `F_5`.

Let `Y=B[:,S]`. Its 13 rows are

```text
(2,0),
(0,4),(0,4),(0,4),(0,4),
(4,4),(4,4),(4,4),(4,4),
(1,0),(1,0),(1,0),(1,0).
```

Then exactly

`B = Y C^{-1} Y^T`.

Thus the relaxed bilinear witness already lives in dimension 2; it is not a near-miss caused by the allowed dimension 3.

## Scientific consequence

The prospectively frozen global bilinear min-rank condition **cannot close** either final k=4 quotient orbit. Both remain compatible with a rank-2 abstract kernel bilinear model.

Therefore the next discriminator must restore information omitted by the position-matrix relaxation, beginning with:

1. whether the symmetric form is an actual ten-block group-algebra/catalecticant form generated by the ten previously removed quotient triples;
2. whether the residual kernel vectors arise from the original 13 lifted positions simultaneously with those ten triple block sums;
3. ultimately, original-index/block-exchange compatibility.

This is a negative result for the relaxed strategy, not evidence for a C15 counterexample. No C15 theorem, novelty, or scientific-authority promotion follows.