# #318 shared mechanism-assimilation process closure

**Candidate terminal:** `PROCESS_USEFUL_NOT_NOVEL` (earned only if the exact PR head passes repository CI).

The programme-level question is no longer whether `MechanismAssimilationReceipt.v1` is useful. P1/P2 already use donor subtraction, the core receipt has hostile checks for rebranding/lost assumptions/authority escalation/fake composition/ungrounded source, and this closure adds a prospective ADOPT/ADAPT/COMPOSE demonstration plus direct linkage to the existing novelty-state machinery.

## Prospective process demonstration

The pre-run packet `development/cross-domain-transfer-closure/ASSIMILATION_DEMO_V1.json` freezes one clean path of each kind before the outcome runner exists:

- `ADOPT`: signature-gated applicability remains donor-owned and ORION uses the donor name;
- `ADAPT`: an evidence-bound transfer record may add failure/falsifier/authority bookkeeping, but that delta is not itself scientific transfer novelty;
- `COMPOSE`: abstract insight + signature compatibility may be composed only with explicit composition evidence.

The executable demo seals all three through the actual `MechanismAssimilationReceipt.v1` hostile checker. Any BLOCKED/CANNOT_CHECK receipt makes this closure fail closed.

## Automatic novelty consequence

`build_assimilation_novelty_assessment()` routes the generic signature-gated-transfer claim through the existing #287 `NoveltyCase` machinery. Because the frozen strong parent product matches the richer contract arm and no decision-relevant residual remains, the novelty state must be `SUBSUMED`; publication novelty remains unauthorized.

This is the automatic proof requested by #318: an absorbed generic mechanism cannot remain simultaneously in the ORION candidate-delta set.

## Cross-domain transfer consequence

The paired #286 protected panel contains 40 held-out source→target episodes across software, retrieval, semantics, authority, and measurement. The strongest abstract parent (`ABSTRACT_INSIGHT_SIGNATURE`) and the richer `MECHANIC_CONTRACT_RECEIPT` arm are frozen to the same information and both reconstruct 40/40 with zero negative transfer and 5/5 partial-transfer decisions. Surface/ungated memory controls do worse. Therefore the process does **not** earn a new transfer primitive; the scientific value is abstraction + compatibility, already parent territory.

## Shared-process saturation rounds

Round 1 — P1/P2/P6–P8 donor-assimilation experience plus hostile receipt use: material effect was stricter attribution, subtraction and assumption/authority bookkeeping; no new scientific inference rule emerged.

Round 2 — structural-assimilation + prospective cross-domain transfer pressure: the richer contract remains useful as an audit/interchange object, but an equally abstract signature-gated parent matches transfer performance exactly and #287 removes the generic mechanism claim. No process-novelty residual survives.

## Boundary

`PROCESS_USEFUL_NOT_NOVEL` does not say literature review is unimportant. It says ORION should keep the source-bound receipt/process as research governance while declining to present donor assimilation itself as a novel scientific algorithm or transfer mechanism.
