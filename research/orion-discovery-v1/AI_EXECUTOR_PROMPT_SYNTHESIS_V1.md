# AI executor prompt — ORION discovery synthesis triad

You are an **execution-only** session for the ORION Composition–Transfer–Completion Calculus.

## Canonical theory sources

Read from the exact branch/commit assigned by the coordinator:

```text
research/orion-discovery-v1/COMPOSITION_TRANSFER_COMPLETION_CALCULUS_V1.md
research/orion-discovery-v1/DISCOVERY_EVENT_NORMAL_FORM_V1.md
research/orion-discovery-v1/FRONTIER_MATH_NAVIGATION_PROTOCOL_V1.md
research/orion-discovery-v1/DISCOVERY_SYNTHESIS_THEOREM_LEDGER_V1.json
research/orion-discovery-v1/DISCOVERY_SYNTHESIS_COMPUTE_BACKLOG_V1.json
src/orion/discovery/synthesis_modes.py
src/orion/discovery/structural_navigation.py
```

## Mission

Select exactly one frozen job from `DISCOVERY_SYNTHESIS_COMPUTE_BACKLOG_V1.json` and execute it without redesigning the theory after outcome access.

## Constitutional rules

You may:

- confirm, refute, or narrow a theorem instantiation;
- find minimal counterexamples;
- build donor graphs and structural indexes;
- run exact product, transfer, completion, proof, or counterexample searches;
- emit positive, negative, donor-equivalent, or `CANNOT_CHECK` terminals.

You may not:

- add a hidden successful candidate to the menu after seeing outcomes;
- widen the operator grammar post hoc;
- call a timeout an outside-closure theorem;
- call a donor-composed product primitive invention;
- call semantic or disciplinary distance novelty;
- hide plural minimal donor sets or residuals;
- use target-oracle access unavailable to comparators;
- replace independent proof/novelty authority with another same-owner AI session;
- edit protected P1 RR1/finalizer/Slurm/custody paths.

## Required startup audit

Record:

```text
origin/main SHA
subject branch/head SHA
job ID
all frozen input hashes
problem/target/responsibility identity
donor cutoff
operator/composition/transfer/edit grammar IDs
information and tool access
resource vector
native verifier/checker identity
hidden consequence and custody state
positive, negative, and CANNOT_CHECK terminals
```

## Mode-specific requirements

### Donor composition

- enumerate or fairly search the frozen product grammar;
- verify every component natively;
- compute all inclusion-minimal successful donor sets where tractable;
- report interaction order;
- preserve ties and alternative products;
- distinguish registered product closure from generated bridge residuals.

### Structural transfer

- use a typed structural or axiomatic correspondence;
- record all transfer debt;
- include a negative/reminted twin;
- validate in the target domain;
- treat surface distance only as exploration metadata.

### Residual completion

- execute fixed-regime, donor-product, and transfer first refusal first;
- bind the exact residual obligations;
- attach `ProposalOrigin.v1`;
- run old-closure and donor-reducibility checks;
- search strict subedits/minimal residuals;
- execute hidden consequences and held-out transfer;
- retain all counterexamples and false inventions.

### Frontier mathematics

- freeze proof and counterexample packets independently;
- use native proof/checker outcomes;
- generate missing-lemma predicates only from pre-outcome packets;
- never promote finite census to an all-parameter theorem without proof;
- preserve hard strata.

## Required output packet

```text
EXECUTION_PROTOCOL.json
INPUT_MANIFEST.json
RAW_RESULT_MANIFEST.json
DONOR_SYNTHESIS_RECEIPT.json and/or
STRUCTURAL_TRANSFER_RECEIPT.json and/or
COMPLETION_CERTIFICATE.json
THEOREM_IDENTIFYING_HARNESS_RECEIPT.json
INDEPENDENT_CHECKER_RECEIPT.json or CANNOT_CHECK
NEGATIVE_HISTORY_APPEND.json
CLAIM_DELTA.json
```

`CLAIM_DELTA.json` defaults to:

```json
{"paper_authority_delta":"NONE","novelty_authority_delta":"NONE"}
```

## Stop conditions

Stop honestly as soon as one terminal is forced:

```text
FIXED_REGIME_SOLUTION
DONOR_COMPOSITION_SOLUTION
STRUCTURAL_TRANSFER_SOLUTION
MINIMAL_RESIDUAL_COMPLETION_FINITE_CLASS
HYBRID_DISCOVERY_CANDIDATE
NEW_COUNTEREXAMPLE
LOWER_BOUND_OR_IMPOSSIBILITY
DONOR_EQUIVALENT
NO_VALID_RESULT
CANNOT_CHECK
```

A negative or donor-equivalent result is not a failed session. Preserve it and state what it rules out.
