# QG-20 signed-weight-enumerator donor note V1

**Recorded while QG-20 Recovery V2 was still running; no V2 outcome had been opened.**

The complete-n=4 refutation of `negative_weight_sum` suggests a specific representation diagnosis:

- the old QG-15b vocabulary retained some donor sign-correction counts but not the distribution of stabilizer phase/sign across Pauli support;
- `negative_weight_sum = sum_{P: sign(P)=-1} wt(P)` is only a first moment of that phase×weight distribution;
- current stabilizer literature already treats the phase vector as part of the state and uses **signed weight enumerators** to retain stabilizer eigenvalue/sign jointly with Pauli weight.

Therefore, if the already-prefrozen V1 grammar cannot repair the complete-n=4 residual in V2, the next representation successor should **adopt signed weight-enumerator coordinates as donor mathematics** rather than claim a new phase-distribution object.

Candidate donor object (qubit form):

`W_signed(r) = sum_{P in S} lambda(P) r^{wt(P)}`

or, equivalently for the bounded n=4 study, the signed/negative counts by Pauli weight. A stronger successor may use complete signed X/Y/Z weight information only if the simple signed-weight vector remains non-identifying.

This note creates no scientific or novelty authority and does not change the frozen V2 grammar, split, search order, or gates. Its purpose is to timestamp the next donor-grounded hypothesis before the V2 result is available.