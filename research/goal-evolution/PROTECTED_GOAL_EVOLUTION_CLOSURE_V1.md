# Protected goal-evolution closure — issue #288

## Candidate terminal

`REFUTED`

This is a bounded negative incremental-value result, not a claim that objectives never need revision.

`research/goal-evolution/PROTOCOL_V1.json` remains unchanged at its original `DESIGN_FROZEN / outcome_accessed=false` state. The additive execution packet preserves its three-layer authority model: hidden scientific intent is evaluator-only, candidate policies may propose operational-objective changes, and only protected host-side comparison may admit or reject them.

## Prospective result

The execution panel contains 32 invented cases: four variants for each of the eight V1 benchmark families. Candidate-visible diagnostics cover score pattern, constraint behavior, cross-context stability, measurement consistency, implementation status, and a fixed candidate objective portfolio. Gold responsibility, hidden intent scores, and protected constraints are evaluator-only.

The strong matched parent, `FIXED_MULTI_OBJECTIVE_PARETO_PROTECTED`, and `ORION_PROTECTED_GOAL_EVOLUTION` both reconstruct 32/32 correct next actions, zero false objective revisions, zero missed objective revisions, zero protected-constraint violations, zero protected-intent regressions, and 4/4 in every family. The independent implementation path reconstructs the same strong-parent metrics without calling the main policy/scorer.

Unconstrained SAGA-like and performance-triggered LLM rewrite controls each false-revise 12 cases, violate protected constraints on 8, and regress protected intent on 12. The fixed original objective misses all 16 cases that genuinely require objective revision. These controls show that the protected responsibility/admission boundary matters even though dynamic objective generation does not add value over the fixed Pareto parent on this panel.

## Scientific disposition

Keep:

- explicit separation of scientific intent, operational objective, and optimizer/evaluator;
- protected old-vs-candidate objective comparison;
- measurement/implementation responsibility checks before objective rewrite;
- host-side non-self-authorizing admission and rollback boundaries.

Strike at this resolution:

- a claim that ORION-specific objective generation/evolution adds scientific value beyond a strong fixed multi-objective/Pareto candidate portfolio under the same diagnostics and protected admission rule.

Therefore issue #288 closes as `REFUTED` only after exact-head CI and merge. The result does not refute goal revision as a phenomenon or SAGA-style goal evolution in other task regimes; it refutes the registered ORION incremental-value hypothesis on this frozen panel.
