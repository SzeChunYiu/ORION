# Revival backlog successor V1

**Status:** POINTER / CORRECTION LAYER  
**Does not replace** the seven-negative saturation drafts until one of them is on `main`.  
**Does not close** any child of #284.

## Duplicate drafts this document cites

Two open PRs add the same path `research/paper-programme-v1/REVIVAL_BACKLOG_V1.md`:

| PR | Branch | Why it exists |
|---|---|---|
| [#272](https://github.com/SzeChunYiu/ORION/pull/272) | `claude/revival-backlog-v1` | Doctrine-compliance pass: frozen +0.02 margin with no relaxation path; Deep zero-hit diagnostic folded in (PR #266). **Preferred merge.** |
| [#273](https://github.com/SzeChunYiu/ORION/pull/273) | `revival-backlog-v1` | Same seven-negative catalogue; overlapping correction notes. **Duplicate — close after #272 lands.** |

This successor does **not** copy that file. Child lanes should read the landed backlog (once merged) **and** the corrections below before treating any row as current-main authority.

## Corrections relative to those drafts (current main)

Verified on the parent worktree after fast-forward to current `main`:

1. **P4 V2 has already executed** and is `PEER_REVIEW_READY`. A backlog row that still treats the protected/hostile campaign as unrun is stale for the paper. The live scientific child is #281 (new acquisition study for the H3 null), which must not mutate V2.
2. **P1 power is two numbers, not one.** Claim ledger: H1 required n = 385; H2 required n = 2401. A revival lever that says “expand to TIER_B and assess H2” conflates the H1 precision tier with H2's tighter margin. Sample expansion remains a valid lever; calling TIER_B sufficient for H2 is not.
3. **Margin relaxation after outcome access is forbidden** as a confirmatory revival lever. #272 already removed that path; keep it removed.
4. **P5 local attribution is 21 correct of 24**, with three adjacent-level errors. That is diagnosis evidence for #282, not a 24-of-24 terminal and not fresh-transfer evidence.

Deep probe stage attribution **is** on `main` after #266: `papers/orion-12-open-world-scientific-discovery/evidence/external_results/DEEP_ZERO_HIT_STAGE_ATTRIBUTION_2026-08-17.json`. Candidate generation remains the failing stage; revival is corrected retrieval, not re-scoring.

## What this programme still owns

Machine-readable child status lives in `PROGRAMME_TRACKER_V1.json`. All ten scientific children are `OPEN` / not terminal. N1–N4 live in `NOVELTY_HYPOTHESES_V1.json` as certificates-to-be, not claimed novelty.
