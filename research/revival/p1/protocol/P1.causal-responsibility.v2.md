# P1.causal-responsibility.v2 — discriminator protocol

**Status:** `DESIGN_FROZEN`  
**`outcome_accessed`:** `false`  
**Does not mutate:** `P1.hidden-formulation.v1.1`, V1 results, or the #137
substrate protocol `P1.responsibility-diagnosability.v2`.

This freeze is the scientific object for GitHub issue #278. Repair outcomes,
live-provider scores, and holdout accuracy numbers must not be written into
this file. The machine-readable twin is
`P1.causal-responsibility.v2.json`.

## Why a new protocol

Issue #137 already landed diagnosability + scoped licensing. The residual
#278 tests is stricter: a probe may update causal *support*, but it must not
grant mutating *authority*. Authority requires a diagnosis-specific
intervention whose predicted observable moves in the expected direction, or a
frozen mechanical discriminator. `UNRESOLVED` is a competing class, not a
low-confidence guess.

## Competing hypotheses (frozen before outcomes)

| id | claim | discriminator | confusable with |
|---|---|---|---|
| H-R1 | Surface non-diagnosable from allowed observations | oracle-on-PublicView still UNRESOLVED; gold permutation does not move candidate accuracy | H-R2 |
| H-R2 | Signal exists; incumbent representation suppresses it | same observations, frozen representation transform recovers unique argmax | H-R1, H-R3 |
| H-R3 | Identifiable only after probe/intervention | passive UNRESOLVED; cheapest allowed probe/intervention separates | H-R2, H-R4 |
| H-R4 | Diagnosis adequate; licensing matrix wrong | oracle diagnosis + incumbent matrix still unauthorized or refuses warranted | H-R3, H-R5 |
| H-R5 | Diagnosis/licensing adequate; reframe/reopen fails | correct license issued; reframe/reopen still misses gold | H-R4 |
| H-R6 | Majority reduction masks per-seed H1 gains | seed-mean paired Δ disagrees with majority-reduced H1 in sign or vs +0.05 | — |

Do not choose among these by prose. Execute the discriminator named in the
JSON. A discriminator that cannot be run is `CANNOT_CHECK`, not a negative.

## Cause-confusable evaluation set

Fresh held-out **pairs/triples** share surface symptoms and differ in true
stage. Families:

1. retrieval miss vs search-universe miss
2. implementation failure vs environment/tool failure
3. representation gap vs method-basis gap
4. measurement miss vs formulation miss
5. evidence insufficiency vs genuine need to reframe

Gold stays in host custody. Candidates see `PublicView` plus allowed
probe/intervention ids. Constructed known-answer fixtures in
`orion.study.p1_causal.cases` are for hostile unit tests, not the prospective
holdout. The holdout split hash is `UNBOUND` until execution freeze.

## Baselines and ablations

Baselines and ablations are listed in the JSON. The oracle-diagnosis ceiling
is explicitly **non-runnable** and must not enter the candidate set.
Resource match is mandatory; extra probes are not a free lunch.

## Margins (frozen)

- responsibility accuracy superiority: **+0.10** vs strongest attribution baseline
- unauthorized mutation superiority: **−0.03** vs one-shot classifier
- root-success non-inferiority vs V1 `orion_full`: **−0.02** (safety guard)

Wilson 95% on the member unit; paired bootstrap 10000, seed `20260817`.
Five stochastic repeats nested inside the member; reduce before the interval.

## Positive-result verification

Any later positive must survive the list in the JSON (independent scorer,
raw-record regeneration, leakage probe, label permutation, post-freeze
holdout, optional second provider, no gold identifiers in traces). Route
positives through issue #283.

## Terminal

`MECHANISM_SUPPORTED` / `MECHANISM_REFUTED` / `CANNOT_CHECK` only.
Green tests do not close #278.
