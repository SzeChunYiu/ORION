# Live campaign status — #278

**Status:** `P1_MUTATION_NECESSITY_SUPPORTED` (primary and disjoint replication)

The credential-free v2.2.4 mutation-necessity campaign is complete. The primary
and pre-bound disjoint replication each contain 2,882 worlds, nine runnable
arms, five direct ablations and 40,348 raw rows. Every frozen support, safety,
non-regression, multiplicity and cost gate passes in both runs. Independent
rescoring reports zero score mismatches, zero analysis mismatches and matching
supported terminals.

Canonical evidence is under `confirmatory/v2.2/`; the cross-run verdict is
`PRIMARY_REPLICATION_CONCORDANCE.json`. The authorized result remains a
credential-free mechanical claim. Provider/model-general or open-ended
scientific superiority is outside this protocol and is not inferred.
