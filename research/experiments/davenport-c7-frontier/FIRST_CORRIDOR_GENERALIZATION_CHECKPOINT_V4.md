# First-corridor Davenport generalization checkpoint — 2026-09-04 V4

Status: **live theorem-development checkpoint after eliminating `a=1` shared multiplicities one through three; no `D_3` closure claim**.

## New theorem since V3

For the `a=1` support-three equality normal form

`V=e3^c x^r y^t`,

the new file

`A1_LIGHT_SUPPORT3_THREE_SHARE_ELIMINATION_V1.md`

proves for every prime `p>=7` that

`boxed{c!=3}`.

Together with the previous two branch theorems,

`boxed{c>=4}`

is now necessary for every hypothetical `a=1` exact-support-six support-three companion.

For `c=3`, the relation-multiple mechanism is already highly rigid: after `r>=q-2`, only three boundary multiplicities exist. Explicit multipliers split merely by `p mod 3` and kill every symbolic case for `p>=11` except one `p=13` boundary; exact radial-depth bases close that case and all of `p=7`.

## Current local frontier

The prime-uniform first-corridor support-seven target remains

> support-four maximal atom, `p>=7` => maximal pair support at least seven.

The unresolved equality mechanisms are now:

- `a=1` support-three with shared multiplicity `c>=4`;
- `a=2` light-share support-three;
- rank-three four-support companions, especially the `a=2` model;
- any other support-four type surviving the inverse-residue overlap selector.

The `a=1` family is no longer an amorphous two-dimensional box problem at small overlap: its first three radial layers are eliminated, and the reusable multiplier cost gives a systematic arithmetic attack on higher `c`.

## Next proof target

Do not continue layer-by-layer indefinitely. The `c=1,2,3` pattern suggests extracting a **uniform multiplier-existence lemma**:

> for `a=1`, if an exact-support-six support-three companion survives, then either a relation multiple gives a forbidden short zero-sum, or the multiplicity vector lies in a narrow centered/high-`c` window.

The second half should then be attacked by the mixed-subsum anti-depth phenomenon already visible in exact controls (`x y^j` is typically enough after radial pruning).

This would replace infinitely many `c` layers by two analytic regimes.

## Distance to the larger theorem

A complete first-corridor support-seven theorem now looks like a small number of serious structural lemmas rather than a large finite classification. It remains only a local bridge: the full generalized Davenport formula still needs the other first-failure corridors, larger-support maximal atoms, and the global rank-three positive-gain conformal refactor/augmentation mechanism.

## Claim ceiling

No `D_3(C_7^3)` value, all-prime `D_k(C_p^3)` formula, or novelty/priority claim is made here.
