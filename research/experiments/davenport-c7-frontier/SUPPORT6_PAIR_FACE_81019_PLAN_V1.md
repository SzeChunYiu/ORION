# Support-six pair-face census plan — V1

Status: bounded computational control only; no theorem claim.

The existing exact `(19,10)` pair enumerator proves the frozen stage-one counts `538,24,0` for the three canonical support-four maximal-atom types. The next control is to stratify those already validated pair candidates by the exact support-six normal form:

- total pair support exactly six;
- companion support three/rank two versus support four/rank three;
- light/heavy unsaturated maximal-atom overlap;
- exact overlap multiplicities.

This census is intended only to identify which equality branch is load-bearing before further prime-uniform proof work. It must reproduce the frozen `538,24,0` totals before any stratified output is trusted.
