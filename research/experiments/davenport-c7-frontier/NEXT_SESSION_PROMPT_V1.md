# Next-session prompt — C7 / general rank-three Davenport frontier

Continue the LLM-assisted ORION research on branch `shadow/davenport-c7-frontier-20260903` from its latest head. Read the current progress ledger, `CRITICAL_SHORTFREE_SUPPORT_MINIMUM_V1.md`, `CROSS_CONTEXT_REFRAME_V1.md`, and `SUPPORT8_ONE_PROJECTIVE_COLLISION_REDUCTION_V1.md` before doing new work.

Goal: push toward a general formula/mechanism for `D_k(C_p^3)`, not only `D_3(C_7^3)`. Borrow structure aggressively from factorization theory, generalized Noether numbers, restricted sumsets/additive-basis diameter, hypergraph matching, affine-semigroup/Graver-Hilbert methods, finite geometry and coding theory.

First targets: (1) kill or sharply reduce the `(8,10,19)` and `(9,9,19)` corridors using the new proper-subsum avoidance lemma by studying 9/10-term atoms rather than classifying all maximal atoms; (2) exactly close the support-8 branch with one projective collision using the existing 54 seven-point arc classes; (3) only then attack eight distinct projective directions. Generalize every successful lemma to `C_p^3` when possible.

Use ORION discipline but override immature scheduling when justified. Commit only verified deductions/checkers, keep donor attribution and `PRIORITY=CANNOT_CHECK`, preserve failed routes, and do not merge to `main`.