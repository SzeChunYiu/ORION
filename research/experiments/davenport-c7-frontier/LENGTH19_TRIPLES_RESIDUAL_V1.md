# Length-19 atom triples — inverse-structure residual V1

Status: **OPEN_METHOD_GAP**. No elimination claim.

The six-triple corridor leaves two patterns containing a maximal atom of length `D(C_7^3)=19`:

- `(8,10,19)`,
- `(9,9,19)`.

These are attractive because a length-19 atom is extremal for the classical Davenport constant, so one expects stronger inverse structure than for the remaining length-16--18 atoms.

## Donor search outcome

The current donor pass inspected general inverse zero-sum literature and papers on products of two atoms / sets of lengths. The strongest readily retrievable detailed product-of-two-atoms results are rank-two or concern different invariants. No rank-three theorem was found whose hypotheses are verified here and whose conclusion forces either a `10+19` or `9+19` zero-sum product to refactor into three atoms.

Accordingly, **no claim is made that either corridor pattern is impossible**.

## Exact residual

A hypothetical length-37 obstruction with one length-19 atom leaves a total-zero near-`D_2` complement of length 29 or 28:

- `10+19` at complement length 29;
- `9+19` at complement length 28.

Both complements have packing number exactly two and are 7-short-zero-free. The next useful theorem would be an inverse classification of such near-extremal `M_2` objects, strong enough to force a third factorization of the pair or to reduce them to a finite geometry family.

## Next discriminators

1. Classify zero-sum length-29 sequences over `C_7^3` with packing number two and shortest atom length ten.
2. Classify zero-sum length-28 sequences with packing number two and shortest atom length nine; the `4p=28` Zhao resonance explains why the simple Zhao route is silent here.
3. Search for donor results phrased in factorization-language aliases (`sets of lengths`, `products of two atoms`, `minimal zero-sum sequences`) before attributing any inverse lemma to ORION.

This residual remains open rather than being converted into a computational or novelty claim.
