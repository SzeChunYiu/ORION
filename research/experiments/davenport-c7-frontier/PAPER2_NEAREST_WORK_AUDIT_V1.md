# Paper 2 nearest-work and priority audit — V1

Status: **targeted public-source subtraction; priority remains `CANNOT_CHECK`**.

This audit was refreshed on 2026-09-04 after the `a=2` radial, depth-envelope, maximal-overlap, and `p=7` rank-three increments. It is sufficient to shape an honest manuscript and referee-facing related-work section. It is not an exhaustive MathSciNet, zbMATH, thesis, preprint, or non-English literature search, and it does not authorize a novelty or priority claim.

## 1. Multiwise Davenport framework

Michael Freeze and Wolfgang A. Schmid, “Remarks on a generalization of the Davenport constant,” *Discrete Mathematics* 310 (2010), 3373–3389, DOI `10.1016/j.disc.2010.07.028`, defines the multiwise constants `D_k(G)`, develops general bounds, and proves eventual arithmetic progression with difference `exp(G)`.

**Subtraction:** definitions, eventual linearity, general factorization interpretation, and broad `D_k` motivation are donor-owned.

The weighted multiwise/coding-theory direction is developed by L. E. Marchan, O. Ordaz, I. Santos and W. A. Schmid, “Multi-wise and constrained fully weighted Davenport constants and interactions with coding theory,” arXiv `1407.1966`.

**Subtraction:** links between multiwise zero-sum constants, constrained constants, codes, and projective caps are adjacent prior art. The present paper should not market the use of projective support geometry by itself as new.

## 2. Cyclic index-one donor

Y. Li, C. Plyley, P. Yuan and X. Zeng, “Minimal zero sum sequences of length four over finite cyclic groups,” *Journal of Number Theory* 130 (2010), 2033–2048, DOI `10.1016/j.jnt.2009.12.005`, proves index one for length-four minimal zero-sum sequences over prime-power cyclic groups of order coprime to six.

Later papers extend or reorganize the length-four index problem, including Li–Peng, arXiv `1303.1682`; Shen–Xia–Li, arXiv `1402.0219`; and Zeng–Qi, *Colloquium Mathematicum* 146 (2017), 157–163, DOI `10.4064/cm6702-1-2016`.

**Subtraction:** every invocation of the length-four index-one theorem in the `a=3` boundary lane is donor-owned. The ORION contribution is the capacity-aware embedding, exact radial surcharge, and explicit multiplier constructions around that donor theorem.

## 3. Short-zero-sum inverse structure

X. Zeng and P. Yuan, “On Sequences Without Short Zero-Sum Subsequences,” *Electronic Journal of Combinatorics* 30(4) (2023), P4.21, DOI `10.37236/11963`, studies inverse structure for sequences without short zero-sum subsequences under sumset-size hypotheses.

**Subtraction:** inverse short-zero-sum structure is an active established topic. The manuscript must distinguish its constrained maximal-pair setting, exact occurrence-depth method, and multiwise packing objective from general inverse zero-sum sequence results.

## 4. Objects not located in the targeted audit

The targeted title/abstract/keyword audit did **not** locate a primary source stating any of the following exact objects:

1. the canonical type-`a=2` radial formula
   
   `lambda_{2,c}(D)=D+2 ceil(max(D-c-2,0)/2)`;
2. the complete piecewise maximum representation-depth envelope on fibers `[P+Q]_p=w` for the type-`a=2` support-four maximal atom;
3. the inverse-quarter/half-step selector eliminating both standard high-multiplicity families on the top-overlap face;
4. the explicit 14-entry mixed-certificate closure of the `p=7`, type-`a=2`, rank-three support-four equality face;
5. the combined statement that both maximal `C_7^3` corridors exclude a support-four maximal atom with a support-six maximal-pair obstruction.

This negative search result is evidence for manuscript positioning, not a priority certificate. Exact or equivalent formulations may appear under factorization-theory, block-monoid, cross-number, lattice, coding-theory, or computational zero-sum terminology.

## 5. Provisional contribution boundary

The strongest defensible provisional contribution is:

> an exact representation-depth calculus for canonical support-four maximal atoms, combined with analytic and independently replayed finite eliminations that force support growth in both first generalized-Davenport corridors of `C_7^3`.

The paper should **not** presently claim:

- the exact value `D_3(C_7^3)=36`;
- a complete all-prime formula for `D_3(C_p^3)`;
- novelty of the generalized-Davenport framework, maximal-atom method, projective normalization, index-one theorem, or computer-assisted finite enumeration;
- a full classification of the surviving all-prime `a=2` high-multiplicity face;
- priority over unpublished theses, non-indexed preprints, or private computations.

## 6. Required external subtraction before submission

1. MathSciNet and zbMATH searches over `multiwise Davenport`, `generalized Davenport`, `representation depth`, `minimal zero-sum support`, `C_p^3`, and block-monoid factorization invariants.
2. Citation-forward and citation-backward audit from Freeze–Schmid 2010 and the Li–Plyley–Yuan–Zeng index papers.
3. Search theses and conference proceedings around rank-three elementary `p`-groups and exact `D_k` values.
4. Send the exact radial and fiber-envelope statements to at least one external zero-sum theorist for equivalence/prior-art checking.
5. Preserve priority as `CANNOT_CHECK` until that subtraction is signed off independently.

## 7. Venue positioning

The current theorem stack is plausibly sufficient for a strong specialist submission if written as a structural/computer-assisted zero-sum paper rather than an exact-value announcement. Suitable venue authority remains external and `CANNOT_CHECK`; no acceptance probability is asserted.
