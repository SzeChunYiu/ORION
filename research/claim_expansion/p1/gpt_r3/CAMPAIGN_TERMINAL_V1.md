# P1-U GPT-R3 campaign terminal V1

Parent: #649  
Campaign: #716  
PR: #717

Terminal: `P1_R3_CANNOT_CHECK_SOURCE_UNIVERSE`

## Exact frozen outcome

The dedicated `p1-u-r3-outcome` workflow ran the immutable 2022 acquisition package through the frozen evaluator and passed its fail-closed assertions.

Acquisition:
- 40/40 frozen query dispositions present;
- disposition/case linkage valid;
- 37 admitted source-disjoint cases;
- 37 recorded actual domains;
- minimum case and domain quotas pass;
- gold-class counts: SEARCH 4, REPRESENTATION 4, IMPLEMENTATION 6, MEASUREMENT 6, OBJECTIVE/MODEL 5, PROBLEM_BOUNDARY 5, UNRESOLVED 4, NO_HIGH_LEVEL_REFORMULATION 3;
- minimum-per-gold-class gate fails only because clean no-high-level controls are `3 < 4`.

`policy_outcomes_generated = false`.

No ORION/B3 policy was executed on the held-out corpus, so R3 provides no positive or negative performance evidence for ARD. It establishes a source-universe construction failure: independent web acquisition can populate genuine failure responsibilities more readily than matched controls, and forcing CTRL-5 into the control class would erase its real implementation/artifact defect.

## Successor lesson

R4 must not solve this by relaxing the control quota or hand-picking another replication paper. Instead it should prospectively construct **matched within-source control/adverse pairs** so the no-reformulation condition is generated from the same scientific source family as the failure episode and cannot become a separate search bottleneck.
