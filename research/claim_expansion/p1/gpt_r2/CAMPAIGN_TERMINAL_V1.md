# P1-U GPT-R2 campaign terminal V1

Parent: #649  
Campaign: #711  
PR: #714

Terminal: `CANNOT_CHECK_ACQUISITION_COVERAGE`

## What was earned
- pre-outcome R2 candidate/comparator policy and evaluator were frozen;
- dedicated P1 policy/evaluator workflow passed;
- gold/source fields were hidden from candidate policy and probe access was evaluator-gated;
- no candidate or comparator held-out outcome was generated before acquisition failure.

## Why the campaign cannot answer #649
The frozen acquisition design required every registered query to yield the first qualifying source inside a fixed first-eight ranked scan. After individual-query ranking was made observable without changing query text or qualification rules:
- `CBIO-3` had no defensible qualifying computational-biology episode inside the frozen first-eight scan without stretching the registered domain or scanning deeper;
- `CBIO-4` likewise had no qualifying representation/interface episode inside its first eight.

The earlier batched search attempt is separately preserved as invalid because merged result streams made per-query rank unauditable; no cases were admitted from it.

## Scientific disposition
No GRS, ORION-vs-B3 contrast, superiority, harm, or replication claim exists for R2. The failure is acquisition identifiability/coverage, not evidence for or against ARD scientific value.

The successor #716 changes the source-universe construction while retaining the R2 decision rule and preserving this negative history.
