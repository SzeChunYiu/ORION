# #318 shared assimilation process — no-material-change rounds V1

Scope: programme-level `MechanismAssimilationReceipt.v1`, **not** P1/P2 paper-local literature.
Authority: **NONE**.

## Round A — prospective clean-path pressure

Question: after executing one programme-level full-text `ADOPT`, one `ADAPT`, and one evidence-backed `COMPOSE`, does the shared receipt process require a new field, new authority state, or new hostile rule to represent those paths honestly?

Inputs are the three source-grounded donors in `PROGRAMME_ASSIMILATION_EXPERIMENT_V1.py`, each already bound upstream to a full-text `ScientificStructureAssimilationReceipt.v1` from #454.

Expected machine check: all three seal as `ADMITTED`, all remain content-addressed and non-self-authorizing, and the disposition set is exactly `{ADOPT, ADAPT, COMPOSE}`.

**Round-A result if CI reproduces the check:** `NO_MATERIAL_CHANGE`. The current receipt schema represents the three materially different absorption paths without schema extension or authority escalation.

Reopen if any path requires inventing an ORION-side field from donor prose, if a structural binding is absent, or if a supposedly clean path triggers a hostile finding.

## Round B — hostile inversion pressure

Question: from the opposite direction, can the current process detect the known ways a clean programme receipt can be laundered after the fact, or does a new hostile operator have to be added?

The executable hostile round mutates the prospective drafts one axis at a time and requires the existing checker to detect:

- rebranding: rename a donor mechanism while removing the stated delta;
- lost assumptions: drop a donor assumption without justification;
- authority escalation: raise ORION authority above donor authority;
- fake composition: retain `composed_with` while deleting composition evidence;
- missing structural prerequisite: mark a conceptual donor material while deleting its structural receipt identity.

**Round-B result if CI reproduces all five attacks:** `NO_MATERIAL_CHANGE`. These attacks are already represented by the frozen hostile vocabulary; no additional process operator is justified by this tranche.

Reopen if any mutation seals `ADMITTED`, if a definite hostile defect is masked by `CANNOT_CHECK`, or if a future clean absorption cannot be represented without weakening one of these checks.

## Scientific boundary

Two no-change rounds establish bounded **process saturation for the tested receipt mechanics**, not scientific superiority or novelty of the assimilation method. #318's process terminal still depends on the programme-level transfer consequence under #286 and on the #287 novelty consequence. `AssimilationNoveltyImpactReceipt.v1` supplies the latter on this branch. A cross-domain scientific transfer terminal must come from #286; a green unit test cannot substitute for it.
