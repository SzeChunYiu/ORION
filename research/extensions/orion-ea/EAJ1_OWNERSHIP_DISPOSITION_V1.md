# EAJ-1 ownership disposition V1 — generated-coordinate synthesis is State-as-Computation

**Date:** 2026-08-23  
**Issue:** #963  
**Protocol preserved:** `EAJ1_GENERATED_SCHEMA_PROTOCOL_V1.md`  
**Terminal:** `EAJ1_GENERATED_COORDINATE_ROUTED_TO_P11_STATE_COMPILATION`.

## 1. Re-review trigger

After freezing the EAJ-1 generated-coordinate protocol, the programme re-read current ORION publication ownership rather than assuming the new issue owned the experiment.

P11-U #471 has the central thesis:

> state can be computationally constructed for the current task;

and explicitly owns:

- query/task-specific state compilation;
- learned compilers;
- representation rank vs compiler/decoder complexity;
- no-answer-laundering;
- total state-construction resource accounting;
- optionality/recompilation after semantic regime change.

EAJ-1 V1 proposed exactly:

```text
raw observation O
-> synthesize derived coordinate z := p(O)
-> Compile_R1(O) = (Compile_R0(O), z)
-> same downstream solver performs better on fresh tasks
```

This is a **state compiler** study even though the new coordinate was described as a schema edit.

## 2. Why terminology does not create a new scientific object

Calling `z := p(O)` a `new primitive`, `schema coordinate`, or `epistemic relation` does not change the functional experiment:

- the meta-language is fixed;
- the candidate is a program in that language;
- it compiles raw evidence into a task-relevant state;
- downstream state/interface remains fixed except for the compiled coordinate;
- success is measured by downstream accessibility/solve value.

That is squarely P11's State-as-Computation object.

A separate EAJ paper/claim here would duplicate P11 under new vocabulary.

## 3. Disposition of EAJ-1 V1

`EAJ1_GENERATED_SCHEMA_PROTOCOL_V1.md` is preserved as a **struck/routed design**, not deleted.

Its useful components should be consumed by P11 if they strengthen an existing P11 learned-compiler protocol:

- exact information-collision prerequisite;
- generated typed coordinate program;
- same downstream solver before/after compilation;
- origin examples removed before fresh utility scoring;
- symbolic/CEGIS/library-learning baselines;
- false-compilation/no-op controls;
- explicit compiler/verifier cost.

No EAJ result should be executed under this identity unless P11 explicitly delegates a distinct sub-question.

## 4. What would be above P11 compilation?

To remain distinct from ordinary state compilation, a candidate would need to change the **compiler/schema meta-language itself**, for example:

```text
M0 (allowed type/relation/operator language)
  -> M1 = M0 + genuinely new constructor/operator/type
```

where no program in `M0` under the registered bound can provide the required state interface.

But this object immediately overlaps other owners:

- ORION Jump #500 / J3 #512 — concept/primitive/regime invention;
- P10-U #663 — method/operator/representation-space expansion after certified obstruction;
- DreamCoder/Stitch/LILO/TheoryCoder and grammar-learning donors — learned reusable abstractions/operator-language growth;
- Self-Revising Discovery Systems — verified scientific schema/regime transition.

Thus moving “one level higher” does not recover an independent EAJ owner; it routes into Jump/P10 plus donor-complete language-learning work.

## 5. Scientific consequence

The ownership study closes a common research failure mode:

```text
same experiment + new vocabulary != new scientific lane
```

The original ORION-EA intuition remains useful as a **cross-program design philosophy**:

- P9 identifies which state coordinates are load-bearing;
- P11 constructs/compiles accessible state;
- exact kernels perform deterministic state revision;
- P10/Jump propose method/schema changes only after bounded insufficiency;
- P6/P7 transport/revalidate obligations;
- P4/P8/P5 verify/authorize/adopt;
- quantum/QG supplies exact hostile domains.

But that composition does not require an independent EAJ scientific owner.

## 6. Terminal

`EAJ1_GENERATED_COORDINATE_ROUTED_TO_P11_STATE_COMPILATION`

No learned-proposer execution under EAJ-1 V1 is scientifically justified after this ownership audit.
