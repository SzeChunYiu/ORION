# ORION-EA / EAJ programme closure V1

**Date:** 2026-08-23  
**Parents:** #957, #963  
**Scientific terminal:**

`ORION_EA_PROGRAMME_COMPLETE__FIXED_SCHEMA_INFORMATION_VALUE_SUPPORTED__EXACT_REVISION_SUFFICIENT__NO_DISTINCT_EA_EAJ_ARCHITECTURE_OWNER`

**Package status:** research closure; exact-head CI/merge remains an independent repository gate.

## 1. Original aspiration

ORION-EA began from the hypothesis that a future LLM might benefit from learning a persistent typed epistemic state update process in addition to next-token prediction:

```text
P(x_{t+1} | x_<=t)
+
P(Delta E_t | E_t, observation_t, latent_t).
```

The long-horizon aspiration was a self-expanding epistemic model able to change its own reasoning representation when the current language became inadequate.

The programme was intentionally permitted to contract or disappear under donor pressure.

It did.

## 2. EA-0 — fixed-schema donor saturation

Fresh nearest-work research found strong current parents for every broad fixed-schema ingredient:

- continuous/recurrent/intrinsic latent state;
- state commitment / temporary-computation versus persistent-state training;
- explicit learned belief bottlenecks;
- atomic beliefs with uncertainty;
- belief-policy decoupling and belief-aware RL;
- dynamic learned belief graphs;
- state-first memory, supersession and stale-memory revocation;
- exact dependency retraction and rollback repair;
- explicit epistemic-logic state updates;
- reasoning-consistent knowledge editing;
- verified scientific regime revision.

The initial residual was reopened when closer work appeared rather than defended.

Final fixed-schema saturation terminal:

`EA_FIXED_SCHEMA_BELIEF_UPDATE_SATURATED__EAJ_SCHEMA_EVOLUTION_RESIDUAL_SURVIVES`.

Interpretation: **learning an explicit/recurrent/structured belief state is an active and already occupied field; it is not a distinct ORION-EA novelty surface.**

## 3. EA-1 exact information study — positive bounded result

The programme implemented `EpistemicState.v0` with exact evaluator semantics for:

- claims/evidence/methods/failures/obligations;
- support/require/defeat relations;
- representation occurrence versus semantic identity;
- scoped failure reopening;
- obligation transportability;
- explicit `UNKNOWN` distinct from refutation.

Two exact hostile pair families were constructed.

### 3.1 Representation/failure scope pair

A representation **remint** and a material semantic representation change have identical weaker typed payloads but require different failure-reopen deltas.

Balanced pair:

```text
TYPED deterministic ceiling = 1/2
FULL deterministic ceiling  = 1
```

### 3.2 Obligation transport pair

Transportable versus nontransportable old-regime obligations have identical weaker typed payloads but require different post-change states.

Again:

```text
TYPED deterministic ceiling = 1/2
FULL deterministic ceiling  = 1
```

Combined four-case panel:

```text
TYPED: 2 fingerprints / 4 cases, 2 collision classes, ceiling 1/2
FULL : 4 fingerprints / 4 cases, 0 collision classes, ceiling 1
```

Terminal:

`EA1_TYPED_SCOPE_COORDINATES_LOAD_BEARING__FULL_V0_IDENTIFIES`.

This supports only the bounded statement that the hidden scope/semantic coordinates can carry necessary information on the frozen hostile worlds.

## 4. EA-1 exact learned-value study — negative analytic result

The protocol originally asked a model to predict the minimal state delta given:

```text
full E_t + explicit primitive intervention I_t.
```

But the V0 rules are deterministic. Therefore the exact kernel defines:

```text
K(E_t, I_t) -> (E_{t+1}, Delta E_t*).
```

A typed serialization + exact kernel reaches the exact ceiling by construction. No learned propagator can strictly beat it on the same semantics/information.

Terminal:

`EA1_DONOR_EXACT_REVISION_SUFFICIENT`.

Scientific lesson:

```text
missing information != missing model capacity
```

and

```text
full required information + known deterministic mechanic
-> exact mechanic gets first right of refusal.
```

This preserves P9's strongest design doctrine and prevents architecture theatre.

## 5. EAJ — attempted escape to schema evolution

The programme then moved one level higher: perhaps the real residual was not updating state inside a fixed schema but changing the schema/type language itself.

### 5.1 Finite-menu theorem

The first exact boundary was proved:

if candidate schema edits form a finite set, an exact verifier can evaluate every candidate within budget, and the proposer must choose from that same set, complete exhaustive evaluation has pointwise solve rate at least that of any proposer.

Terminal:

`EAJ_FINITE_MENU_CLASS_CLOSED`.

Therefore a hidden finite edit menu cannot establish learned schema-invention superiority.

### 5.2 Donor preflight

The high-order search absorbed:

- DreamCoder/Stitch/LILO/library learning;
- TheoryCoder-style learned reusable abstractions;
- grammar/DSL evolution and grammar adaptation;
- ontology/schema construction;
- unsupervised concept discovery;
- latent-variable/causal structure learning;
- M-open model discovery;
- verified scientific regime transitions;
- ORION Jump/#512 itself.

The only candidate conjunction left was necessity-gated generated schema change + executable semantics + preservation/transport + protected fresh consequence + donor-complete comparison.

Terminal:

`EAJ0_BOUNDED_RESIDUAL_SURVIVES__HIGH_RISK`.

## 6. Ownership audit closes the independent EAJ lane

A generated-coordinate protocol was frozen as the first possible EAJ experiment:

```text
raw O -> derive z := p(O) -> compile improved state -> same downstream solver.
```

Re-reading current ORION ownership showed that this is exactly **P11 State as Computation**:

- task-specific state compilation;
- learned compilers;
- accessibility versus downstream capacity;
- compiler-cost accounting;
- future optionality/recompilation.

Calling `z` a “new epistemic primitive” does not change the experiment.

Terminal:

`EAJ1_GENERATED_COORDINATE_ROUTED_TO_P11_STATE_COMPILATION`.

Moving one level higher again — changing the compiler/meta-language itself — enters objects already owned by:

- ORION Jump/#512 (concept/regime invention);
- P10-U #663 (method/operator/representation-space expansion after certified obstruction);
- abstraction/library/grammar-learning donors;
- verified self-revising scientific-system donors.

Thus there is **no clean independent scientific owner left for EAJ**.

## 7. Final research council verdict

### Neural architecture lead

No native epistemic-state architecture is justified by current evidence. Fixed-schema belief/state learning is already occupied, and the exact V0 propagation task is algorithmically closed.

### Formal systems lead

The useful ORION contribution is the state/obligation/failure semantics and exact verification discipline, but those are upstream ORION objects rather than a new neural primitive.

### Causal/statistical lead

The exact hostile-pair result is valuable because it distinguishes information insufficiency from compute/model insufficiency. No learned-model claim follows from it.

### AI-for-science/discovery lead

Schema/regime invention remains a legitimate frontier, but ORION Jump/P10 and close current discovery/abstraction parents already own the conceptual object. A future positive should be run under those owners, not a parallel EAJ identity.

### Hostile novelty/reproducibility lead

Keeping EA/EAJ open as an independent architecture lane would now create terminology duplication and incentivize a model run whose target is already exact/donor-owned. Close and route.

## 8. What survives and where it goes

### P9-U #662 — representation accessibility/scaling

Consume the EA information result as another reminder that representation can change what is learnable/identifiable. Any real LLM scaling effect remains P9-U's empirical responsibility.

### P11 #471 — State as Computation

Consume the struck EAJ generated-coordinate design if useful:

- require exact non-identifiability before compiler escalation;
- compare generated coordinate programs with symbolic/CEGIS/library baselines;
- remove origin examples before fresh utility scoring;
- charge compiler work;
- preserve no-op/no-compilation controls.

### P10-U #663 + Jump #500/#512

Own future language/method/schema expansion after independently certified insufficiency. Use the finite-menu theorem to forbid small hidden-edit-menu “invention” benchmarks.

### P13 #666 — responsibility-carrying state

The V0 failure/obligation/representation semantics are directly relevant to responsibility-relative reuse/reopening. P13 is the natural owner for richer certificates/scopes.

### QG-20 #863 / ORION-Q

Quantum remains an excellent exact test laboratory. If QG-20 exposes a missing compiler coordinate, a learned proposer may be evaluated as a **proposal-side baseline** under P10/P11/Jump ownership; the quantum programme retains the quotient/coordinate theorem.

The exact Q-EA belief-surgery cases remain useful as hostile evaluator fixtures for access/resource/error/failure changes.

## 9. What ORION can still contribute to better LLM systems

The closure does **not** imply ORION is irrelevant to LLMs. It changes the architecture thesis.

The strongest current synthesis is:

```text
LLM / learned proposer
        |
        v
P9/P11 accessible structured state
        |
        +-- exact deterministic mechanics where known
        |
        v
P10/Jump proposal of genuinely new method/schema only after bounded obstruction
        |
        v
P6/P7 preservation / revalidation / transport
        |
        v
P4/P8 verification / scientific authority
        |
        v
P5 governed persistent adoption
```

This is a **system architecture / research operating system hypothesis**, not a newly discovered neural learning law.

A revolutionary result remains possible if the composed system wins on prospectively protected real tasks under matched resources. The owner of that claim is the existing ORION programme stack, especially P9-U/P10-U/P11/P5-U, not a duplicate EA paper.

## 10. Final nonclaims

No claim that:

- tokens are obsolete;
- ORION discovered belief-state learning;
- ORION-EA improves LLM reasoning;
- native epistemic state beats serialization;
- current LLMs can invent new scientific concepts;
- ORION has solved scientific Jump;
- ORION-EA improves quantum research;
- the V0 epistemic schema is universal.

## 11. Final terminal

`ORION_EA_PROGRAMME_COMPLETE__FIXED_SCHEMA_INFORMATION_VALUE_SUPPORTED__EXACT_REVISION_SUFFICIENT__NO_DISTINCT_EA_EAJ_ARCHITECTURE_OWNER`

The correct research action is to **close #957/#963 as independent scientific lanes and route the surviving discriminators into their existing owners**, while retaining every negative and exact artifact in this directory.
