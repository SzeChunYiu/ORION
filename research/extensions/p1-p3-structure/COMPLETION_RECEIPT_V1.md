# P1/P3 structural bridge — bounded completion receipt

Parents: #404/#405/#408/#409. Execution: #445. Coordinator: #443 / #403.

Integration revalidation base: `main@98c1c34a97e05d87628ffb648cf2f2b85d8ed884` (2026-08-18). Any older CI run whose merge base predates this identity is historical only; closure requires a fresh exact-head run against current main.

## Scientific disposition

This tranche closes the executable **structural-language substrate**, while deliberately narrowing the requested natural-paper expert studies to an exact-ground-truth non-vacuity pilot.

- `#404`: target terminal `P1_METHOD_REALIZATION_BRIDGE_MERGED`.
- `#405`: `P1_METHOD_RECONSTRUCTION_NARROWED` — a deterministic three-domain panel supports recoverability/corruption discrimination; no human/expert real-paper generality is claimed.
- `#408`: target terminal `P3_METHOD_STRUCTURE_PROJECTION_BRIDGE_MERGED`.
- `#409`: `P3_METHOD_PORTRAIT_NARROWED` — six prospectively frozen exact-ground-truth pairs test aligned/obstruction/unresolved semantics; the 50–100-paper, two-annotator expert corpus is retired from the present paper and remains a possible later P9 data programme.

## P1 `MethodRealization.v1`

Implemented in `src/orion/transfer/v2/p1_method_realization.py`.

It binds source/version identity, target role, preconditions, assumptions/resources, representation in/out, mechanic dependency graph, invariants, progress, effect, terminal, reconstruction, failure semantics, lineage, authority boundary and explicit unknown coordinates. Canonical serialization is content addressed. Completeness is conjunctive for mandatory coordinates; invalid authority widening and broken dependency references fail closed. `material_differences` treats every typed coordinate as potentially material; only P6 may later erase coordinates under a declared claim.

Hostile tests cover invariant deletion, reconstruction deletion, dependency reversal, precondition swapping, generic-failure collapse, lineage deletion, unknown fabrication, authority widening and digest tampering. A no-alarm round trip and claim-relative substitutability/unresolved controls are present.

The Paper-I manuscript now contains `Method realizations as reconstructable transformation structures`, a concrete midpoint false-analogy example, and the P3/P6/P9 ownership boundary.

## P3 `MethodStructureProjection.v1`

Implemented in `src/orion/transfer/v2/p3_method_projection.py`.

A source-local projection binds exact source/span and realization identities, target/obstruction, assumptions/preconditions, representation changes, auxiliary objects, mechanics/dependencies, invariants, progress/termination, reconstruction, failure statements, source-supported rationale and unknowns. `MethodAlignment.v1` compares declared coordinates with states `ALIGNED / RELATED / OBSTRUCTION / UNRESOLVED`, retains unmatched assumptions, and emits a recoverable obstruction certificate. It explicitly cannot claim P6 equivalence/fibre membership. The P1→P3 adapter maps upstream unknown coordinate names such as `progress_measure` and `reconstruction_map` into the P3 comparison vocabulary rather than misreading unsupported source coordinates as known mismatches.

Paper III now includes `Method-structure projection`, `Cross-domain method alignment and plural views`, a clean vocabulary-changing example, a false-merge case, an unresolved case and a downstream P6/P9/P10 boundary. Its evaluation text explicitly separates the existing public-reference mapping result from the new bounded structural pilot.

## Frozen pilot

Files:
- `research/extensions/p1-p3-structure/PROTOCOL_V1.json`
- `research/extensions/p1-p3-structure/run_structure_pilot.py`
- `research/extensions/p1-p3-structure/RESULTS_V1.json`

The protocol was committed before the result receipt. It contains three domains (`numerical_methods`, `graph_algorithms`, `transactional_workflows`), six true/false/unresolved method pairs and eight controlled corruptions.

Frozen result:
- typed P3 pair-label accuracy: `6/6 = 1.0`;
- transcript/action-overlap comparator: `2/6 = 0.333333...`;
- P1 complete-realization recoverability on five fully specified fixtures: `1.0`;
- corruption-detection recall: `8/8 = 1.0`;
- false-substitutability rate on the prospectively false pair: `0.0`;
- correct `UNRESOLVED` rate: `1.0`.

Protocol digest: `sha256:c41b7e055b1886ce27cf0f05969ad645a615431b1acaaa83dbcdf045326c3368`.
Result digest: `sha256:79a8884b060baeb093a3d065b6528c29e0dfa998b366772a9d27e0de77fc1e48`.

## Claim ceiling

The pilot is authored exact-ground truth. It does **not** establish annotation reliability over arbitrary papers, expert agreement, five-domain natural-corpus stability, downstream P9 advantage, or general method equivalence. Those stronger questions are not left as ambiguous unfinished requirements in P1/P3; they are explicitly outside the bounded present-paper claim and may be reopened as a P9 corpus study if needed.
