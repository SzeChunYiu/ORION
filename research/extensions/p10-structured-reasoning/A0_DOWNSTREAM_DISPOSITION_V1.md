# P10 A0 downstream disposition V1 — frozen before official A0 outcome

This file prevents post-outcome rescue of the refounded P10 paper by changing the research question after A0 is seen.

## If A0 = `P10_RESPONSIBILITY_CONTROL_INCREMENTAL_VALUE`

Only then:

1. freeze A1 live proposal-generation protocol separately;
2. hold the A0 controller and candidate interface fixed;
3. compare proposal generators/models/providers under matched controller/information/tool budget;
4. require at least one verified external/exact task-family gain before any `LLM + P10` problem-solving claim;
5. keep Pioneer/Stage D optional and downstream.

A0 alone would still be a bounded controller result, not an LLM paper.

## If A0 = `P10_EXISTING_STRUCTURED_CONTROL_SUFFICIENT`, `P10_GRAPH_OR_SYMBOLIC_CONTROL_SUFFICIENT`, or `P10_NO_CONTROL_RESIDUAL`

The structured-reasoning paper route closes rather than escalating model complexity.

Required disposition:

- #542 closes on the exact earned donor-sufficiency terminal;
- #470 closes `P10_NO_INCREMENTAL_REASONING_VALUE` at the refounded structured-control scope;
- #392 closes/narrows to `P10_PIONEER_ONLY_FOLLOWUP` or `NO_INCREMENTAL_REASONING_VALUE`, explicitly **not peer-review-ready as a standalone structured-reasoning paper**;
- no live LLM/provider A1 is executed to manufacture a residual;
- no language fluency, extra test-time compute, or model-scale comparison is permitted to override the negative control result;
- old content-bound evaluation remains P11 #471;
- Stage D / Pioneer #427/#429/#430 is removed from the P10-paper critical path and may continue only as an independent Jump/discovery successor coordinated by #500/#507, with its own future paper identity if it ever earns one.

## If A0 = `CANNOT_CHECK`

Do not broaden scope. Repair only the stated evaluator/custody defect under a versioned protocol if it is pre-outcome/non-result-bearing. If A0 is scientifically non-identifiable under available evidence, close P10 `CANNOT_CHECK` and retain the design as programme knowledge.

## Paper-count rule

A missing P10 standalone paper is an acceptable scientific terminal. No portfolio target authorizes converting donor equivalence, null results, or a future invention programme into a P10 positive.

## Reopen rule

After donor-sufficiency/no-residual closure, P10 structured reasoning reopens only if a genuinely new prospectively frozen application exposes a controller coordinate not expressible by the saturated donor-composed baseline. Better LLM proposal quality alone is not sufficient because A0 intentionally holds proposal generation outside the tested residual.
