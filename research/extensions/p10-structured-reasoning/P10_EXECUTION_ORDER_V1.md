# P10 execution order V1

1. Derive and audit held-out-module robustness from frozen V2.1 block receipts.
2. Preserve the completed source-transfer result as the bounded paper baseline.
3. Execute the frozen native-state incremental-value protocol on the exact Mathlib subject.
4. If R1 passes, execute module-invariance and semantic-orbit controls.
5. If state/dependency signal survives, embed it into verifier-backed proof search with matched Lean-call/node budgets.
6. Run a faithful TacMiner-class comparator before any standalone tactic-structure novelty claim.
7. Refresh nearest work before manuscript freeze.
8. Promote only the highest rung whose complete frozen gate passes.
