# ORION-Q donor structural matrix V0

Status: ACTIVE / NOT SATURATED / NO NOVELTY CERTIFICATE

Parent programme: #633
S0 saturation: #638

## Rule

For every close parent, extract the strongest reusable structure, mark it as donor-owned, and only retain an ORION residual that survives a direct discriminator.

## Current donor rows

| Donor family | Strong structure to absorb | ORION claim struck | Surviving pressure |
|---|---|---|---|
| DreamCoder quantum circuit components | wake-sleep library growth; reusable composite gate discovery; neural search policy | "learn reusable quantum gate macros" | require obstruction-grounded edit rather than compression/library growth |
| Rouillard et al. automated quantum algorithm design | scalable DSL; evolutionary search; learns `n -> algorithm(n)` from small instances | "discover parametric algorithm structure from small n" | require language/representation edit beyond fixed DSL and held-out transfer |
| QSynth | recursive quantum program synthesis; sound logic; SMT-backed verification | "synthesize recursive quantum program families" | pressure on language inadequacy, invariant synthesis, oracle/interface limits |
| Reachability/expressibility analysis | separates unbounded gate-set expressivity from depth-bounded reachability | "distinguish expressivity from bounded reachability" | actionable certified obstruction diagnosis and justified edit remains candidate residual |
| AutoQResearch | LLM-guided closed-loop adaptive quantum solver policy search; diagnostic feedback; staged confirmation | "use diagnostics to evolve quantum solver policy" | distinguish policy/configuration adaptation from method-language expansion |
| SCALAR | quantum simulation + symbolic conjecture generation + LLM reasoning | "LLM-assisted quantum conjecture discovery" | require method-space edit with semantic/resource verification |
| Classical numerical quantum synthesis | optimize composite target unitary, then factor into elementary gates | "automated numerical quantum algorithm synthesis" | representation/operator-language choice above fixed factorization pipeline |
| Generative quantum-code systems | syntax/semantic generation with verifier/RL/diffusion/agents | "generate verified quantum circuits/code" | exact structural capability expansion, not code generation |

## Provisional residuals under attack

1. `NZ-A`: certified distinction between search failure and language inadequacy, plus a minimal justified edit.
2. `NZ-B`: transferable method state across semantically equivalent quantum representations.
3. `NZ-C`: representation + encoding + transform + implementation co-design under a multi-resource contract.
4. `NZ-D`: proof-carrying algorithm-language growth whose promoted operators expand bounded reach on held-out families.

None is novel by declaration. Each remains `CANNOT_CHECK` until saturation and a frozen discriminator.
