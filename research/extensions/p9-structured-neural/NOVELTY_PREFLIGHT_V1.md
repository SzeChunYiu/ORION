# P9 novelty preflight V1 — frozen before M1/D1/A5 outcomes

This document narrows P9's possible paper claim **before** reading the result-bearing M1, D1 and A5 artifacts.  It is an input to programme novelty authority #287, not a novelty certificate by itself.

## Claims already owned by prior work — not eligible as P9 novelty

| Candidate phrase | Disposition | Representative donor pressure |
|---|---|---|
| Transformers can operate on graph structure | `STRIKE` | Graphormer |
| typed/heterogeneous node and edge attention | `STRIKE` | HGT |
| combine local message passing and global attention | `STRIKE` | GraphGPS |
| learned local vector spaces / restriction maps / sheaf consistency | `STRIKE` | Knowledge Sheaves, Neural Sheaf Diffusion |
| one learned processor can reuse algorithms across tasks | `STRIKE` | Generalist NAR / CLRS |
| language interface + separate structured algorithmic reasoner | `STRIKE` | TransNAR |
| learn reusable program/reasoning modules or libraries | `STRIKE` | DreamCoder, Stitch, LILO, reusable-module RL |
| reason in continuous latent state rather than language | `STRIKE` | Coconut / recurrent latent reasoning |
| factor reusable rules from entity bindings | `STRIKE` | Neural Production Systems |
| Transformers cannot learn systematic binding | `STRIKE/REFUTED PREMISE` | Wu–Geiger–Millière variable binding |
| scientific AI should organize reusable explanatory mechanisms | `STRIKE` | Mechanistic World Models |
| compositional causal components improve transfer | `STRIKE` | causal modular world-model literature / WM3C |
| select computation by expected value/cost | `STRIKE` | rational metareasoning / VOC |
| counterexamples can guide neural synthesis | `STRIKE` | CEGNS |
| GNN hidden states can be designed as epistemic states | `STRIKE` | Khalid & Schockaert |
| relation-focused bottlenecks can improve abstraction/composition | `STRIKE` | relational bottleneck |
| structured relational representations enable cross-domain analogy/transfer | `STRIKE` | DORA/LISA lineage |
| learn minimal task-relevant causal state abstractions | `STRIKE` | causal bisimulation modeling |
| formal preservation-aware state abstraction | `STRIKE` | behavioral-semantics / P6 formal donors |
| non-monotonic dependency/revision reasoning | `STRIKE` | truth/belief maintenance, argumentation, P1 upstream ownership |
| explicit UNKNOWN/abstention as a generic idea | `STRIKE` | selective prediction / uncertainty / upstream P4 boundary |
| information-theoretic/Bayes best-achievable error benchmarking | `STRIKE AS NOVELTY; RETAIN AS METHOD` | Bayes-error estimation literature |

## P9-owned research objects that are not automatically novel

P9 has built useful programme infrastructure:

- exact hostile structural worlds with nested model views;
- an exact deterministic representation-identifiability ceiling on those worlds;
- contamination-safe reminted train/dev/test generation;
- candidate-relative mechanic tasks with evaluator/model separation;
- exact algorithmic/procedural method-structure transfer benchmark;
- donor-complete escalation gates.

These objects may support a benchmark/methodology contribution **only if the final literature/novelty review finds the exact combination and discriminator not already substantially present**.  Creating an object does not itself establish paper novelty.

## Result-dependent claims allowed to remain live

### R1 — exact coordinate sufficiency / non-sufficiency on hostile pairs

Possible bounded statement:

> On prospectively constructed hostile pairs, progressively exposing relation semantics, local transport values and admitted negative history raises the exact achievable prediction ceiling; models restricted to weaker views cannot resolve targets that differ only in hidden load-bearing coordinates.

Status before outcomes: `RESULT_DEPENDENT`.

Novelty risk: high.  Information-sufficiency/Bayes-error methodology is established.  The possible residual is the **specific typed epistemic/mechanic coordinate hierarchy + hostile falsification design**, not the existence of a ceiling.

### R2 — exact typed method-coordinate transfer under whole-domain holdout

Possible bounded statement:

> Exact, source-grounded method coordinates support structural comparison across a held-out procedural domain and unseen combinations of coordinate corruptions better than reminted transcript/untyped or same-information unstructured serialization baselines.

Status before outcomes: `RESULT_DEPENDENT`.

Novelty risk: high.  Cross-domain structural analogy and relational bottlenecks are established.  The possible residual is the **specific recoverable method-coordinate contract, explicit source-insufficient UNRESOLVED state, and prospectively protected corruption/whole-domain protocol**.

### R3 — complexity escalation is empirically unnecessary or localized

Possible bounded statement:

> After exact information ceilings and simple candidate-relative baselines are established, only particular residual computations require additional machinery; where explicit exact inference closes the gap, neural architecture escalation is scientifically unjustified.

Status before outcomes: `RESULT_DEPENDENT`.

Novelty risk: medium/high.  This may become a negative methodology/result paper rather than a new architecture claim.  It survives only if M1/A5/D1 produce a nontrivial, reproducible pattern and novelty review finds the combined falsification/escalation doctrine scientifically distinct enough.

## Forbidden post-outcome expansion

A positive M1/D1/A5 result may **not** be used to add any of these claims without a new protocol and reopened donor search:

- general scientific reasoning;
- natural-science method understanding;
- universal neural architecture superiority;
- causal mechanism discovery;
- method invention;
- LLM problem-solving improvement;
- formal mathematics improvement;
- general theorem about optimal state abstraction;
- general theorem about epistemic computation.

## Standalone-paper gate

After results and independent verification, P9 becomes a standalone manuscript only if all are true:

1. at least one of R1–R3 is prospectively supported at its exact bounded scope;
2. the result is not explained by leakage, extra information, model-selection asymmetry or trivial solution code;
3. the exact claim survives an updated nearest-work search after absorbing all donors above;
4. the paper has an independent research residual beyond P1/P3/P6/P8 and beyond a reusable benchmark note;
5. negative/null outcomes remain visible;
6. #283 independent verification and #287 novelty authority are current.

Otherwise the correct P9 terminal is one of:

- `MERGE_P9_INTO_PROGRAMME`;
- `P9_TECHNICAL_BENCHMARK_NOTE_ONLY`;
- `EXISTING_DONOR/EXPLICIT_INFERENCE_SUFFICIENT`;
- `NO_STANDALONE_RESIDUAL`.

No paper count target overrides this gate.
