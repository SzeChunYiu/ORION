{
  "schema": "P9.M1Protocol.v1.2",
  "supersedes_for_execution": "P9.M1Protocol.v1.1",
  "outcome_accessed": false,
  "review_findings": [
    {
      "finding": "row-order sentinel was tautological because it rebuilt maps from the same scored prediction list instead of re-running inference on a permuted batch",
      "correction": "refit the frozen selected configuration from train, evaluate the untouched test records in canonical and reversed row order, and compare world-id->prediction mappings"
    },
    {
      "finding": "one pooled view ceiling could hide a task-specific leakage violation because mechanic ranking and gluing use separately selected models",
      "correction": "compute exact identifiability ceilings separately on the mechanic-ranking and gluing test subsets; any task-specific accuracy above its own ceiling invalidates the run"
    }
  ],
  "unchanged": [
    "corpus/split identities",
    "candidate order seeds",
    "feature maps",
    "model grid",
    "train->dev selection",
    "one protected test evaluation per selected configuration",
    "train-size curve handling from v1.1",
    "metrics other than corrected sentinels",
    "authority"
  ],
  "terminal_rule_addition": "M1_LEAKAGE_OR_EVALUATOR_FAILURE if any task-specific ceiling violation occurs",
  "protected_test_effect": "No model/feature/hyperparameter change; corrected evaluation only",
  "authority": "NO_SCIENTIFIC_AUTHORITY_PROTOCOL_AMENDMENT_ONLY"
}
