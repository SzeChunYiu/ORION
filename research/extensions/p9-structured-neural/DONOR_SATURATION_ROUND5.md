# P9 donor saturation — round 5 analogy / structural-transfer pressure

Status: **MATERIAL CHANGE**. Search vocabulary: analogy, structure mapping, schema induction, role-filler transfer, cross-domain relational representations.

This round again resets the no-material-change counter.

## A Theory of Relation Learning and Cross-domain Generalization

**Primary source:** Doumas, Puebla, Martin & Hummel, 2019, arXiv:1910.05065.

### Essence

The work explicitly models cross-domain generalisation as analogical inference over learned **structured relational representations**. The DORA/LISA lineage learns relational content/format and transfers it to novel domains, including zero-shot cross-domain settings in the studied tasks.

### P9 consequence

P9 may not claim either of these broad statements as novel:

- `structured relational representations enable cross-domain generalisation`;
- `transfer should compare underlying relational structure rather than surface statistical similarity`.

### Disposition

`ADOPT analogical-structure-transfer philosophy + CLAIM CONTRACTION`.

D1's whole-domain result, if positive, is evidence that ORION's *specific typed method coordinates* support a bounded transfer discriminator. It is not evidence that relational structure transfer itself was invented by ORION.

### Residual discriminator

- exact source/evidence-bound method coordinates;
- explicit load-bearing obstruction vs source-insufficient `UNRESOLVED`;
- prospectively frozen corruption/combinatorial holdouts;
- lineage into verification/authority without using authority as a predictive feature.

A future analogical/structure-mapping baseline becomes mandatory only if D1 grows beyond exact coordinate comparison into learned analogy retrieval/mapping.

## Learning to Perform Role-Filler Binding with Schematic Knowledge

**Primary source:** Chen et al., 2019, arXiv:1902.09006.

### Essence

External-memory networks can learn schematic role-filler relationships and retrieve arbitrary unseen fillers even without explicit role/filler labels in the studied setting.

### P9 consequence

This strengthens A9's baseline map. P9 cannot claim that unseen filler binding requires an explicit symbolic ORION binding object.

### Disposition

`BASELINE/CLAIM CONTRACTION` for any A9 experiment; no final paper claim unless explicit binding earns independent OOD/failure/audit value.

## Semantic Structure-Mapping in LLM and Human Analogical Reasoning

**Primary source:** Musker et al., 2024, arXiv:2406.13803.

### Essence

Tests transfer of semantic structure/content between domains and reports advanced LLMs approaching human performance on many variations, while retaining specific differences and distractor sensitivities.

### P9 consequence

P9/P10 must not treat semantic structure mapping as intrinsically unavailable to LLMs. Any later language-vs-structured comparison needs strong current LLM structure-mapping baselines and hostile distractors.

### Disposition

`P10-FACING BASELINE + P9 CLAIM CONTRACTION`.

## Round-5 impact

After round 5, D1's likely positive is **not itself a novelty terminal**. The paper-level residual, if any, must combine:

1. exact information-identifiability of declared epistemic/method coordinates;
2. protected corruption/UNKNOWN handling;
3. cross-domain transfer of those exact coordinates;
4. a later nontrivial computation or downstream utility beyond ordinary analogical/relational transfer.

If later experiments do not provide item 4, P9 should be narrowed to a benchmark/representation paper or merged into the broader ORION programme rather than claiming a new reasoning architecture.

## Saturation counter

- Round 4: material — epistemic-state GNN + relational bottleneck.
- Round 5: **material** — analogical/structured cross-domain transfer claim contraction.
- Consecutive no-material-change rounds: **0/2**.

Saturation remains OPEN.
