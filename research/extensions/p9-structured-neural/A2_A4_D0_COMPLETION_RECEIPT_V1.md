# P9 A2/A4 D0 completion receipt V1

Terminal: **`A2_A4_D0_EXPLICIT_INFERENCE_SUFFICIENT`**  
Verification: **`BOUNDED_VERIFIED`**

## Relation-semantics atom

On 256 protected relation pairs (512 worlds):

- `SURFACE`/`TOPOLOGY` omit the decisive relation/effect semantics; the explicit selector remains `AMBIGUOUS` on every example;
- `TYPED`, `CURRENT` and `SEMANTIC` expose relation type and candidate effect contracts; a payload-only exact relation/effect match reaches 1.0 accuracy.

## Failure-history atom

On 256 protected history pairs:

- `SURFACE`/`TOPOLOGY` omit both cost/history signal and remain ambiguous;
- `TYPED`/`CURRENT` expose candidate costs but not admitted failure history; the clean and negative worlds remain representation-identical and therefore receive the same prediction, yielding exactly 0.5 across the hostile pair;
- `SEMANTIC` exposes the admitted failed-candidate record; explicit candidate exclusion reaches 1.0.

## Scientific consequence

These D0 atoms do **not** justify neural mechanic learning, modular memory, recurrent latent computation or advanced training. Their current residuals are exhausted by simple explicit operations once the load-bearing coordinate is represented.

This does not imply that general mechanic selection or failure-directed inquiry is rule-based; richer A2/A4 research is simply not load-bearing for the final bounded D0 result.

## Downstream issue consequence

- #475 A2 may close for final P9 scope as `BOUNDED_EXPLICIT_OPERATOR_SUFFICIENT`; broader mechanic-learning questions are successor/P10 work;
- #477 A4 may close for final P9 scope as `BOUNDED_ADMITTED_HISTORY_SUFFICIENT`; active inquiry/VOC research is not claimed;
- #428 cannot use these atoms to justify neural escalation.
