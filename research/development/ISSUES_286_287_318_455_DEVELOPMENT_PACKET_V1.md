# Development packet — issues #286 / #287 / #318 / #455

Impact: **HIGH**  
Implementation branch: `shadow/issues-286-287-318-455-coordination`  
Authority: **NONE**

This packet records the development question, atomic fibres, saturation basis, basis challenge, reopen triggers, and frozen implementation hypotheses required by `development/README.md`. The implementation remains bounded by each issue's scientific terminal and cannot promote engineering evidence into protected/independent evidence.

## Atomic development questions

### D286-A — transfer baseline plumbing
Can the already-frozen #286 source/target representation be exercised under a common one-object information budget across every requested representation family while preserving negative-transfer controls and an oracle analysis ceiling?

### D287-A — external novelty benchmark freeze
Can an official external novelty benchmark be pinned before outcomes with exact repository/artifact identities, an evaluator that treats `CANNOT_CHECK` as abstention, and no LLM score capable of setting novelty authority?

### D318-A — assimilation → novelty invalidation
Can an admitted material donor absorption touching a certificate mechanic automatically stale the prior #287 certificate without editing it in place or granting new authority?

### D318-B — prospective programme paths
Can the existing receipt schema honestly represent one clean full-text structurally bound `ADOPT`, `ADAPT`, and evidence-backed `COMPOSE` outside P1/P2 paper-local ledgers?

### D455-A — revision responsibility Stage 0
Can cause-confusable white-box cases separate the minimal licensed response from broad self-revision while keeping protected gold out of candidate-visible state and exposing fake adaptivity through outcome permutation?

## Recovered incumbent mechanics / negative history

- #286 already has a frozen mechanic transfer object, fail-closed selector, hostile applicability fixtures, and an explicitly engineering-only toy ablation. The open scientific gates require measured held-out transfer, not another selector rewrite.
- #287 already has `ScientificNoveltyCertificate.v1`; the issue's self-certificate is narrowed and preserves forced abstention. The missing object is shared-input external benchmark execution, not another LLM judge.
- #318 already has `MechanismAssimilationReceipt.v1`, full hostile checks, completed P1/P2 consumer tranches, and #454 structural prerequisites. Earlier real-data pressure caught the abstract-only fail-open defect, so new programme paths must be full-text and structurally bound.
- #455 inherits merged claim-relative responsibility hypotheses from ORION foundations. A fixed universal failure taxonomy would regress that foundation; Stage-0 class names are therefore benchmark labels only.
- #454 retains MDA as `ABSTRACT_ONLY -> CANNOT_CHECK`; no downstream lane may launder it into a full structural comparator.
- #283 retains fresh-holdout/cross-host verification as a programme-wide unavailable surface.

## Bounded saturation assessment

Knowledge/formulation closure for this implementation tranche is **bounded closed** because the relevant incumbent mechanisms and mandatory external parents are already frozen in the linked issue programmes:

- #286 literature/representation/split saturation is already ticked on the issue;
- #287 full benchmark-cluster literature saturation and self-certificate regeneration are already recorded;
- #454 supplies the structural donor packet and two structural no-material-change rounds;
- #455 issue history supplies the later counterexamples (no self revision, containment, linked revisions, active diagnostics, common-state incompatibility, simple update, history split, matched compute, alignment vs regime change).

The bounded closure is only for the **implementation questions above**. It does not assert scientific saturation for an unexecuted external/held-out campaign.

## Saturation-basis challenge

Saturation could be falsely flat if:

1. a strong baseline uses an abstraction level absent from the frozen #286 representation list;
2. RINoBench scalar labels cannot validly map to certificate authority states;
3. a material assimilation changes a novelty residual without sharing an explicit mechanic identity;
4. a donor's structural assumptions are broader/narrower than the existing #454 receipt records;
5. two V3 response labels are observationally indistinguishable under admissible diagnostics;
6. the apparent V3 advantage is only extra compute or privileged gold;
7. a provider-independent evaluator still embeds model-specific assumptions;
8. a new fail-closed path silently drifts the #322 `CANNOT_CHECK` inventory.

Potentially missing parent domains: selective prediction/abstention calibration, case-based/analogical transfer, safe containment under model uncertainty, Bayesian model criticism, causal fault localization, continual-learning negative transfer, provenance-preserving schema migration, and verification under independent custody.

## Frozen implementation hypotheses

- **H-D286:** a Stage-0 baseline harness can expose representation-dependent negative transfer without being mislabeled as the scientific held-out result.
- **H-D287:** external benchmark identity/evaluator semantics can be frozen independently of model/provider execution, with abstention separated from accuracy.
- **H-D318-1:** material admitted absorption that intersects a certificate mechanic must force effective novelty to `CANNOT_CHECK` until a new certificate is sealed; the bridge must never upgrade novelty.
- **H-D318-2:** the current assimilation receipt schema is sufficient for one prospective ADOPT/ADAPT/COMPOSE tranche if every material donor is full-text and structurally bound.
- **H-D455:** on known-answer Stage-0 cases with one matched diagnostic budget, an evidence-responsive responsibility-gated policy should change under outcome permutation and should avoid the false broad revisions of an M-open-only policy; this remains instrumentation, not the protected scientific result.

## Reopen triggers

Reopen implementation/formulation if:

- any new clean programme absorption requires weakening a hostile check;
- a supposedly clean receipt grants authority or seals from abstract-only evidence;
- a #287 evaluator maps abstention into positive novelty;
- Stage-0 candidate-visible state contains protected gold;
- V3 decisions do not change under non-trivial outcome permutation;
- a simpler matched baseline dominates the responsibility-gated Stage-0 policy;
- CI's re-derived `CANNOT_CHECK` inventory differs from the committed inventory;
- independent held-out/cross-host authority becomes available, in which case the scientific `CANNOT_CHECK` terminals must be reopened rather than treated as permanent negatives.

## What this packet does not establish

It does not establish cross-domain transfer, external novelty-benchmark superiority, assimilation-method novelty, a universal self-revision hierarchy, objective-revision authority, or any protected scientific terminal. Those remain governed by the issue-specific evidence rules and #283.
