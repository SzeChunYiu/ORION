# P8 method-authority extension — development packet

Parents: #423/#424. Paper: #390. Coordinator: #403. Stacked on P6 #436 and P7 #437.

## Ownership

P8 owns the legal coercion matrix from capability outputs to scientific-authority coordinates plus forward revocation/reopen lineage. P6 owns fibre semantics; P7 owns navigation; P4 owns validity/applicability/transfer/utility evidence; novelty review owns novelty; P5/host owns adoption; external task authority owns global search closure. P9/P10 are capability producers only.

## RED families

- P9 confidence -> validity/adoption;
- P10 generation -> novelty;
- P6 fibre -> applicability;
- P7 local exhaustion -> global stop;
- P4 validity -> novelty;
- novelty -> correctness;
- legal lane with missing bound evidence -> `CANNOT_CHECK`;
- prior art/counterexample/protected harm revocation;
- representation/evaluator/subject changes reopen stale authority;
- clean legal P4/novelty/host/task lanes must pass.

## Terminal

Bridge target: `P8_METHOD_AUTHORITY_BRIDGE_MERGED`. The closed synthetic coercion/revocation matrix may close #424 as `P8_P9_P10_ANTI_LAUNDERING_CLEAR` only for that bounded contract suite; no real method validity, novelty, utility or adoption is inferred. Since 2026-08-21 that terminal is derived from the suite's four rates rather than written beside them, so it is `…_VIOLATED` on any failing case and `…_CANNOT_CHECK` on any empty slice; the ceiling sentence is emitted as `declared_claim_ceiling_from_input` because the input panel supplies it. The receipt's `result_digest` moved to `sha256:3103fcd0…` with that change.

Because this module emits genuine authority-layer `CANNOT_CHECK`, repository CI must regenerate and ratchet the canonical `research/development/cannot_check_inventory.json` rather than rename unresolved authority states away.
