# Seven-paper saturation — final rendered-package closure

**Date:** 2026-08-19  
**Base:** `main@f2d6b8b3f58da3e3577975ac5215c7439a9ffc5f`

The literature/reviewer saturation PRs for P1/P2/P3/P4/P6/P7/P8 are merged. This final lane closes only the rendered/package consistency obligations that remain after manuscript-level citation/prose changes.

## Scope

- P1: rebuild the post-saturation manuscript and visually inspect it; then rebind the journal package to the new source/PDF.
- P2: independently rebuild the post-saturation manuscript, prove the tracked journal PDF matches the rebuilt content, and visually inspect the final tracked PDF.
- P3: rebuild the post-I-ADOPT manuscript, visually inspect it, and rebind the tracked journal package so the strict P3 manuscript audit can pass again.
- P4: rebuild the post-provenance-literature TMLR manuscript with the repository's pinned official style/audit path and visually inspect the fresh rendered PDF; retain a durable saturation render receipt without changing protected-V2 scientific evidence.
- P6/P7/P8: no render mutation here because their saturation PRs deliberately changed no frozen candidate manuscript/PDF bytes; their previously audited submission packages remain authoritative.

## Integrity rule

The first workflow is **read-only** and uploads rendered artifacts. No issue is closed from compile success alone. The PDFs are rendered to images and visually inspected before any package write-back. Any write-back is followed by ordinary repository/package CI on a connector-authored final head.
