# Reusable sealed longitudinal self-improvement v1

Combined ORION-15/ORION-24 successor. Run:

```bash
python build_controls.py
python independent_checker/check_controls.py
```

The committed control terminal is `CONTROL_PLANE_VERIFIED__EMPIRICAL_CAMPAIGN_NOT_RUN`. No frontier agent or protected task is executed by this packet.
