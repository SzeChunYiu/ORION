# R30 disposition — historical donor, not current authority

## Controlling terminal

```text
R30_NOT_MATERIALIZED__CUSTODY_AND_FINAL_BINDING_FAILED
```

This terminal concerns the R30 **status wrapper and release claim**. It does not
retract, delete, or weaken any valid theorem, adverse/null result, retraction,
hostile control, or `CANNOT_CHECK` boundary carried by its donor branches.

## Why the advertised R30 terminal is not repository authority

1. PR #1489 is an open R19 FiberGuard story draft with a non-`main` base. It is
   not a clean current-main R30 programme PR.
2. PRs #1471, #1485, and #1488 were not closed or marked superseded when the
   R30 report said they were.
3. R30 generator/workflow code was appended to stacked draft PR #1492. The
   intended `chatgpt/five-paper-r30-current-main-20260827` branch does not
   exist.
4. The proposed R30 output receipts, status files, reviewer PDF, and release
   archives are absent from current `main` and from the #1492 tip.
5. Runs `33047609008`, `33048246343`, `33048471274`, `33048664001`,
   `33048836610`, and `33048974820` failed. The R18 custody run
   `33048978721` also failed. The final clean-branch steps ended with a missing
   branch (`HTTP 404`), not a custody PASS.
6. No successful R30 finalizer/current-main custody run or materialized R30
   output receipt binds the proposed cross-language status to the advertised
   clean release. Any independently valid R20 checker result retains only its
   original same-owner, branch-local authority ceiling.
7. The R20 disposition consumed by the generator itself leaves ORION-01
   production transfer, ORION-03 theorem/domain authority, ORION-04 full
   replay authority, and ORION-05 measured resource value open.

## Valid material retained from the stack

- ORION-02 R18 exact null:
  `FIBERGUARD_R18_NO_PAIRED_ROUTE_VALUE`;
- permanent retraction of the unsupported R18 positive interpretation;
- ORION-02 R19 exact joint-profile repair:
  `FIBERGUARD_JOINT_ROUTE_R19_REPLACEMENT_PASS`;
- hostile controls `35 -> 70` and `0 -> 50`;
- C-NBR `CERTIFICATE_INVALID` and the later C-NBR2
  `VALID_WITHOUT_COVERAGE_OR_VALUE` boundary;
- every valid proof/protocol/manuscript element, subject to its original
  authority ceiling and current-source reconciliation.

R30 may therefore be cited as **historical branch-local donor intent**. It may
not be cited as an executed clean-main science, journal, release, or submission
terminal.

## Supersession rule

After this convergence package is merged and verified, it supersedes R30 only
for current status, custody, and coordination. Historical commits and raw
scientific outcomes remain immutable provenance. Manuscript and release
packaging must be rebuilt only after each ORION-01–05 lane receives an honest
science disposition.
