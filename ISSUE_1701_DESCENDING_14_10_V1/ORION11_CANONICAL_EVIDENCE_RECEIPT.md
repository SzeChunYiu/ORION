# ORION-11 canonical evidence receipt

No recovery branch is needed for ORION-11. Live `main` contains all required
objects.

## Faithful comparator

Primary (2,882 worlds; 480 hidden-shift worlds):

- ORION joint criterion: `1.0`;
- ordered Active-VOI joint criterion: `1.0`;
- ordered Active-VOI forbidden mutation rate: `0.0`;
- terminal: `H_R4_FALSIFIED__FAITHFUL_COMPARATOR_MATCHES_ORION`.

The original replication remains visible with
`INSTRUMENT_FAULT__ANCHOR_REPRODUCTION_FAILED__NO_CLAIM_READ`. The parameterized
repair does not erase that history. It changes only the per-world-set anchor
reference interface, runs stage 1 with anchor arms only, freezes the reference
before new-arm outcomes, and then reproduces the same falsification terminal.

## Costed ordering

The cost-aware successor is also adverse:
`H_FALSIFIED__PC_BASELINE_MATCHES_OR_BEATS_ORION`. Its anchor gate passes and the
terminal is stable across the pre-stated alternate bootstrap seeds; cost-ratio,
DP-gap and donor-baseline gates fail.

The bounded paper must therefore avoid comparative superiority language.
