# RAKL → ORION donor atlas V3

**Status:** TERMINAL DISPOSITION CANDIDATE / FROZEN DONOR SEARCH CLOSED FOR PHASE-1 SCOPE / REOPEN ON DECLARED TRIGGERS

Frozen donor: `SzeChunYiu/RAKL@70f5f7c4a6771ffd1158765b42ac9f8aee8a270f`.

This document supersedes `DONOR_ATLAS_V2.md` for Phase-1 closure. V2 deliberately retained `OPEN_EQUIVALENCE_AUDIT` rows; V3 resolves every frozen public donor family and engineering E1–E20 fibre into a terminal search disposition recorded in `DONOR_DISPOSITION_V3.json`.

A terminal donor-search disposition is **not** a statement that every legacy capability is implemented. `DEFER_WITH_TRIGGER` is an executable conceptual boundary: when the trigger occurs, the donor must be reopened before clean-generation invention.

## Exact/reconstructed high-value donors now in ORION

Already merged before V3:
- bounded epistemic context compiler;
- typed similarity/analogy witnesses;
- measurement-aware relation trials;
- generator LIFT/RELATE/PROJECT transport.

Added by V3:
- factorized retrieval benchmark separating corpus coverage, conditional retrieval recall, structural-witness ground truth and transfer-validity ground truth;
- longitudinal route-family continuity/health with non-compensatory root progress and chronology;
- backward sufficient-obligation expansion + 13 structured seed families + connection/meet-in-middle/cut-residual diagnostics;
- frozen hidden-path A–F matched-budget benchmark with hard contamination ceilings.

These donors remain search/control/proposal mechanics. Their result types explicitly grant no scientific, solution, route, capability or promotion authority.

## Public family terminal summary

`DONOR_DISPOSITION_V3.json` freezes 24 public-family rows. Terminal classes are:
- `EXACT_REUSE` for contracts whose assumptions/interfaces survive;
- `RECONSTRUCTED` where the mechanic survives but ORION owns a different state/interface model;
- `TESTS_RESULTS_REUSED` where the negative/hostile result is more valuable than copying legacy implementation;
- `DEFER_WITH_TRIGGER` where the donor remains required knowledge but activation is premature;
- `REJECT_WITH_REASON` if a future audit demonstrates the donor does not survive the target comparison.

There are **no OPEN rows** in the V3 Phase-1 inventory.

Notable deferred reopen triggers include:
- Atlas gluing exact cycle/global-existence obstruction battery before Paper-III global portrait is promoted beyond proposal-only;
- bridge-composition path/target-transfer falsifiers before live multi-hop bridge search;
- formalism packet grammar before symbolic equation-AST/mechanism-graph tasks exceed current mechanic contracts;
- multiresolution memory before lossy compaction/rehydration;
- promotion/subject attestation after the final protected-transition subject/authorization design lands;
- production repository/workflow/security/telemetry/SLO donors when those deployment surfaces actually exist.

## Engineering E1–E20

Every E1–E20 fibre has a terminal Phase-1 search disposition. Production-only fibres are deliberately deferred instead of being simulated locally. Their RAKL hostile findings remain regression obligations, including CAS/sequence rewind, payload-hash binding, crash/idempotency, replay-before-staleness, coverage-vs-reachability, telemetry-outage isolation, schema repair refusal, ghost-index prevention and context-ready-with-nothing.

## Fresh-audit closure rule

Phase-1 donor search is eligible to close only if CI confirms:
1. the inventory is pinned to the frozen RAKL revision;
2. the exact public-family set is complete;
3. E1–E20 are all present;
4. every row is terminal;
5. reuse/reconstruction rows bind ORION evidence paths;
6. every deferred row has an explicit reopen trigger;
7. donor-derived mechanics preserve their authority refusals and hostile regressions.

This closes only **the donor-search obligation**. It does not close Phase 1 until the protected-transition lifecycle and final authority/core audit in issue #75 also pass.
