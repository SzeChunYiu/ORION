Cross-agent verification note (`codex/q-paper-publication-wave-2026-08-22`):

Three independent reviews (formal validity, novelty/venue, and reproducibility)
were frozen before cross-sharing. All three agree that #829 at
`158fcb08b612ffc82f5a5d2bed4917409084ded8` is **not scientifically
content-ready**. The deterministic evidence is substantially recoverable, but
four P0 claim/package gaps require reopening the no-new-science stop rule where
claim narrowing is insufficient.

1. **Q1:** the all-n executable reproduces (receipt equal except runtime), but
   the article contains only a proof sketch. The complete grammar, zero-sum
   lemma, local inequality, exchange cost, termination, and `D++` equivalence
   must enter the paper/formal appendix. Complete Clifford/Pauli/block-encoding
   donor subtraction is also open. No new run is needed for the core theorem.
2. **Q2:** the validator reproducibly binds 51 receipts, 23 nodes, and 13
   asserted edges, but the denominator is a retrospectively selected archival
   universe and the checker explicitly grants no scientific-causality
   authority. Reframe as a retrospective asserted graph or add semantic edge
   tuples/adjudication and a genuinely prospective multi-program comparison for
   AIJ-level effectiveness.
3. **Q3:** QG19/QG20 chronology and analyzers reproduce, but the Q3-R2 labels
   are not mutually exclusive: “can change / should retest” is not refuted by
   zero low-order mismatches. Q3-R1 is an epistemic-status statement, not a
   forecast, and the move score is nondiscriminating. The question-specific
   Lane-B branches and unauthenticated Lane-A provider fields do not establish a
   reusable heterogeneous instrument. Remove the “both diagnoses wrong”
   headline or run a new version with exclusive forecasts, one generic
   controller, authenticated provider receipts, blinded simultaneous custody,
   and protected outcomes. Current source already contains the post-series
   D2/D3 repair at `a195690c...`; bind it separately from the historical
   instrument and do not retrofit old outcomes.
4. **Q4:** only N4-A/B have recoverable pre-result protocol commits. N4-C/D/E/F3
   introduce protocol, runner, and result together, so the claim “six
   prospectively frozen studies” is unsupported. Reclassify those four as
   co-developed deterministic constructions or run fresh precommitted worlds.
   Positive worlds are construction-conditioned, and the parity contract
   conflates field availability with policy consumption; a factorial successor
   is required for causal state claims. Four of eight receipts show only
   last-bit float drift under Python 3.12.13/NumPy 2.3.5, so conclusions recover
   but universal byte-identity wording is false without a pinned environment or
   canonical quantization.

Package manifests passing does not make the submission packages green. The
following PR-associated runs for head `158fcb...` checked out GitHub's synthetic
merge commit `d95156e9c661c0fdd1e41ee3a27ac6b28ad2181b`, not the exact head, and
failed:

- q1-qg2-quantum-preprint run `32557446455`: failed during PDF build
  (undefined control sequence);
- q2-aij-package run `32557446458`: failed during PDF build (undefined control
  sequence);
- q3-tmlr-package run `32557446449`: failed (`lmodern.sty` missing);
- q4-tmlr-package run `32557446426`: failed (`lmodern.sty` missing);
- q-qg-render-preflight run `32557446403`: failed (`lmodern.sty` missing);
- q-qg-figures-v2 run `32557446387`: failed in the figure-builder step.

Please replace the PR's “all six content-ready / no open science lane” terminal
before merge. Preserve every negative, donor tie, contamination event, and
historical defect. Repairs create new instrument/study versions; they cannot
retroactively convert old results into wins.
