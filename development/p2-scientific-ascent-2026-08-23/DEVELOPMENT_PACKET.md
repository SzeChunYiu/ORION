# Paper 2 scientific-ascent packet

Date: 2026-08-23  
Paper: `papers/orion-12-open-world-scientific-discovery/`  
Working title: **The Acquisition--Authority Envelope for Open-World Scientific Discovery**

## 1. Scientific diagnosis

Paper 2 could not reach a top publication tier because its strongest empirical
objects and its stated theory lived at different scales.

- The controlled-index campaign is exact and useful, but synthetic and
  underpowered for its own frozen superiority margin.
- The MetaSyn run is external, but it tests a pinned retrieval/screening path,
  not the full multi-route controller.
- The external Wide acquisition family did not establish a positive acquisition
  effect.  In the archived adverse capture, the governed routes emitted only
  **7 of 229** gold identifiers; **222** never entered the candidate pool.
- Equal call counts hid unequal scientific opportunity: the lexical arm used
  three scoring-admissible calls per task, whereas the governed arm used one
  admissible call and two calls to interfaces unable to emit scorer-valid
  identities in that capture.
- The 400-case P2-X battery strongly isolates unresolved-route authority, but it
  supplies route materiality and state exactly.  It is not naturalistic
  open-world retrieval evidence.  Its ideal information-equivalent product
  ties 400/400, so centralized expressivity is not the residual.
- The previously frozen 768-cluster successor is a design, not an executed
  result.  Its four source-disjoint worlds, comparator reproductions,
  capability-matched external comparator, live receipts and independent
  evaluator/gold custody are not yet bound.

The scientific mistake would be to narrow the paper to whichever local metric
passed.  The upward correction is to compare complete scientific discovery
controllers on a joint frontier and make every adverse terminal identify which
part of that frontier is currently unreachable.

## 2. New theory: the acquisition--authority envelope

The manuscript now models a controller as four coupled operations:

1. route generation/selection;
2. evidence acquisition;
3. question-conditioned processing and option-preserving state construction;
4. scientific task closure.

For a common task distribution, source-access contract and charged budget, the
**acquisition--authority envelope** is the set of attainable vectors over:

- decision-relevant evidence discovery;
- final scientific utility;
- future-search optionality under hidden query/route changes;
- false task closure;
- valid closure yield; and
- fully charged cost.

False closure and validity are non-compensatory.  A high-quality report cannot
purchase authority to omit a decisive unresolved route.  A candidate earns the
wide claim only by exceeding the guarded frontier of the frozen donor-complete
policy class, not by beating one weak baseline.

### 2.1 Donor envelopment

The comparison class explicitly absorbs, rather than rhetorically excludes:

- SAGE-style lexical and corpus-side augmentation;
- AgentIR-style reasoning-aware retrieval;
- SIEVE-style fielded inspection and selective fetching;
- bibliography/citation expansion;
- HALT-style claim-evidence coverage;
- DeepControl/value-of-information and decision-theoretic continuation;
- conformal and evidence-aware stopping;
- finite obligation certificates and evidence-linked obligation graphs; and
- question-conditioned evidence plans/memory.

Adding any valid donor mechanism monotonically enlarges the attainable policy
class.  If a new donor reaches the candidate point after all costs are charged,
the claimed superiority is falsified.  This is intentional: nearest work raises
the frontier the theory must surpass.

### 2.2 Analytic results

The manuscript adds four distinct theorem identities.

1. **Run-conditional acquisition ceiling.**  A downstream selector cannot
   submit relevant evidence absent from the union emitted by all admissible
   routes.  This makes `7/229` an exact causal diagnosis of that capture: route
   generation was binding and reranking could not repair 222 never-acquired
   identifiers.  A companion constructive proposition proves that equal call
   counts alone place no nontrivial bound on the difference between arms'
   acquisition ceilings.
2. **Closure factorization.**  A product can implement the contract-correct
   closure decision iff closure truth is constant on each fibre of the state it
   retains.  This is necessary and sufficient, not an architectural preference.
3. **Indistinguishable-world lower bound.**  For a closable and materially open
   world with transcript laws `P0` and `P1`, false closure plus valid-closure
   shortfall is at least `1 - TV(P0,P1)`.  If the transcripts are identical, one
   error must be at least one half.
4. **Maximally permissive robust completion.** Relative to a declared
   compatible-world class, the exact maximal sound rule closes precisely when
   every compatible world admits closure. The registered obligation/validity
   rule is a sound lower approximation and reaches that maximum iff its
   predicate is both sufficient and complete for the robust safe set.

The fourth result is deliberately contract-relative. Adversarial realizability
of unresolved obligations proves they are unsafe to close; it does not prove
that every safe history is recognized by a finite registry. Independent route
observability, a closed registry, a validated population model, or
`CANNOT_CHECK` is still required.

### 2.3 Prospective positive mechanism family

The manuscript turns the prior ODAE idea into an explicit policy class rather
than a slogan.  At each step it selects among route invention, retrieval,
processing, contradiction resolution, state preservation/recovery and closure
verification using a charged index with three numerator terms:

1. a lower-confidence estimate of decision-relevant unresolved-obligation
   reduction;
2. option value retained for prospectively hidden future questions; and
3. expected separation of mixed closable/open state fibres.

The denominator is total charged action cost.  Missing route support produces
`CANNOT_CHECK`, not an assumed zero.  Generic value-of-information and adaptive
search remain donor-owned; the residual hypothesis is their non-compensatory
coupling to scientific obligations and closure identifiability.  This is frozen
as `P2-H-ODAE-ENVELOPE`, a prospective untested mechanism family.

## 3. Historical terminals preserved

No historical result is renamed or made positive.

| Historical object | State after ascent | What it now teaches |
|---|---|---|
| 390-task complete-gold controlled suite | descriptive, mandatory underpowered label retained | mechanism non-vacuity under a complete denominator |
| MetaSyn 86-review probe | bounded external retrieval/screening evidence | stage attribution; not full-system superiority |
| Wide V2 development | `P2_V2_ACQUISITION_NOT_PROMOTED` | diversified acquisition did not clear the recall-first gate |
| OpenAIRE/Crossref matched campaign | provider-invalid / undetermined | source transport validity precedes comparison |
| 7/229 emitted-gold diagnosis | adverse acquisition ceiling | candidate generation, not downstream selection, was binding |
| coverage-first acquisition successor | unsupported and once harmful | lexical/core coverage is not decision-relevant obligation reduction |
| P2-X 400-case exact-contract battery | positive exact-contract authority evidence only | missing unresolved-route state creates mixed closure fibres; equal state ties |
| 768-cluster successor | prospective, no outcomes | the intended external cross-arena discriminator |

The authorized empirical terminal remains `P2_NARROWED` until a new protected
campaign earns otherwise.

## 4. Every adverse result becomes a distinct research family

### N1 — Candidate-generation ceiling (`222/229` never acquired)

**Research topic:** support-deficit-directed route invention.  Estimate which
evidence identity/source classes remain outside the current action support,
generate a materially different route, and verify that route outcome-blind
before protected use.

**Positive discriminator:** on fresh source-disjoint clusters, candidate
acquisition ceiling and decision-relevant route-use recall improve over a
donor-complete controller under identical admissible exposure.

### N2 — Equal calls, unequal admissible exposure

**Research topic:** exposure-equivalent experimental design.  Replace call-count
matching with pre-outcome interface-class witnesses and charged opportunity
measures.  An empty response marks the query outcome, not proof that the backend
could never emit the target identity class.

**Positive discriminator:** both arms have the same prospectively witnessed
source/interface opportunity; no outcome-bearing gold is leaked to workers.

### N3 — Coverage-first merge unsupported or harmful

**Research topic:** obligation-reduction acquisition.  Optimize expected
reduction of decision-relevant unresolved obligation mass, including
contradiction sides and downstream processing availability, rather than overlap
with the original query vocabulary.

**Positive discriminator:** fresh tasks where lexical/core coverage and
obligation reduction disagree, with the latter improving route-use recall and
scientific utility without a false-closure increase.

### N4 — Lexical echo can empty a common-vocabulary query

**Research topic:** hierarchical discriminativeness without a hard document-
frequency cliff.  Candidate replacements include domain-conditioned likelihood
ratios, shrinkage across corpus/domain/task levels, and an outcome-blind fallback
that preserves at least one semantically material term.

**Positive discriminator:** a frozen surface-confusable family in which all
material vocabulary is common globally but discriminative conditionally, plus a
clean family guarding against query broadening.

### N5 — Retrieved but not decision-usable

**Research topic:** option-valued state preservation.  Give state items value
for both current decision contribution and their ability to unlock a hidden
future question or route; charge compilation, caching and recovery.

**Positive discriminator:** future questions/routes frozen before state
construction and hidden from both arms; better future decision utility per total
cost than a donor cache/compiler.

### N6 — Provider invalidity and route censoring

**Research topic:** temporal common-world observability.  Bind route identities,
source snapshots/interface classes and pre-execution accessibility witnesses,
then distinguish common provider drift from arm-specific scientific failure.

**Positive discriminator:** high valid paired-task fraction across every arena,
with provider-invalid tasks mapped to `CANNOT_CHECK`, never to comparator loss.

### N7 — False closure versus always-open overcaution

**Research topic:** authority-complete but utility-preserving closure.  Infer or
acquire the minimal state refinement that splits mixed closable/open fibres,
rather than blocking on every unavailable non-material source.

**Positive discriminator:** lower or non-inferior false closure, retained valid
closure yield on clean closable tasks, and correct handling of non-material
unavailable decoys.

## 5. The 768-cluster upward successor

The existing `protocol/P2_TASK_WORLD_SUCCESSOR_V2.json` freezes:

- four strata with 192 source-disjoint task-world clusters each;
- 768 independent units total;
- four arenas;
- three primary comparators;
- identical query/provider/budget/scoring namespace and matched admissible route
  exposure;
- intersection across all comparator contrasts;
- planned effect `0.15` versus superiority margin `0.05`;
- declared joint target power `0.90`.

It still requires, before any claim-bearing execution:

1. archived reproduction of every baseline task world;
2. four immutable source-disjoint arena snapshots;
3. a capability-matched external comparator and donor-complete matched-host
   controller;
4. independent evaluator and gold/hidden-route custody;
5. live response, latency and total-cost receipts;
6. pre-execution route/interface observability witnesses;
7. a frozen output schema that independently reconstructs false-closure
   opportunities and valid-closure yield.

The protected hypothesis is **`P2-H-ENVELOPE-768`**, not a renamed version of a
failed V2 candidate.  A positive result must clear all acquisition, utility,
safety, closure-yield, validity, power and cost gates.  A failure opens the
responsible N-family above and a materially changed successor; it does not permit
margin relaxation, baseline deletion, task exclusion, or endpoint switching.

## 6. Manuscript changes completed in this tranche

- Retitled the paper around the acquisition--authority envelope.
- Replaced the abstract with the wider theory, exact theorem identities,
  preserved adverse `7/229` result, exact-contract boundary, and explicit
  prospective 768-cluster status.
- Rewrote the contribution list from four interface rules to a five-part
  comparison/composition theory.
- Added `manuscript/sections/acquisition_authority-envelope.tex` with formal
  definitions, four analytic results and proofs, donor absorption, historical
  causal attribution and the prospective discriminator.
- Reframed acquisition/closure as a coupled frontier while retaining the
  authority separation.
- Rewrote related work to grant the strongest neighbouring mechanisms to the
  donor class rather than claiming them as novelty.
- Expanded limitations to distinguish theorem generality from empirical
  external generality and to list the missing 768-cluster bindings.
- Rewrote the conclusion around the wider upward target.
- Added theorem/hypothesis identities to `evidence/CLAIM_LEDGER_V2.md` without
  changing any historical empirical terminal.

## 7. What is not claimed

- No 768-cluster task world has been executed.
- No new external provider result was generated.
- No independent evaluator custody was obtained in this tranche.
- No theorem establishes that the open web has a complete finite obligation
  basis.
- No archived negative, invalid or `CANNOT_CHECK` outcome was converted into a
  positive.
- No general or deployed ORION superiority claim is authorized by the manuscript
  revision alone.

## 8. Public cross-dataset transport execution

The next material empirical lane used Zenodo record `10423427`, a 53,881,052-byte
CC BY 4.0 physiotherapy citation-screening corpus outside the SYNERGY family.
The first frozen identity failed binding because two post-incident rows used
noncanonical class literals and three canonical rows duplicated an earlier
class-free record identity.  Its terminal is retained as
`P2_ZENODO_ACTIVE_ACQUISITION_TRANSPORT_CANNOT_CHECK_BINDING`; no model outcome
was produced.

A materially new V2 identity froze exact canonical-class filtering and
earliest-row identity deduplication before computing any model outcome.  On the
resulting 25,534-record population, all gates passed:

- active recall@10%: `0.703409`;
- better-performing registered within-pool baseline recall@10%: `0.335989`
  (`STATIC_SEED_CENTROID`);
- difference: `+0.367419`;
- active WSS@95: `0.744744`;
- producing-lane rerun: reported byte-identical, without a separate archived
  replay receipt or independent verification.

This supports one conditional within-pool contrast after fitting a new active
SGD model with the previously fixed estimator family, hyperparameters and
batch-size rule, but a source-specific
title--abstract--MeSH representation and identity adapter. All arms use a
label-informed warm start selected from the complete public labels, so the
result does not measure fitted-model, representation, initialization,
cold-start, or population-effect transport.
It does not support protected confirmation, multi-review inference,
ORION specificity, open-world route invention or closure authority.  The V2
population was schema-repaired after V1 and no strong source-native screening
implementation or external custody was available.  The manuscript and claim
ledger now include the result at this bounded authority; `P2_NARROWED` remains
the authorized empirical terminal.

## 9. SWIFT cross-review controller transport

The next bounded public-development lane moved from one physiotherapy pool to
five SWIFT review decisions.  After label-independent within-review
deduplication, the corrected V3 population contained **96,241 canonical rows**.
The reviews were BPA, Fluoride, Neuropain, PFOS-PFOA and Transgenerational.
They are distinct screening decisions, not proven independent samples; the
maximum pairwise shared-content count was 68.

### Retained mechanics failures

V1 and V2 are immutable `CANNOT_CHECK` population-binding failures and neither
fitted a candidate nor comparator:

1. V1 used a casefolded newline-delimited identity that did not reproduce the
   preflight population.
2. V2 exposed that the original direct-review preflight had inserted spaces
   between characters.  Reproducing that expression would collapse token
   boundaries.

V3 bound the intended case-sensitive whitespace-normalized,
label-independent identity audit after class counts had been opened but before
any candidate/comparator outcome.  It therefore carries public-development
authority only.

### Exact adverse V3 result

- five reviews; 96,241 canonical rows;
- unweighted review-level mean candidate-minus-u4 recall@10%:
  `-0.021717971642`;
- unweighted review-level mean candidate-minus-u4 WSS@95:
  `-0.050396333672`;
- u4 better on both endpoints in all five reviews;
- primary `+0.05` recall-margin gate: **FAIL**;
- nonnegative relative-work-saving gate: **FAIL**;
- harm gate: **PASS** (worst recall difference `-0.039215686275`, Fluoride,
  versus the `-0.05` floor);
- absolute-work-saving gate: **PASS** (candidate WSS@95 positive in every
  review).

Harm and absolute work saving are non-compensatory and do not repair the
primary and relative-work-saving failures.  The exact terminal is
`P2_SWIFT_V3_CROSS_REVIEW_CONTROLLER_FAILS_ONE_OR_MORE_PUBLIC_DEVELOPMENT_GATES_REQUIRES_SUCCESSOR`.
The earlier terminal remains verbatim:
`P2_ZENODO_V2_ACTIVE_COMPARATOR_TIES_OR_WINS_REQUIRES_CONTROLLER_SUCCESSOR`.

This is not cold-start evidence, exact ASReview application execution,
protected confirmation, independent custody, population transport, ASReview
software inferiority, or ORION-specific superiority.  It cannot change the
authorized empirical terminal above `P2_NARROWED`.

### V4 diagnosis: learner/balancer component selected, never promoted

The frozen post-outcome 2-by-2 representation-by-learner/balancer
factorization ran on the exact V3 population, seeds, review retention, metrics
and aggregation: 96,241 canonical rows per arm and 384,964 row-arm
evaluations.  Both V3 corners reproduced exactly.  The unweighted review-level
learner/balancer main effect was `+0.022618035379` on recall@10%, with the
aggregate sign in 5/5 reviews, and `+0.041725819160` on WSS@95.  The
representation recall@10% effect was `-0.000900063738` with aggregate-sign
agreement in 1/5; the interaction was `+0.003067576381` with agreement in 3/5.
The frozen classifier therefore selected only the learner/balancer component.

Exact terminal:
`P2_SWIFT_V4_POST_OUTCOME_FACTORIZATION_IDENTIFIES_STABLE_COMPONENT_REQUIRES_CONTENT_DISJOINT_OUTCOME_UNOPENED_FAMILY`.
The result is a mechanism diagnosis designed after V3 outcomes.  It may not
replace the candidate, promote an arm, or support a superiority claim on
SWIFT.  The selected mechanism must now be frozen without outcome access and
tested on a content-disjoint family with the same active u4 comparator and
unchanged safety gates.  The integrated handoff is
`development/p2-swift-controller-factorization-v4-2026-08-23/`.

### V5 content-disjoint transport: primary attribution gates fail

The prospectively frozen V5 successor selected the five largest review
decisions in the CC0 SYNERGY V1 release before label values or model outcomes
were opened.  Label-blind content filtering left 101,044 canonical records
across five review-level units and removed all SWIFT overlap and all pairwise
successor overlap.  The learner/balancer mean recall@10% effect was
`+0.008834277869`, strictly positive in 3/5 decisions and exactly zero in 2/5.
It therefore failed both the locked magnitude threshold (`+0.010858985821`)
and the four-of-five strictly-positive sign rule.  Recall@5%, recall@20%, and
WSS@95 effects (`+0.031876154087`, `+0.019244987149`, `+0.030373086810`) are
retained as tail diagnostics only.  Candidate-minus-u4 recall@10% and WSS@95
remained adverse (`-0.011841648130`, `-0.041523905587`).

Exact terminal:
`P2_SYNERGY_V5_CONTENT_DISJOINT_LEARNER_BALANCER_ATTRIBUTION_FAILS_ONE_OR_MORE_FROZEN_TRANSPORT_GATES_REQUIRES_SUCCESSOR`.
The five decisions share one release, representation and local adapter, and
the same session held download and execution custody.  No arm is promoted.
The next discriminator is a new source family, a before-outcome continuous
recall-effort estimand with recall@10% retained as a locked coprimary endpoint,
unchanged u4/safety gates, and preferably independent custody.  The integrated
handoff is `development/p2-swift-content-disjoint-v5-2026-08-23/`.

### V6 lawful new-source and rank-continuous freeze: execution withheld

V6 attacks the next highest-fan-out root cause---a lawful, source-disjoint,
outcome-unopened family with a continuous recall-effort estimand---without
reopening the V5 result.  It binds all 14 revision-one CSV review decisions at
OSF node `vt3n4`, Medical Guidelines Dutch Association Medical Specialists.
The node declares CC-BY-4.0, and each source body is fixed by OSF file/GUID,
revision-one URL, byte count and SHA-256.  Label-blind processing used only
stable key, title and abstract plus the declared PubMed field.  It reduced
5,074 raw rows to 4,934 canonical rows.  One provisional row matched a V5 raw
content identity; 65 identities appeared in multiple KIFMS reviews and affected
132 rows.  All were excluded symmetrically before KIFMS outcomes, leaving zero
final SWIFT/V5 content overlap.  All 5,074 KIFMS `pubmed_id` values are empty,
so PMID-overlap zeros are vacuous and the content-union SHA-256
`731d87e66b3e1195826c82e0a94fef19c044d63503ba2a36e41d38f811df0b12`
is the operative disjointness receipt.

The new coprimary `CRE20` is the normalized area under the complete
recall-effort step function over effort `[0,0.20]`.  R@10 retains its adverse
V5 magnitude threshold, and both coprimary endpoints require a strictly
positive review-level learner/balancer effect in at least 12/14 decisions.
The four cells, active u4 comparator, deterministic class seeds, batch cadence,
unweighted review aggregation, learner WSS/harm gates, full candidate-minus-u4
margin/WSS/harm gates and candidate absolute-work-saving gate are unchanged.
No endpoint compensates another.

No KIFMS label value, class count, seed, exact V6 ranking or comparative score
was opened.  This boundary is context-specific rather than global: the pinned
companion repository contains paths to historical public ASReview simulation
artifacts for the named datasets, although their metric contents were not
inspected in V6.  The source is new relative to SWIFT and V5, not globally
unstudied or historically outcome-sealed.  `expert_inclusion` semantics, both
classes in every review, a signed runner/dependency/container/evaluator and
independent protected custody remain unresolved.  The lawful exact source-body
interface is found, but confirmatory execution is not ready.

Exact terminal:
`P2_KIFMS_V6_LAWFUL_EXACT_SOURCE_AND_LABEL_BLIND_DISJOINT_POPULATION_FROZEN__INDEPENDENT_PROTECTED_EXECUTION_CANNOT_CHECK`.
The V5 adverse terminal remains unchanged and no performance arm is promoted.
The next discriminator is independent protected execution of the exact V6
freeze without changing source, reviews, rows, label, CRE20 window, R@10, u4 or
any harm/work-saving gate.  Packet:
`development/p2-continuous-recall-effort-v6-2026-08-23/`; protocol SHA-256
`55a5363e6f9360c4722979d0cb11751575f050ce2eea2147a7883d679ea09633`;
manifest SHA-256
`cb7aac3ea6cc4b1070515dff882c4e3cb0a85632e5829af379208dd70448f710`.

### V7 transparent public execution: productive donor component, adverse full stack

V7 removes the unproductive wait for an external signature without claiming
that local hashes create independence. Its protocol, implementation and exact
V6 source/population identities were frozen before KIFMS labels. All 14 source
files, 4,934 canonical rows, two classes per review and four complete rankings
then passed. The run required 18.03 seconds.

The learner/balancer mean effects are `+0.084471533530` on CRE20,
`+0.148820475160` on R@10 and `+0.119813186484` on WSS95, so both magnitude
and mean work-saving gates pass. Strict-positive counts are 11/14 and 9/14,
and worst-review R@10 is `-0.125`, so sign and harm gates fail. The complete
candidate remains below u4 by `-0.201647856112` R@10 and `-0.119159673071`
WSS95, failing all three relative gates; candidate absolute WSS95 is positive
in every review.

Exact terminal:
`P2_KIFMS_V7_TRANSPARENT_PUBLIC_EXECUTION_FAILS_ONE_OR_MORE_LOCKED_PERFORMANCE_GATES_REQUIRES_SUCCESSOR`.
The upward mechanism is donor envelopment: make the strong u4 component the
mandatory base, learn only a leave-one-review-out residual with a hard u4
fallback, and test the frozen controller on a new source-disjoint family. V7 is
same-workspace public development and explicitly not confirmation. Packet:
`development/p2-kifms-transparent-execution-v7-2026-08-23/`.

### V8 donor envelopment: exact u4 retained, residuals not admitted

V8 replaces the failed attempt to beat u4 from outside with the stronger and
cheaper question: can a bounded residual be admitted safely above exact u4?
It reproduces the V7 u4 order exactly in all 14 KIFMS reviews and evaluates
three residual families at two frozen strengths, for 84 review--configuration
executions. Development selection is nested leave-one-review-out with exact u4
as a hard fallback.

Twelve of fourteen outer folds select fallback. The two residual activations
are exclusion-fragile: `F1_WORD_PRUNED_A100` loses `-0.333333333333` R@10 on
its held-out shoulder-dystocia review, and `F2_TITLE_EMPHASIS_A250` loses
`-0.0625` R@10 on its held-out knee-replacement review. Cross-fitted means
versus u4 are `-0.002009123382` CRE20, `-0.028273809524` R@10 and
`-0.000991113755` WSS95; worst-review R@10 is `-0.333333333333`. Every
cross-fitted safety check therefore fails and no residual is admitted.

The raw execution selector terminal records only that two folds activated a
residual. The authoritative scientific terminal is instead
`P2_V8_DONOR_ENVELOPMENT_CROSSFIT_FAILS_CRE20_WSS95_AND_HARM__NO_RESIDUAL_ADMITTED__EXACT_U4_FALLBACK`
from `SCIENTIFIC_ADJUDICATION_V8.json`. Title emphasis remains only the nearest
bounded diagnostic: over the full open grid its alpha-0.25 version has mean
CRE20 `+0.010693125060` and mean WSS95 `+0.002042792819`, while its
`-0.0625` worst-review R@10 is the explicit falsifier.

The resource-efficient next discriminator is one source-disjoint title-
emphasis stability test with unchanged gates, not another wide residual search.
Failure discards title emphasis and keeps exact u4; a mixed result identifies
review-family heterogeneity and cannot erase the KIFMS harm. V8 remains open
same-workspace development and authorizes neither a safe controller nor
independent confirmation. Packet:
`development/p2-donor-envelopment-v8-2026-08-23/`; result SHA-256
`8aa6438b470a7facd7e630a8280afdbc034120c631eac4fdfe744d48a0e63e75`;
adjudication SHA-256
`fe1f5633a465c0b6d8e21673d867d77e3ccc8fc3af58c86703a25ea3f3d0ec25`.

### V9 source-disjoint title-emphasis binding stop

V9 freezes seven public reviews, retains 21,896 canonical rows with zero final
overlap against the prior SWIFT/SYNERGY/KIFMS union and zero cross-review
duplicate excess, and stops before vectorization when one normalized
title--abstract identity has conflicting labels. This is `CANNOT_CHECK`, not a
negative title-emphasis effect. No arm is executed and exact u4 remains the
fallback. Packet:
`development/p2-source-disjoint-title-emphasis-v9-2026-08-23/`.

### V10 provider-native conflict resolution and one-shot result

V10 changes only the population identity semantics. Provider records 1003 and
1018 have distinct author and DOI hashes and no provider duplicate link; both
are retained without choosing a label. One frozen execution covers 21,897 rows
and takes 357.789818 seconds. Mean deltas versus exact u4 are
`+0.000779992745` CRE20, `-0.005382681125` R@10 and `+0.001765466550`
WSS95; CRE20 and R@10 are positive in 4/7 and 1/7 reviews; worst R@10 is
`-0.044444444444`. Both coprimary magnitude and sign gates fail. The residual
is discarded without tuning and exact u4 remains the fallback. Result SHA-256:
`c69e5634b8d0e82a1fa393dbae276d728e9a2f7ba4c9db81a27c0329e2e66742`.

### V11 safe residual envelope and donor-fibre separation

V11 performs no new empirical or model run. It proves that a
certificate-switched residual envelope embeds exact donor fallback and that a
simultaneous noncompensatory certificate controls false admission. Finite V8
and V10 failures identify neither universal u4 optimality nor a positive unseen
family. Strict same-contract separation from a saturated donor-complete class
requires fibre-separating information/state, acquisition support outside the
donor, or proof that the donor class was incomplete. A changed cost, access,
world or authority contract changes the estimand rather than establishing
original-contract dominance. Finite witnesses pass. Packet:
`development/p2-safe-residual-envelope-theory-v11-2026-08-23/`; theory SHA-256
`a3c9107ad152766efc2a08d722657d550781f1f181f47f3ceeba041c0a710176`;
receipt SHA-256
`1694e2f33bc195eb848e7ec93d3fd31793c936db96f62d43d17d09b5dac8dd6f`.
