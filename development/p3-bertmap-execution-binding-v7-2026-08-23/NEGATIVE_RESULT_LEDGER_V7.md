# P3 BERTMap V7 recursive negative-result ledger

Every negative remains an active research object. Progress is bounded to what the evidence establishes.

| ID | Negative result | Cause | Positive progress | Residual | Next discriminator |
|---|---|---|---|---|---|
| P3V7-N01 | V6 stopped before training because Transformers 4.51.3 rejected DeepOnto's evaluation_strategy keyword. | The source lock's Python-3.10 branch selects an API-incompatible Transformers version. | The exact V7 four-package tuple imports on Python 3.10.20 and TrainingArguments accepts the source-native keyword in a constructor-only probe. | DeepOnto, JVM, model, ontologies and BERTMap were not executed; native K3 smoke is still CANNOT_CHECK. | Freeze a full repaired runtime only after the nonempty table-reader defect and rights/SBOM layers are closed, then run the unchanged no-gold five-artifact smoke. |
| P3V7-N02 | The V7 four-package tuple is not the source lock's Python-3.10 resolution. | Those versions coexist in uv.lock only under Python below 3.9, although matching cp310/macOS-arm64 wheels exist. | A separately hashed 26-distribution Python-3.10 compatibility island was resolved and installed with hashes. | It excludes DeepOnto's full Python/JVM dependency closure and cannot be called a complete native lock. | Build a complete content-addressed Python-3.10 and Java runtime for a separately named repaired successor. |
| P3V7-N03 | Transformers 4.46.3 already deprecates evaluation_strategy. | The compatible constructor retains the old keyword only through a deprecation bridge to eval_strategy. | The exact source hash and successful constructor are retained. | The repair is version-fragile and cannot justify floating upgrades. | Keep 4.46.3 exact or source-version a DeepOnto keyword repair; never float Transformers. |
| P3V7-N04 | Pinned DeepOnto's non-reference table reader uses dp['Score'] on rows returned by pandas.DataFrame.itertuples(). | itertuples returns namedtuples, which support positional/attribute access rather than string-key indexing. | AST evidence and a synthetic nonempty namedtuple reproduce TypeError without opening outputs. | Any nonempty raw TSV reaching MappingRefiner is conditionally blocked; an empty table avoids only this loop. | Freeze a separately identified source repair (for example dp.Score), then exercise nonempty and empty synthetic tables before any model run. |
| P3V7-N05 | raw_mappings.json and raw_mappings.tsv are checkpoint/resume artifacts, not intrinsically completion proofs. | Pinned source saves at index 0, every 100 classes and the final class, and loads existing JSON on resume. | The V7 parser requires the exact predeclared eligible source-key set and JSON/TSV equivalence. | No native artifacts were produced in V7. | Apply the closed parser to a fresh isolated execution directory and reject any missing source key or stale sidecar. |
| P3V7-N06 | Pinned refinement trusts pre-existing extended, filtered or repaired TSV files after only native table loading. | The released resume paths do not bind provenance or derivation completeness. | The external parser binds universe, raw retention, exact filter derivation, repaired-pair containment and hashes. | Parser conformance is authored adapter evidence, not native execution evidence. | Require an empty execution directory plus before/after file census and the closed parser on every future run. |
| P3V7-N07 | Complete runtime rights and SBOM closure is absent. | DeepOnto tracks 208 JARs and the original repository 106 JARs; the constructor island is not the full runtime. | Both root Apache-2.0 source layers, all exact source hashes, metadata censuses and 26 installed distribution licence metadata records are retained. | Bundled Java components, full Python closure, model, generated artifacts and evaluation custody remain unadjudicated. | Produce component-level Python/Java SBOM and rights decisions before retaining or distributing a full runtime. |
| P3V7-N08 | The original BERTMap repository contains a paper data.zip described as ontologies plus reference mappings. | The canonical research repository packages outcome-bearing paper data beside source. | Only ls-tree metadata was recorded; the 2,017,453-byte blob was not opened and is excluded. | Paper-data rights, custody and independence are CANNOT_CHECK. | Use only separately rights-cleared, independently custodied inputs under a frozen evaluation protocol. |
| P3V7-N09 | BERTMap still has zero of five required native artifacts in the retained evidence chain. | V7 intentionally prohibited model and ontology execution while resolving source/runtime/parser blockers. | Dependency constructor and fail-closed parser bindings are now independently reproducible. | Three-family native smoke remains 2/3. | After a versioned source repair and complete runtime/rights closure, run one fresh no-gold BERTMap smoke under the unchanged five-artifact gate. |
| P3V7-N10 | No BERTMap scientific comparison is available. | No gold/reference, naturalistic ontology, protected outcome, training, prediction, repair or scoring was opened or performed. | The evidence boundary is explicit and machine-checked. | V5 scientific comparator readiness remains 0/3; correctness, coverage, harm, transport and superiority are CANNOT_CHECK. | Only after 3/3 native readiness, freeze independently custodied rights-valid evaluation data and score all comparators under one no-feedback protocol. |
