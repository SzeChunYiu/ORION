# P1 V8 source-native target-semantics successor

## Terminal

`P1_V8_PUBLIC_TARGET_SEMANTICS_PARTIALLY_BOUND__R7_POSTPUBLICATION_DENOTATIONS_RIGHTS_AND_OWNER_RATIFICATION_CANNOT_CHECK`

V8 adds stronger public primary-source evidence but does not change the R7
adapter verdict. Missing target evidence is not treated as impossibility, and
no owner ratification is inferred.

## Question and evidence boundary

G16 asks whether the six frozen source-native postpublication terminals can be
transported into the seven R7 target identifiers without changing sort,
coordinate, licensed/forbidden operations, unresolved behavior, or authority.
R7 previously left **720** injective maps not disproved but uncertified.

V8 inspected only target semantics, provenance, and rights. It accessed no
case text, row-level outcome, system output, protected datum, comparator run, or
performance table. All R7 source profiles, gates, rejection witnesses, and
conditional statements remain unchanged.

## New positive semantic evidence

The public repository `SzeChunYiu/ORION` is accessible at immutable commit
`390267dfa9c6669e506ba67b5dde5dddd8f96232`. Seven frozen semantic-source files
were fetched from commit-specific raw URLs. All **7/7** remote bodies were
byte-equal to the corresponding local Git objects; V8 retained only hashes,
byte counts, Git blob identities, URLs, line anchors, and conservative
paraphrases.

The corpus now binds:

- seven identifiers as **scientific decisions**;
- seven separately declared **diagnostic probes/actions**;
- an intensional scientific-process anchor for every decision;
- fail-closed `UNRESOLVED` semantics;
- a proposal/adoption boundary that grants no self-adoption, promotion, or
  merge authority; and
- direct executable witnesses that diagnostic probes are not one-to-one
  terminal-decision denotations.

For example, `SEARCH_MORE` can distinguish `KEEP_SEARCH` from `KEEP_COMPILE`,
and `TEST_PROBLEM_BOUNDARY` can distinguish objective from boundary
reformulation. This formally closes the earlier ambiguity about whether the
ARD action list may simply be paired positionally with the R7 decision list:
it may not.

## Residual target-semantic gap

None of the six actionable target decisions has a public, exhaustive:

1. postpublication coordinate;
2. licensed postpublication operation set;
3. forbidden postpublication operation set;
4. owner-ratified decision-to-postpublication bridge or explicit declaration
   that no bridge exists; or
5. target-specific abstention/error/timeout/malformed/unsupported rule.

Scientific-process anchors do not fill those fields. A measurement anchor may
make one pairing plausible, but lexical or causal overlap is not extensional
equality. Conversely, a differently named process anchor is not an
authoritative inequality proof. V8 therefore changes all 30 actionable cells
from “unresearched target meaning” to “public intent known, essential
postpublication denotation still absent,” while retaining `CANNOT_CHECK`.

## Exhaustive adjudication

The unchanged finite audit was rerun over all **117,649** maps:

| Disposition | R7 | V8 | Delta |
|---|---:|---:|---:|
| fully certified | 0 | 0 | 0 |
| rejected | 116,929 | 116,929 | 0 |
| not disproved but uncertified | 720 | 720 | 0 |

All 720 survivors are recorded individually in
`P1_V8_720_ADAPTER_REGISTRY.json`. Each fixes
`UNRESOLVED -> UNRESOLVED`, has five actionable images, and remains blocked by
the same five target field classes above. V8 certifies **0/720** and newly
rejects **0/720**. The unique certified partial adapter remains
`UNRESOLVED -> UNRESOLVED`.

The strongest formal result is therefore semantic underdetermination relative
to the frozen public corpus: target intent and probe/decision typing are known,
but the evidence required by G2--G4 is not. Additional source-side taxonomy
search cannot change this partition; only target-owner semantic authority can.

## Rights and provenance

GitHub reports the target repository as public, but its repository-license
field is null, the license endpoint returns HTTP 404, and the pinned tree has
no repository-level or target-corpus-applicable `LICENSE`, `COPYING`, `NOTICE`,
or `COPYRIGHT` file. Six licence files exist only in unrelated
journal-package/benchmark subtrees and are not generalized to the target
corpus. The commit is public but unsigned. Consequently:

- semantic provenance is stronger and independently refetchable;
- public visibility is not treated as a reuse or redistribution licence;
- the V8 handoff retains **zero** remote source payloads; and
- neither repository authorship nor public access is treated as a signed
  owner-ratification of missing semantic fields.

Inherited source-side rights remain unchanged: Crossref is checked CC BY 4.0;
NISO is checked but restricted; NLM/JATS/MeSH item-specific applicability
remains `CANNOT_CHECK`; and the retained COPE transport remains HTTP 403.

## Exact next discriminator

`P1_V8_REQUIRED_OWNER_ALGEBRA_SCHEMA.json` defines the minimum machine-readable
algebra. `P1_V8_REQUIRED_FIELD_CUSTODIAN_REGISTRY.json` assigns the missing
fields to four non-substitutable roles:

- R7 vocabulary owner or formally delegated taxonomy custodian;
- host authority owner for recommendation/execution limits;
- applicable target-corpus rights holder or authorized licensor; and
- an independent semantic reviewer with no algebra authorship or case-outcome
  custody.

The completed algebra must be closed-world, licensed, content-addressed and
signed before any case/outcome access. It must state all seven target profiles,
probe/decision typing, error and timeout terminals, and authority boundaries.
An independent reviewer must return `CONFORMANT`; only then may the unchanged
117,649-map audit be rerun. If any required field remains absent, all 720 remain
`CANNOT_CHECK`.

## Manuscript disposition

**Main-manuscript integration is not currently warranted.** The manuscript
already states the decision/probe type separation and the target-semantic
`CANNOT_CHECK` result. V8 materially strengthens provenance, rights accounting,
and the formal sufficiency diagnosis, but changes no adapter count, scientific
terminal, naturalistic evidence, or title-level authority. The packet may be
referenced from the development/claim ledger; manuscript prose should change
only after a completed owner algebra changes the adjudication or an editor
requires the provenance detail.
