# P5 V2 six-arm resource/configuration ledger

**Artifact:** `P5.SIX_ARM.RESOURCE.CONFIG.RIGHTS.LEDGER.V2`  
**Date:** 2026-08-23  
**Authority:** outcome-blind primary-source resource/config/runtime/rights preflight only. No comparator, dataset row, label, patch, test, outcome example, protected result, pytest, or CI was opened or executed.

## Verdict

**Confirmatory-ready: 0/6.** All six semantic identities, official paper-linked implementations, licence bytes, and native entrypoints remain bound. That does not bind a matched P5 execution. No arm has a complete content-addressed combination of candidate-visible bytes, model/provider/services, environment/container/compute, whole-run budgets, retry policy, isolated write surface, typed eight-class adapter, lawful content/service rights, and independent protected scorer.

- Execution: `P5_SIX_ARM_EXECUTION_CONFIG_RESOURCE_RIGHTS_AND_EIGHT_CLASS_ADAPTERS_CANNOT_CHECK`
- Scientific: `P5_COMPARATOR_PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`
- H1–H4: `CANNOT_CHECK`
- Protected freshness: `CANNOT_CHECK`

`BOUND_NATIVE_METADATA` below means only that the pinned primary source documents a value. It does **not** select that value for P5. `FREEZABLE_NOT_FROZEN` means a lawful prospective choice is identifiable but unhashed. `UNBOUND` and `CANNOT_CHECK` remain noncompensatory blockers.

## Six-arm matrix

| Arm | Pinned identity | Native resource/config facts retained | Confirmatory fields still unbound or unchecked | Ready |
|---|---|---|---|---:|
| C1 fixed | SWE-agent `3ea751c…`; MIT `7610ed…` | `sweagent = sweagent.run.run:main`; model/config driven; isolated issue-solving patch/trajectory interface | exact dependencies/container/task environment; model/provider/prompt; wall/token/USD/tool/compute/network/retry limits; rights beyond source; hashed P5 adapter and protected scorer | **No** |
| C2 direct self-edit | MOSS `5453f1f…`; composite Apache/MIT/mixed | Docker 24+, Node 22+, pnpm 10, Python 3.11+; `moss evo`; three depth tiers; stage timeouts; provider/token/cost receipts when exposed | selected provider/model/coding CLI; image/dependency/compute identity; matched depth and whole-run caps; replay/session rights; isolated outcome-blind adapter; protected scorer | **No** |
| C3 archive self-edit | Darwin Gödel Machine `a565fd2…`; Apache-2.0 `84b750…` | Python/Docker plus separate SWE-bench pin; 80 generations, 2 attempts, 2 workers; dated diagnosis/coding models; 4096 output-token calls; bounded inner retries/timeout | exact dependency/image/task subset; declared handling of pinned argparse defect; effective whole-attempt and aggregate caps; provider/content rights; hashed P5 adapter; protected custody | **No** |
| C4 issue-centric | ADIAS `fbcf0c7…`; restricted CC BY-NC-SA `047d22…` | Python 3.12/Docker/domain image; general and Tau-bench script defaults; `deepseek-v4-flash` role default; 21,600 s improve/learn timeout | lawful restricted use; exact environment/domain/provider; aggregate token/tool/USD/retry limits; conflicting native write-surface descriptions; hashed eight-class mapper; third-party rights; protected scorer | **No** |
| C5 evaluator-only | Double Ratchet metric-only `0f14e91…`; Apache-2.0 `09e8a9…`, NOTICE `5d86be…` | Python >=3.11/Bedrock; repository 8-round and paper 100-round regimes; 8192 tokens/call; bounded Bedrock/soft-label retries; `CLEAN/DRAWBACK/ABSTAIN` | exact dependency/host/frozen solver; chosen regime and aggregate caps; P5 inputs/services; development-only locked surrogate; hashed evaluator adapter; external one-shot protected custody and rights | **No** |
| C6 modern source-grounded | ScienceClaw `38b2f68…`; Apache-2.0 `c71d23…` | hashed setup/entrypoints; `scienceclaw-post ... --dry-run`; typed immutable artifacts and JSONL lineage | dependency/profile/tool/model/provider/service identities; every matched budget; topic-to-case/eight-class adapter; API/tool/content/artifact rights; protected scorer/freshness | **No** |

## Cross-arm matched-exposure ledger

The following must be frozen identically or capability-matched before protected use: candidate-visible packet bytes; allowed repository/evidence bytes; network policy and service revisions; model/provider capability; wall time; token and monetary caps; tool/step budget; CPU/RAM/GPU/parallelism and container identity; retries/fallbacks; isolated arm-specific writes; independent responsibility/preservation/fresh-transfer/harm scoring; and typed retention of invalid/error/timeout/abstention/`UNRESOLVED` states.

At this V2 checkpoint, those fields are not jointly frozen across any arm. Native defaults are heterogeneous descriptive facts, not matched allocations. In particular:

1. **C1 and C6** expose almost no numeric whole-run envelope in the retained metadata.
2. **C2–C5** expose useful native defaults, but no source freezes them as the P5 allocation or supplies a common whole-run token/USD/tool/compute envelope.
3. **C3** has a static argparse choice defect and a nominal 5400 s future timeout that is not a running-future watchdog; a successor must declare rather than silently repair these facts.
4. **C4** has conflicting prompt-versus-CLI write-surface descriptions and restricted source rights.
5. **C5** computes `eval_locked` each round in the official flow; P5 needs a development-only surrogate and a once-only external protected score.
6. **Every arm** lacks an implemented, content-addressed P5 adapter and an independently bound protected scorer/freshness custody path.

## Rights and service boundary

Repository licences cover only their respective source scopes. They do not automatically cover task repositories, issues, sessions, benchmark rows, patches, tests, containers, model APIs, scientific tools, hosted services, or generated artifacts. ADIAS is additionally CC BY-NC-SA rather than OSI-open; MOSS is composite; Double Ratchet requires NOTICE retention; ScienceClaw’s paper is CC BY-NC-ND while its code is Apache-2.0. Rights must close per content and service root before execution.

## Negative result and next discriminator

**Cause:** the earlier lane bound semantic/source identities, not complete confirmatory manifests.  
**Residual:** every arm still lacks at least one noncompensatory resource, configuration, rights, adapter, or custody field; most lack several.  
**Next discriminator:** before any outcome access, create a successor amendment that chooses and hashes (1) a full per-arm runtime/resource/service manifest, (2) one lawful P5 adapter per arm, and (3) one common exposure and independent scorer manifest. Re-audit every content/service root. Any remaining `UNBOUND` or `CANNOT_CHECK` field keeps the arm non-ready; it must not be narrowed away or converted into a positive claim.

Machine-readable field-level detail and native terminal lists are in `P5_V2_RESOURCE_CONFIG_LEDGER.json`.
