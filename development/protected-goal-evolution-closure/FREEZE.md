# #288 protected goal-evolution execution freeze

This packet is additive to immutable `research/goal-evolution/PROTOCOL_V1.json`. The V1 protocol is not edited: it remains the pre-outcome authority contract that separated hidden scientific intent, operational objective, and optimizer/evaluator.

## Pre-outcome hypothesis

On the bounded eight-family hidden-intent panel, protected objective evolution is expected to be **unnecessary beyond a strong fixed multi-objective/Pareto parent that is given the same public responsibility diagnostics and the same host-side protected admission rule**. If that parent ties ORION on all registered actions and protected gates, issue #288 must close `REFUTED`, not `PROTECTED_GOAL_EVOLUTION_SUPPORTED`.

## Falsifiers

Reopen the negative hypothesis if ORION, under matched public information and protected admission, correctly handles any case the strong fixed-Pareto parent cannot; if the parent incurs more proxy gaming/constraint violations/intent regression; if hidden scientific intent leaks to candidate policies; or if independent reconstruction disagrees.

## Panel

32 invented cases: four variants for each V1 family, preserving the exact eight family IDs/classes frozen in `PROTOCOL_V1.json`. Candidate-visible diagnostics are cause-oriented but never expose the hidden intent score or gold responsibility. Protected evaluation compares old/new objective outcomes only after the candidate action is frozen.

## Arms

- `FIXED_ORIGINAL_OBJECTIVE`
- `UNCONSTRAINED_SAGA_LIKE`
- `LLM_REWRITE_AFTER_POOR_PERFORMANCE`
- `FIXED_MULTI_OBJECTIVE_PARETO_PROTECTED`
- `ORION_PROTECTED_GOAL_EVOLUTION`
- analysis-only `HUMAN_ORACLE`

The strong Pareto parent receives the same fixed candidate objective portfolio, public diagnostic packet, and protected host admission boundary as ORION. The comparison therefore tests whether *generating/evolving new objective content* adds anything on this bounded panel, not whether protected evaluation is useful.
