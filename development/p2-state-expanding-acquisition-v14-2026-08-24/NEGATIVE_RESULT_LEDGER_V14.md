# P2 V14 recursive result ledger

V14 preserves V13 unchanged. It is a post-discovery, same-session source-identity reproduction, not an independent source census or performance experiment.

| Item | Exact result | Causal interpretation | Smallest successor |
|---|---|---|---|
| V13 state-coordinate witness | **preserved** | One provider-native keyword coordinate separates an exact-u4 text fibre; no screening benefit follows | Preserve unchanged |
| V13 frozen route | commit `38b35218...`, path `index_v1.json`, expected SHA-256 `5d829c6...` | Commit/path and expected bytes were frozen together but are mutually inconsistent | Repair only the snapshot identity in V15 |
| Frozen commit lineage | tree `49f437c...` -> blob `f4f5007...`, 22,135 bytes -> SHA-256 `f34c17b...` | Exact Git tree and independently recomputed Git blob identity agree | Preserve as coherent V15 snapshot |
| Expected-byte lineage | commit `dc2dadf...`, tree `2173535...` -> blob `ada2668...`, 23,118 bytes -> SHA-256 `5d829c6...` | The expected bytes are real, but belong to a later commit | Provenance only; never substitute into V14 |
| Frozen route acquisition | **FAIL** | `FROZEN_INDEX_SHA_DOES_NOT_MATCH_PINNED_COMMIT_PATH` | Independent custodian binds coherent `38b35218...` tuple |
| Index/census boundary | **0 parses, 0 censuses, 0 review CSVs** | Fail-closed stop prevents post-mismatch selection | Resume only after V15 lock |
| Performance boundary | **0 labels, class counts, models, rankings or metrics** | No population and no custody authorize performance | Preserve exact u4, V10 gates and costs |
| Independent custody | **0/3 roles bound** | Same-session content addressing is not independent custody | Source custodian first; outcome and result custodians later |

## Exact adverse terminal

`P2_V14_FROZEN_INDEX_IDENTITY_MISMATCH__PINNED_COMMIT_PATH_RESOLVES_F4F5007_BLOB_SHA256_F34C17B3__EXPECTED_ADA2668_BLOB_SHA256_5D829C66_BELONGS_TO_DC2DADF__STOP_BEFORE_CENSUS_AND_PERFORMANCE`

## Recursive conversion of the negative result

The failure is no longer an undifferentiated data-availability blocker. It is localized to one inconsistent snapshot tuple. V15 therefore preserves the originally frozen commit and dataset-template authority, replaces only the incompatible inherited index hash in a new protocol, requires independent source custody, and leaves the seven-review selection rule and every performance component unchanged.
