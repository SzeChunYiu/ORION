# P5 conflict-safe manuscript repair packet

**Date:** 2026-08-23
**Status:** proposed wording only; not applied because the P5 claim ledger and
manuscript sections are concurrently modified in the shared worktree

These repairs narrow ambiguous freeze/execution wording while adding the new
public-development/protected-freshness bridge. They do not change H1--H4 from
`CANNOT_CHECK` and do not create an empirical result.

## 1. Evaluation: replace execution-readiness ambiguity

Current sentence in `manuscript/sections/07-evaluation.tex` says the development
instrumentation establishes that the experiment "can be run and audited."

Use instead:

> This establishes only that development instrumentation and a fail-closed
> preflight exist. Confirmatory executability is not established: the subject,
> protected case and split registries, six exact comparator adapters, evaluator
> images, fresh-transfer targets, harm adjudicator, access history, evaluation
> epoch and verifier receipts remain unbound.

Reason: the current confirmatory terminal is `CANNOT_CHECK`, and the issue-8
packet/execution receipt remains `REFUSED_UNBOUND`.

## 2. Evaluation: separate predecessor menu from the six-arm family

Where the manuscript lists the predecessor's broader development policy menu,
add:

> Those development controls are not the six-arm confirmatory multiplicity
> family. The wide successor's six primary slots are separately identified in
> `development/p5-public-development-protected-freshness-bridge-2026-08-23/
> SIX_ARM_COMPARATOR_BINDING_LEDGER_V1.json`; naming the slots does not make
> their exact execution adapters complete.

Reason: nine local development policies and six primary comparator slots must
not appear to be the same frozen family.

## 3. Evaluation or limitations: add the remote-evaluator adverse result

Add a short paragraph:

> An official-source audit found no off-the-shelf protected-freshness route.
> SWE-bench Multimodal's current public dataset and task surfaces expose gold
> patches/tests and local gold evaluation, while `sb-cli` pins only the client
> and API URL rather than the served evaluator, split, images or access history.
> Scale SWE-Bench Pro's commercial/private set is the strongest contractual
> provider candidate, but no signed agreement binds this study's rights,
> genuinely new post-freeze cases, six matched arms, evaluator hashes,
> per-task receipts or verifier access. Both remain non-result preflight facts.

## 4. Limitations: add the public-source boundary

Add:

> SWE-bench Multilingual and Defects4J provide narrow development paths after
> per-root content filtering. SWE-bench Verified dataset rights remain
> `CANNOT_CHECK`; BugSwarm's infrastructure licence does not settle mined
> projects or containers; BugsInPy lacks a root licence at the pin. Public
> fixes and gold cannot establish protected freshness, same-symptom eight-class
> responsibility, harm, or independent custody.

## 5. Limitations: prohibit reuse of the published 24 cases explicitly

Add:

> The published 24-case fixture exposes plaintext labels and has already been
> scored. It cannot be repaired by resealing and must never be reused for
> confirmation. Its 21/24 value remains descriptive only.

## 6. Ethics/reproducibility: scope "frozen"

Use `locally hash-pinned prospective design artifact` for V3, wide, and bridge
artifacts unless and until all external confirmatory identities are bound.
Reserve `confirmatory execution freeze` for a provider-signed, identity-complete
registry issued before candidate generation.

## 7. Claim ledger: exact bridge row

Proposed row for `THEORY_CLAIM_LEDGER_V2.md`:

| ID | claim | status | evidence | boundary |
|---|---|---|---|---|
| P5-S4 | The public-development/protected-freshness bridge names eight domain rubrics and six primary comparator slots, specifies two 768-cluster source/provider/time-disjoint waves plus no-revision harm sentinels, and provides a fail-closed external-provider contract. | PROSPECTIVE DESIGN | `development/p5-public-development-protected-freshness-bridge-2026-08-23/` | Zero of six comparator adapters is confirmatory-ready; no provider, protected cases, outcomes or rights-complete 64-cell panel is bound. H1--H4 remain `CANNOT_CHECK`. |

## 8. Claim boundary: do not overstate the successor

Safe current sentence:

> The bridge improves the denotation of a future test; it does not improve the
> tested system, demonstrate benefit, establish independent custody, or close a
> paper-level empirical blocker.

Conditional future sentence, usable only after signed execution and both waves:

> On the exact frozen panel, minimal authorized revision improved protected
> fresh transfer relative to every frozen arm while satisfying preservation,
> sentinel, harm, worst-domain and worst-class gates; replication and all
> remaining boundaries are reported exactly.

Do not use the conditional sentence before the signed archive exists.
