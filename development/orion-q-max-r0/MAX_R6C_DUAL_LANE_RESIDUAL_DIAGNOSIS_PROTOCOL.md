# ORION-Q MAX-R6C dual-lane residual diagnosis protocol

Date: 2026-08-21
Parent programme: #679 / PR #689
Predecessor negative: `research/failures/2026-08-orion-q-exact-tare3-frame-only-collapse/README.md`
Predecessor donor-first lane: `MAX_R6B_TARE_TRANSFORMATION_REUSE_DONOR_ABSORPTION_PROTOCOL.md`
Status: frozen before accepting the R6B chemistry outcome.
Authority: residual diagnosis / research routing only; not R6, not novelty, not promotion authority.

## Purpose

Run canonical ORION and specialized ORION-Q as matched diagnosis lanes after the
frozen R6B TARE transformation-reuse donor experiment. Both lanes receive the
same R6B receipt and the same preserved exact-TARE3 negative. They may disagree
on routing, but neither may change the R6B selector, comparator, resource model,
protected-subject rule, or the earlier exact-TARE3 gates.

## Frozen evidence

Both lanes receive exactly these decision-relevant facts:

1. the exact TARE3 compiler passed hostile exactness, proof witnesses and the
   exact H4/equilibrium-N2 top-four panel;
2. it beat the older canonical TARE frame but tied `B_FRAME_ONLY_STRONG` on all
   frozen open-subject rows, so its incremental value over the incumbent is NONE;
3. the R6B donor-reuse receipt, including its hostile gate map, H4/N2 batch
   eligibility, `C_separate`, `C_reuse`, development support bit and protected
   access flag;
4. the stretched-N2 prospective discriminator must still be unread.

No protected stretched-N2 coefficient content is admissible to R6C.

## Canonical ORION lane

The canonical lane must execute the current `orion-research-harness` backed by
canonical `OrionRuntime`/`OrionSolver` and the canonical responsibility model.
It must also expose the relevant mechanics through the 59-cell mechanics bridge.

Registered claim-relative responsibilities are:

- `RESP:R6B_DONOR_REUSE_UNABSORBED`: R6B is verified positive and therefore
  incumbent donor capability must be absorbed before invention;
- `RESP:METHOD_LANGUAGE_INADEQUATE_AFTER_DONOR_CLOSURE`: R6B is verified
  negative, or verified positive and then explicitly treated as absorbed, while
  the exact isolated-TARE3 candidate has no incremental value over the strongest
  incumbent;
- `RESP:SOFTWARE_OR_EVIDENCE_UNRESOLVED`: the R6B hostile/integrity receipt is
  incomplete or invalid.

The canonical lane may recommend only the mechanics supported by that diagnosis:

- donor positive -> `ABSORB` first, then reopen `REFRAME.METHOD.v0` only after
  donor absorption is represented;
- donor negative -> `REFRAME.METHOD.v0` / `REOPEN.FIBRE.v0` directly;
- software/evidence unresolved -> no scientific revision.

`FRAME.DECOMPOSE.v0`, `REOPEN.FIBRE.v0`, `REFRAME.METHOD.v0`, bounded saturation,
and the Self-ORION `DevelopmentFibre` surface must remain discoverable.

## ORION-Q lane

The specialized lane receives the identical frozen evidence but preserves the
quantum-specific responsibility vocabulary.

- verified R6B positive -> `ABSORB_TARE_TRANSFORMATION_REUSE`, zero novelty;
- verified R6B negative -> `GROW_METHOD_LANGUAGE_BEYOND_INDEPENDENT_TARE3`;
- after a positive is absorbed, the current exact-TARE3 candidate remains a
  known no-incremental-value method and the next residual is again method-language
  inadequacy rather than more isolated frame search.

The specialized lane must not reopen the protected subject and must not relabel
known donor reuse as original capability.

## Agreement / disagreement handling

The two lane receipts are preserved independently. A matched conclusion is useful
routing evidence but does not itself authorize a new method. If they disagree,
both feasible routes remain live counterfactuals under matched budgets until a
verification receipt dominates one path.

## Next-method eligibility

A new original method-language protocol may be frozen only when:

1. R6B hostile/integrity verification passes;
2. the R6B scientific sign is preserved rather than coerced;
3. any positive donor reuse is absorbed into the incumbent with zero novelty;
4. canonical ORION and ORION-Q both have explicit residual receipts;
5. the protected stretched-N2 subject remains unread.

If eligible, the next candidate must attack composition beyond the strongest
absorbed donor. It may not merely search more isolated TARE3 frames or weaken the
frame-only/reuse comparator.

## R6 authority

R6 remains closed throughout R6C. A later candidate requires a new frozen
open-subject development protocol, hostile exactness, strongest-donor comparison,
then a separately frozen prospective protocol before any protected stretched-N2
access. Independent replay and final hostile novelty closure remain mandatory.
