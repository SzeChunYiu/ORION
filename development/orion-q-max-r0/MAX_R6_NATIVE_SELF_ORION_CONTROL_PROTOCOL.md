# ORION-Q MAX-R6 native Self-ORION control protocol

Date: 2026-08-20
Parent: #679
Branch: `shadow/orion-q-max-r0`
Status: frozen before native-controller outcome generation.
Authority ceiling: system-attribution experiment only. This protocol cannot by itself authorize R6 or novelty.

## Reopen cause

Prior MAX-R4/R5 work followed ORION research governance but the human/ChatGPT operator still chose many research transitions. That is not sufficient evidence that ORION itself diagnoses a failed research state, selects the next computation/revision class, and drives a prospective method-language edit.

The system boundary is therefore changed. From this packet onward, the research-control decision must be emitted by the existing ORION formal control stack on `main`, not by prose reasoning in the operator.

## Native stack under test

The executable controller must import and use the repository's existing production/formal modules directly:

- `orion.transfer.v2.epistemic_responsibility`;
- `orion.transfer.v2.interface_adequacy`;
- `orion.transfer.v2.higher_order_epistemic_mechanics`;
- `orion.self_orion.revision_gate`;
- `orion.transfer.v2.epistemic_computation`;
- `orion.self_orion.epistemic_control`.

No duplicate local implementation of responsibility, minimal revision selection, computation precedence, or epistemic-control composition is allowed.

## Blinding / contamination rule

The visible state for the first native decision is the frozen R5H H4 failure state only. It must not contain:

- `ADT3`;
- `anchored dependent TARE`;
- the formula `R2=R0R1`;
- a recommendation to optimize the auxiliary frame;
- any R5J result;
- any stretched-N2 coefficient content.

Existing R5J files are considered **operator-proposed contaminated candidates**. They may later be used only as a comparison/tool after ORION independently selects a compatible revision coordinate. They cannot establish ORION-origin R6.

## Frozen visible scientific state

The controller receives these facts, all already earned before this protocol:

1. R5H H4 exact 12-term set-partition search completed.
2. Donor-only direct anticommuting clique frontier contains a balanced point:
   - `Lambda = 4.862157873932594`;
   - block-internal controlled `CNOT = 1428`;
   - block-internal projected `T = 5568`;
   - `blocks = 246`;
   - `ancilla = 0`;
   - reference total T `31028`.
3. The mixed alphabet (singletons + direct cliques + exact controlled m=2 TARE) returns the **same** balanced resource point and partition.
4. The selected mixed balanced point uses zero TARE blocks.
5. Therefore the current mixed alphabet gives no incremental value over the absorbed direct-clique donor on H4.
6. General-m TARE donor closure is unresolved because R5H costed corrected TARE exactly only at m=2.
7. FOQCS-LCU supplies a strong outer-SELECT donor, but matched generic chemistry PREP status remains a donor-closure question.
8. R6/novelty authority remains closed.

## Frozen generic action / revision grammar

The controller may reason over only these generic classes. The labels describe coordinates, not preferred answers.

### Computation actions

`COMPUTE:DONOR_CLOSURE_PACKET`

- discharges hard obligations `DONOR_GENERAL_M_TARE_CLOSURE` and `DONOR_FOQCS_GENERIC_PREP_CLOSURE`;
- may read current evidence/donor registry;
- cannot mutate method language or claim authority.

`COMPUTE:CURRENT_ALPHABET_REPLAY`

- re-runs already-exact R5H evidence;
- does not discharge either donor-closure obligation.

### Revision mechanics

`REV:SEARCH_CURRENT_ALPHABET`

- write footprint: `SEARCH_POLICY` only.

`REV:ABSORB_DONOR`

- write footprint: `DONOR_REGISTRY` only.

`REV:CHANGE_INTERFACE`

- write footprint: `INTERFACE` only.

`REV:GROW_METHOD_LANGUAGE`

- write footprint: `METHOD_LANGUAGE`, `REPRESENTATION`.

No candidate contains an implementation-level quantum construction.

## Frozen responsibility hypotheses

The controller must assess the following claim-relative hypotheses by registered discriminator predictions, not by label priority.

`RESP:CURRENT_SEARCH_INCOMPLETE`

- predicts `R5H_CURRENT_ALPHABET_EXACT = NO`.

`RESP:DONOR_CLOSURE_INCOMPLETE`

- predicts `R5H_CURRENT_ALPHABET_EXACT = YES`;
- predicts at least one of `GENERAL_M_DONOR_CLOSED = NO` or `FOQCS_GENERIC_PREP_CLOSED = NO` before the donor-closure computation.

`RESP:INTERFACE_INADEQUATE`

- predicts `R5H_CURRENT_ALPHABET_EXACT = YES`;
- after closure predicts `GENERAL_M_DONOR_OUTCOME = NONDOMINATING_OR_CLOSED` and `FOQCS_GENERIC_PREP_OUTCOME = GENERIC_MATCHED_ROUTE_AVAILABLE`.

`RESP:METHOD_LANGUAGE_INADEQUATE`

- predicts `R5H_CURRENT_ALPHABET_EXACT = YES`;
- after closure predicts `GENERAL_M_DONOR_OUTCOME = NONDOMINATING_OR_CLOSED` and `FOQCS_GENERIC_PREP_OUTCOME = NO_GENERIC_MATCHED_ROUTE`.

Missing required discriminator outcomes must keep responsibility unresolved.

## Frozen responsibility -> revision bindings

- `RESP:CURRENT_SEARCH_INCOMPLETE` -> `REV:SEARCH_CURRENT_ALPHABET`;
- `RESP:DONOR_CLOSURE_INCOMPLETE` -> `REV:ABSORB_DONOR`;
- `RESP:INTERFACE_INADEQUATE` -> `REV:CHANGE_INTERFACE`;
- `RESP:METHOD_LANGUAGE_INADEQUATE` -> `REV:GROW_METHOD_LANGUAGE`.

## Stage N0 expected control semantics

Before donor-closure evidence exists:

- responsibility must remain unresolved because registered donor discriminators are missing;
- both donor-closure obligations are active and non-compensatory;
- `COMPUTE:DONOR_CLOSURE_PACKET` is the only action that covers the active hard obligations;
- existing Self-ORION control should therefore emit `COMPUTATION_REQUIRED` selecting that action.

This is **not** a success gate chosen after the fact. It is a sanity test that the native controller is actually in charge and withholds P10 revision while donor closure is incomplete.

## Donor-controller shadow arm

A P9/no-P10 shadow arm receives the same visible evidence and the same computation actions, but no revision mechanics. It should also request donor closure at N0. No ORION superiority is claimed for this stage.

## Stage N1 after donor closure

The donor-closure packet is executed only after N0 emits the required action. Its outcomes are then added as new discriminator observations and the same native controller is rerun.

Permitted N1 outcomes:

- donor capability remains live -> ORION must absorb/verify it; no P10 credit;
- interface route is responsible -> native gate may select `REV:CHANGE_INTERFACE`;
- current method language is responsible -> native gate may select `REV:GROW_METHOD_LANGUAGE`;
- responsibility remains plural/unresolved -> controller must compute/abstain rather than operator-invent a method.

## Method-generation rule after N1

Only if native Self-ORION selects `REV:GROW_METHOD_LANGUAGE` may an isolated optimizer expand the quantum representation grammar. The optimizer must be generic over a frozen search grammar and must not receive an operator-authored ADT3 target. An already-written contaminated candidate may be evaluated only after the optimizer has independently generated an equivalent construction.

## Prospective R6 rule

The stretched-N2 DUCC coefficient file locked in the R6 pre-audit remains unread until:

1. native N0 and N1 decisions are frozen;
2. any selected method-language edit is generated by the allowed isolated optimizer;
3. strongest donor composition is bound;
4. R6 discriminator/gates are frozen.

## Failure interpretation

If Self-ORION cannot choose the next computation/revision from this state, that is a **real ORION control failure** rather than a quantum-method failure. Preserve it and improve the missing P9/P10 mechanism instead of substituting ChatGPT reasoning.