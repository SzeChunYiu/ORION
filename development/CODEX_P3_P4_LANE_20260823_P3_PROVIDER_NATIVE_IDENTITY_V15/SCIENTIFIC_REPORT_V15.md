# P3/P4 parallel lane: P3 provider-native identity closure V15

## Blocker selected

P3 V14 established that the completed V12/V13 BERTMap execution used a local
synthetic ontology pair with no lawful provider-native reference identity. Its
shortest successor required one exact provider-native pair with version,
rights, both ontology hashes, a reference hash, and a same-universe comparator
identity **before** another matcher run.

This was selected ahead of P4's seven residual publication--archive--tag--commit
edges because P3 already has 3/3 native execution readiness and a typed output
adapter; a valid input/reference/comparator identity is the last local gate
before an actual scientific observation. P4 still additionally requires
external lineage/natural-pair adjudication, non-GitHub replication, custody,
and comparator executions, so another local identity probe has lower immediate
scientific leverage.

## Exact terminal

`P3_V15_PROVIDER_NATIVE_OAEI_101_103_IDENTITY_PASS__EXACT_VERSION_RIGHTS_ONTOLOGY_REFERENCE_AND_SAME_UNIVERSE_AML_BOUND__REFERENCE_SEMANTICS_UNPARSED_ONE_BERTMAP_SUCCESSOR_AUTHORIZED__SCIENTIFIC_READINESS_ZERO_OF_THREE_UNCHANGED`

## What closed

One live, provider-native OAEI development identity is now exact:

| Field | Frozen value |
|---|---|
| Provider | Zenodo record 15827226, DOI `10.5281/zenodo.15827226` |
| Dataset | `oacontest17`, release date `2004-10-10`, CC-BY-4.0 |
| Archive | 486,043 bytes; MD5 `31676c68912a22622f6ca6d031519df9`; SHA-256 `f22aac206773e4eacdd54cf9519ffe997332a430326bdae29f4210a24efab0b4` |
| Source | `101/onto.rdf`; 69,721 bytes; SHA-256 `d6143780103217a4a562e4982f0d4c724c09d3a3e7dc146a6b42cecc3e9f1064` |
| Target | `103/onto.rdf`; 70,677 bytes; SHA-256 `e3643a6fcd237a162d120e58227df2e24e0398668c6f2428e4fea850180405cf` |
| Reference identity | `103/refalign.rdf`; 29,568 bytes; SHA-256 `0afb0f2c9764201d91c226c5fb60d4b25425168acf6c2604fb191be906e9ec16` |
| Provider-stated stress | Test 103, “Concept test: Language generalisation” |
| Candidate | BERTMap/DeepOnto 0.9.3 at `74ca8d47f01bad0b8739f19ee2c392bdf6d9c090`, with the exact V8/V11/V12/V13 repair lineage |
| Same-universe comparator | AML v3.2 at `d54a6650818d3474fe36090c2bc7dfe5bf4dfcb6`; exact JAR SHA-256 `a5b831a6c000e49aa4702b16486dabdf38e40bb68203a16a8019414fecc2ecf3` |
| Comparator output | Pre-reference, byte-identical replay on exact 101--103 inputs; 46 alignment cells; SHA-256 `0141182140af358cf6bed245d2ed307efc8302551f272d0bf89811d0c0520657` |

The live Zenodo metadata and archive returned HTTP 200. The gate matched exact
provider metadata, MD5, SHA-256, three member paths/hashes, rights conditions,
existing OAEI receipts, BERTMap source/runtime lineage, and the same-input AML
output. The successful invocation passed **70/70** exact checks in
**1.001309083 seconds**.

The downloaded archive was never written to the repository or disk by V15.
The source and target ontologies were XML-parsed. The reference member was read
only as bytes for its exact hash; V15 parsed zero reference cells and computed
zero metrics.

## Primary-source and top-tier relevance

- The provider record describes `oacontest17` as the OAEI 2004 benchmark test
  library and test 103 as a language-generalisation stress derived from the
  bibliographic seed ontology.
- The BERTMap identity is tied to the primary paper
  “BERTMap: A BERT-Based Ontology Alignment System,” DOI
  `10.1609/aaai.v36i5.20510`, and its maintained DeepOnto implementation.
- AML is a source-native, rights-bound ontology-alignment comparator on the
  **same exact input bytes**, rather than a count-only or interface-incompatible
  donor.

This matters because it converts the V14 synthetic-to-provider transport gap
into an executable, falsifiable scientific gate. It does not make one old
single-seed public benchmark a protected or broadly representative study.

## Honest failure preservation

The first runner invocation failed before any result or receipt because it
looked for V13's no-gold field under a nonexistent `outcome_boundary` key.
`ATTEMPT_1_FAILURE.txt` preserves the exact `KeyError`. The correction only
read the already frozen `execution.gold_or_reference_opened` field; no pair,
hash, rights rule, comparator, success gate, or scientific protocol changed.
No matcher or outcome was opened in either invocation.

## Blocker delta and claim boundary

- provider-native pair identity: `CANNOT_CHECK -> BOUND`;
- same-universe comparator identity: `CANNOT_CHECK -> BOUND`;
- separately authorized prospective BERTMap attempts: `0 -> 1`;
- P3 scientific comparator readiness: `0/3 -> 0/3`;
- mapping truth, performance, harm, coverage, transport, superiority, and
  protected confirmation: all remain `CANNOT_CHECK`;
- top-tier submission readiness: unchanged `false`.

All numbered OAEI 2004 tests share one altered bibliographic seed family.
Therefore this pair is one public-development observation, not an independent
replication unit and not evidence for multi-domain transport.

## Next discriminator

Freeze a new execution protocol using this exact identity. Run exactly one
no-feedback BERTMap attempt on `101/onto.rdf -> 103/onto.rdf`; retain raw and
decoded artifacts and complete all structural/typed-decoder gates **before**
semantically parsing `103/refalign.rdf`. Then score BERTMap and the already
frozen AML output under one declared exact-pair metric contract. Any runtime,
identity, decoder, or universe-membership failure remains `CANNOT_CHECK`; no
threshold may be changed after output.

No pytest, repository CI, Git operation, prohibited C4 validator, matcher,
Java, training, prediction, repair, or scientific scoring was run in V15.
