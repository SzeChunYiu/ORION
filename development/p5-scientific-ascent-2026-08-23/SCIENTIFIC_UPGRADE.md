# P5 scientific ascent: minimal method revision under observational equivalence

## Upward move

P5 no longer stops at a governance architecture for a named controller. Its
central object is the **minimal authorized revision front** induced by a latent
failure situation, a partial order of scientific changes, and hard
causal/preservation/transfer/authority constraints. A reviser observes only an
evidence interface. This reframes failure-driven self-improvement as a general
decision problem under observational equivalence.

## Analytic results added

1. **Revision-factorization theorem.** Exact revision selection through an
   interface is possible iff the required revision front is constant on every
   interface fibre.
2. **Arbitrary-loss mixed-fibre bound.** The best possible revision policy has
   the conditional Bayes risk inside each measurable fibre. Zero-one loss
   reduces to decision-fibre impurity when every target label is an available
   action; otherwise the general loss formula governs.
3. **Protected-authority impossibility.** When two internally identical
   transcripts require opposite protected promotion decisions, no internal
   rule is both sound and complete.
4. **Discriminator-cover theorem.** On a finite ambiguous fibre with a finite candidate-test family, a fixed test
   panel permits exact selection iff it distinguishes every pair requiring a
   different revision. The least-cost fixed panel is the induced weighted
   set-cover optimum. Adaptive testing is exact iff every terminal leaf is
   revision-decision-pure.

The factorization and Bayes identities are standard decision-theoretic facts
specialized to typed revision fronts; they are not advertised as new general
statistics. The distinctive composition is the revision target, the
pair-cover intervention criterion, and its protected-authority consequence.

## Strongest nearest work absorbed

- Blackwell comparison of experiments: information is evaluated by the
  decisions it supports.
- Reiter and de Kleer/Williams model-based diagnosis: competing hypotheses,
  conflicts and discriminators.
- Chernoff sequential experimental design and Golovin/Krause adaptive
  submodularity: information acquisition and adaptive test selection.
- ADAS/DGM/ADIAS, SAGE/CausalFlow, failure-driven self-improvement, MOSS,
  Ratchet, Verifier-as-Gatekeeper, PACE/SEA, PAST-Bench and SEVA: proposer,
  issue-state, diagnosis, repair, acceptance, integrity and transfer
  mechanisms.

These mechanisms become donor components. They improve P5 only when their
outputs split a previously mixed **required-revision** fibre. None is displaced
by component renaming, and no component becomes its own promotion authority.

## Every retained error opens a new research problem

The 21/24 diagnostic remains descriptive. Its three errors are immutable and
now seed independent successor studies in
`papers/orion-15-self-orion/protocol/P5_RESIDUAL_DISCRIMINATOR_SUCCESSORS_V1.json`:

| Successor | Retained collision | Decisive experiment | Non-positive terminals retained |
| --- | --- | --- | --- |
| P5-RD-01 | retrieval vs representation | 2x2 retrieval access x representation regime | interaction, joint null, cannot-check |
| P5-RD-02 | environment vs implementation | 2x2 candidate bytes x independently reconstructed environment | interaction, joint null, cannot-check |
| P5-RD-03 | representation vs method basis | 2x2 representation regime x method class | interaction, joint null, cannot-check |

A main effect, interaction, joint null and unresolved pair are different
scientific outcomes. No adverse terminal is converted into a positive claim.

## Wide empirical target

`P5_WIDE_REVISION_LEVEL_SUCCESSOR_V1.json` is the external test of the wider
theory:

- 768 source-disjoint project/issue-family clusters;
- eight domains x eight revision classes x twelve clusters;
- same-visible-symptom blocking;
- six frozen comparator families;
- endpoint: correct minimal revision **and** preservation **and** protected
  fresh-transfer success, with non-compensatory harm gates;
- joint max-T planning target 0.90 under the registered planning model.

The count-only comparator defect is now repaired at the semantic/source-identity
layer. The six roles are SWE-agent, MOSS, Darwin Godel Machine, ADIAS, Double
Ratchet's metric-only arm and ScienceClaw, bound by exact papers, repositories,
revisions, licence bytes and native entrypoints in
`development/p5-six-arm-comparator-panel-2026-08-23/`. Zero of six has a
complete eight-class adapter, matched execution configuration or protected
scorer. The expanded public metadata frame contains ten source identities but
zero with full payload rights, protected eight-class gold and protected
freshness.

This campaign is not executed. It still requires lawful payload/source
snapshots, final split commitment, arm-native execution adapters, matched
model/provider/resource identities, independent fresh-transfer scoring, and
protected verifier custody.

## Terminal-adapter identification result

The outcome-blind V2 adapter successor instantiates the revision-factorization
theorem on the six fixed native interfaces. Before any native output example,
it froze one same-visible-symptom counterexample per arm. Patch success, replay
improvement, archive-best selection, a compiling issue patch, DRAWBACK and an
open need were each compatible with at least two minimal revision classes.
Therefore zero raw native symptoms license a singleton P5 class.

A singleton proposed-action type is conditionally lawful only after the
observation is refined by one host-validated input-native class certificate and
one complete exclusive-front write set that agree, with every other front
invariant and each arm's custody rules satisfied. Conditional support counts
are seven for SWE-agent, four each for MOSS, DGM and ADIAS, one
(evaluator repair only) for Double Ratchet metric-only, and zero for
ScienceClaw. A 90-case synthetic conformance run passed all 90 cases; 70
remained UNRESOLVED and 20 exercised certified action-type projections. These
counts are interface checks, not independent units or performance evidence.

The complete protocol, source-interface ledgers, resource/configuration audit,
fibre theorem, fixtures, receipt and hashes are retained under
development/p5-six-arm-terminal-adapters-v2-2026-08-23/. Zero of six arms is
confirmatory-ready, and H1--H4, protected freshness, performance and
superiority remain CANNOT_CHECK.

The stricter V3 successor preserves that obstruction while making the refined
contract total on exactly 231 declared synthetic cases. Forty case records in
20 supported constant fibres emit a typed proposed action, 191 remain
UNRESOLVED, raw-native singleton licences remain zero, and no conformance or
native-terminal-retention failure occurs. Exact outcome-free execution
preflight identifies 108 blockers, 18 per arm, and 0/6 ready. The archive is
development/p5-six-arm-adapter-refinement-v3-2026-08-23/. It contains no
comparator, native-output example, scientific unit, performance table or
protected outcome; therefore the same H1--H4 and superiority boundary remains.

The outcome-blind C1 SWE-agent V4 then repairs six execution-interface fields
without opening a model or outcome: native trajectory parsing, isolated write
surface, closed fallback policy, wallclock, compute and a 152-package lock.
With three prior identity fields, C1 is 9/21 bound and 12/21 blocking. At this
C1-only snapshot, the five other arms retained 18 blockers each, so the merged
panel had 102 blocker instances and 0/6 ready. The parser preserves exact native status, refuses
protected keys, hashes patch bytes and emits only `UNRESOLVED` at the revision
layer; the isolation wrapper refuses execution. The exact packet is
development/p5-swe-agent-execution-binding-v4-2026-08-23/, terminal
`P5_C1_V4_SWE_AGENT_NATIVE_PARSER_AND_ISOLATION_POLICY_BOUND__TWELVE_C1_FIELDS_BLOCKING__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The separate outcome-blind C2 MOSS V4 binds four additional fields beyond its
three prior source identities: a schema-v6 native parser, closed fallback set,
21,600-second wallclock watchdog, and exact host/OpenClaw locks. C2 is 7/21
bound and 14/21 blocking. The authoritative paper-linked commit/tree/archive
are pinned, but the complete tree contains zero `benchmark/` paths although
the native trial runner requires `benchmark/claw-eval/runner/benchmark.py`.
The exact CLI help and no-gateway error surfaces are retained; full native
smoke is `CANNOT_CHECK`. With C1 at 12 blockers, C2 at 14 and four other arms
at 18, the merged panel has 98 blocker instances and 0/6 ready. Exact packet:
development/p5-moss-execution-binding-v4-2026-08-23/, terminal
`P5_C2_V4_MOSS_NATIVE_PARSER_DEPENDENCY_LOCKS_FALLBACK_AND_WALLCLOCK_BOUND__FOURTEEN_C2_FIELDS_BLOCKING__RELEASED_BENCHMARK_RUNTIME_ABSENT__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The distinct outcome-blind C3 DGM V4 binds three fields beyond the source
identities: a closed patch-capture parser, an empty fallback set and a
21,600-second outer process-group watchdog. C3 is 6/21 bound and 15/21
blocking. Its 23 Python dependency declarations contain zero exact pins and no
lockfile, so their hashes are not misreported as a lock. The parser preserves
native status and patch identity but emits only `UNRESOLVED` at the revision
layer. The argparse choice concatenation, ineffective future timeout, mutable
base/dependencies and untrusted-code warning remain source defects. No released
outcome payload was opened and no DGM, model, task, benchmark, Docker or
protected outcome was run. With C1 at 12, C2 at 14, C3 at 15 and C4--C6 at 18,
the merged panel has 95 blockers and 0/6 ready. Exact packet:
development/p5-dgm-execution-binding-v4-2026-08-23/, terminal
`P5_C3_V4_DGM_SOURCE_NATIVE_PARSER_EMPTY_FALLBACK_AND_OUTER_WALLCLOCK_BOUND__UNPINNED_DEPENDENCIES_NOT_MISLABELED_AS_LOCK__FIFTEEN_C3_FIELDS_BLOCKING__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The distinct outcome-blind C4 ADIAS V4 binds three fields beyond the source
identities: a hashed generation-report parser, empty fallback/search policy and
a 21,600-second foreground process-group watchdog. C4 is 6/21 bound and 15/21
blocking. Its 1,578-file tree contains bundled task data but no component
licence/NOTICE files under `data/`; four VCS dependencies are unpinned, the base
image is mutable and native containers use host networking with provider keys.
The parser distinguishes native empty/partial/error/artifact states but emits
only `UNRESOLVED` at the revision layer. No ADIAS model, task, benchmark or
protected outcome was run. With C1 at 12, C2 at 14, C3/C4 at 15 and C5--C6 at
18, the merged panel has 92 blockers and 0/6 ready. Exact packet:
development/p5-adias-execution-binding-v4-2026-08-23/, terminal
`P5_C4_V4_ADIAS_SOURCE_TREE_NATIVE_PARSER_EMPTY_FALLBACK_AND_WALLCLOCK_BOUND__FIFTEEN_C4_FIELDS_BLOCKING__DEPENDENCY_LOCK_TASK_RIGHTS_ISOLATION_AND_CUSTODY_UNBOUND__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The distinct outcome-blind C5 Double Ratchet metric-only V4 binds six fields
beyond the source identities: a guarded parser, results-only isolated write
surface, empty fallback policy, wallclock/compute envelopes and a 46-entry uv
lock. C5 is 9/21 bound and 12/21 blocking. The official loop regenerates hosted
solver outputs and evaluates, persists and prints `eval_locked` each round; no
frozen solver-output tree, P5-native task adapter or external one-shot panel is
bound. Its six native-shaped parser fixtures contain no substantive case and
license no raw native singleton. No model, benchmark, P5 task or protected
outcome was run. With C1/C5 at 12, C2 at 14, C3/C4 at 15 and C6 at 18, the
merged panel has 86 blockers and 0/6 ready. Exact packet:
development/p5-double-ratchet-execution-binding-v4-2026-08-23/, terminal
`P5_C5_V4_DOUBLE_RATCHET_SOURCE_PARSER_ISOLATION_FALLBACK_WALLCLOCK_COMPUTE_AND_DEPENDENCY_LOCK_BOUND__TWELVE_C5_FIELDS_BLOCKING__OFFICIAL_RUNNER_REGENERATES_SOLVER_OUTPUTS_AND_REPORTS_DEVELOPMENT_LOCKED_EACH_ROUND__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The distinct outcome-blind C6 ScienceClaw V4 binds two fields beyond the source
identities: a strict dry-run-draft shape parser and an outer process-group
wallclock. C6 is 5/21 bound and 16/21 blocking. The released CLI has no native
revision selector; nominal dry-run executes the investigation first, and the
model/reasoning/skill/dependency fallbacks cannot all be closed. Thirteen
prior-result-prefix files were hashed and censused without decoding and remain
excluded from future candidate seeds. No investigation, model, service,
benchmark, prior outcome or protected outcome was run. With C1/C5 at 12, C2 at
14, C3/C4 at 15 and C6 at 16, the merged panel has 84 blockers and 0/6 ready.
Exact packet: development/p5-scienceclaw-execution-binding-v4-2026-08-23/,
terminal
`P5_C6_V4_SCIENCECLAW_SOURCE_TREE_DRAFT_PARSER_AND_OUTER_WALLCLOCK_BOUND__NATIVE_SELECTOR_UNSUPPORTED_AND_FALLBACKS_OPEN__SIXTEEN_C6_FIELDS_BLOCKING__PRIOR_OUTCOME_PREFIXES_HASHED_NOT_DECODED__ZERO_OF_SIX_PANEL_READY__PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`.

The panel-wide V5 efficiency successor then recomputes the six 21-field
registries as 42/126 bound and 84/126 blocking, with 0/6 ready. It partitions
the 84 blockers into 14 exact equivalence classes and five evidence-producing
root packages: 25 model/service/metering/egress instances, 18 independent
custody instances, 18 common-case/content-rights/native-task instances, 15
runtime/dependency/artifact-rights instances and eight outer
isolation/compute instances. This compresses the operational queue from 84
entries to five packages (delta -79) but binds zero new arm fields and removes
zero scientific blockers. The strongest single-artifact discriminator is a
genuine signed external six-arm custody/scorer/freshness/no-feedback bundle,
which targets 18 fields; the strongest directly authorable discriminator is a
substantive rights-cleared visible-case packet, which targets 12 before six
native environment receipts.

The panel matched-exposure gate remains blocking even though all six
wallclock fields are individually bound: C1 freezes 3,600 seconds with 30
seconds of grace, C2--C6 freeze 21,600 seconds with 120 seconds of grace, and
C5 adds a 10-second candidate-subprocess cap. Fresh assurance also retains a
C4 temporary-path-dependent audit receipt that breaks its frozen checksum and
a C6 native validator that is `CANNOT_CHECK` after `.source-audit` cleanup.
Exact packet:
development/p5-panelwide-root-closure-v5-2026-08-23/. Its 74-check validator
and own `SHA256SUMS` pass, but no arm, model, benchmark, protected scorer or
protected outcome was executed; H1--H4, performance and superiority remain
`CANNOT_CHECK`.

The P5 V6 common-case successor then supplies the directly authorable evidence
that V5 identified. One substantive arm-neutral Defects4J Lang-1/Apache
Commons Lang case core freezes 302 source files (5,122,012 uncompressed bytes),
authored task bytes, Apache-2.0 licence/NOTICE and a CC0-1.0 authored-content
grant. Six exact acceptance receipts bind identical candidate-visible bytes
and task/benchmark-content rights to C1--C6 while keeping the known fixed
commit/patch outside candidate visibility. This closes 12 shared fields: the
panel moves to 54/126 bound, 72/126 blocking and 0/6 ready; root R2 falls from
18 blockers to six, all arm-native `runtime.task_environment` fields.

The V7 byte-level fan-out closes one of those six fields with actual C1 offline
setup bytes and a fixed effective configuration. The current panel is therefore
55/126 bound, 71/126 blocking and 0/6 ready; root R2 is five distinct
arm-specific environment bundles. This pass executes no arm, model, benchmark,
protected scorer or outcome and supplies no performance authority.

The C3 V8 successor then attacks only the highest-ranked remaining environment.
It constructs a 69-member candidate-safe seed from 55 filtered DGM source
members, six unchanged Lang-1 core members and eight control files, while
excluding 1,595 prior-outcome files and 49,707,333 blob bytes. Eight
materialization/policy gates pass, but the unchanged native initializer requires
`initial/` plus prior `overall_performance` fields. The C3
`runtime.task_environment` instance therefore remains blocking. Exact packet:
development/p5-c3-native-environment-v8-2026-08-23/.

V9 freezes an outcome-free initialization adapter before one static exact-byte
and AST dataflow microgate. At released DGM commit
`a565fd2d1dca504ef5104a7cc0f3bdc4ab9b4fd2`, every nondegenerate self-edit
route requires an eligible parent, and the sole initial parent requires prior
accuracy plus resolved/unresolved/empty-patch partitions. Restoring those bytes,
imputing scores, removing the self-edit action or selecting without scores is
not a semantics-preserving adapter. V9 stops before materialization, closes zero
fields and leaves the panel at 55/126 bound, 71/126 blocking and 0/6 ready.
This is an exact-source interface impossibility witness, not a claim about
future DGM designs. Exact packet:
development/p5-c3-outcome-free-successor-v9-2026-08-23/.

The result is public development only because the known fix and predecessor
outcomes predate selection. Any native content outside the frozen common core
reopens rights review. Packet-local validation passes 741/741 checks and all 22
checksum entries without running an arm, model, benchmark, protected scorer or
outcome. Exact packet:
development/p5-common-visible-case-rights-v6-2026-08-23/. H1--H4, performance
and superiority remain `CANNOT_CHECK`.

## Publication boundary

The theory is general across finite decision alphabets and arbitrary
well-founded revision partial orders. The original cover theorem is finite and
deterministic. Stochastic fixed-policy exactness and finite known-kernel
controlled-state policy optimization are now characterized; unknown-kernel
finite-sample power, kernel misspecification, unrestricted infinite horizon,
and naturalistic protected transfer remain open. The existing local hostile
tests establish implementation semantics only. Transferable governed method
improvement remains `CANNOT_CHECK` until the protected campaign runs.
