# P4 protected-closure development packet — 2026-08-16

Task: issue #101 / execution dependency #59. Impact: HIGH (publication-authorizing evaluation infrastructure).

## Atomic development questions

1. Can the final protected P4 battery be generated after the already-frozen method without exposing per-case attack family or gold labels to the candidate?
2. Can the candidate arm exercise production ORION non-compensatory gate machinery rather than the existing simplified `campaign_runner.py` reference?
3. Can the six frozen baseline families be implemented as candidate-visible deterministic mechanism baselines without reading protected gold?
4. Can protected evaluation, access telemetry, exact content/provenance snapshots, raw verdicts, false-positive/false-negative retention and paired statistics be reproduced from immutable artifacts?
5. Can an independent second runner recompute the headline result without trusting the first evaluator implementation?
6. If the first frozen run exposes a subject defect, can the failure be preserved and any repaired subject be evaluated only on a fresh hidden split under a new freeze?

## Bounded saturation assessment

Knowledge boundary: the current P4 protocol, custody policy, attack schema, statistical plan, #59 execution contract, production kernel authority/evidence/hard-gate code, and the 2026-08-16 nearest-work audit have been reviewed. Search-universe boundary: repository implementation plus public nearest-work already frozen by the P4 audit. Formulation boundary: no new primary metric, attack family, or novelty claim is introduced.

The implementation hypothesis is bounded closed for an execution attempt because every remaining repository-solvable obligation maps to one of: hidden-case generation, candidate isolation, production gate execution, frozen-family baselines, protected scoring/statistics, artifact retention, reproduction, manuscript/results generation, or final packaging. External institutional replication remains a limitation rather than a hidden substitute.

## Saturation-basis challenge

Saturation could be false if the existing campaign runner were mistaken for the production subject, if a baseline read `protected_gold`, if a hidden seed were reconstructable from public run metadata, if the candidate inherited a token/path that could fetch protected artifacts, if the evaluator and reproducer shared one process lineage, or if publication figures were populated from stubs rather than immutable raw records.

Potentially missing parent domains: confidential benchmark custody, CI job-isolation boundaries, reproducible-build attestations, and secure evaluation artifact retention.

Prior-miss hypotheses: earlier readiness work conflated design completeness with execution evidence; the existing baseline/campaign runners are explicitly stubs/reference implementations; and the final hidden split had not existed, so no amount of repository unit testing could supply the external result.

## Reopen triggers

- any candidate process receives attack family, gold labels, protected manifest, artifact-download credentials, or evaluator write authority;
- any baseline implementation reads protected gold;
- any output is inspected before the execution-freeze commit;
- the production ORION arm does not execute `orion.kernel.hard_gates.evaluate_hard_gates` from the frozen subject revision;
- the first protected run fails or reveals an implementation defect (preserve the run; repair only under a new subject revision and fresh hidden split);
- result statistics cannot be independently reproduced byte-for-byte from retained raw artifacts.

## Frozen implementation hypothesis

A GitHub-hosted preparation runner will generate a secret-seeded 420-case mechanical-gold battery (60 clean positives plus 30 cases for each of 12 hostile families) after method freeze. Only an opaque candidate-visible manifest and hashes leave host custody before execution. The frozen subject revision is `46977ea104162c4cf64da8138a4c4759065fe6d4`. The ORION arm will translate candidate-visible observations into production non-compensatory hard-gate observations and obtain its terminal from production `evaluate_hard_gates`; no protected gold is available in that job. Frozen-family baselines will use the same candidate-visible packet and no gold. A later protected evaluator job will join outputs to gold, retain all cases and telemetry, compute the registered metrics/statistics, and emit a content-addressed panel. A distinct reproduction job will independently recompute the headline counts/statistics. No outcome-dependent design edits are permitted.
