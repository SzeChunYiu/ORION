# P1 R7A public-naturalistic execution preflight

**Date:** 2026-08-23  
**Successor identity:** `P1.R7A.PUBLIC.NATURALISTIC.PREFLIGHT.V1`  
**Observed repository head:** `1b27b32b7d1dda3793d8247689f81e6c2f78d83d` plus the shared uncommitted working-tree state  
**Authority:** outcome-blind source-rights, eligibility, comparator-interface, and execution-custody preflight only  
**Current terminal:** `P1_R7A_CANNOT_CHECK_EXTERNAL_BINDINGS`  
**Execution authorized:** **no**

This packet does not edit the manuscript, alter the R7/R7A power result, execute
a candidate or comparator, open protected case fields, create external custody,
or grant scientific authority. It is not legal advice or external peer review.

## Scientific reason for this lane

P1 is not blocked by another local mechanical test. It already has exact
mechanism evidence, including the powered mechanical result and the
comparator-fair 400-contract battery. The remaining top-tier empirical question
is whether typed scientific-transition licensing is useful on noisy natural
evidence after strong donor mechanisms receive the same information and actions.
The current manuscript states that its formal generality does not establish how
often mixed decision fibres occur in natural work or whether a language model can
identify the required order and hard gates
(`papers/orion-11-recursive-epistemic-reconstruction/manuscript/sections/07-limitations.tex:25-35`).

The current evidence also fixes the exact blockers:

- the provider-diverse metadata census has 16 candidates but zero assessed cases,
  unresolved content rights for all 16, and zero downstream quota credit
  (`development/provider-diverse-metadata-census-2026-08-23/DEVELOPMENT_REPORT.md:176-212`);
- the 12-repository preflight binds repository revisions and licence bytes but is
  GitHub/software-only and never opened issue/case content
  (`development/naturalistic-panel-harness-2026-08-23/DEVELOPMENT_PACKET.md:24-52`);
- Agent Laboratory and AI Scientist-v2 have exact source identities, but neither
  consumes the R7A dossier/action contract or emits a target-pure responsibility
  terminal under matched resources
  (`development/comparator-binding-harness-2026-08-23/SCIENTIFIC_REPORT.md:62-66`);
- same-repository construction, scoring, and review do not create independent
  source selection, gold, execution, or result-verification custody
  (`development/p1-p2-readiness-audit-2026-08-23/READINESS_VERDICT.md:89-108`).

### Pre-outcome estimand amendment

`R7A_INTERFACE_DEFICIENCY_ESTIMAND_AMENDMENT_V1.json` makes the wider theory
operational before any case or system outcome is opened. The primary target is
now the source-cluster-weighted legal-action deficiency and decision-mixed
fibre prevalence of every native candidate-visible interface, stratified by
domain, provider and modality. Candidate-versus-comparator excess loss remains
a secondary contrast. A native system whose outputs do not identify one legal
R7A action on a fibre is `CANNOT_CHECK` on that fibre; adding responsibility
logic creates a new proxy arm rather than a victory over the named system.
This reframing preserves the 896-cluster design, all nine comparator slots, the
14 external bindings, every predecessor terminal and execution authorization
`false`.

## Preserved predecessor outcomes

No adverse, null, equivalence, or `CANNOT_CHECK` identity is overwritten:

1. P1.H1 V1 remains `NOT_SUPPORTED`: candidate and registered baseline each
   achieved 1/48 root-task successes.
2. R2--R4 acquisition failures remain `CANNOT_CHECK` acquisition records, not
   empirical zeros.
3. R6 remains conditional: its candidate received unmatched evidence, its
   deterministic host was corpus-coupled, and its comparator cost/stopping
   envelope was not fair.
4. The mechanical and exact-contract positives remain exact/mechanical evidence,
   not naturalistic transition superiority.
5. The ideal information-equivalent donor product's 400/400 tie remains the
   negative expressivity boundary. R7A must test real mixed-fibre prevalence and
   transition value; it may not promise to defeat a semantic copy of itself.

## Strongest lawful source state now bound

`PUBLIC_SOURCE_RIGHTS_LEDGER_V1.json` derives a non-result-bearing ledger from
the exact existing receipts:

| source tranche | exact roots | provider/modality breadth | case-content rights | eligibility | R7A quota credit |
|---|---:|---|---:|---:|---:|
| provider-diverse census | 16 | four providers and four modalities per candidate wave | 0/16 bound | 0/16 assessed | 0 |
| GitHub seed census | 12 | one provider and software repositories only | exact repository-licence bytes, but issue/comment/attachment scope unverified | 0/12 assessed | 0 |
| **combined** | **28** | five metadata-provider classes and five candidate modalities | **0 admissible case roots** | **0 eligible cases** | **0/896** |

The ledger names every exact provider identity, public URL, pre-cutoff revision,
record digest, content-class rights state, and fail-closed legal gate. A public URL
is not permission. Metadata, article bodies, supplements, dataset files,
repository code, issues, comments, pull requests, and attachments are separate
content classes. Each class must have a bound licence or written permission for
the intended storage, machine processing, derived annotation, audit quotation,
and release tier before its bytes are opened.

### Rights-first acquisition order

1. Bind an identified rights owner and distinct source-selection/eligibility
   custodians for both waves.
2. Enumerate provider metadata only; bind immutable record/revision identities and
   canonical family-deduplication roots.
3. Have the rights owner adjudicate every intended content class before content
   access. Ambiguity is `CANNOT_CHECK_CONTENT_CLASS_RIGHTS`.
4. Have the eligibility custodian apply the frozen scientific criteria and return
   sealed counts/exclusions before case text reaches candidate authors.
5. Retain zero-eligible cells as source-universe negatives. Do not replace them
   after seeing system outcomes or delete the cell.
6. Only after complete rights and eligibility may owner-separated constructors
   produce source-grounded dossiers and protected labels.

### Outcome-blind eligibility

Every paired cluster must provide adverse and control members from the same
study/artifact family, sharing objective, method, artifact family, and scientific
context as closely as the source permits. One source-grounded factor must change
the licensed transition responsibility. Constructors may not use candidate logic,
model outputs, filename/template cues, provider identity, ordering, action
availability, or private author intent to create the label. All six registered
probe dispositions must be independently codable as `SUPPORT`, `REFUTE`, or
`INCONCLUSIVE`. A genuinely non-identifying source is admitted to the separate
`UNRESOLVED` frame; it is not forced into the nearest causal family.

The active R7A shape is preserved: 8 causal families x 4 domains x 12 source-pair
clusters = 384 paired clusters per wave, plus 64 unresolved clusters, repeated on
one completely source-disjoint wave. The total is 896 independent source
clusters. Pair members, retries, model samples, probes, rows, and bootstrap draws
do not increase `n`.

This is a large-effect design. The frozen max-T receipt projects joint power
0.920216 at delta 0.20 and discordance 0.40 for 384 paired clusters. At delta
0.15 and discordance 0.40, projected power at 384 is only 0.06778 and the
simulated minimum is 896 paired clusters **per wave**
(`research/claim_expansion/p1/gpt_r7a/R7A_MAXT_POWER_RECEIPT_V1.json:20-29`).
No positive outcome is assured.

## Comparator information/action matching

`COMPARATOR_INTERFACE_CONTRACT_V1.json` freezes one common, byte-canonical
candidate-visible payload; six opaque probes with a two-probe budget; seven
terminal actions; forbidden evaluator fields; full transcript retention; and a
matched model/provider/token/retrieval/tool/probe/time/cost envelope.

It also names all nine primary comparator slots. The result is deliberately
adverse: **0/9 are presently R7A-ready**, and the R7A candidate adapter is also
unbound.

- Four slots have relevant exact/legacy code bytes but no R7A adapter.
- Four slots lack an exact R7A implementation/wrapper.
- The external slot names Agent Laboratory at MIT commit
  `d9017d90e329112d2a80b7712f37ee9094d2cd27`, but its topic-to-artifact
  interface is not a P1 responsibility interface.
- AI Scientist-v2 at
  `96bd51617cfdbb494a9fc283af00fe090edfae48` remains a separate
  restricted-licence pressure test, not a primary arm.

An external adapter is lawful only when the target R7A decision is constant on
each candidate-visible native-output/interface fibre. A mixed fibre means
`INTERFACE_LIMITED__CANNOT_CHECK_AS_R7A_PRIMARY_ARM`. A prompt wrapper that adds
responsibility logic or forces a non-native decision is a new common-interface
proxy, not the named external system. Installation success, a workflow trace, or
a prose claim of equivalence cannot repair this boundary.

## External binding and custody receipt

`EXTERNAL_BINDINGS_TEMPLATE_V1.json` requires 14 independently verifiable
bindings, including:

- content-class rights owner;
- separate primary and replication source-selector/eligibility custodians;
- owner-separated case construction and gold adjudication;
- candidate and all nine comparator adapters;
- Agent Laboratory terminal-preserving adapter;
- primary and changed replication semantic host/model/provider revisions;
- independent primary and replication evaluator/scorer custody;
- external result verifier custody; and
- primary and replication protected execution environments.

A complete row needs an owner/system identity, role and write boundary, immutable
artifact URI, SHA-256, signed or independently verifiable attestation, and proof
that it was bound before case/outcome access. This repository supplies none of
those external attestations, so all 14 rows correctly remain `MISSING`.

## Executable preflight and receipt

The preflight is executable but fail-closed:

```bash
rtk python development/p1-public-naturalistic-successor-2026-08-23/build_assets.py
rtk python development/p1-public-naturalistic-successor-2026-08-23/validate_preflight.py \
  --output development/p1-public-naturalistic-successor-2026-08-23/PREFLIGHT_RECEIPT_V1.json
```

The validator checks every bound upstream SHA-256, all 28 source records, the
nine-arm comparator registry, candidate binding, rights/eligibility/source-frame
counts, and all external binding rows. The present expected exit is `3`, not
success. `PREFLIGHT_RECEIPT_V1.json` records:

- 0 integrity errors;
- 28 candidate metadata roots;
- 0 rights-bound, 0 eligibility-assessed, and 0 admissible R7A source clusters;
- 9 required comparator rows, none R7A-ready;
- 14 external binding rows, none bound; and
- terminal `P1_R7A_CANNOT_CHECK_EXTERNAL_BINDINGS`.

No pytest, repository CI, manuscript build, or scientific outcome execution was
run for this packet.

## Earliest lawful next action

Do **not** run systems. First obtain two owner-separated deliverables:

1. a content-class rights/custody receipt for the exact 28 seed roots plus the
   provider routes needed to populate every R7A cell; and
2. an outcome-blind eligibility census reporting all ranks, exclusions, exact
   duplicate roots, rights failures, and per-cell attainable counts without
   revealing protected case content or decisions to candidate authors.

In parallel, a separate comparator owner may implement the common-interface
conformance fixtures and determine whether Agent Laboratory has a target-pure
native adapter. If it does not, that is a scientific interface result and the
external slot remains `CANNOT_CHECK`; it is not permission to weaken the
comparator family.
