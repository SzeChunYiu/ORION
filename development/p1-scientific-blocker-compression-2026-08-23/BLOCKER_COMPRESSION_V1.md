# P1 scientific blocker compression V1

**Date:** 2026-08-23  
**Authority:** same-repository scientific synthesis and prospective successor
binding only  
**Case/outcome access:** none  
**Execution authorized:** no

## Verdict

After separating a manuscript-alignment prerequisite from missing science, P1's
current top-tier gap compresses to **two scientifically independent,
high-fan-out roots**:

1. **owner-ratified target algebra and construct validity**; and
2. **naturalistic source-family transport under protected custody**.

This is the smallest scientifically adequate set. A lawful target algebra can
be completed without observing a single naturalistic case. Conversely, a large
rights-cleared corpus can exist while the target action remains semantically
unowned and cannot serve as scientific-action gold. Neither root implies the
other; the title-level empirical claim requires both.

The exact terminal is:

`P1_TWO_HIGH_FAN_OUT_SCIENTIFIC_ROOTS_IDENTIFIED__OWNER_ALGEBRA_AND_NATURALISTIC_CUSTODY_UNRESOLVED`

## Not a third scientific root: dominant-question alignment

P1's earlier manuscript diluted its science-specific residual with generic
factorization, POMDP and representation results. That was an editorial and
claim-architecture defect, not an additional missing observation.

The **current shared worktree now integrates** the repair: the title, abstract,
foundations, limitations, conclusion and claim ledger centre typed
responsibility-to-authority licensing, make generic mathematics
supporting/donor-derived, publish an authority ladder and preserve the adverse
predecessor. `MANUSCRIPT_ALIGNMENT_CHECK_V1.json` binds the observed files and
markers.

The earlier patch at
`../lane-handoffs/p1-naturalistic-transport/P1_DOMINANT_CLAIM_MANUSCRIPT.patch`
is now historical integration provenance, not an applicable patch. A batch
forward dry-run exits 1: most hunks are already applied/reversed and remaining
contexts drifted after concurrent source-feasibility integration. **Do not apply
it wholesale.** The current text has not been built or externally reviewed and
creates no new scientific evidence.

## Root 1: owner-ratified target algebra and construct validity

### Exact evidence

V8 binds seven public target semantic sources and proves that diagnostic probes
are not terminal decisions. It then exhaustively retains the frozen map audit:

| map disposition | count |
|---|---:|
| rejected | 116,929 |
| not disproved but uncertified | 720 |
| certified | 0 |
| total | 117,649 |

V4 adds eight official documents from four institutional families. Nine of
twelve owner-algebra requirement groups have structural analogues, but **0/12**
has named-custodian ownership/delegation and **0/12** is sufficient. Scientific
action gold remains **0 cells**. The public standards solve vocabulary
scaffolding, not R7 authority.

Upstream evidence:

- `development/p1-source-native-target-semantics-v8-2026-08-23/P1_RESULT_V8.json`
  (`47d1d6b38c96b7046500188c84b3b3cdb6afb1260a9acf87e4731dadd2ed76ed`);
- `development/p1-owner-algebra-construct-validity-v4-2026-08-23/RESULT_V4.json`
  (`cb09da0c5299b7f34837d8bef63cc4bd1348e38e202fb85840549fdcdfd91f12`);
- V4 validation status `PASS`, with 0 scientific-action gold cells, 0 sufficient
  owner groups, nine structural analogues and 720 `CANNOT_CHECK` maps.

### High fan-out

This one root blocks all of the following downstream claims:

- source-native terminal meaning;
- protected scientific-action gold;
- legal-action deficiency interpretation;
- anti-leak label construction;
- responsibility-to-authority ablation denotation;
- native external-adapter target purity; and
- terminal-preserving comparator comparison.

More public taxonomy search cannot close it. The missing fact is owner authority,
not terminology.

### Bound successor

`OWNER_ALGEBRA_V9_COMPLETION_CONTRACT.json` binds the already registered
`P1_R7_LICENSED_OWNER_RATIFIED_TARGET_ALGEBRA_AND_INDEPENDENT_REVIEW_V9`.
It requires four non-substitutable deliverables:

1. a closed-world seven-target algebra from the R7 vocabulary owner or formal
   delegate;
2. signed adoption/execution/error boundaries from the host authority owner;
3. an exact rights grant from the target-corpus rights holder; and
4. a content-addressed review by an independent semantic reviewer who did not
   author the algebra.

Only then may the unchanged 117,649-map audit rerun. All four bindings are
currently absent, so the V9 contract remains `CANNOT_CHECK`; it does not pretend
that this repository can self-authorize the missing semantics.

## Root 2: naturalistic source-family transport and protected custody

### Newly resolved source fact

Crossref's official metadata-licensing table explicitly lists the Retraction
Watch database as **CC0**. The immutable development backbone is:

- repository `crossref/retraction-watch-data`;
- commit `7bb2ced143b764974c53c6c61abfdd2379f5307d`;
- CSV blob `40a049f02044fab8286c0304fd296bf1fa2cb8ca`;
- CSV SHA-256
  `ceaab201d728dfcf9929ec1e229acd2ad88c650c847ec922ba9ffe831e366abb`;
- 65,984,968 bytes; and
- 71,944 rows counted without displaying or retaining fields.

This resolves Retraction Watch **metadata-field rights**, not linked article,
abstract, notice, attachment, supplement or blog-post rights. It does not turn
post-event reason codes into causal-responsibility gold.

A later prospectively frozen reducer materially advances the metadata bridge:

- 49,878 structurally admitted Retraction Watch relations;
- 29,768 relations with PMIDs on both endpoints;
- **12,038 exact both-endpoint rights-pass relations in 11,602 source
  families**;
- 17,730 allowlist-search absences retained as `CANNOT_CHECK` rather than
  negatives; and
- endpoint licence incidences of 50 CC0, 25,534 CC BY and 4 CC BY-SA.

The structured-action successor classifies 11,976 of 11,991 unique notice
records into exactly one provider-native stratum: 11,251 retraction, 256
correction/erratum and 469 expression-of-concern records. Fifteen remain
`OTHER_OR_CANNOT_CHECK` and zero are ambiguous. This is a rights-valid
**metadata relation pool**, not 12,038 eligible dossiers or action-gold items.

### Candidate replication universe

Five prospectively frozen Europe PMC count-only queries returned:

| OA/PMC publication type | hits |
|---|---:|
| Published Erratum | 93,796 |
| Correction | 105,342 |
| Retraction of Publication | 17,360 |
| Retracted Publication | 18,056 |
| Expression of Concern | 1,516 |

No result row was inspected or persisted. The notice-class sum 218,014 is not
deduplicated and is not a pair or case count.

### What remains zero

The existing R7A preflight still records:

- 12,038 rights-valid metadata relations in 11,602 families, but 0 eligible
  R7A case dossiers;
- 0 complete source clusters;
- 0 scientific-action gold cells and 0 case-level construct-validity decisions;
- 0/9 ready primary comparator arms;
- 0/14 external custody bindings; and
- execution authorization `false`.

The registered target remains 384 paired plus 64 unresolved source clusters in
each of two source-disjoint waves, 896 independent clusters total. Repeated
rows, notice reasons, samples, probes and bootstrap draws never increase `n`.

### High fan-out

This root simultaneously blocks:

- natural mixed-fibre prevalence;
- noisy responsibility inference;
- exact-contract circularity repair;
- template, filename and blind-responder leakage repair;
- provider/modality and worst-domain transport;
- licensing-relation incremental value;
- changed-host replication; and
- owner-independent empirical authority.

### Bound bridge

`RW_CC0_NATURALISTIC_BRIDGE_V1.json` does not create a new claim identity. It
connects the CC0 development source and Europe PMC/PMC replication candidate to
the existing successors:

- `ORION.SOURCE.UNIVERSE.ELIGIBILITY.V1`;
- `P1.NATURALISTIC.TRANSITION.TRANSPORT.V1`;
- `P1.LICENSING.RELATION.NECESSITY.V1`; and
- `P1.R7.EXTERNAL.ADAPTER.V1`.

The independent unit is a connected scientific-update family, never a CSV row.
Update/reason/notes/identifier fields stay evaluator-side. Any promoted action
cell requires at least 20 independent families. Titles, filenames, paths, URL
slugs, headings, publication types, relation types and explicit action tokens
are removed before scoring. Blind phrase/path/provider/publication-type
responders, leave-one-family/provider audits, and the historical 33/66 shortcut
remain noncompensatory gates.

The Retraction Watch wave has public-label development authority only. A
protected Europe PMC/PMC wave must be rights-cleared, relation-explicit,
family-disjoint and owner-separated. A later Retraction Watch temporal wave is
not owner-disjoint cross-provider replication and must be reported separately.

## Dependency compression

The correct execution order is:

1. integrate the dominant-question patch from a drift-checked clean lane;
2. complete the V9 owner algebra—do not replace owners with more public sources;
3. use that algebra to freeze gold, anti-leak labels and target-preserving
   adapters;
4. have an owner-separated custodian run the frozen source-family eligibility
   census and return counts/exclusions before case text;
5. only then execute the naturalistic transport and licensing-necessity
   successors under matched information/actions/resources; and
6. admit a native external arm only after fibrewise target purity is established.

This ordering removes three false parallel lanes. External adapter work,
construct gold and leakage scoring all depend on Root 1. Naturalistic execution,
prevalence and independent authority all depend on Root 2. Running any of those
downstream tasks early creates more local artifacts without advancing review
authority.

## Resolved facts versus unsupported claims

### Resolved

- The dominant scientific residual is typed responsibility-to-authority
  licensing; a clean patch expresses the hierarchy.
- Factorization, controlled-state, transcript and rectangularity tools are
  supporting or donor-derived.
- The 400/400 information-equivalent tie is the required zero-deficiency
  boundary.
- Seven target semantic sources and a four-family public standards scaffold are
  bound.
- The scaffold has 9/12 structural analogues but zero owner-sufficient groups.
- Retraction Watch metadata is CC0 at an immutable source identity.
- The finite source reducers establish 12,038 rights-valid metadata relations
  in 11,602 families and a nonambiguous three-stratum provider classification.
- Europe PMC also supplies a larger frozen count-only candidate universe.

### Still unsupported

- any certified source-to-R7 action map;
- any protected scientific-action gold cell;
- any eligible naturalistic source cluster;
- any natural mixed-fibre prevalence estimate;
- any naturalistic licensing-relation benefit;
- any external-system or model-general superiority;
- any owner-independent replication or peer-review authority; and
- top-tier submission readiness.

## Validation boundary

`validate_packet.py` verifies the exact upstream hashes, the two-root
minimality declaration, current manuscript alignment markers, all count
invariants, successor identities and the fail-closed execution state. It uses
no network and runs no pytest, CI, PDF build or scientific outcome. The legacy
patch was checked separately with batch forward `patch --dry-run`; exit 1
confirms it is partly integrated and context-drifted, so it must not be applied.
