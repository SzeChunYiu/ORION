# Five-paper top-tier hardening R2 — development packet

Date: 2026-08-25
Branch: `shadow/five-paper-hardening-r2-20260825`
Stack base: `shadow/five-paper-top-tier-20260824@c3f1f4297c5025461c7fa11e6b25d57519ecf018`
ORION main inspected at start: `03f3892dcd8e0a08c4a285b214a86da7701362be`
Academic-paper workflow: `SzeChunYiu/academic-paper-skills@188e83e639571c435344630ae68fdc66072650d2`

## Objective

Run a further recursive publication cycle on Papers A, B, C, D and the non-quantum `C_5^3` paper. The cycle is allowed to promote only statements supported by a written all-size proof or an exact committed parent. It may not turn author-side literature search, bounded computation, internal dual implementations, or editorial enthusiasm into novelty, theorem, replication, or acceptance authority.

## Expert group

1. **Quantum compilation algebra lead** — tests the signature-rank argument outside elementary binary spaces and separates compiler semantics from proof language.
2. **Zero-sum and proof-complexity lead** — identifies the exact restricted-alphabet invariant and checks compiler realization of lower witnesses.
3. **Algebraic-statistics and decision-theory lead** — compares Paper C with Markov-basis/hierarchical-model donors and derives query-specific minimax lower bounds.
4. **Formal methods and provenance lead** — absorbs positive-Datalog/provenance donors and internalizes evidence types as lattice-valued authority.
5. **Additive-combinatorics lead** — attacks the `D_4(C_5^3)` obstruction using multiplicity, rank-forcing, and atom-factorization structure.
6. **Hostile theorem referee** — searches for quantifier leaks, unproved sharpness, invalid direct-product inference, and computation-to-theorem promotion.
7. **Journal editor/release chair** — resolves the strongest defensible target separately for each paper and keeps the five-paper release gate fail-closed.

## Recursive rounds

### Round 1 — theorem pressure

- A/B: replace ambient binary rank as the primitive object by the maximum zero-sum-free word length of the realized signature alphabet.
- C: convert indistinguishable pair-information fibers into exact additive and multiplicative minimax lower bounds.
- D: move evidence types inside the fixed-point semantics using a finite powerset license lattice and rule caps.
- Non-quantum: derive the exact rank-forcing phase `support+c4<=24` and an atom-overlap budget identity.

### Round 2 — donor subtraction

Primary-source families are inspected before novelty language is frozen: sparse integer optima and zero-sum invariants for A/B; Diaconis–Sturmfels, Dobra, Hosten–Sullivant and binary graph-model Markov bases for C; truth maintenance, semiring/recursive-Datalog provenance and 2026 minimal-support causality for D; Freeze–Schmid, `C_0`, inverse zero-sum theory and the 2025 rank-two generalized-Davenport inverse result for the non-quantum paper.

### Round 3 — independent reviewer lenses

Validity, positioning/significance, and reproducibility/boundary reports are frozen before editor synthesis. These are distinct expert lenses in one agent execution context; they are not represented as mutually blind human reviews.

## New theorem increments

### AB-R2 — alphabet-Davenport normal form

For a finite abelian signature group `H` and allowed alphabet `A`, let `zsf(H;A)` be the maximum length of a sequence over `A` with no nonempty zero-sum subsequence. Any deletion-dominant compiler state whose signature word has nonzero total admits a normal form of support at most `zsf(H;A)`. In `F_2^d`, any alphabet containing a basis has `zsf=d`.

### C-R2 — minimax information lower bounds

For the registered pair-information fiber, every real-valued pair-only estimator has worst-case additive error at least `(2t-1)/2`, every integer estimator at least `t`, every symmetric multiplicative estimator factor at least `sqrt((12t-2)/(10t-1))`, and no uniform factor below `sqrt(6/5)` is possible. One-sided certified upper or lower estimators require asymptotic factor at least `6/5`.

### D-R2 — typed authority lattice

Let labels be subsets of a finite license universe. A rule may transmit only the intersection of premise licenses and an explicit rule cap. The least fixed point equals the union of licenses carried by finite untainted proof trees. Unsupported cycles remain bottom, refutation is pointwise monotone, and a post-outcome or bounded-computation cap cannot manufacture prospective or theorem authority.

### N-R2 — multiplicity/rank phase and atom budget

For a saturated length-31 5-short-free sequence with multiplicities `1,2,4`, support `s`, and `c4` multiplicity-four points,

`c2=31-s-3c4`, `c1=2s-31+2c4`.

The multiplicity-at-least-two stratum has length `62-2s-2c4`; using `eta(C_5^2)=13`, it is forced to span rank three exactly throughout the symbolic region `s+c4<=24`. For any occurrence-disjoint atom factorization,

`31-s = sum_i(|U_i|-|supp U_i|) + sum_g(r_g-1)`,

so a support-at-least-23 hypothetical extremal has total internal-repetition plus cross-atom-overlap budget at most eight.

## Validation and merge rule

The additive verifier independently checks small finite groups/alphabet invariants, the exact C minimax formulas, typed-lattice least fixed points against exhaustive fixed-point search, non-quantum multiplicity patterns, the rank phase, and the atom budget identity. Focused tests bind the R2 manuscripts and forbid overpromotion.

This branch must enter through a stacked PR. It must remain draft while any paper carries a top-tier blocking concern. In particular, no non-quantum top-tier pass is allowed without exact `D_4`, `31 in C_0(C_5^3)`, or a genuinely comparable global obstruction theorem.
