# P6 public selective-revalidation live result diagnosis

## Retained terminal

`P6_PUBLIC_SELECTIVE_REVALIDATION_V1_COVERAGE_AND_SAVINGS_MET__NATIVE_EQUIVALENT__SUPERIORITY_NOT_MET`

The clean-main run retained 100 eligible first-parent change sets in each of
three frozen public repositories (300 total). Exact-head acquisition, frozen
license-blob identity, and result byte bindings are retained in the result.

## What the run supports

- the registered public-data coverage task: three domains and 100 change sets
  per domain;
- execution of SELECTIVE_REVALIDATION, parsed native dependency closure, and
  FULL_RESET on every retained change set;
- positive descriptive savings relative to full reset in all three repositories;
- extensional equality between SELECTIVE_REVALIDATION and the strongest
  registered donor under the same parsed frozen-head graph;
- rejection of all three registered implementation mutants on at least one row.

This is `P6_DATA_AND_COMPARATOR_EXECUTION_ONLY` authority.

## What the run does not support

The donor is not an actual invocation of Nextflow, Lean/Lake, ROBOT, an ontology
reasoner, or repository-native build/test tooling. It is the dependency closure
computed from repository syntax parsed by the same ORION runner. Therefore the
zero invalid-certificate result is conformance inside the parsed graph, not
independent construct validity or native dependency completeness.

The parser retained 188 unresolved references: 8 for nf-core/rnaseq, 141 for
mathlib4, and 39 for go-ontology. Those references are adverse evidence against
any claim that the parsed graph is complete. Historical native build/test replay
is `CANNOT_CHECK`.

The block-resampling lower quantiles are deterministic sensitivity summaries
over serially dependent Git histories. They do not establish a simultaneous 95%
population guarantee, which remains `CANNOT_CHECK`. Population inference is
false. Independent adjudication and protected custody also remain
`CANNOT_CHECK`; public Git data cannot bypass either requirement.

The strongest donor was extensionally equivalent, so scientific superiority is
`NOT_MET`. This result cannot close the P6 publication gate.
