# P5 C3 DGM V4 recursive negative-result ledger

Every row is an active research problem. `positive_progress` records only the boundary or mechanism actually repaired; no negative is rewritten as empirical success.

| ID | Cause | Positive progress | Residual | Next discriminator |
|---|---|---|---|---|
| `P5.C3.V4.ARGPARSE.CHOICE.CONCATENATION` | DGM_outer.py lines 227-228 omit a comma between 'score_child_prop' and 'best', so Python forms the single choice 'score_child_propbest'. | The exact source defect and affected option values are frozen; the unvalidated default caveat is separated from explicit CLI acceptance. | Explicit --choose_selfimproves_method score_child_prop and best are rejected, while omitting the option may still pass the default into program logic. Native task semantics are defective. | Preserve this behaviour for native C3; if repaired, freeze a new commit as a separately named successor and test all intended choices without benchmark outcomes. |
| `P5.C3.V4.INEFFECTIVE.FUTURE.TIMEOUT` | DGM_outer.py lines 302-305 call future.result(timeout=5400) only after as_completed yields an already-completed future. | A separate 21,600-second process-group watchdog plus 120-second TERM/KILL grace is byte-frozen. | The released timeout cannot bound a still-running future; the outer watchdog does not itself prove Docker-descendant termination or cleanup. | Verify VM-wide timeout and destructive cleanup on a disposable executor; a source-level timeout repair requires a distinct successor identity. |
| `P5.C3.V4.MUTABLE.ENVIRONMENT` | Dockerfile uses python:3.10-slim without a digest; requirements.txt has 19 entries and requirements_dev.txt 4, with zero exact pins and no authoritative lockfile. | All declaration, Dockerfile and source hashes are retained and the dependency-lock field is explicitly adjudicated UNBOUND. | Rebuilds may resolve different OS image and Python dependency bytes. V4 is honestly 6/21 BOUND, not 7/21. | Resolve and hash every transitive dependency for the selected Linux/Python platform, pin base/task/derived image digests, and inventory licences/SBOM. |
| `P5.C3.V4.UNTRUSTED.GENERATED.CODE` | README line 88 warns that model-generated code can behave destructively; released execution grants mutable Git and Docker-backed task surfaces. | A minimal allowed/forbidden write-surface and disposable reset theorem is frozen; execution remains fail closed. | No native isolation proof, rootless daemon, egress allowlist, complete mutation digest or cleanup receipt exists. | Execute only in a dedicated disposable VM with a private rootless daemon, filtered mounts, denied ambient credentials/egress and full before/after mutation evidence. |
| `P5.C3.V4.OUTCOME.PAYLOAD.CUSTODY` | The authoritative tree tracks 1,595 files (49,707,333 bytes) under initial/, initial_polyglot/ and swe_bench/ref_agent_results/; Dockerfile COPY . would place them in the container. | A metadata-only census is frozen without opening payload contents, and all three prefixes are forbidden from the candidate seed. | No filtered, content-addressed source seed or lawful public-development archive/case packet has been produced. | Build a filtered source seed by path allowlist, prove excluded blob absence, and freeze a separate pre-protected public-development archive before candidate access. |
| `P5.C3.V4.NATIVE.PARSER.BOUNDARY` | Released DGM exposes archive/evaluation metadata rather than a stable pre-evaluation result schema; V3 had only a generic synthetic adapter. | V4 freezes an adapter-authored patch-capture schema, rejects prohibited exact/prefix families and unknown keys, preserves native status, and maps even valid PATCH_CAPTURED to UNRESOLVED. | Parser conformance is not DGM execution, and a patch remains a mixed scientific fibre until the separate certificate/action proof is valid. | After all 21 fields bind, capture one lawful patch terminal and apply the byte-frozen V3 certificate/action/fibre operation outside candidate custody. |
| `P5.C3.V4.MODEL.RESOURCE.BINDING` | Historical model-name strings, ambient API keys, provider endpoints, retries and aggregate calls/tokens/USD caps are not reproducibly bound. | Fallbacks are frozen empty and network defaults to deny except exact future endpoints. | Primary service identity, effective configuration, cost accounting and egress remain UNBOUND. | Bind exact diagnosis and coding-agent service identities, no-fallback configs, pre-call aggregate counters, provider receipts and a final egress allowlist. |
| `P5.C3.V4.TASK.RIGHTS.RUNTIME` | The README pins a separate SWE-bench source commit, but its tasks, data, project repositories, tests, images and reference outputs have distinct identity and rights layers. | The documented SWE-bench commit is retained as a discriminator rather than treated as a complete task/runtime binding. | Exact task environment, task content rights, container images and generated-artifact authority remain CANNOT_CHECK or UNBOUND. | Freeze an exact rights-cleared task subset and all project/container identities, then build a reproducible environment without importing protected/reference outcomes. |
| `P5.C3.V4.CUSTODY.PANEL` | Independent scorer identity, one-shot no-feedback enforcement and fresh protected-panel custody cannot be established locally. | The required attestations and no-feedback boundary are byte-specified and local self-attestation is prohibited. | Six-arm confirmatory readiness remains 0/6; H1-H4, transfer, harm, performance and superiority are CANNOT_CHECK. | An independent custodian signs all three content-addressed attestations only after every arm and the public-development protocol are frozen. |
