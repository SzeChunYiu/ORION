# P2 state-expanding acquisition successor V12

## Prospectively frozen question

V12 targeted the smallest concrete G17 discriminator inside the exact V10 population: two provider-native Appenzeller-Herzog records (`1003`, `1018`) that V10 retained separately while binding one shared title--abstract identity. Before opening keyword values, V12 froze the source bytes, exact pair, new `keywords` coordinate, unchanged u4 learner/balancer/active loop, matched resource and authority contract, one conjunctive outcome-blind gate, zero model runs and zero retries.

Protocol SHA-256: `7588ecdddc7d421178e4eb18b140977bb18eb11e8ed2523d071f53a5ed5f09b7`. Matched-contract SHA-256: `e158fb0fe799b38cd328cda9a09b332d7bc555939cfe0e4f662dbaeeb5b3ef14`. Runner SHA-256: `907451bafe1f6b94301ae64c71ab5563d2111cdaba0dbf69e99a83baac305037`.

## One authorized gate execution

The source and all five predecessors bind. The exact public source is 5,807,809 bytes with SHA-256 `456f9306b0ce1aee840ac0244f11df3b2a168aa46007a92f5dcf59916d309a69`. Both frozen record IDs occur exactly once. The gate interpreted and retained only `record_id`, `title`, `abstract` and `keywords`; it did not interpret or retain label values and computed no class counts, rankings or performance outcomes.

The two normalized keyword payloads are genuinely distinct: 282 versus 572 bytes, with SHA-256 `84d5e7ad...` and `3a376c64...`. Their exact u4 model text is tied, with SHA-256 `77baa667...` for both. This is the desired candidate state-expansion pattern.

## Why the frozen gate nevertheless fails

The predeclared gate also required both rows to reproduce V10's exact frozen content identity `d1b054ed...`. V12 produced `77baa667...` because its runner normalized the concatenated title--abstract string. The abstract is empty. V9/V10 instead hashes `normalize(title) + literal space + normalize(abstract)`, retaining a trailing space; exact u4 model text later calls `.strip()`. Thus the V10 population identity and u4 model-text identity intentionally differ on this edge case.

Because the exact V10 identity was a frozen conjunct, the keyword distinction cannot repair the binding after execution. G1 is **FAIL CLOSED**. The provider-native coordinate is a post-gate diagnostic candidate, not an admitted signal, performance result or promotion.

## Exact actions and boundaries

- Gate executions: **1**.
- Retries: **0**.
- Model executions/performance arms: **0/0**.
- Label values interpreted or retained: **no**.
- Class counts, rankings, CRE20, R@10 or WSS95 computed: **no**.
- Network requests: **0**.
- Manuscript/shared-ledger edits: **none**.
- Exact u4 remains the comparator and fallback.

## Next discriminator

Do not retry V12. A new source-disjoint successor must import the exact V9/V10 content-identity function by hash, separately bind the exact u4 model-text hash, predeclare the complete keyword-capable population rather than this now-known pair, and retain the unchanged V10 learner, resource charges, simultaneous gates and independent custody.

## Exact terminal

`P2_V12_PROVIDER_NATIVE_KEYWORDS_DIFFER_ON_EXACT_U4_TEXT_TIE__FROZEN_V10_CONTENT_IDENTITY_BINDING_FAILS_TRAILING_EMPTY_ABSTRACT_CANONICALIZATION__GATE_FAIL_CLOSED__NO_PERFORMANCE_RUN`
