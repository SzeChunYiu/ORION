# Comparator binding harness, 2026-08-23

This directory records a bounded P1--P5 primary-source, repository, licence,
entrypoint, information-interface, and output-terminal audit. Start with
`SCIENTIFIC_REPORT.md`; use `COMPARATOR_BINDING_LEDGER_V1.json` for machine
consumption and `PROBE_RECEIPTS_2026-08-23.json` for exact probe boundaries.
`SHA256SUMS` binds every report, metadata record, receipt, and bounded probe
artifact in this directory.

The directory grants no scientific authority. The current terminal is
`CANNOT_CHECK_FULL_COMPARATOR_FREEZE`.
