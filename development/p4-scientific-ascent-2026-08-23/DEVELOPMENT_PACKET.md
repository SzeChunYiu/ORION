# Paper 4 scientific-ascent packet

**Date:** 2026-08-23  
**Paper:** `papers/orion-14-verified-scientific-discovery/`  
**Scope:** scientific content first; no pytest or repository CI was run. Targeted
P4 PDF rebuild and visual QA followed manuscript integration.

## Why the previous paper was not yet top-tier

The protected execution was unusually strong on custody and exact reproducibility, but the scientific object was still narrower than its machinery:

1. V2's only discriminating primary axis was false promotion. Clean coverage and correct `CANNOT_CHECK` were saturated at 1.0 for all eleven systems.
2. V2 H3 was not a negative about abstention competence. An empty evidence list exposed the family, so the construction could not discriminate.
3. V3 repaired the registered shortcuts on the exact `CANNOT_CHECK` axis, but nine of ten comparators could not emit that terminal. The positive result therefore measured interface expressiveness before finer-grained epistemic judgement.
4. P4-X engulfed provenance, verification, custody, version/epoch handling and generic authorization, but remained a 400-case exact-contract study. Its ideal typed donor product tied, correctly ruling out a centralization or inherent-expressivity claim.
5. No blinded naturalistic panel, external `CANNOT_CHECK`-capable comparator execution, independent raw-case/evaluator custody, or source-disjoint replication exists yet.

The repair is not to hide these limits. It is to turn them into a general theory and a harder experiment.

## Scientific ascent delivered

### 1. Verification-axis theory

New identity: `P4.AXIS.T1.EXACT_ATTAINABILITY.V1`.

For target terminal `Y`, system representation `D`, and available output alphabet `A`, the manuscript now derives the exact finite-space optimum

`R*(D,A) = 1 - E_D max_{t in A} P(Y=t | D)`.

Zero error is attainable iff every positive-mass `D`-fibre carries one gold terminal and that terminal is in `A`. This separates:

- an unidentifiable task (mixed semantic fibres),
- a representation/interface-limited system (missing state or terminal),
- a nuisance-decodable construction,
- a saturated panel,
- and an actual decision-rule failure.

### 2. Donor-product factorization theorem

New identity: `P4.AXIS.T2.DONOR_FACTORIZATION.V1`.

A product of provenance, verification, custody, freshness and authorization donors is pointwise extensionally equivalent to the target scientific-promotion relation iff the relation factors through every state fibre exposed by that product. Under a benchmark distribution, zero error requires this only on positive-probability fibres; positive-mass mixed fibres impose an irreducible Bayes-error lower bound, while null fibres are not certified. This both widens and disciplines P4:

- it explains why a generic donor relation can fail even when its local components succeed;
- it predicts the ideal P4-X B3 tie when all target-sufficient coordinates and the same predicate are supplied;
- it rules out any inherent centralization advantage.

### 3. Claim-level identifiability bound

New identity: `P4.AXIS.T3.CLAIM_IDENTIFIABILITY.V1`.

If two scientific worlds with opposite competence claims induce observable
records at total-variation distance `d`, every equal-prior adjudicator has error
at least `(1-d)/2`.  Identical records therefore force `CANNOT_CHECK` for the
wider competence interpretation even when a narrower output score separates.
This turns the difference between terminal expressiveness and epistemic
judgement into a formal identifiability question.

### 4. Verification-axis adequacy envelope

New identity: `P4.AXIS.D1.ADEQUACY_ENVELOPE.V1`.

A broad scientific interpretation now requires four conditions jointly:

1. **semantic attainability** from intended scientific information;
2. **nuisance resistance** under a declared audit class;
3. **matched terminal attainability** for the comparison arms;
4. **nonzero panel resolution** on the claimed axis.

This yields a research action for each adverse outcome rather than a post-hoc relabeling: repair the construction, enlarge the interface, expose target-sufficient state, or improve the decision relation.

### 5. Strongest donors absorbed rather than competed away

The manuscript now treats the following as donor-owned:

- Partial Evidence Bench-style incomplete-view and gap reporting;
- AuthGraph-style provenance/authorization alignment;
- FAVA-style evidence-backed permission graphs and deterministic authorization;
- CVA-style principal/request/context/policy binding;
- existing source-aware verification, attribution, claim auditability, evidence escalation, abstention, contamination auditing, evaluator auditing, and protected execution.

The residual is target scientific-promotion sufficiency and its verification-axis conditions, not provenance or authorization in the abstract.

## Negative-result ascent map

| Preserved adverse result | Scientific diagnosis | Upward successor |
|---|---|---|
| V2 H3 `NOT_SUPPORTED`, all systems 30/30 | construction saturation via empty evidence list | V3 exact-axis nuisance repair; V2 terminal unchanged |
| V3 exact `CANNOT_CHECK` positive but nine comparators score zero | unmatched terminal alphabet / interface attainability | external panel restricted to `CANNOT_CHECK`-capable comparators |
| V3 four off-axis residuals | exact-axis audit does not imply whole-register clearance | keep residuals; require multi-axis naturalistic audit |
| H2 clean coverage 1.0 for every system on V2 and V3 | no difficult-benign resolving power | pair each unidentifiable naturalistic unit with a same-source identifiable control and enforce a joint endpoint |
| P4-X ideal B3 ties 400/400 | no inherent expressivity or centralization advantage | make factorization/sufficient-state characterization the theory contribution |
| P4-X remains exact and mechanical | no naturalistic or provider generality | execute `P4.NAT.AXIS.768.V1` under external custody and source-disjoint replication |

## Frozen naturalistic discriminator

Identity: `P4.NAT.AXIS.768.V1`.

The existing protocol is scientifically appropriate because it makes the unique naturalistic source--case-family cluster the independent unit, not the pair member, seed, probe, or metric cell. It contains:

- 4 scientific domains × 8 unidentifiability mechanisms;
- 24 independent clusters per stratum, 768 total;
- one identifiable same-source control paired with every unidentifiable member;
- a joint endpoint requiring both correct `CANNOT_CHECK` and correct control resolution;
- four frozen `CANNOT_CHECK`-capable primary comparators;
- familywise multiplicity and worst-domain gates;
- independent evaluator/raw-case custody;
- a source-disjoint replication panel.

Its status remains `PROSPECTIVE_LOCAL_PREFLIGHT_COMPLETE__EXTERNAL_NATURALISTIC_PANEL_REQUIRED`. `outcomes_accessed=false`. The 0.90 joint power is a planning quantity under the declared model, not evidence. No panel or evaluator was fabricated.

## Public-source initialization — arXiv CC BY pool

New binding: `P4.NAT.AXIS.768.ARXIV_CC_BY_POOL_1536.V1`.

An outcome-blind official arXiv OAI-PMH harvest now binds 1,536 unique,
complete, non-withdrawn CC BY 4.0 article records: 384 candidates for each of
the four P4 domains, a 2:1 pool for the planned 192 adjudicated clusters per
domain. Earth/environment is internally balanced 128/128/128 across
atmospheric-oceanic physics, geophysics and earth/planetary astrophysics. The
other routes are quantitative biology, software engineering, and electrical
engineering/systems science.

The source-pool SHA-256 is
`47dd24657752731cdf45bab95852f3e18b50946af8a29b5acee95956ec81d895`.
All 24 official OAI XML pages are retained and byte-hashed. Every row binds the
exact versioned arXiv identifier, version history, authors, title, abstract,
categories, licence URL, immutable URLs and attribution string. A live
four-domain sample found 4/4 exact abstract pages displaying CC BY 4.0 and 4/4
exact PDF URLs returning PDFs.

This is a P4-only content-rights successor to the shared provider/modality
metadata census, not a replacement for it. The shared census established
provider/modality diversity but left content-class rights unresolved. This
binding resolves attributed arXiv-text reuse for one provider only. It does not
satisfy provider diversity, modality diversity, exact eight-mechanism quotas,
case eligibility, native three-terminal comparator identity, external custody,
or source-disjoint replication.

Metadata and content are kept separate: arXiv metadata is CC0, while article
content is admitted only when the official record explicitly declares CC BY
4.0. The default arXiv non-exclusive licence, missing licence, noncommercial/no-
derivatives licences, and share-alike records are excluded. Publisher PDFs,
third-party figures/data, and differently licensed versions remain outside the
binding. Full eligibility gates and alternatives are documented in
`PUBLIC_NATURALISTIC_SOURCE_AUDIT_V1.md`.

## Manuscript changes

- retitled the paper around verification-axis identifiability and attainability;
- replaced the abstract with theory + three explicitly separated evidence layers;
- rewrote the contribution list;
- added the formal verification-axis section and proofs;
- integrated the existing P4-X section into the LaTeX manuscript;
- expanded related work to the strongest partial-evidence and formal-authorization donors;
- strengthened limitations with the exact V3 off-axis/interface diagnoses and the frozen 768-cluster prospective boundary;
- replaced the conclusion with the theory/factorization contribution and explicit open naturalistic target;
- added `CLAIM_LEDGER_V4.md` so the new theorem, diagnostic, exact results, and prospective successor cannot be conflated.

## Next scientific work (no pytest/CI)

1. Transfer the frozen 768-cluster protocol to genuinely external panel/evaluator custody.
2. Acquire outcome-blind source snapshots and record source-cluster hashes before annotation.
3. Freeze four runnable `CANNOT_CHECK`-capable external comparators with matched information and output alphabets.
4. Run the paired primary endpoint and worst-domain gate once.
5. If negative, classify each discordance by the adequacy envelope and mint a disjoint successor identity; never edit V2, V3, P4-X, or the naturalistic V1 outcome after access.
6. Require source-disjoint replication before any naturalistic/general headline.

The current top-tier blocker is therefore experimental custody and external naturalistic evidence, not another synthetic benchmark or another round of test-suite repair.

## Identity completion and source-universe discriminator

`P4_NATURALISTIC_IDENTIFIABILITY_SUCCESSOR_V2_AMENDMENT.json` now names the
four domains, eight natural pair origins, four comparator roles, eligibility
rules, and per-cell allocation that V1 specified only by counts.  Each of the 32
cells requires 24 primary, eight source-disjoint replication and 16 reserve
candidates before adjudication.

An outcome-free screen of the 1,536-row arXiv pool then tested whether that
single-provider universe could even supply nominal discovery signals.  Only
abstract--full-text and earlier--later-version mechanisms reach 48 signals in
every domain.  The lowest-domain counts for protocol--result,
article--correction, article--data, article--code, conference--paper and
article--supplement are 2, 5, 38, 6, 16 and 2.  These are keyword/version upper
bounds, not eligible cases; the actual panel capacity can only be lower.

The retained terminal is
`P4_NATURAL_PAIR_METADATA_SIGNALS_COUNTED__32_CELL_ELIGIBILITY_CANNOT_CHECK`.
It opens the prospectively frozen multi-provider routes in
`P4_NATURAL_PAIR_SOURCE_EXPANSION_FREEZE_V1.json`: arXiv version states, PMC OA
linked records, Zenodo/DataCite dataset and software relations, separately
licensed open proceedings/full-paper pairs, and metadata-only discovery routes.
Domains, mechanisms, quotas and margins remain unchanged.  Spare M1/M2 records
cannot be reassigned to short cells.

## Zenodo source expansion: retained V1 failure and V2 discriminator

The first frozen Zenodo identity requested 200 rows on an unauthenticated API
call.  Zenodo returned HTTP 400 and stated that unauthenticated requests may not
exceed 25 rows.  No census or model outcome ran; the retained terminal is
`P4_ZENODO_RELATED_OBJECT_CENSUS_V1_TRANSPORT_CANNOT_CHECK_HTTP_400`.

A distinct V2 successor disclosed and excluded 162 pre-freeze probe identities,
fixed nine pages of 25 rows for each of eight M5/M6 domain cells, repaired the
Zenodo licence vocabulary, required public file-link evidence, and applied the
unchanged 48-signal gate to publication-typed related identifiers.  All 72
responses passed schema/pagination validation with no within-query duplicates.
The result contains 173 query-specific candidates representing 98 unique
records.  Publication-linked counts were 37, 44, 29 and 30 for the four data
queries, and 6, 10, 10 and 7 for the software queries.  None passes 48.

This is a useful source diagnosis, not a naturalistic result.  The data route is
near quota; the software route has abundant relations but few
publication-typed targets.  The terminal is
`P4_ZENODO_RELATED_OBJECT_V2_SOURCE_CELL_SHORTFALL__EXPAND_DISJOINT_PROVIDER`.
The next unchanged discriminator is a disjoint DataCite plus exact repository-
rights route for M5, a repository-release/Software-Heritage identity route for
M6, and PMC OA linked records for M3/M4/M8.  Linked-object rights, pair identity,
case eligibility, comparator outcomes and external custody remain unbound.

## DataCite M5 mechanics and source-signal successors

The first provider-disjoint DataCite identity froze four M5 queries and
returned 4,000 valid metadata rows, but its sparse field selection omitted the
client relationship required by the provider-disjointness rule. All rows
therefore failed closed before rights, file-availability or relation screening.
The immutable terminal is
`P4_DATACITE_M5_CENSUS_V1_CANNOT_CHECK_TRANSPORT_OR_SCHEMA`; this is a mechanics
failure, not evidence against M5 or DataCite coverage.

A distinct V2 successor bound and excluded all 3,479 unique DOI identities
disclosed by V1, added the client relationship, retained exact Zenodo/source
disjointness and the 48 gate, and corrected only the literal documentation
assertion. All response schemas, pages and evidence assertions passed. Of
4,000 raw rows, 968 were excluded by the V1 disclosure, 3,032 passed provider
disjointness, and 2,719 carried exact CC BY 4.0 or CC0 declarations. None
exposed the frozen HTTPS `contentUrl`, so no row reached publication-relation
adjudication and zero candidates were admitted. The M5 counts remain 37, 44,
29 and 30; M6 remains 6, 10, 10 and 7.

The V2 terminal is
`P4_NATURAL_PAIR_SOURCE_CELL_SHORTFALL__EXPAND_DISJOINT_SOURCE_UNIVERSE`.
The zero relation count is a sequential not-reached count, not a global absence
claim. The next discriminator must bind an exact repository-specific public-file
manifest or repository-record API without dropping file identity, rights,
publication relation or natural-pair adjudication. DataCite metadata CC0 and
deposited rights declarations do not establish repository-content authority.

## Multi-provider M5/M6 transport successor

A separately frozen Dryad/Figshare/Harvard Dataverse/DataCite expansion is
retained under
`development/p4-source-universe-successor-v3-2026-08-23/`. It verified 1,263
successful raw API responses, 26 provenance captures and 2,371 candidate rows
without opening any case or model outcome. Strict deduplication yielded
M5/M6 lower bounds of `6/2`, `10/0`, `4/1`, and `3/6` across the four domains.

Those counts do not establish cell failure. Figshare captured only 659 of
2,463 frozen full-record identities and Harvard Dataverse 512 of 1,540; the
missing 1,804 and 1,028 identities remain frozen. Dryad anonymous downloads
returned HTTP 401 and exact version/file lineage remained undetermined.
DataCite supplied 2,958 discovery DOIs but zero repository-bound independent
units. The exact terminal is
`P4_NATURAL_PAIR_SOURCE_TRANSPORT_CANNOT_CHECK`. A valid recursion must resume
the frozen missing identities after provider recovery or open a separately
frozen provider family; it may not add convenient pages or records to the
current census.

## M6 JOSS/GitHub source-provider successor V4

A new pre-outcome identity is retained under
`development/p4-m6-source-provider-successor-v4-2026-08-23/`. It froze the
first 200 recent JOSS publication DOIs before metadata access and treated
JOSS/GitHub as one content-provider family. Search hits, papers, repositories,
organizations, releases, tags, assets, files, commits, versions, authors and
API responses could not multiply the independent concept count.

The bounded route transported all 200 JOSS pages. It bound 191 labelled GitHub
repository relations, 180 latest releases whose tags resolved to immutable
commits, 118 accepted exact-tag licences with licence-blob SHA, and 120 unique
domain assignments under the unchanged token rule. Their intersection contains
80 provider-qualified DOI/repository concepts: Earth 5, Life 7, Scientific
Software 62 and Physical 6.

No promotion follows. JOSS relates a paper to a repository, but the V4 identity
does not prove that the current GitHub release is the version evaluated by the
paper. The bounded V3 packet also omits its candidate rows, preventing exact
cross-provider publication/repository/artifact deduplication. Public author and
owner metadata does not establish author-lineage independence or natural-pair
eligibility. The exact counts are therefore **0 paper-to-release relations, 0
author-lineage adjudications and 0 natural pairs**.

The no-overlap union is only an optimistic diagnostic: Earth, Life and Physical
are each 7/48. Scientific Software is 68/48 in total but has only 6/8 disjoint
Figshare replication units when JOSS/GitHub is primary. It does not pass the
allocation gate. The V4 terminal is
`P4_M6_JOSS_GITHUB_BOUNDED_TRANSPORT_COMPLETE__EXACT_PUBLICATION_RELEASE_VERSION_RELATION_AND_AUTHOR_LINEAGE_CANNOT_CHECK__M6_CELL_FRAME_NOT_READY`,
while `P4_NATURAL_PAIR_SOURCE_TRANSPORT_CANNOT_CHECK` remains unchanged.

Next research must recover the V3 M6 concept identities for exact deduplication,
freeze a same-200 JOSS software-archive DOI to GitHub tag/commit bridge, and
open separately frozen Bioconductor/CRAN or domain-provider identities for the
three 41-unit optimistic gaps and the software two-unit replication shortfall.
Every adverse cause, residual and discriminator is retained in
`NEGATIVE_RESULT_LEDGER_V4.{json,md}`.

## M6 JOSS/archive exact-version bridge V5

The no-extension V5 successor retained the same 200 JOSS publication DOIs and
recovered 9/9 exact V3 M6 identities for DOI-level deduplication. DataCite plus
source-native archive metadata exposed 103 same-repository tag/commit
identities; 101 resolved to immutable commits. Requiring every original V4
provider-qualified gate plus one distinct archive version/concept relation,
accepted archive and commit rights, unchanged repository/domain identity and
V3 deduplication leaves 39/80 exact bridges: Earth 3, life 4, software 30 and
physical 2.

The deduplicated V3-strict plus V5-exact totals are 5/48, 4/48, 36/48 and 3/48;
Figshare replication is 2/8, 0/8, 6/8 and 1/8. All current exact-frame gates
fail without proving a global source deficit, while
author-lineage adjudications and natural pairs remain 0. Thirty-two of the 80
lack an archive-explicit same-repository tag/commit relation, 12 lack a
distinct archive-version/concept relation and 3 lack accepted exact archive
rights; overlap leaves 41 incomplete. Evidence is retained under
`development/p4-m6-joss-exact-version-bridge-v5-2026-08-23/`. The terminal is
`P4_M6_JOSS_ARCHIVE_EXACT_VERSION_BRIDGE_PARTIAL_39_OF_80__M6_CELL_FRAME_AUTHOR_LINEAGE_AND_NATURAL_PAIR_CANNOT_CHECK`;
the V4 and programme adverse terminals remain immutable.

## M6 JOSS same-identity content-addressed repair V6

The distinct V6 successor under
`development/p4-m6-joss-bridge-repair-v6-2026-08-23/` retained exactly the 41
V5 failures and added or replaced zero publication identities. It repaired
17/41: 12 by exact normalized source-archive/GitHub immutable-commit manifest
equality and 5 by equality between a qualified source-native SWHID directory
and the immutable Git commit root tree. Exact version identity and accepted
archive/commit software rights remained mandatory.

The final exact bridge is 56/80. Twenty-four identities remain unresolved: 12
exact-publication-version failures, 2 exact-archive-rights failures and 10
archive-to-commit content-identity failures under the primary cause partition.
Deduplicated V3-strict plus V6-final totals are 6/48, 4/48, 51/48 and 4/48;
Figshare replication remains 2/8, 0/8, 6/8 and 1/8. Scientific Software clears
its total and JOSS-primary quotas but still fails replication, so all four full
cells fail. Author-lineage adjudications, natural-pair adjudications and
eligible natural pairs remain 0.

The exact terminal is
`P4_M6_JOSS_SAME_IDENTITY_CONTENT_ADDRESSED_BRIDGE_REPAIR_PARTIAL_17_OF_41__FINAL_EXACT_56_OF_80__AUTHOR_LINEAGE_AND_NATURAL_PAIR_CANNOT_CHECK`.
This is development transport evidence with a disclosed pre-freeze route
probe, not confirmatory or global-provider evidence. The programme terminal
`P4_NATURAL_PAIR_SOURCE_TRANSPORT_CANNOT_CHECK` remains unchanged.

## M6 JOSS targeted unresolved-identity repair V7

The targeted V7 packet under
`development/p4-unresolved-identity-v7-2026-08-23/` revisits exactly the 24
V6-unresolved publication identities without extending the 200-publication
frame. It requires an exact Crossref-to-JOSS-review relation, a unique
publication-time archive child, a source-native tag resolved to a full commit,
archive-authenticated content or origin identity, provider-bound checksums and
accepted software rights at archive and commit. Editorial assertion alone does
not close an identity gate.

Fourteen of 24 identities are repaired, raising the cumulative exact JOSS
bridge from 56/80 to 70/80. Domain repairs are Earth 0, life 2, scientific
software 11 and physical 1. Ten identities remain unresolved under a mutually
exclusive primary partition: three archive-to-commit authenticated-identity
failures, two exact-archive-software-rights failures, two exact archive-version
DOI failures and three exact tag-to-immutable-commit failures. No publication
DOI is added or replaced, and files, versions, tags, commits and requests add
zero units.

The programme terminal remains
`P4_NATURAL_PAIR_SOURCE_TRANSPORT_CANNOT_CHECK`. Author-lineage adjudications,
natural-pair adjudications and eligible natural pairs remain 0. V7 is targeted
development transport evidence, not naturalistic, confirmatory, performance or
superiority evidence. Its packet-native validator passes 25/25 and all 12
manifest entries pass. The efficient recursion is limited to the ten disclosed
identities; broad provider recrawls and replacement publications are excluded.

## M6 exact ten-target provider-native repair V8

The V8 packet under `development/p4-unresolved-identity-v8-2026-08-23/`
targets only the ten V7 residual identities. Ten of ten exact provider archives
were fetched once and checksum-verified. Provider-native licence, embedded Git
provenance and DOI-version/tree evidence repair indices 59, 108 and 196,
raising the cumulative exact bridge from 70/80 to 73/80. Seven identities
retain one exact authoritative-edge residual each. The measured pass took
36.011085 seconds and used no replacements, proxy units, broad harvest,
target execution, comparator outcome or lineage/natural-pair adjudication.
