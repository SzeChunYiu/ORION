# P1 owner-custody positive successor V13

## Why V13 does not search again

V11 bound a real pre-cutoff standards-governance record but found no R7 owner or complete algebra. V12 then checked the exact official issue and exact-commit repository routes: the issue bound chronology but contained zero of seven formal delegation fields, while the exact-commit licence and algebra routes returned 404. Repeating repository metadata cannot create an external owner, delegation, acceptance, rights grant or review.

V13 therefore moves from metadata diagnosis to the smallest lawful execution object. It accesses no cases or outcomes and does not rerun the map audit.

## Genuine positive result

The V13 execution packet binds:

- eight administrative components by SHA-256;
- exactly three semantic input artifacts authorized by the V10 adapter-blind allowlist;
- all seven R7 target identifiers and both delegated powers;
- two-stage adapter-blind delivery and no-prior-access receipts;
- separate owner/delegate, host, rights-holder and independent-reviewer acts;
- seven machine-named external return artifacts;
- GPG, SSH or Sigstore signatures, exact hashes, ordering and noncompensatory acceptance rules.

The signing specification also resolves an otherwise circular content address. The owner first signs the RFC8785-canonical algebra payload excluding `ratification`; the reviewer signs a review of that sealed payload hash; the final schema-conformant V8 instance binds both hashes in `ratification` and receives a separate envelope hash and signature. No artifact is required to contain its own full-file hash.

The packet is ready to deliver to verified external actors without supplying adapter assignments, survivor maps, audit results, cases, labels, models, scores or outcomes. This is a positive execution-readiness result.

## Exact unresolved boundary

No externally verified R7 owner/delegator, delegate, host authority, rights holder, independent reviewer or authorized delivery channel was supplied. Consequently zero of seven signed external outputs were received and zero of four authority acts closed. Candidate-author or AI population of identities, ownership evidence, rights, semantic values, review dispositions, keys or signatures would fabricate authority and is prohibited.

Unsigned forms do not close any scientific gate. The 720 survivor maps remain CANNOT_CHECK; 116,929 remain rejected; zero are certified. No manuscript claim changes.

## Efficient successor

V14 is no longer another discovery search. It is a deterministic acceptance gate over the complete externally signed return bundle. It verifies identities, signatures, exact hashes, schema conformance, stage ordering, adapter blindness, separate host/rights acts and a content-addressed CONFORMANT review. Only complete success authorizes the unchanged 117,649-map audit.

## Exact terminal

`P1_V13_CONTENT_ADDRESSED_EXTERNAL_EXECUTION_PACKET_COMPLETE__ZERO_OF_SEVEN_EXTERNAL_SIGNED_OUTPUTS_RECEIVED__ZERO_OF_FOUR_AUTHORITY_ACTS_CLOSED__720_MAPS_UNCHANGED`
