# ORION-01 successor Round 2: retained fail-closed result and bounded positive

Date: 2026-08-27
Status: **successor evidence only; the frozen ORION-01 paper tree is unchanged**
Scientific authority delta: **NONE**

## Outcome

The frozen full study remains `CANNOT_CHECK_MOVE_COMPLETENESS`. Eight of 585 primary words hit the predeclared 20,000-state cap, so the protocol correctly refuses an approximate full-domain result. The committed raw receipt contains all 585 primary rows plus 16 boundary probes and has SHA-256 `db1253e52a44741613abb9217eb4a865d190c3948abdab9f8fbd344ada035efd` (13914625 bytes). Two fresh full executions produced byte-identical receipt bytes.

## Retained bounded positive

Among completed rows, four words have a strict in-registry resource gap: the native result `[0, 4, 8]` versus exact registered optimum `[0, 2, 5]`. All four survive the declared hostile extension battery; the seeded generic control misses the optimum on two. This is a replicated bounded certificate-realization signal for those four words only. It is **not** full-domain move completeness, all-PyZX optimality, compiler superiority, external novelty, journal authority, or submission authorization. The replication is a fresh deterministic re-execution, not an independent implementation.

## Negative preservation and stop decision

Round 1's macro-language `CANNOT_CHECK_MOVE_COMPLETENESS` is retained unchanged. Round 2 records a different mechanism: state-space size at the frozen cap, not guard unsoundness. No cap-hit row is promoted to an approximate outcome. The bounded positive does not resolve the material full-domain `CANNOT_CHECK` terminal. Under the revival policy that negative remains **ACTIVE**. A separately frozen Round-3 attempt will extend exact search only for the eight cap-hit words. They are **not** placed on an unsolvable list because no scoped impossibility proof exists.

## Freeze boundary

PR #1574 already froze the current ORION-01 paper and science object. This successor package was moved additively under `development/` after outcome capture so `papers/orion-01-certificate-realization` remains byte/tree-identical to `origin/main`. This package does not refreeze, reopen, promote, or mutate the paper.
