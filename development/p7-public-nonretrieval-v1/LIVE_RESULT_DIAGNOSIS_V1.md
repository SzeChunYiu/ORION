# P7 public non-retrieval V1 result diagnosis

## Frozen terminal

`P7_PUBLIC_NONRETRIEVAL_V1_DATA_COVERAGE_MET__STRONGEST_DONOR_EQUIVALENT`

The unchanged runner executed from clean `main` at
`9469c08eaa4eca3522e2dcc98b94b14084664659`. It retained 100 unique directed
partition contracts: 50 over the UCI Zoo class vocabulary and 50 over the UCI
Letter Recognition vocabulary. Each domain has 25 preserve and 25 reopen
decisions, zero false-closure retention, and all three frozen mutations killed.

## Decisive adverse result

The equal-information partition-refinement oracle produced exactly the same
metrics and decisions as exact containment. The strongest-donor result is
`EXTENSIONALLY_EQUIVALENT`; therefore the scientific-superiority gate is
`NOT_MET`. This is a retained null comparison, not evidence of novelty.

## What the result can support

The execution supports only the literal issue data-coverage statement: two
public non-retrieval subject domains and 50 unique directed transitions per
domain. The result records `P7_DATA_COVERAGE_BOX_ONLY`; it does not support the
separate superiority criterion.

## What remains out of scope

These are prospective class-partition constructions, not observed upstream
ontology changes. The two domains belong to one classification-partition
structural family and rows are not independent replications. Gold and system
instantiate the same mathematical predicate, so reported accuracy is conformance
rather than independent construct validation. Population inference is false;
independent adjudication and protected custody remain `CANNOT_CHECK`.

The post-execution authority seal makes those bounds machine-readable while
leaving the raw execution-result bytes and receipt unchanged.

No public observations or features were downloaded or redistributed. Public
metadata does not bypass protected confirmation or create external authority.
