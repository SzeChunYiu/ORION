\subsection{Outcome-blind V6 comparator-native preflight}

V6 refined one local component of the V5 comparator boundary without opening
any reference alignment, gold, matcher performance or protected outcome. AML
v3.2 produced its prospectively required native artifact under OpenJDK 17,
with the retained qualification that upstream names Java 8. LogMap 4.0
produced its required RDF output and mandatory TSV sidecar. Its pinned source
and output confirmed that the RDF header repeats ontology 1 in both ontology
positions, so row-namespace validation and RDF--TSV equivalence remain
mandatory guards.

BERTMap through DeepOnto 0.9.3 did not pass. The exact model revision and
weight hash matched; both 16-class ontologies loaded; and input-derived
corpora reached 108 training and 12 validation records. Before training,
DeepOnto passed the keyword \texttt{evaluation\_strategy} to a Transformers
4.51.3 constructor exposing \texttt{eval\_strategy}. All five prospectively
required mapping artifacts were absent. The exact result is therefore
\textsc{CannotCheck} from a pinned dependency-API incompatibility, not an empty
alignment or a negative scientific outcome. A separate V7 successor freezes
an exact dependency tuple already co-present in the upstream lock; it remains
prospective.

The native-smoke total is consequently 2/3. Under
Theorem~\ref{thm:authority-fibre}, these results refine local executable-
artifact predicates only. Naturalistic multi-family truth, performance,
coverage, harm, superiority and independent custody were not observed, so the
V5 scientific comparator-readiness state remains 0/3.

