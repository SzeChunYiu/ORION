# P3 V7T next discriminators

## Theory lane

Commission an independent adversarial review that must attempt to falsify:

1. the placement of `bottom_authority` outside the truth codomain;
2. the equivalence in the authority-aware fibre criterion;
3. refinement monotonicity when gate definitions are frozen;
4. the claim that the finite countermodel defeats local-to-global composition;
5. the completeness of the proposed comparator gate set; and
6. novelty against formal workflow/provenance, partial-function, selective
   prediction, benchmark-validity and scientific-inference literature.

The review must return counterexamples or exact line-level approval; a
same-lane validation is not independent.

## Empirical lane

Do not open performance outcomes yet. In order:

1. execute the separately frozen BERTMap dependency-only V7 successor;
2. bind explicit cross-stage identities for repository, method, task,
   population, reference and scorer;
3. acquire at least three rights-valid independently authored ontology-pair
   families with explicit binary negatives or authoritative exhaustivity;
4. freeze source-disjoint outcome custody and all harm/noninferiority gates;
5. only then evaluate candidate, AML, LogMap and BERTMap.

Any missing stage remains `CANNOT_CHECK`; it is never imputed.
