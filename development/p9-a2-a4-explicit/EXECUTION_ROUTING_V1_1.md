# P9 A2/A4 execution-routing receipt V1.1

Status: `PRE_OUTCOME_EXECUTION_ONLY`.

The corrected A2/A4 scientific protocol and executable hostile checks are unchanged. This receipt exists only because the prior official workflow remained queued behind the shared GitHub runner backlog after the evidence-integrity repair.

## Superseded execution

- workflow: `p9-a2-a4-d0-explicit`;
- stale run: `32244457457`;
- protected result accessed: **no**;
- scientific outcome available: **no**.

## Unchanged scientific content

- frozen corpus seed and 256-pair counts per relation/history family;
- relation selector semantics;
- failure-history selector semantics;
- view ladder;
- coverage/full-task metrics;
- candidate-order invariance requirement;
- evaluator-gold falsification invariance requirement;
- hidden-history hostile-pair equality requirement;
- terminal rule;
- claim ceiling.

The earlier archive that attested unmeasured hostile checks remains invalidated. Only the executable corrected bulk runner may restore a sufficient terminal.

## Official-result rule

The stale queued run is superseded. Only a workflow triggered by this receipt commit or a descendant with no scientific A2/A4 changes may count as the official corrected replay.

Authority: `NONE_EXECUTION_ROUTING_ONLY`.
