# P3 V6 recursive negative-result ledger

A negative is a research object, not material to erase. Missing or nonselected rows are never obstruction.

| ID | Negative result | Cause | Residual | Next discriminator |
|---|---|---|---|---|
| P3V6-N01 | AML was run on Java 17 although upstream names Java 8. | Only Java 17 arm64 was available in the bounded runtime. | Cross-JDK reproducibility remains untested despite a successful smoke. | Prospectively repeat the same artifact-only smoke under an exact Java-8 image and compare structural artifact hashes without gold. |
| P3V6-N02 | Frozen LogMap filename did not match pinned source. | Pinned source writes logmap2_mappings.rdf, not logmap_mappings.rdf. | The original interface assertion is false for this commit. | Use only the pre-confirmation source-derived interface amendment; future versions must re-inspect the source filename before execution. |
| P3V6-N03 | LogMap RDF header names ontology 1 in both ontology positions. | Pinned LogMap2Core passes onto_process1.getOntoIRI() twice to createOutFiles. | Header-trusting consumers can misattribute the target even though row namespaces are correct. | Require row-namespace guards and test a separately versioned upstream-fix or adapter successor; do not patch V6 output. |
| P3V6-N04 | The original three-class BERTMap input implied logging_steps=0. | Pinned source computes int(epoch_steps * 0.02); the original synthetic corpus was too small. | The three-class K3 input cannot safely reach TrainingArguments. | The source-justified 16-class amendment was frozen before K3 execution and produced nonzero logging/evaluation steps. |
| P3V6-N05 | A supervisory Path.resolve call discarded venv activation. | The venv Python symlink was resolved to the base interpreter before subprocess launch. | That attempt never imported DeepOnto and has zero native authority. | The frozen launcher amendment retains the literal .venv/bin/python path; no scientific parameter changed. |
| P3V6-N06 | The first native BERTMap entrypoint aborted at an undocumented-in-protocol interactive JVM prompt. | Pinned source uses click.prompt during import when the JVM is not started. | Noninteractive launch cannot pass the prompt without an explicit stdin binding. | Pinned DeepOnto documentation justified the frozen exact 2g newline stdin binding before ontology loading or outcomes. |
| P3V6-N07 | BERTMap failed before training with a Transformers keyword incompatibility. | DeepOnto 0.9.3 passes evaluation_strategy; its Python-3.10 lock resolves Transformers 4.51.3 which exposes eval_strategy. | All five required native artifacts are absent, so K3 is CANNOT_CHECK. | Execute the separately frozen V7 compatibility successor using the exact lock-present 4.46.3/0.20.3/1.0.1/2.5.1 tuple; do not modify V6. |
| P3V6-N08 | Only two of three comparator families passed native artifact smoke. | K3 stopped at dependency API construction while K1 and K2 completed. | Three-family native readiness is not established. | Require a fresh V7 K3 pass under the unchanged five-artifact gate. |
| P3V6-N09 | No comparator has scientific readiness in V5. | V6 intentionally used synthetic inputs, no gold, and no scoring. | Performance, harm, coverage, superiority, and transport remain CANNOT_CHECK; V5 stays 0/3. | Only an independently custodied, rights-valid, frozen evaluation can change scientific readiness. |
| P3V6-N10 | Native row presence on parallel synthetic labels has no truth authority. | The smoke inputs were constructed for interface execution, not evaluation. | Rows cannot be claimed as correct mappings or broader empirical support. | Keep smoke artifacts separate from any future independently scored evaluation and never interpret absence as obstruction. |
