# P3 V20 scientific report: structural BERTMap native execution

V20 is a distinct successor to the V17 HermiT failure and the V18/V19
compatibility gates. Before execution it froze the V19-authorized structural
reasoner entrypoint, removed only `owl:Thing` from each exact raw 37-class
DeepOnto runtime index, retained the 36-by-36 provider-native matcher universe,
changed only the empty output identity, and held the V17 model, learner,
thresholds, parser and common evaluator fixed.

The one native attempt completed in 281.646897292 seconds with exit code zero.
Both outcome-blind class-surface markers were present; the direct LogMap child
returned zero; and all five required artifacts were regular non-symlink files.
The repaired output contained 33 mappings.

The inherited V13 decoder then failed exactly as designed because it accepts
only the earlier `Optional.of(ontologyIRI)#fragment` surface while the V20
native repaired table contains plain absolute IRIs. The exact error was:

`ValueError: source surface string violates frozen typed grammar`

V20 did not open the gold alignment and did not invoke common scoring. Its
terminal remains:

`P3_V20_BERTMAP_NATIVE_PASS__TYPED_DECODER_OR_STRUCTURAL_CONTRACT_FAIL__COMMON_SCORING_NOT_AUTHORIZED`

This is a positive native-execution and artifact-interface result, not mapping
truth or performance evidence. V21 is a separate post-output, pre-gold identity
decoder successor; its result does not overwrite V20's decoder failure.
