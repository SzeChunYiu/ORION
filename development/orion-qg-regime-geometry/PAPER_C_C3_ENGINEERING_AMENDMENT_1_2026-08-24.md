# Paper C / C3 — engineering amendment 1

Date: 2026-08-24  
Parent: `PAPER_C_C3_RWISE_VALUE_SEPARATION_PROTOCOL_2026-08-24.md`  
Scientific construction: unchanged.

## Independent-verifier timeout

The first independent verifier computed every block factor by rescanning every column inside every candidate set partition. The source lane had already completed, but this generic lane was terminated with process status `143` before producing a generic JSON artifact or scientific decision.

Adverse engineering terminal:

`PAPER_C_C3_GENERIC_VERIFIER_NAIVE_PARTITION_SCAN_TIMEOUT__NO_SCIENTIFIC_DECISION`

The repair precomputes the complete exact factor table once per constructed instance and then evaluates every partition against that table. It neither reads source optimizer rows nor changes the construction, objective, thresholds, formula, or acceptance gates. The repaired independent lane returned `ACCEPT_RWISE_VALUE_SEPARATION`.

This timeout is not negative scientific evidence and is not relabeled as positive evidence. It is retained as an implementation-boundary event.

## Workspace cleanup

The C3 runner begins with failure-safe unique `/tmp` workspaces and `finally` cleanup. A focused injected-failure test verifies removal of both paths. Four complete fresh-workspace executions produced the identical native trace, dual receipt digest, and dual file SHA-256.

## Authority boundary

This amendment supplies engineering provenance only. It grants no theorem, novelty, physical-resource, cross-grammar, cross-objective, minimal-padding, or venue authority.

