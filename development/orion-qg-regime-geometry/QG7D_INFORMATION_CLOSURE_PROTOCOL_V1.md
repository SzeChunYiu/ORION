# QG-7d information-sufficiency closure — frozen protocol

Parent #836. Base lineage includes protected padding-ablation negative at `1fb8c2723f6c9431ab02b715139f233092dbd42c`.
Parent all-n chain: QG-7c `QG7C_PARTIAL__L4B_OPEN`.

## Purpose
Decide whether the existing pinned-T4b state is sufficient to authorize the proposed whole-system J5 closure. This packet does NOT try to prove the all-n identity from finite absence.

## Load-bearing parent facts
- T4b exact local domain has 135,604 lemma failures, worst residual +2.
- The PP subfamily contributes 30,500 + 440 + 1,616 = 32,556 failures.
- T4b's serialized state is `(case,ja,R_b,R_a,p,coreB,envB,coreA,envA)` and its `best` tensor is 64^4 for each fixed outer parameter tuple; there is no phantom-home target/F3-environment coordinate.
- QG-7c's PP realization constructs the pinner with an additional home at q=2, i.e. a support-two frame whose home contribution is outside the T4b b/a state.

## Complete hidden-home ambiguity test
For a PP pinner whose home frame letters are `(Z,X)` on the two target branches, hold the visible T4b state fixed and enumerate every six-tuple of phase-free target letters at the hidden home, one target letter for each of the three blocks x two branches: `4^6 = 4096` environments.

Compare the two-branch F3 restore contribution before vs after deleting/rebuilding the PP home frame letters while the other two block multipliers at that hidden coordinate are fixed to identity. Record
`Delta_restore = F3_after - F3_before`.

If the set of deltas has more than one value, then the existing T4b quotient does not determine the price of that J5 component. If it spans both negative and positive values, no sign-safe exact J5 adjudication can be made from the visible quotient alone without either:
1. extending the state with the hidden home environment; or
2. proving a separate worst-case bound whose other structural credits dominate the full positive range.

No such expanded state or worst-case J5 theorem is frozen in the parent programme.

## Padding-ablation binding
Bind the protected QG-7d padding result: 160 exact realizations under four frozen spectator policies, zero B''' witnesses, generic/native ACCEPT_BOUNDED_NEGATIVE. This is evidence against an easy counterexample but not all-n authority.

## Terminal
If parent binding + padding negative + hidden-home ambiguity all pass:
`QG7D_CANNOT_CHECK_ALL_N_PINNED_CLOSURE__PP_HIDDEN_HOME_ENVIRONMENT_NOT_IN_PARENT_STATE__PADDING_ABLATION_NEGATIVE`.

This terminal scientifically closes QG-7d under the programme stop rules. It explicitly leaves the all-n identity `C_DP=min(C_D+,f_B',f_B'')` UNPROVED and B''' UNFOUND. A future programme may reopen only with an expanded PP home-state quotient or an independently proved worst-case J5 bound.

No novelty/R6/physical-advantage authority.