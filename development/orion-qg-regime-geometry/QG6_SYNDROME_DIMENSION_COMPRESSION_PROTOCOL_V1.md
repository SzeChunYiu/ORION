# ORION-QG QG-6 — Syndrome-dimension compression / automatic rank inference protocol V1

Date: 2026-08-21
Parent programme: #740
Issue: #756
Base revision: `164462bf7c7f3d3c2e559fa5aaf19726bb6ec388`
Status: **FROZEN BEFORE QG-6 MACHINE OUTCOME**.
Authority: research/theorem instrumentation only; no novelty, paper, merge, physical-advantage or cross-grammar authority.

## 1. Scientific question

Can the finite-support dimensions behind ORION-Q's two all-n theorem lanes be inferred directly from the *production exact-DP transition semantics*, rather than hand-specified from a proof sketch?

The candidate abstract theorem is deliberately elementary:

> Let active coordinates carry syndrome contributions `sigma(q) in F_2^d` whose global XOR is a required nonzero syndrome. If every nonempty proper zero-sum subset can be deleted by a semantics-preserving rewrite with no cost increase (or equal cost with a strict well-founded support decrease), then an optimum has a representative with at most `d` active coordinates.

Linear dependence in `F_2^d` is donor mathematics and receives zero novelty credit. The research object is **automatic identification of the relevant compiler syndrome quotient and binding it to a legal, cost-certified rewrite**.

## 2. Frozen production modules

Read-only modules:

- `research/extensions/orion-q/max_r6m_exact_three_tare2_shared_factor_dp.py` (R6M, 9-bit exact XOR DP);
- `research/extensions/orion-q/max_r6i_exact_rank2_shared_tag_dp.py` (R6I, 10-bit exact XOR DP);
- `research/extensions/orion-q/MAX_R6S_ALL_N_COMPOSITION_RESULTS.json` (already-earned R6M all-n support-two theorem receipt).

No chemistry loader is invoked. No protected subject path is read.

## 3. Automatic local transition-rank experiment

### 3.1 R6M frame-letter deletion

Enumerate the full frozen local option domain `(rA0,rA1,rB0,rB1,rC0,rC1,s) in {I,X,Y,Z}^7` (16,384 rows).

For each of the six frame slots independently:

1. compute the production 9-bit `_DELTA` for the original local tuple;
2. replace only that frame letter by identity, leaving every other local letter fixed;
3. compute `change = delta_old XOR delta_new`;
4. collect every realized change vector and compute its exact GF(2) rank.

Expected ranks are **2 for every frame slot**. The checker also reconstructs the expected coordinate forms from the documented state definition rather than fitting them after outcome:

- `A0`: anticommutation bit `b0` plus one Tag-syndrome bit replicated into `(b3,b4,b7)`;
- `A1`: `b0` plus syndrome replicated into `(b5,b6,b8)`;
- `B0`: `b1` plus `b3`;
- `B1`: `b1` plus `b5`;
- `C0`: `b2` plus `b4`;
- `C1`: `b2` plus `b6`.

For every slot the realized change set must be contained in, and span, the two-dimensional subspace generated by the frozen analytic basis.

### 3.2 R6I block-generator deletion

Enumerate the full local domain `(rA0,rA1,rB0,rB1,s0,s1) in {I,X,Y,Z}^6` (4,096 rows).

For block A, replace `rA0=rA1=I` simultaneously; for block B, replace `rB0=rB1=I` simultaneously. Compare the production 10-bit `_DELTA` old/new values and compute the exact GF(2) span rank of the change set.

Expected rank: **5 for each block**.

Frozen analytic basis for block A:

- one bit changing `<RA0,RA1>` (`bit0`);
- four Tag-syndrome coordinates, each duplicated between the A/B-difference bit and the A-actual bit:
  - `(bits2,6)`, `(bits3,7)`, `(bits4,8)`, `(bits5,9)`.

For block B:

- one bit changing `<RB0,RB1>` (`bit1`);
- the four A/B-difference bits `bits2..5`.

The production-derived change set must exactly lie in and span the declared rank-5 subspace.

## 4. Automatic quotient claim

For each rewrite, define `d_auto := rank(span(change_vectors))`. QG-6 may claim only:

- the production transition semantics expose a rewrite-relevant linear quotient of dimension `d_auto`;
- if the corresponding rewrite is separately proven semantics-preserving and non-increasing, the abstract support-compression theorem yields support `<= d_auto`.

It may **not** infer a support theorem from rank alone.

## 5. Concrete theorem bindings

### I1 — R6M

The QG-6 checker must bind the production-derived `d_auto=2` to the already-committed R6S receipt:

- authority string contains `MAX_R6S_ALL_N_COMPOSITION_THEOREM_MACHINE_CHECKED`;
- `lemma_e_zero_violations`, `lemma_b_w3_to_w8_zero_failures`, `bindings_exact`, and `no_new_subject_data` gates are true;
- the R6S class definition is `(alpha,beta)` and its zero-sum deletion is exactly the one-frame-letter rewrite analyzed here.

If bound, QG-6 may report `R6M_SUPPORT2_THEOREM_RECOVERED_FROM_PRODUCTION_SYNDROME_RANK`.

### I2 — R6I

QG-6 independently machine-corroborates the local block-deletion cost inequality from production local algebra, but **must not** grant the all-n support-five theorem unless the separate QG-1 dual-harness receipt exists and is positive. Until then the allowed statement is only:

`R6I_SYNDROME_DIMENSION5_AUTOMATICALLY_INFERRED__THEOREM_PROMOTION_PENDING_QG1`.

This prevents QG-6 from self-authorizing its own motivating theorem.

## 6. R6I local-cost corroboration

For all active `(a,b)!=(I,I)`, target letters `(p0,p1,p2)`, Tag letters `(s0,s1)` and three central choices, compare the frozen raw one-block frame+Restore contribution before and after deleting both generators (`R0=R1=R2=I`). Production local multiplication/weight/symplectic tables are bound exactly.

Expected complete domain: 46,080 rows.
Expected: zero cases with `Delta > -4`; exact maximum `Delta = -4`.

This is corroboration only until QG-1 independently closes its proof/harness gates.

## 7. Abstract theorem / search-complexity corollary

The receipt records the following non-novel mathematical implication as a checked proof object:

- `d+1` vectors in `F_2^d` are dependent;
- nonzero total syndrome makes the zero-sum subset proper;
- admitted deletion preserves feasibility and does not increase cost;
- lexicographic `(cost, support)` minimality gives support `<=d`.

Bounded search corollary for a finite local alphabet `A`:

`N_support(n,d,A) = sum_{k=0}^d binom(n,k) A^k = O(n^d A^d)` for fixed `d`.

This applies only to the certified compilation component/rewrite, not to an arbitrary full compiler.

## 8. Independent generic-harness verification

The generic verifier must **not import `_DELTA`**. It reconstructs the documented state-coordinate formulas from the production local symplectic primitive and independently enumerates local tuples, then checks:

- production result counts/hashes;
- rank 2 / rank 5;
- exact analytic basis equality;
- R6S receipt binding;
- R6I local-cost count and max delta;
- novelty/authority false.

## 9. Native ORION-Q admission

A typed campaign consumes only the serialized analyzer + generic-verifier receipts and chooses:

- `ACCEPT_R6M_AND_R6I_RANK_FINDINGS`;
- `REJECT_BINDING_OR_RANK`;
- `CANNOT_CHECK`.

R6I theorem promotion is a separate coordinate and must remain `PENDING_QG1` unless an independently supplied QG-1 positive receipt is bound.

## 10. Positive/honest terminals

Strongest current positive:

`QG6_PRODUCTION_SYNDROME_RANK_INFERENCE_VERIFIED__R6M_D2_RECOVERS_SUPPORT2__R6I_D5_FOUND_THEOREM_PENDING_QG1`

Other honest terminals:

- `QG6_PRODUCTION_RANK_BINDING_REFUTED`
- `QG6_R6M_INSTANCE_BINDING_REFUTED`
- `QG6_R6I_LOCAL_COST_REFUTED`
- `QG6_NATIVE_GENERIC_DISAGREEMENT`
- `QG6_CANNOT_CHECK`

## 11. Novelty boundary

No novelty claim for finite-field dependence, Carathéodory/support sparsification, fixed-parameter complexity, Pauli symplectic representations, R6S, or QG-1. Candidate novelty is limited to a reusable **compiler-analysis method that infers a theorem-relevant conserved-syndrome quotient from production transition semantics and turns a separately certified zero-sum rewrite into a finite-support normal form**. External review remains required.
