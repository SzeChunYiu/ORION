# ORION-Q MAX-R4F / QG-11D — architecture-conditioned resource responsibility V1

Date: 2026-08-22
Issue: #907
Parents: #903 (MAX-R4E QG-derived skills), #764 (QG-11 FT lift), #698 (MAX-R4 real-method programme), #633/#679.
Branch: `codex/orion-q-qg11d-stack-remint-20260822`
Base: main `c5ba39fef4f25c46de5fb69bf07f50530f4693ca`
Status: **FROZEN BEFORE ANY TARE-SPECIFIC NEW-STACK OUTCOME.**

## Frozen donor stack identities

D0 — conventional rotation-synthesis / Clifford+T-style baseline.
- Exact instance/model identity must be reconstructed in Stage 0 from committed ORION parents or under a fresh local receipt.
- Open PR #892 is evidence only until merged; no authority may be copied from it.

D1 — Dyadic Phase Fixing (DPF).
- Kalloor et al., `arXiv:2606.05397v1`, submitted 2026-06-03.
- General multi-qubit numerical resynthesis greedily extracts dyadic rotations, then uses phase kickback / a selected phase-gradient register.
- Donor headline metrics are not TARE-specific authority.
- The donor itself reports that T-count and surface-code spacetime can diverge; T-count-only physical authority is forbidden.

D2 — syndrome-resolved / weak-transversal Clifford+phi architecture.
- Yoshioka et al., `arXiv:2510.08290v2`, revised 2026-06-25.
- Partially fault-tolerant architecture with in-place Pauli rotations through syndrome-resolved weak-transversal gates and repeat-until-success.
- This is a distinct architecture/reliability contract, not a cheap Clifford+T coefficient point.

D3 — reusable logical dyadic-phase catalysts.
- Xu and Wang, `arXiv:2606.27358v1`, submitted 2026-06-25.
- Surface-code cultivation of reusable logical catalysts for exact fixed fine dyadic phases by phase kickback.
- Offline cultivation/reuse/ancilla assumptions remain explicit and distinct from D1/D2.

Generic phase-kickback, synthesis, resource estimation and weak-transversal methods are donor-owned. Candidate ORION value is only a protected compiler-specific transfer/counterexample and responsibility-control result.

## R0 — the scientific remint

Old ORION-QG FT work asks whether a structural compiler resource vector transfers through a frozen physical resource map. QG-11D changes the stack identity itself.

Primary question:

> Can two TARE compiler states that were physically equivalent or nearly irrelevant under an old resource projection become distinguishable after a modern rotation-lowering / FT architecture remint?

Secondary ORION-Q question:

> Does a stack-identity-aware responsibility skill reopen the right scientific layer more reliably and with less wrong-layer work than a static reuse of an old physical receipt?

## R1 — stack identity is a required-same authority coordinate

Freeze:

`ResourceResponsibility.v1 = {logical_semantics_id, compiler_family_id, lowering_stack_id, rotation_implementation_id, qec_architecture_id, mapping_id, error_budget_id, control_status, resource_coordinates, responsible_coordinates, negligible_coordinates, unresolved_couplings}`.

Every physical/resource receipt must declare which identifiers are `required_same`. A change to D0/D1/D2/D3, QEC code, mapping strategy, error budget, phase-gradient policy, controlled/uncontrolled status or reliability contract reopens the responsibility decision unless a transfer theorem explicitly bridges it.

Candidate MAX-R4E skill upgrade:

`S6R_STACK_REMINT = stack identity changed -> reopen resource responsibility before pruning/optimizing a layer`.

## R2 — Stage-0 baseline reconstruction

Before opening any new-stack TARE outcome:

1. bind exact TARE semantics from committed main parents;
2. independently re-establish any load-bearing logical invariants used in pair matching, including fixed logical rotation count where applicable;
3. if the old QG-21 result is needed and PR #892 remains unmerged, reproduce the minimal old-stack resource calculation under a new receipt rather than importing its verdict;
4. keep any `OLD_STACK_GEOMETRY_SUBPERCENT` statement explicitly stack-scoped;
5. record donor paper/version and code/source-access status.

If a faithful D1/D2/D3 implementation cannot be obtained/reproduced, the corresponding execution arm returns `DONOR_LOWERING_CANNOT_CHECK`; no surrogate headline-number substitution.

## R3 — DPF structural-vector insufficiency attack

Primary executable arm when a faithful DPF implementation becomes available.

Freeze matched TARE circuit/configuration pairs with the same old summary coordinates wherever possible:
- same target/semantic unitary;
- same logical arbitrary-rotation count;
- same old structural/FT resource summary used by the parent projection;
- different local Clifford/rotation context visible to DPF.

Before reveal freeze:
- input circuit canonicalization;
- block partition/width policy;
- numerical synthesis distance and tolerance;
- total/block error budget;
- dyadic precision candidate set;
- phase-gradient register selection policy;
- surface-code mapping strategy if mapped resources are evaluated.

Compare exact donor/reproduced DPF outputs:
- residual arbitrary rotations;
- extracted dyadic rotations / precision distribution;
- selected phase-gradient register width;
- T-count;
- Clifford/CNOT additions;
- depth/serialization;
- ancilla/register cost;
- mapped spacetime metric if available.

Strong counterexample terminal:

`QG11D_OLD_STRUCTURAL_FT_VECTOR_INSUFFICIENT__DYADIC_SYNTHESIS_CONTEXT_COORDINATE_REQUIRED`.

The missing coordinate is outcome-driven. Candidates are only hypotheses: dyadic-extractability signature, local rotation-context signature, shared-precision demand, residual-angle count, register pressure, or mapping serialization.

## R4 — resource-proxy ordering attack

Because D1 explicitly finds T-count and mapped spacetime can diverge, every matched-pair analysis keeps both logical and mapped physical resource vectors.

If T-count ordering and final mapped spacetime ordering differ, earn:

`QG11D_RESOURCE_PROXY_ORDERING_FLIPS_AFTER_FT_MAPPING`.

This is a direct ORION-Q S4/S6 lesson: the query must name the final authority coordinate; optimizing a proxy does not transfer authority automatically.

## R5 — D2 direct-Pauli-rotation remint

Treat D2 as a separate, partially fault-tolerant architecture.

Only map an ORION/TARE logical Pauli rotation when donor code/gate assumptions apply. Preserve:
- QEC code identity;
- physical error rate;
- target logical error;
- repeat-until-success policy;
- decoding/feedforward assumptions;
- partial-FT boundary.

Track costs removed from D0 (factories/small-angle synthesis/routing where donor semantics remove them) and costs added by D2 (syndrome rounds, retries, coherent physical rotations, decoding/feedforward, code constraints).

Possible terminal:

`QG11D_DIRECT_PAULI_ROTATION_STACK_REMINTS_THE_RESPONSIBLE_COMPILER_COORDINATE`.

No cross-stack statement of physical superiority is allowed without matched reliability/resource semantics.

## R6 — D3 exact dyadic-catalyst remint

Use D3 only for fixed dyadic phases that the donor construction actually supports.

The resource state must distinguish:
- offline catalyst cultivation cost and success probability;
- reuse count/amortization horizon;
- online controlled-U depth/Clifford cost;
- catalyst logical size/code distance;
- residual non-dyadic rotations outside the catalyst set.

Prospective question: can a compiler representation that increases reusable shared dyadic structure reduce amortized total cost enough to change the responsible coordinate? If yes, serialize the exact reuse/crossover condition. No claim for arbitrary angles.

## R7 — stack-indexed responsibility map

After any two executable stack arms, build only the earned map:

`stack identity -> {dominant coordinates, negligible coordinates, missing couplings, exact transfer/CANNOT_CHECK boundary}`.

A coordinate may be negligible in D0 and load-bearing in D1/D2/D3. This is a resource-responsibility phase map, not a physical phase transition.

## R8 — prospective S6R transfer discriminator

After calibration freeze one held-out reminted stack/circuit pair before resource reveal.

Compare under matched information/tool budget:
- `S6-static`: reuses prior responsibility unless an explicit failure is already visible;
- `S6R`: stack remint automatically reopens responsibility and chooses the minimum decisive resource/lowering checks;
- donor-composed full resource analysis.

Hard outcomes:
- correct responsible-layer decision;
- stale physical receipt reused after required-same change: 0 allowed;
- wrong resource coordinate optimized: 0 allowed for positive authority;
- verified final resource ordering;
- decisive query count / unnecessary lower-layer work;
- correct CANNOT_CHECK.

Only an independent held-out positive can advance #903 to `MAX_R4E_QG_SKILLS_REAL_METHOD_SEARCH_VALUE`.

## Mandatory authority false

- `OLD_STACK_SUBPERCENT_RESULT_UNIVERSAL=false`
- `DPF_MAKES_TARE_BETTER=false`
- `CLIFFORD_PHI_FULLY_FT_EQUIVALENT_TO_CLIFFORD_T=false`
- `T_COUNT_ALONE_AUTHORITATIVE=false`
- `PHYSICAL_QUANTUM_ADVANTAGE=false`
- `MAX_R4E_QG_SKILLS_REAL_METHOD_SEARCH_VALUE=false` until R8 passes
- novelty/R6 authority false.

## Stop/continuation rule

A donor-arm CANNOT_CHECK does not close the scientific lane if another materially different new stack is executable. A negative matched-pair result does not universalize structural-vector sufficiency; widen under a new freeze or prove a transfer theorem. Stop only at an exact transfer theorem for the registered stack domain, a concrete insufficiency witness, or bounded multi-stack CANNOT_CHECK/saturation.
