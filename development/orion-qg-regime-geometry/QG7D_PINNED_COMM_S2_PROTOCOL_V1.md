# QG-7d — pinned comm-s2 last-link closure

Date: 2026-08-21
Issue: #836
Base: `c5ba39fef4f25c46de5fb69bf07f50530f4693ca`
Parent result: `QG7C_CLASSIFICATION_RESULTS.json`, digest `0b127438dac9dc844a52176873eb5769a99ff52b34851d9c82c4b1feded656b6`
Status: **FROZEN BEFORE QG-7d ANALYZER OUTCOME.**

Authority ceiling: frozen unit-support R6M/TARE classification theorem or exact B''' obstruction only. No R6, novelty, chemistry, or physical-advantage authority.

## Exact parent frontier

QG-7c leaves one proof link open: the pinned comm-s2 sector of T4b.

Required bit-exact parent census:

- domain `536870912`;
- failures `135604`;
- worst delta `+2`;
- `PA_ja0_delta1=97072`;
- `PA_ja0_delta2=2376`;
- `PA_ja1_delta1=3600`;
- `PP_ja0_delta1=30500`;
- `PP_ja0_delta2=440`;
- `PP_ja1_delta1=1616`.

No QG-7d terminal is valid unless this parent census is reconstructed or cryptographically bound and the relevant subdomain is re-enumerated.

## Scientific hypothesis

T4b G1–G4 are too auxiliary-preserving. They re-letter the selected comm-s2 block and a pinner but retain most of the two-anchor shared-Tag geometry. QG-7d tests whether the residual disappears when the local theorem is allowed to reconstruct the shared Tag and all three participating blocks jointly.

The latest QG-6 result is a methodological control only: a conserved syndrome quotient can be a sound proof-system upper bound while stronger whole-system reconstruction yields smaller intrinsic complexity. QG-7d must therefore search auxiliary reconstruction before declaring the `+2` residual intrinsic.

## Phase D1 — J5 PA theorem attempt

Freeze the `PA` subfamily first because all affected frame support lies on the two T4b coordinates `b,a`; no hidden phantom-home environment is touched.

For every exact T4b PA state and every parent G1–G4 move, add **J5** candidates generated independently of target outcome:

### J5-A — all-anchored relocation to b

- replace all three blocks by support-one anchored blocks on `b`;
- replace the shared weight-two Tag by one nonidentity Tag letter on `b`;
- enumerate all 3 local Tag letters and, for each block, both local frame-1 letters anticommuting with that Tag letter;
- frame-0 is the Tag letter, giving the canonical `(0,1)` labels;
- all central choices are immaterial to frame support because both frames are weight one, but the checker must verify that statement from the production objective.

### J5-B — all-anchored relocation to a

Same construction on `a`.

For PA, J5 changes no other qubit. Therefore its F3 delta is exactly determined by the existing complete T4b `(coreB,envB,coreA,envA)` domain. The structural saving relative to the pinned comm-s2 reference must be derived from production resource counts, not hard-coded.

Positive PA gate:

- every T4b PA parent failure becomes `delta<=0` under `min(G1..G4,J5-A,J5-B)`;
- exact expected parent PA failure count is `103048`;
- no new failure may be hidden by dropping parent rows.

If PA closes but PP does not, terminal is only `QG7D_PA_PINNED_CLOSED__PP_OR_CHAIN_RESIDUAL_OPEN`.

## Phase D2 — PP home-aware theorem attempt

PP has a phantom home outside `b,a`. Any move that deletes or retargets its home letter must include the complete adversarial home environment.

Allowed implementations:

1. exact home-domain enumeration over both target letters and the two other block residual letters for both branches; or
2. a complete primitive bound proving the worst home F3 surcharge, then adding that exact worst surcharge to the b/a J5 table.

The first implementation is frozen to use the exact primitive bound as a hostile upper bound **and** serialize equality witnesses. If the bound is too weak to close PP, a later version may enumerate the full home domain, but no current positive may assume the home is free.

The primitive bound under test is the complete local maximum for deleting/changing one nonidentity frame letter against arbitrary `(target,u,v)` in one F3 branch. It must be derived by exhaustive `4 x 3 x 4 x 4` enumeration.

## Phase D3 — chain composition

A local J5 certificate is all-n only if it decreases a well-founded global measure. Freeze the measure:

`M = (#comm_s2 blocks, number of pinned comm_s2 incidences, Tag weight)`

lexicographically.

For a positive all-n terminal, the analyzer must prove that every admitted J5 move strictly decreases M and does not create a new comm-s2 block outside the touched triple. If the pinner itself is comm-s2, the chain subcase remains OPEN unless a complete two-comm-s2 joint exchange is machine checked.

## Padding-ablation hostile realization

The QG-7c Arm-C realization inserts the same spectator Z in all six targets. Freeze these QG-7d policies before exact outcomes:

- `COMMON_Z = (Z,Z,Z,Z,Z,Z)` — parent control;
- `NO_COMMON_FACTOR = (X,Y,Z,X,Y,Z)`;
- `PAIRWISE_MISMATCH = (X,Y,X,Y,Z,X)`;
- `MINIMAL_NONZERO`: no spectator on already-nonidentity branches; for an otherwise identity target use deterministic branch letter `(X,Y,Z,X,Y,Z)[branch]`.

For each of the six T4b census classes, select deterministic representatives:

- lexicographically first delta-1 row;
- lexicographically first delta-2 row if the class has one;
- lexicographically last row in that class;
- up to 16 additional rows from an evenly spaced deterministic index schedule.

For every realized target instance run exact `C_D++`, `C_D+`, `f_B'`, and `f_B''`. Only if `C_D++ < min(C_D+,f_B',f_B'')` may unrestricted DP be opened. A replay-confirmed strict row is a B''' obstruction.

The padding arm is counterexample evidence only; finite absence cannot prove all-n closure.

## Independent generic ORION

Generic ORION must independently rebuild `I,X,Y,Z`, multiplication, symplectic product, local weight and F3; reconstruct the PA J5 search without importing the production PA table; verify the primitive PP home bound; and replay every selected hostile target from raw serialized data.

## Native ORION-Q authority

Distinct decisions:

- `ACCEPT_ALL_N_PINNED_CLOSURE`;
- `ACCEPT_PA_ONLY_PARTIAL`;
- `ACCEPT_BTRIPLEPRIME_WITNESS`;
- `REJECT_PARENT_BINDING`;
- `REJECT_GENERIC_DISAGREEMENT`;
- `CANNOT_CHECK`.

## Honest terminals

Strong theorem:

`QG7D_PINNED_COMM_S2_CLOSED_ALL_N__BDOUBLEPRIME_COMPLETENESS_MACHINE_CHECKED`

Counterexample:

`QG7D_BTRIPLEPRIME_REGIME_FOUND__PINNED_COMM_S2_EXACT_WITNESS`

Partial:

`QG7D_PA_PINNED_CLOSED__PP_OR_CHAIN_RESIDUAL_OPEN`

or

`QG7D_J5_DOES_NOT_CLOSE_PA_PINNED_SECTOR`.

## Claim boundary

A strong theorem may only be combined with the already-protected R6S/QG-7/QG-7b/QG-7c chain to state `C_DP=min(C_D+,f_B',f_B'')` for every n under the frozen unit-support objective. No other objective, compiler family, novelty, R6, or physical advantage follows.