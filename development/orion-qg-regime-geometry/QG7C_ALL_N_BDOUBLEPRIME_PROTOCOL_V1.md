# QG-7c — all-n B-double-prime closure or B-triple-prime discovery

Date: 2026-08-21
Issue: #816
Parent TARE lane: #757
Parent finite B-double-prime closure: QG-7b result recorded on #757
All-n support parent: R6S `C_DP=C_D++`
Status: **FROZEN BEFORE QG-7c RESULT-BEARING EXECUTION.**

Authority ceiling: bounded theorem/counterexample result for frozen R6M unit objective only. No novelty/R6/physical-advantage authority.

## Question

QG-7b enlarged B-prime to B-double-prime by absorbing the verified weight-2-Tag + phantom hybrid discovered by QG-7. B-double-prime has zero residual gap on 10,481 verified instances, including all 64 fourth-regime witnesses and a fresh adversarial panel.

QG-7c asks whether this finite closure is the all-n truth:

`C_D++ = min(C_D+, f_Bdoubleprime)` for every n,

or whether a fifth elementary support-two configuration B-triple-prime exists.

## Frozen hostile shape grammar

The first execution must enumerate these shapes in this order; no post-outcome shape addition:

H1. two simultaneous phantom blocks with Tag support exactly 2;
H2. cyclic phantom/home dependencies across all three blocks;
H3. disjoint two-anchor hybrids where different blocks borrow different Tag anchors;
H4. within-block `(2,2)` frame support with shared/crossed/disjoint homes;
H5. adversarial F3 columns: all-equal nonidentity, two-equal/one-different, anti-aligned and cross-anchor Restore patterns;
H6. Tag support >=3 under support-two frames.

The initial finite quotient domain is n in {3,4}. Canonicalize by qubit permutation and globally valid Pauli-letter relabeling only. Preserve first representative in frozen lexicographic order.

## Exact referee

For every representative:

1. compute exact D++ optimum/witness using the frozen support<=2 family;
2. compute D+;
3. compute frozen B-double-prime minimum/witness exactly from the QG-7b definition;
4. verify `C_D++ <= min(C_D+,f_Bdoubleprime)` by containment;
5. any strict row `C_D++ < min(C_D+,f_Bdoubleprime)` is a fifth-regime discovery and must be independently replayed.

Positive discovery terminal:
`QG7C_BTRIPLEPRIME_REGIME_FOUND`.

Finite no-gap evidence alone cannot authorize the all-n theorem.

## Normalization theorem arm

If the frozen hostile search is negative, machine-check the following proof obligations.

### L4b multi-block consolidation

Show that any optimum with >=2 simultaneous phantom blocks either:
- decomposes into independent B-double-prime contributions; or
- admits a no-cost-increase rewrite consolidating phantom/home coordinates into one frozen B-double-prime member.

Cyclic dependencies and crossed homes must be included explicitly.

### L4c Tag-support2 sufficiency

Prove every support-two D++ optimum has a cost-equivalent optimum with shared Tag support <=2. The proof may use target/Restore structure but must account for all-three F3 factor interactions.

### F3 composition

Enumerate a complete local interaction table for the normalization moves against arbitrary other-block Restore letters. A local inequality may only be summed globally after this table proves the required separability/composition property.

## Machine domains

The implementation must record:
- hostile representative counts per H1-H6;
- exact referee rows and first/minimal gap witnesses;
- complete local F3 normalization table domain and extrema;
- any Tag-support reduction table / exceptional classes;
- deterministic replay.

A theorem terminal requires both the finite hostile search to have no gap and every named L4b/L4c/F3 obligation to be independently reconstructed.

## Independent generic ORION

Generic ORION must:
- reconstruct selected D++/B-double-prime witnesses from primitive Pauli algebra;
- recompute the F3 local tables independently;
- verify every claimed normalization inequality and Tag-support reduction condition;
- distinguish finite search absence from theorem authority.

## Native ORION-Q

Responsibilities are exactly:
- `BTRIPLEPRIME`
- `ALL_N_NORMALIZATION`
- `PARTIAL_OPEN_SHAPE`
- `PARENT_BINDING_GAP`
- `CANNOT_CHECK`

The native lane must reject any theorem packet that omits H1-H6 or L4b/L4c/F3.

## Honest terminals

- `QG7C_BTRIPLEPRIME_REGIME_FOUND`
- `QG7C_BDOUBLEPRIME_COMPLETENESS_ALL_N_MACHINE_CHECKED`
- `QG7C_TAG_SUPPORT2_SUFFICIENCY_ALL_N_MACHINE_CHECKED`
- `QG7C_PARTIAL_NORMALIZATION__CYCLIC_OR_F3_SHAPE_OPEN`
- `QG7C_CANNOT_CHECK`

## Claim boundary

All R6S/D+/B-prime/B-double-prime machinery and the finite 10,481-instance closure are donor parents. QG-7c earns only a new all-n classification or a genuinely new B-triple-prime configuration. No physical quantum-speedup claim follows.
