# Cross-domain transfer / shared assimilation closure

This directory contains the pre-outcome freeze for issues #286 and #318. Files prefixed `FREEZE`, `EXPECTED`, `PANEL`, and `ASSIMILATION_DEMO` predate the executable runner. They are immutable experiment-design inputs; later result files must not rewrite them.

The registered hypothesis is deliberately negative-complexity: equally abstract insight + explicit signature compatibility is expected to match the richer mechanic-contract receipt. If so, high-level abstraction transfers, but the extra contract representation has no incremental scientific transfer value and #286 closes `ABSTRACTION_ONLY`. The assimilation receipt remains useful for source/assumption/authority bookkeeping but does not earn a novel inference-method claim; #318 closes `PROCESS_USEFUL_NOT_NOVEL` after exact-head CI and independent reconstruction.
