# #286 / #318 prospective closure freeze

This packet is frozen before the executable transfer runner exists.

## Scientific hypothesis

The richer ORION mechanic-contract representation is useful engineering structure, but it will **not** outperform an equally abstract insight representation once both are given the same explicit source/target signature-compatibility test. The expected scientific terminal for #286 is therefore `ABSTRACTION_ONLY`, not `TRANSFER_MECHANISM_SUPPORTED`.

The corresponding #318 process hypothesis is `PROCESS_USEFUL_NOT_NOVEL`: source-bound ADOPT/ADAPT/COMPOSE receipts improve bookkeeping and fail-closed ownership, but this finite transfer test is not expected to establish a new assimilation inference method.

## Falsifiers

Reopen the negative terminal if any of the following occurs on the frozen held-out panel:

- `MECHANIC_CONTRACT_RECEIPT` solves a case that `ABSTRACT_INSIGHT_SIGNATURE` cannot under the same public information;
- the contract arm has lower negative-transfer rate or higher partial-transfer accuracy than the abstract-signature parent;
- the abstract-signature parent requires access to a protected target label unavailable to the contract arm;
- the result depends on surface terminology rather than registered assumptions/obligations;
- any target family is omitted from the denominator;
- an independent reconstruction disagrees with the main scorer.

## Frozen panel

40 invented held-out source→target episodes across five domains: software diagnosis, scientific retrieval/stopping, semantic mapping/transport, protected evidence authority, and measurement/interface repair. Every mechanic family has three positive-transfer cases, two near-miss assumption violations, two no-transfer negatives, and one partial-transfer case. Target domains are rotated so no family is evaluated only in its source domain.

The public packet exposes opaque mechanic family identity, source/target signatures, declared assumptions, available tools, surface cues, and abstract insight/mechanic-contract representations. It never exposes `gold_action` to candidate policies.

## Frozen arms

`NO_TRANSFER`, `RAW_TRAJECTORY`, `SUMMARY_MEMORY`, `ABSTRACT_INSIGHT`, `SKILL_INSTRUCTION`, `SURFACE_SIMILARITY`, `ABSTRACT_INSIGHT_SIGNATURE`, `MECHANIC_CONTRACT_RECEIPT`, and analysis-only `ORACLE`.

No arm may be tuned after seeing the protected table. The main and independent scorers must be separately implemented.
