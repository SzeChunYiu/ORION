# Development packet — issue #282 P5 causal-repair revival

## Development question

How can the three immutable GLM-5.2 adjacent-level attribution errors become frozen discriminators and a non-compensatory causal-repair acceptance doctrine without treating architecture completion, replay gain, or a missing live campaign as transfer evidence?

## Atomic fibres

1. regenerate the 21/24 attribution result from raw records and content-address the receipt;
2. preserve `P5-HC-002`, `P5-HC-012`, and `P5-HC-018` exactly;
3. freeze retrieval-vs-representation, environment/tool-vs-implementation, and representation-vs-method-basis discriminators before any repair outcome;
4. freeze additive `P5.causal-repair.v2` with seven required stages and licensed change classes;
5. bind fresh-transfer split identities as `UNBOUND` until host freeze;
6. reject greedy replay-only promotion;
7. block missing independent fresh transfer;
8. retain negative-history fingerprints and block recurrence;
9. keep the only positive terminal as host-only `RECOMMEND_HOST_PROMOTION`;
10. record live-campaign credentials as `CANNOT_CHECK` when absent, without printing secrets.

## Incumbent mechanics and negative history

P5 V1/V2 already freeze `STATIC -> REPLAY -> FRESH -> PROTECTED` with harmful-result precedence. The GLM-5.2 campaign produced 21/24, not 24/24. Stale perfect-attribution prose is a process failure. Replay-overfit and self-certification are already forbidden. This packet must not rewrite V1/V2 or collide with Phase-2 preflight identity work.

## Parent-domain recovery

REFLECT and Causal Agent Replay already use intervention to test attribution hypotheses. EvoAgentBench and PAST-Bench already separate experience/replay from transfer. PACE/SEA already supply anytime-valid acceptance. The residual is the composition: adjacent-cause discriminators that license only a matching change class, then non-compensatory fresh/protected acceptance with immutable negative history and no self-merge.

## Saturation assessment

Cause discrimination, candidate generation, replay/fresh split design, acceptance/admission, harmful-transfer accounting, and evaluator/negative-history custody were checked against the #282 seed set plus the merged P5 nearest-work closure. A second pass added EvoAgentBench, REFLECT, and Causal Agent Replay as `ADAPT` baselines and did not change the residual composition. Stop rule met for this design freeze.

## Challenge to saturation basis

A discriminator freeze can still launder a method rewrite if it is treated as a repair license. A seven-stage protocol can still promote if missing FRESH is coded as `IN_PROGRESS`. Negative history can exist and still miss recurrence if fingerprints are not checked. Local gate tests can be mistaken for a live campaign. Hostile tests target each shortcut.

## Miss hypotheses

- regenerating `report.json` from memory rather than `results.jsonl`;
- copying unfinished table files from another worktree instead of archived records;
- editing `phase2_preflight.py` while PR #289 owns that surface;
- treating credential absence as a negative empirical result.

## Reopen triggers

Reopen if the three residual errors change under honest replay; if a host binds split/provider/evaluator identities; if a protected campaign becomes executable; or if nearest work already combines these exact authority/custody requirements.

## Frozen implementation hypothesis

Stdlib-only `orion.study.p5` modules can (1) replay archived attribution records into a hashed diagnosis-only receipt, (2) validate the frozen three-error discriminator artifact, and (3) evaluate causal-repair candidates under BLOCK/REJECT/CANNOT_CHECK/`RECOMMEND_HOST_PROMOTION` without self-merge authority. Live transfer remains `CANNOT_CHECK` until credentials and protected custody exist.
