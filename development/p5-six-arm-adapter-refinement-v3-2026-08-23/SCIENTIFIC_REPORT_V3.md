# P5 V3 outcome-free adapter-refinement result

## Terminal

`P5_V3_REFINED_ADAPTER_CONTRACT_TOTAL_ON_DECLARED_SYNTHETIC_DOMAIN__ZERO_RAW_SINGLETONS__ZERO_OF_SIX_EXECUTION_READY__P5_COMPARATOR_PERFORMANCE_AND_SUPERIORITY_CANNOT_CHECK`

This is a formal interface and synthetic-contract result. It is not comparator
performance, responsibility correctness, preservation, fresh transfer, harm,
or superiority. No comparator or model was executed; no native comparator
output example, public/protected outcome row, protected datum, or performance
table was opened.

## Refined contract

V3 freezes and implements five missing interface components:

1. a minimal candidate-visible class certificate issued by a host input
   validator before candidate action, with input-native provenance and no
   native-output or protected-outcome access;
2. a complete action/write-surface record enumerating every mutation and the
   before/after digest of all seven actionable fronts;
3. an eight-class front registry with seven actionable roots and a nonactionable
   `UNRESOLVED` terminal;
4. a fibre-constancy and nonselected-front invariance validator with exact
   native-terminal retention; and
5. a matched six-arm resource, configuration, rights, and custody template.

The adapter-visible key includes arm, exact native terminal, certificate set,
and complete action record. Within the declared finite synthetic domain, a
singleton is lawful only if all fictional worlds in that fibre have the same
authored minimal class, certificate and action agree on that class, the class
is in the arm's frozen support set, exactly that front changes, all mutations
lie under its registry root, protected/scorer surfaces are untouched, and all
arm-specific guards pass.

## Declared synthetic domain

The deterministic validator evaluated **231/231** prospectively authored,
outcome-free fictional cases:

| Family | Cases | Required disposition |
|---|---:|---|
| raw mixed fibres | 12 | `UNRESOLVED` |
| supported constant fibres | 40 | supported class |
| miscertified mixed fibres | 10 | `UNRESOLVED` |
| unsupported arm-class cells | 22 | `UNRESOLVED` |
| certificate defects | 42 | `UNRESOLVED` |
| action/invariance defects | 66 | `UNRESOLVED` |
| noncomplete native terminals | 36 | `UNRESOLVED` |
| Double Ratchet guard defects | 3 | `UNRESOLVED` |

There are 200 adapter-visible fibres: 189 constant and 11 deliberately mixed.
The 40 singleton case records occupy **20 supported constant arm-class
fibres**; their duplication supplies two fictional worlds per fibre and is not
40 independent scientific units. **191** cases remain `UNRESOLVED`.

Totality checks:

- unhandled exceptions: **0**;
- outputs outside the eight-class vocabulary: **0**;
- exact native-terminal retention failures: **0**;
- synthetic contract expectation failures: **0**.

This licenses only the statement that the deterministic contract is total on
the frozen synthetic domain. It does not show that any real native fibre is
constant or that any emitted class would be scientifically correct.

## Arm support boundaries retained

| Arm | Raw native singleton licences | Supported synthetic arm-class fibres |
|---|---:|---:|
| SWE-agent | 0 | 7 |
| MOSS | 0 | 4 |
| Darwin Godel Machine | 0 | 4 |
| ADIAS | 0 | 4 |
| Double Ratchet metric-only | 0 | 1 (`EVALUATOR_REPAIR`) |
| ScienceClaw | 0 | 0 |

ScienceClaw remains outside singleton support. Adding a minimal-class selector
would be a separately named successor method, not a semantics-preserving
adapter. Double Ratchet retains the frozen-solver, evaluator-only mutation,
and development-validity guards.

## Execution blockers

The generic V3 contract closes a schema/validator gap but is not an arm-native
runtime binding. The preflight records **108 blocking field instances**, 18
for each of six arms. Only the retained source commit, licence bytes, and
native entrypoint bytes are bound per arm. Every arm still lacks at least:

- exact candidate-visible P5 case bytes;
- dependency, container/environment, task-runtime, and compute identities;
- selected model/provider/service plus closed fallback identities;
- wallclock, call/token/USD, retry, timeout, and network envelopes;
- an arm-native parser binding and enforced isolated write/reset wrapper;
- closed task/benchmark, service, container, and generated-artifact rights;
- independent protected-scorer identity, protected-panel freshness, and an
  enforced one-shot no-feedback custody barrier.

Arm-specific residuals are material: SWE-agent lacks lawful issue/task and
provider bindings; MOSS lacks the authenticated coding-agent/runtime packet;
DGM lacks a frozen task subset and treatment of its retained entrypoint defect;
ADIAS retains restricted-source and write-allowlist conflicts; Double Ratchet
still lacks a development-surrogate/one-shot wrapper for `eval_locked`; and
ScienceClaw has no supported native singleton selector. Exact field paths and
causes are in `P5_V3_EXECUTION_BLOCKER_LEDGER.{json,md}`.

Thus confirmatory readiness remains **0/6**. H1--H4, protected freshness,
performance, harm, and superiority remain `CANNOT_CHECK`.

## Next discriminator

Prospectively bind the 108 arm-field instances without opening outcomes,
beginning with one lawful exact P5 case packet and one arm-native parser plus
enforced write-isolation wrapper per supported arm. ScienceClaw must either
remain `UNRESOLVED` or enter a separately preregistered successor-method arm.
An independent custodian must then freeze a fresh protected panel and one-shot
no-feedback scorer before any confirmatory execution. Any unresolved field
retains the arm-level failure and prohibits execution.
