# Development Packet — P1 V2 causal responsibility (#278)

Date: 2026-08-17
Branch lane: `cursor/issue-278`

## Development question

Why did frozen P1 V1 fail to show root-task superiority (H1 null 0.0000), and can
an intervention-backed responsibility state license only the epistemic mutations
the evidence warrants?

## Atomic fibers

1. Independently re-derive V1 headline facts from raw artifacts on current main.
2. Saturate nearest work and freeze mechanism dispositions before coding a repair.
3. Freeze competing hypotheses H-R1..H-R6 and a cause-confusable discriminator
   protocol with `outcome_accessed: false`.
4. Implement belief / discriminator / intervention / licensing scaffolding
   without editing frozen V1 runners or relabeling V1 evidence.
5. Record live-campaign status honestly if credentials are absent (`CANNOT_CHECK`).

## Saturation assessment

Knowledge routes: agent failure localization, counterfactual trace attribution,
active diagnosis, value of information, fault isolation, adaptive testing,
causal debugging, change-impact authorization, epistemic action policy,
diagnostic experimental design.

Parent papers already in the P1 matrix: AREX, Iris, EviGraph, Who&When /
Who&When Pro, REFLECT, ARTS, MAST, AgenTracer, TRAIL. CAR (arXiv:2606.08275)
was named in #278 and is not in the frozen matrix; this session does not fetch
arXiv.

Search-universe status: sufficient to freeze a discriminator protocol and a
local mechanism; insufficient for a paper-level novelty claim.

## Challenge to saturation

Saturation could be false if a parent-discipline method already couples
intervention-supported attribution to an authority gate over formulation /
search-universe mutation. Vocabulary drift (fault isolation, change-impact,
VoI experimental design) could hide that coupling.

## Frozen implementation hypothesis

A diagnosis is a belief state over `EVIDENCE | EXECUTION | FORMULATION |
SEARCH_UNIVERSE | UNRESOLVED`. Probes update support and never grant authority.
A mutating license requires a diagnosis-specific intervention whose observable
moves in the predicted direction, or a frozen mechanical discriminator.
`UNRESOLVED` and conflicting states issue `CANNOT_CHECK` / probe requests, never
broad W/M mutation.

## Reopen triggers

- a close mechanism is shown to already attach permission to a causal class;
- V1 artifact hashes fail reproduction;
- an intervention-free path can grant a mutating license;
- gold for cause-confusable cases becomes visible to a candidate;
- live evidence later supports or refutes the residual at matched budget.
