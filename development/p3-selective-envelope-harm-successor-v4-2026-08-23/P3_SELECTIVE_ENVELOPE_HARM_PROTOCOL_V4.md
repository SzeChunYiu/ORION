# P3 V4 prospective selective-envelope/harm protocol

**Protocol:** `P3.PUBLIC.OAEI.SELECTIVE.ENVELOPE.HARM.SUCCESSOR.DEV.V4`  
**Frozen:** `2026-08-23T15:22:14Z` before any new-family reference outcome  
**Authority:** public development only; V1--V3 and all protected identities remain unchanged.

The candidate is a certified-consensus set-valued controller. It may emit a singleton only from an input-native logical certificate or a V3-frozen zero-failure evidence cell; matcher nonselection and closed-world absence never license obstruction. Otherwise it returns the full binary envelope.

The design passes only if at least three ontology-source families are eligible, binary coverage is 1.0 overall and within every family, every family has at least 5% proper envelopes, the independent same-information implementation ties exactly, and harm is no worse than every runnable frozen comparator in every family and all three inherited regimes. Pairs, seeds, regimes and coordinates are not independent units.

No new-family outcome has been opened. Missing rights, immutable source, comparator identity, runtime, or family disjointness yields `CANNOT_CHECK`; it is not repaired in prose.
