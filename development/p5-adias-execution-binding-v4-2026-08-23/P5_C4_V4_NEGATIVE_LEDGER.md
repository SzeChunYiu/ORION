# P5 C4 ADIAS V4 negative ledger

Every defect is retained; none is converted into performance evidence.

## NATIVE_JOB_NOT_RUN

- **Failure:** No ADIAS model/domain/benchmark job was executed.
- **Cause:** Outcome blindness and 15 blockers require source preflight only.
- **Residual:** Performance, harm, preservation, transfer, and superiority remain CANNOT_CHECK.
- **Next discriminator:** Bind every field and the six-arm panel before one custodial run.

## DEPENDENCY_LOCK_ABSENT

- **Failure:** No transitive lock exists.
- **Cause:** Source has conflicting declarations and four unpinned VCS requirements.
- **Residual:** Runtime bytes cannot be reproduced.
- **Next discriminator:** Create a hash-complete Python 3.12/platform lock with exact VCS commits.

## ROOT_LICENCE_NONCOMMERCIAL

- **Failure:** Root source is CC-BY-NC-SA-4.0.
- **Cause:** The release intentionally restricts licensed use to NonCommercial purposes and applies ShareAlike conditions.
- **Residual:** Commercial execution/publication use is not authorized by this packet.
- **Next discriminator:** Obtain rights advice/permission for the exact intended use.

## BUNDLED_DATA_RIGHTS_UNCLOSED

- **Failure:** No component licence/NOTICE files occur under data/.
- **Cause:** Acknowledged third-party benchmark material is bundled without component-specific grants in the frozen tree.
- **Residual:** Task/benchmark rights remain CANNOT_CHECK.
- **Next discriminator:** Obtain authoritative grants for every selected task/data component.

## MUTABLE_BASE_IMAGE

- **Failure:** Dockerfile starts FROM python:3.12-bullseye.
- **Cause:** The tag is not digest-pinned and network installs occur during build.
- **Residual:** Container/environment identity is unbound.
- **Next discriminator:** Resolve base manifest digest and offline SBOM build.

## HOST_NETWORK_AND_KEYS

- **Failure:** Native build/task containers use host networking and receive provider API keys.
- **Cause:** build_container hard-codes network_mode=host and passes credential environment values.
- **Residual:** Isolation, egress, and credential exposure are unclosed.
- **Next discriminator:** Demonstrate a dedicated deny-by-default executor/daemon with exact endpoints and scoped keys.

## FINAL_TEST_DEFAULT_ON

- **Failure:** generate_loop defaults final_test=True and formal scripts commonly pass --final_test.
- **Cause:** Native final_test is same-run and writes outcomes locally.
- **Residual:** It cannot serve as the protected one-shot scorer and risks outcome leakage.
- **Next discriminator:** Force --no_final_test; score only through external custody after candidate freeze.

## EXTERNAL_SEARCH_DEFAULT_ON

- **Failure:** generate_loop defaults external search on.
- **Cause:** A one-shot web search can fetch unbound material and use another model surface.
- **Residual:** Network/model/task provenance expands unless explicitly disabled.
- **Next discriminator:** Force --no-enable_external_search and deny its endpoint.

## BACKGROUND_SCRIPT_TERMINAL_DEFECT

- **Failure:** Released run scripts use nohup and background execution.
- **Cause:** The shell can exit zero after writing a PID while the job is running or later fails.
- **Residual:** Script exit is not a native terminal.
- **Next discriminator:** Launch python -u generate_loop.py in the foreground under the V4 watchdog.

## ZERO_TASK_REPORT_DEFECT

- **Failure:** Report writers emit score=0 with total=0 and get_score can return non-None.
- **Cause:** Source valid_parent checks score presence, not positive task cardinality.
- **Residual:** A zero-task fixture/report must not be called performance.
- **Next discriminator:** Parser assigns NATIVE_EMPTY_EVALUATION and retains no score.

## EXIT_ZERO_INSUFFICIENT

- **Failure:** generate() catches exceptions, sets run_eval false, writes metadata, and can return normally.
- **Cause:** Process success and scientific/evaluation success are separate.
- **Residual:** Exit zero cannot establish a completed evaluation or P5 claim.
- **Next discriminator:** Parse metadata/report structure and retain UNRESOLVED.

## DOMAIN_HIDE_NOT_SECURITY_BOUNDARY

- **Failure:** The domain hide helper notes that the agent runs as root.
- **Cause:** chmod 0 is not a true lock against root; host-network container remains powerful.
- **Residual:** hide_domain is a cost increase, not isolation evidence.
- **Next discriminator:** Use an external non-root sandbox with inaccessible protected/domain bytes.

## RESOURCE_HARD_STOPS_ABSENT

- **Failure:** No aggregate calls/tokens/USD/compute hard stops exist.
- **Cause:** Source records usage after calls and uses partial per-call/per-container defaults.
- **Residual:** Resource comparability and spend bounds are unbound.
- **Next discriminator:** Add provider-reconciled hard stops and end-to-end cgroups.

## PRIMARY_MODEL_UNBOUND

- **Failure:** Source model names are defaults only.
- **Cause:** No provider endpoint/snapshot/service/role binding exists; TauBench adds a user simulator.
- **Residual:** Model identity and service rights remain CANNOT_CHECK.
- **Next discriminator:** Bind every role to one exact approved service identity.

## PROTECTED_CUSTODY_ABSENT

- **Failure:** No external scorer, one-shot barrier, or fresh protected panel attestation exists.
- **Cause:** Native ADIAS evaluation is candidate-side and iterative.
- **Residual:** Independent confirmatory measurement is impossible.
- **Next discriminator:** Obtain a signed external custody handoff satisfying the V4 schema.

## SOURCE_FIXTURE_NOT_PERFORMANCE

- **Failure:** Only authored native-shaped smoke fixtures are executed.
- **Cause:** They contain zero task records and no model/environment work.
- **Residual:** Smoke proves parser/gate conformance only.
- **Next discriminator:** First substantive discriminator is a rights-cleared nonzero case under full custody.
