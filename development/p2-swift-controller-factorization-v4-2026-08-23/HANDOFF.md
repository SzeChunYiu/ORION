# P2 SWIFT V4 isolated handoff

## Terminal

`P2_SWIFT_V4_POST_OUTCOME_FACTORIZATION_IDENTIFIES_STABLE_COMPONENT_REQUIRES_CONTENT_DISJOINT_OUTCOME_UNOPENED_FAMILY`

The frozen 2x2 completed on five review decisions and 96,241 canonical rows per
arm. All source bindings, population identities, seeds/batches and both V3
corner reproductions passed.

The learner/balancer main effect is stable under the frozen within-panel rule:
+0.022618035379 at recall@10%, positive in 5/5 reviews, and +0.041725819160 at
WSS@95. The representation main effect (-0.000900063738 at recall@10%) is not
stable, and the interaction (+0.003067576381, 3/5 same sign) is not dominant.

This is post-outcome public-development mechanism diagnosis. No arm is promoted
on SWIFT. A selected mechanism requires a new content-disjoint,
outcome-unopened family with a pre-outcome freeze and independent adapter audit.

## Population by arm

Each of `R0_L0`, `R0_L1`, `R1_L0`, and `R1_L1` used:

- BPA: 7,697 rows
- Fluoride: 4,477 rows
- Neuropain: 29,157 rows
- PFOS-PFOA: 6,309 rows
- Transgenerational: 48,601 rows

## Boundaries

- All V1/V2/V3 adverse terminals are retained.
- The private 90,520,080-byte PubMed snapshot and public source bodies were not
  copied into this V4 directory. Their hashes and byte counts are recorded.
- The five SWIFT decisions remain public development and have up to 68 shared
  content identities; they are not claimed to be independent samples.
- No title or abstract appears in a result artifact.
- No P2 manuscript or main checkout file was edited.
- No pytest, repository CI, commit, push, merge or rebase was run.

## Verification

JSON parse, Python syntax parse, complete source/hash binding, exact frozen
scientific execution, V3 corner reproduction, checksum verification, and
absence checks for source bodies and Python caches.
